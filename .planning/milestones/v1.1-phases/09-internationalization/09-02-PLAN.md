---
phase: 09-internationalization
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/frontend/src/components/AppShell.tsx
  - apps/frontend/src/components/CardFlip.tsx
  - apps/frontend/src/components/RatingButtons.tsx
  - apps/frontend/src/components/SessionProgress.tsx
  - apps/frontend/src/components/ExamTimer.tsx
  - apps/frontend/src/components/MediaUploadToolbar.tsx
  - apps/frontend/src/components/DeckFormModal.tsx
  - apps/frontend/src/components/CardEditorModal.tsx
autonomous: true
requirements:
  - I18N-02
  - I18N-03

must_haves:
  truths:
    - "A language toggle button (showing EN/DE) sits in the AppShell sidebar near the theme toggle and cycles language on click without page reload"
    - "All AppShell nav labels, aria-labels, footer links, and the header current-label render via t()"
    - "All shared components (CardFlip, RatingButtons, SessionProgress, ExamTimer, MediaUploadToolbar) and both modals render UI strings via t()"
    - "The LanguageToggle test from Plan 01 now passes (GREEN)"
  artifacts:
    - path: "apps/frontend/src/components/AppShell.tsx"
      provides: "Translated shell + language toggle button"
      contains: "useTranslation"
    - path: "apps/frontend/src/components/RatingButtons.tsx"
      provides: "Translated rating labels + aria-labels"
      contains: "useTranslation"
    - path: "apps/frontend/src/components/CardEditorModal.tsx"
      provides: "Translated modal labels/placeholders/toasts"
      contains: "useTranslation"
  key_links:
    - from: "apps/frontend/src/components/AppShell.tsx"
      to: "i18n.changeLanguage"
      via: "language toggle onClick handler"
      pattern: "changeLanguage"
    - from: "apps/frontend/src/components/AppShell.tsx"
      to: "react-i18next"
      via: "useTranslation hook"
      pattern: "useTranslation"
---

<objective>
Wrap all UI strings in the AppShell and the shared component layer (CardFlip, RatingButtons, SessionProgress, ExamTimer, MediaUploadToolbar) and both feature modals (DeckFormModal, CardEditorModal) in `t()`, and add the language toggle button to the AppShell sidebar (desktop + mobile drawer) so it cycles EN↔DE and turns the Plan 01 LanguageToggle test GREEN.

Purpose: Delivers the language switcher (I18N-03) and the component-layer slice of string externalization (I18N-02). AppShell is the primary integration point and owns the switcher per D-04/D-05. This plan touches only `components/` files — disjoint from Plan 03's `pages/` files — so it can run in parallel with Plan 03's wave if scheduled, but is sequenced here after Plan 01 (which creates the locale keys and the RED test).
Output: Translated AppShell with working language toggle; translated shared components and modals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-internationalization/09-CONTEXT.md
@.planning/phases/09-internationalization/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate AppShell and add the language toggle button</name>
  <files>apps/frontend/src/components/AppShell.tsx</files>
  <read_first>
    - apps/frontend/src/components/AppShell.tsx (full current file — module-scope navItems array line 20-26, theme toggle Button line 117-124 and 220-227, currentLabel line 34-36, footer line 258-280, two `aria-label="Main navigation"` nav blocks, hamburger `aria-label="Open navigation menu"`, two "Log out" buttons)
    - apps/frontend/src/components/__tests__/LanguageToggle.test.tsx (the RED test this task must satisfy — confirm accessible-name query and changeLanguage('de') expectation)
    - .planning/phases/09-internationalization/09-RESEARCH.md (§File Inventory AppShell.tsx; §Architecture Patterns Pattern 4 language toggle; Edge Cases 7 navItems and 8 currentLabel; Pitfall 4 module-scope navItems)
    - apps/frontend/src/locales/en.json (keys: nav.*, a11y.mainNav, a11y.openMenu, a11y.switchToDark, a11y.switchToLight, a11y.switchLanguage, common.logOut, footer.copyright, footer.github, footer.docs, lang.en, lang.de)
  </read_first>
  <behavior>
    - Language toggle button: accessible name equals translated a11y.switchLanguage; visible text is "EN" when i18n.language === 'en', "DE" when 'de'
    - Clicking the toggle calls i18n.changeLanguage('de') when current language is 'en' (cycles en->de->en)
    - nav labels render t('nav.dashboard') etc.; currentLabel header uses translated labels with t('nav.admin') fallback and untranslated "Kartex" brand
  </behavior>
  <action>
    Add `import { useTranslation } from 'react-i18next'` and call `const { t, i18n } = useTranslation()` at the top of the AppShell component body.

    Refactor the module-scope `navItems` array (Pitfall 4 / Edge Case 7): replace each `label` string with a `labelKey` string — `'nav.dashboard'`, `'nav.decks'`, `'nav.import'`, `'nav.explore'`, `'nav.settings'`. Keep `to` and `icon` unchanged. In both nav render loops (desktop line ~59 and drawer line ~160) render `{t(item.labelKey)}` instead of `{label}` (destructure `labelKey` instead of `label`).

    Update `currentLabel` (Edge Case 8): map the matched nav item via `t(item.labelKey)`; the admin fallback becomes `t('nav.admin')`; the final fallback stays the untranslated brand literal `'Kartex'`.

    Replace the hardcoded `Admin` NavLink text in both the desktop (line ~105) and drawer (line ~208) admin links with `{t('nav.admin')}`.

    Wrap aria-labels: both `aria-label="Main navigation"` → `aria-label={t('a11y.mainNav')}`; hamburger `aria-label="Open navigation menu"` → `aria-label={t('a11y.openMenu')}`; both theme-toggle aria-labels `theme === 'dark' ? 'Switch to light mode' : 'Switch to dark mode'` → `theme === 'dark' ? t('a11y.switchToLight') : t('a11y.switchToDark')`. Leave `aria-expanded`, `aria-controls`, `aria-hidden` unchanged (booleans/ids).

    Replace both `Log out` button texts with `{t('common.logOut')}`.

    Translate the footer (line ~258-280): the `© Kartex` span keeps `v{__APP_VERSION__}` literal but renders `{t('footer.copyright')}` for the "© Kartex" portion; the GitHub link text → `{t('footer.github')}`; the Docs link text → `{t('footer.docs')}`. Keep GITHUB_URL/DOCS_URL hrefs unchanged.

    Add the language toggle button in BOTH the desktop sidebar user area (after the theme toggle Button at line ~124, before the Log out button) and the mobile drawer user area (after the theme toggle at line ~227, before Log out), per D-04/D-05 and Pattern 4. Each button: `<Button variant="ghost" size="icon" onClick={toggleLanguage} aria-label={t('a11y.switchLanguage')}>{i18n.language === 'de' ? 'DE' : 'EN'}</Button>`. Define `const toggleLanguage = () => { void i18n.changeLanguage(i18n.language === 'en' ? 'de' : 'en') }` in the component body. Do NOT create a LanguageContext — i18next self-manages language state (RESEARCH.md Open Question 2).
  </action>
  <verify>
    <automated>cd apps/frontend && pnpm test --run src/components/__tests__/LanguageToggle.test.tsx src/components/__tests__/AppShell.test.tsx</automated>
  </verify>
  <done>LanguageToggle.test.tsx passes (GREEN); AppShell.test.tsx still passes; AppShell.tsx contains useTranslation, a toggleLanguage handler calling i18n.changeLanguage, and language toggle buttons in both sidebar and drawer with the EN/DE label. No hardcoded nav/footer/aria strings remain in AppShell.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translate shared study/media components</name>
  <files>
    apps/frontend/src/components/CardFlip.tsx,
    apps/frontend/src/components/RatingButtons.tsx,
    apps/frontend/src/components/SessionProgress.tsx,
    apps/frontend/src/components/ExamTimer.tsx,
    apps/frontend/src/components/MediaUploadToolbar.tsx
  </files>
  <read_first>
    - apps/frontend/src/components/CardFlip.tsx (aria-label flip button, "Front"/"Back" section labels, "Click or press Space to reveal" hint)
    - apps/frontend/src/components/RatingButtons.tsx (RATINGS array labels Again/Hard/Good/Easy, aria-label "Rate: {label}, keyboard shortcut {shortcut}")
    - apps/frontend/src/components/SessionProgress.tsx ("Card {N} of {M}" + matching aria-label)
    - apps/frontend/src/components/ExamTimer.tsx (aria-label "{mins}:{secs} remaining")
    - apps/frontend/src/components/MediaUploadToolbar.tsx (aria-labels Upload image/audio, toasts Image uploaded/Audio uploaded/Upload failed)
    - apps/frontend/src/components/__tests__/CardFlip.test.tsx (existing assertions — keep green)
    - .planning/phases/09-internationalization/09-RESEARCH.md (§File Inventory entries for these 5 components; §Locale Key Structure groups: a11y, common, rating, session, exam, media; Edge Cases 4 aria-labels and 5 interpolation)
    - apps/frontend/src/locales/en.json (keys: a11y.flashcard, common.frontLabel? use cardEditor.frontLabel/backLabel? — confirm CardFlip section keys; rating.again/hard/good/easy/ariaLabel; session.progress/progressAriaLabel; exam.timeRemaining; a11y.uploadImage/uploadAudio; media.imageUploaded/audioUploaded/uploadFailed)
  </read_first>
  <behavior>
    - RatingButtons: each button's accessible name renders rating.ariaLabel interpolated with its label and shortcut; visible label renders t('rating.again') etc.
    - SessionProgress: renders t('session.progress', { current, total }) text and aria-label t('session.progressAriaLabel', { current, total })
    - ExamTimer: aria-label renders t('exam.timeRemaining', { time }) where time is the mm:ss data string (not translated)
    - CardFlip: flip-button aria-label renders t('a11y.flashcard'); Front/Back section labels and the reveal hint render via t()
  </behavior>
  <action>
    In each of the five component files, add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` at the top of the component.

    CardFlip.tsx: flip-card button `aria-label` → `t('a11y.flashcard')`; the "Front"/"Back" section labels → use dedicated keys (front/back appear under cardEditor.frontLabel/backLabel in the locale tree — reuse `t('cardEditor.frontLabel')` and `t('cardEditor.backLabel')`); the "Click or press Space to reveal" hint → add/use the matching key from en.json (confirm exact key during read_first; if absent it belongs under a11y — use the closest existing key rather than inventing a new top-level group, and if no key exists add it to BOTH en.json and de.json under `a11y` and re-run the Plan 01 key-parity check).

    RatingButtons.tsx: the RATINGS array `label` values render via `t('rating.again')`, `t('rating.hard')`, `t('rating.good')`, `t('rating.easy')`; the aria-label uses `t('rating.ariaLabel', { label: <translated label>, shortcut })` per Edge Case 4 (label and shortcut are interpolated, the surrounding "Rate:" / "keyboard shortcut" wording lives in the translation string).

    SessionProgress.tsx: visible text → `t('session.progress', { current, total })`; aria-label → `t('session.progressAriaLabel', { current, total })`.

    ExamTimer.tsx: aria-label → `t('exam.timeRemaining', { time: \`${mins}:${secs}\` })` — the mm:ss value is data, not translated (D-07).

    MediaUploadToolbar.tsx: button aria-labels → `t('a11y.uploadImage')` and `t('a11y.uploadAudio')`; toast strings → `t('media.imageUploaded')`, `t('media.audioUploaded')`, `t('media.uploadFailed')` (toasts work in event handlers per Edge Case 2; call t() inside the toast argument).

    If any string in these files has no matching key in en.json, add the key to BOTH en.json and de.json under the appropriate existing group (do not create stub values; provide real English + German), then the Plan 03 verification key-parity check still passes.
  </action>
  <verify>
    <automated>cd apps/frontend && pnpm test --run src/components/__tests__/CardFlip.test.tsx</automated>
  </verify>
  <done>All five components call useTranslation and render their UI strings/aria-labels via t(); CardFlip.test.tsx stays green; no hardcoded UI strings remain in these five files. Any newly added keys exist in both locale files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Translate DeckFormModal and CardEditorModal</name>
  <files>
    apps/frontend/src/components/DeckFormModal.tsx,
    apps/frontend/src/components/CardEditorModal.tsx
  </files>
  <read_first>
    - apps/frontend/src/components/DeckFormModal.tsx (dialog title Edit/New Deck, labels Title/Description/Visibility, placeholders, Select options Private/Shared/Public, buttons, toasts)
    - apps/frontend/src/components/CardEditorModal.tsx (dialog title Edit/Add Card, tab labels Edit/Preview, labels Front/Back, placeholders, Tags label/placeholder, buttons, toasts)
    - .planning/phases/09-internationalization/09-RESEARCH.md (§File Inventory CardEditorModal.tsx and DeckFormModal.tsx; §Locale Key Structure groups: deckForm, cardEditor, visibility, common; Edge Cases 2 toasts and 3 form errors)
    - apps/frontend/src/locales/en.json (keys: deckForm.*, cardEditor.*, visibility.public/shared/private, common.cancel/saving)
  </read_first>
  <behavior>
    - DeckFormModal: dialog title renders t('deckForm.editDeck') or t('deckForm.newDeck') based on edit mode; visibility Select options render t('visibility.private'/'shared'/'public'); success toasts render t('deckForm.deckUpdated'/'deckCreated'); error toast renders t('common.somethingWrong')
    - CardEditorModal: dialog title renders t('cardEditor.editCard') or t('cardEditor.addCard'); tab labels render t('cardEditor.editTab'/'previewTab'); toasts render t('cardEditor.cardUpdated'/'cardAdded')
  </behavior>
  <action>
    In both modal files, add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` at the top of the component.

    DeckFormModal.tsx: dialog title → `t('deckForm.editDeck')` / `t('deckForm.newDeck')` per edit mode; labels Title/Description/Visibility → `t('deckForm.titleLabel')`, `t('deckForm.descriptionLabel')`, `t('deckForm.visibilityLabel')`; placeholders → `t('deckForm.titlePlaceholder')`, `t('deckForm.descriptionPlaceholder')`; Select options Private/Shared/Public → `t('visibility.private')`, `t('visibility.shared')`, `t('visibility.public')`; buttons Cancel/Saving.../Save Changes/Create Deck → `t('common.cancel')`, `t('common.saving')`, `t('deckForm.saveChanges')`, `t('deckForm.createDeck')`; toasts → `t('deckForm.deckUpdated')`, `t('deckForm.deckCreated')`, `t('common.somethingWrong')`.

    CardEditorModal.tsx: dialog title → `t('cardEditor.editCard')` / `t('cardEditor.addCard')`; tab labels Edit/Preview → `t('cardEditor.editTab')`, `t('cardEditor.previewTab')`; labels Front/Back → `t('cardEditor.frontLabel')`, `t('cardEditor.backLabel')`; textarea placeholders → `t('cardEditor.frontPlaceholder')`, `t('cardEditor.backPlaceholder')`; tags label/placeholder → `t('cardEditor.tagsLabel')`, `t('cardEditor.tagsPlaceholder')`; buttons Cancel/Saving.../Save Card → `t('common.cancel')`, `t('common.saving')`, `t('cardEditor.saveCard')`; toasts → `t('cardEditor.cardUpdated')`, `t('cardEditor.cardAdded')`, `t('common.somethingWrong')`. Card front/back CONTENT typed by the user is NOT translated (D-07) — only labels/placeholders/chrome.
  </action>
  <verify>
    <automated>cd apps/frontend && pnpm test --run && pnpm typecheck</automated>
  </verify>
  <done>Both modals call useTranslation; all dialog titles, labels, placeholders, Select options, buttons, and toasts render via t(); full test suite green and typecheck clean. No hardcoded UI strings remain in either modal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → client UI | Language toggle writes `i18nextLng`; controls display language only |
| user content → t() interpolation | Deck/card content must NOT pass through t() as a key (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | t() key argument | mitigate | All keys are compile-time string literals; never call t(userInput). User content (card text, deck titles) is interpolated as values, never used as keys (RESEARCH.md §Security Domain, D-07) |
| T-09-04 | Tampering | language toggle onClick | accept | changeLanguage only swaps display locale; no privileged action (RESEARCH.md §Security Domain) |
</threat_model>

<verification>
- `pnpm --filter @kartex/frontend test --run` — all tests green including LanguageToggle (GREEN) and CardFlip/AppShell
- `pnpm --filter @kartex/frontend typecheck` — clean (type-safe keys validate every t() call against en.json)
</verification>

<success_criteria>
- Language toggle button present in AppShell sidebar (desktop + drawer), cycles EN↔DE, no page reload (I18N-03, D-04/D-05)
- LanguageToggle.test.tsx GREEN
- AppShell, 5 shared components, and 2 modals fully wrapped in t() (I18N-02)
- aria-labels translated (D-09)
- No LanguageContext created (i18next self-manages state)
- Full test suite green + typecheck clean
</success_criteria>

<output>
Create `.planning/phases/09-internationalization/09-02-SUMMARY.md` when done.
</output>
