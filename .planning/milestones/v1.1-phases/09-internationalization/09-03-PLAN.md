---
phase: 09-internationalization
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - apps/frontend/src/pages/LoginPage.tsx
  - apps/frontend/src/pages/RegisterPage.tsx
  - apps/frontend/src/pages/DashboardPage.tsx
  - apps/frontend/src/pages/ExplorePage.tsx
  - apps/frontend/src/pages/ImportPage.tsx
  - apps/frontend/src/pages/AdminPage.tsx
  - apps/frontend/src/pages/DecksPage.tsx
  - apps/frontend/src/pages/DeckDetailPage.tsx
  - apps/frontend/src/pages/StudySessionPage.tsx
autonomous: true
requirements:
  - I18N-02
  - I18N-03

must_haves:
  truths:
    - "Every visible string in all 9 page files renders via t() — no hardcoded English JSX text remains"
    - "document.title on each page uses t() and updates on language switch (i18n.language in effect deps)"
    - "All toasts, form errors, aria-labels, plural counts, and interpolated user-content strings render via t() with interpolation (never concatenation)"
    - "User-authored content (deck titles, card text, tags, usernames, filenames) is never passed through t() as a key — only interpolated as values"
    - "Full frontend test suite green and typecheck clean across the whole app"
  artifacts:
    - path: "apps/frontend/src/pages/StudySessionPage.tsx"
      provides: "Fully translated study session (largest string set)"
      contains: "useTranslation"
    - path: "apps/frontend/src/pages/DeckDetailPage.tsx"
      provides: "Fully translated deck detail with dynamic title excluding user content"
      contains: "useTranslation"
    - path: "apps/frontend/src/pages/AdminPage.tsx"
      provides: "Fully translated admin page + badge components"
      contains: "useTranslation"
  key_links:
    - from: "apps/frontend/src/pages/DashboardPage.tsx"
      to: "react-i18next"
      via: "useTranslation + document.title effect with i18n.language dep"
      pattern: "i18n\\.language"
    - from: "apps/frontend/src/pages/DeckDetailPage.tsx"
      to: "deck.title (user content)"
      via: "raw interpolation, not t()"
      pattern: "deck\\.title"
---

<objective>
Wrap every visible UI string in all 9 page files (LoginPage, RegisterPage, DashboardPage, ExplorePage, ImportPage, AdminPage, DecksPage, DeckDetailPage, StudySessionPage) in `t()`, handle document.title effects, toasts, form errors, aria-labels, pluralization, and interpolated user-content strings, then run the full phase verification gate.

Purpose: Completes I18N-02 (no hardcoded English strings remain in JSX) for the page layer and confirms the whole phase: full test suite green, typecheck clean, en/de key parity, and a hardcoded-string sweep. This plan touches only `pages/` files — disjoint from Plan 02's `components/` files. It is Wave 3 so its verification gate runs after both Plan 01 and Plan 02.
Output: All 9 pages translated; phase verification gate passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-internationalization/09-CONTEXT.md
@.planning/phases/09-internationalization/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate simple + medium pages (Login, Register, Dashboard, Explore, Import, Admin)</name>
  <files>
    apps/frontend/src/pages/LoginPage.tsx,
    apps/frontend/src/pages/RegisterPage.tsx,
    apps/frontend/src/pages/DashboardPage.tsx,
    apps/frontend/src/pages/ExplorePage.tsx,
    apps/frontend/src/pages/ImportPage.tsx,
    apps/frontend/src/pages/AdminPage.tsx
  </files>
  <read_first>
    - apps/frontend/src/pages/LoginPage.tsx, RegisterPage.tsx, DashboardPage.tsx, ExplorePage.tsx, ImportPage.tsx, AdminPage.tsx (read each — confirm exact current strings, document.title effects, toast calls, form.setError calls, aria-labels, badge subcomponents)
    - .planning/phases/09-internationalization/09-RESEARCH.md (§File Inventory entries for these 6 pages; §Locale Key Structure groups auth, dashboard, explore, import, admin, common, visibility, table; Edge Cases 1 document.title, 2 toasts, 3 form errors, 5 interpolation, 6 pluralization, 9 toasts with user content; Pitfall 7 document.title deps)
    - apps/frontend/src/locales/en.json (all relevant keys for these pages)
  </read_first>
  <behavior>
    - Each page's document.title renders t('<page>.title') and updates when i18n.language changes (effect dep array includes t and i18n.language)
    - LoginPage form error setError uses t('auth.invalidCredentials'); RegisterPage uses t('auth.invalidInvite')/t('auth.usernameTaken')
    - DashboardPage streak renders t('dashboard.streakDays', { count }); plural card counts use t('common.nCards', { count })
    - ExplorePage byAuthor renders t('explore.byAuthor', { username }) — username interpolated, never a key; addedToLibrary/forkedDeck interpolate the user deck title as a value
    - ImportPage maxSize/cardsSkipped/cardN/showingCards interpolate counts; filename interpolated as value not key
    - AdminPage badge components (InviteStatusBadge, RoleBadge, StatusBadge) render translated status/role strings
  </behavior>
  <action>
    For each of the six page files: add `import { useTranslation } from 'react-i18next'` and call `const { t, i18n } = useTranslation()` at the top of the component (and inside any badge subcomponent that renders strings — e.g. AdminPage's InviteStatusBadge/RoleBadge/StatusBadge, ExplorePage author block).

    document.title (Edge Case 1, Pitfall 7): change each `useEffect(() => { document.title = '...' }, [])` to set `document.title = t('<page>.title')` with dependency array `[t, i18n.language]` so the tab title updates on language switch. Keys: `dashboard.title`, `explore.title`, `import.title`, `admin.title`; for Login/Register use the document.title keys from the auth group (LoginPage title "Sign in — Kartex", RegisterPage "Create account — Kartex"). If no dedicated title key exists in en.json for login/register, add `auth.loginTitle`/`auth.registerTitle` to BOTH locale files with English + German and keep key parity.

    LoginPage.tsx: sr-only heading, CardTitle, CardDescription, form labels Username/Password, placeholders, submit button states (Signing in.../Sign in), footer "Don't have an account?"/Register link, redirect toast "Account created. Please sign in.", generic error toast — use auth.* and common.* keys per §File Inventory. The form.setError message uses `t('auth.invalidCredentials')` (Edge Case 3).

    RegisterPage.tsx: analogous to LoginPage using auth.createAccount*, auth.inviteCode, auth.creatingAccount, auth.alreadyHaveAccount, auth.signIn, and setError messages `t('auth.invalidInvite')` / `t('auth.usernameTaken')`.

    DashboardPage.tsx: cardsDueToday, startStudying, allCaughtUp, noDueCards, table aria-label, deck/due column headers (table group), noDueAnyDeck, reviewedToday, streak label, streak count `t('dashboard.streakDays', { count: stats.streak })`, toasts via common.somethingWrong/common.serverUnreachable or dashboard-specific keys.

    ExplorePage.tsx: pageHeading, empty-state strings, `t('explore.byAuthor', { username: deck.owner.username })`, plural card count `t('common.nCards', { count })`, buttons addToLibrary/adding/forkDeck/forking, toasts: alreadyInLibrary, `t('explore.addedToLibrary', { title: deck.title })`, viewDecks action label, `t('explore.forkedDeck', { title: deck.title })`, viewDeck action label, failedToLoad/failedToAdd/failedToFork, loading. Deck titles are user content — interpolated values only (D-07).

    ImportPage.tsx: pageHeading, subtitle, importFailed alert, zipValidationFail/zipFixHint, drop-zone aria-label + text, `t('import.maxSize', { mb: limitMB })`, browseFile, processing aria-label, parsingFile, upload-progress aria-label, `t('import.cardsSkipped', { count })`, `t('import.cardN', { index })`, back, deckNameLabel, author label, `t('import.showingCards', { count })`, `t('import.skippedCount', { count })`, fileLabel (filename is user content — interpolated value), cancel, importDeck/importing button states, deckNameRequired title, preview aria-label `t('import.previewAriaLabel', { count })`, deckImported, `t('import.importedWithCards', { name: ..., count })` (name is user content), viewDeck, importAnotherFile, zipNoPreview.

    AdminPage.tsx: document.title, pageHeading, section titles/descriptions (inviteCodes*, users*), expiryDaysLabel, generate/generating, table headers via table group (code/status/usedBy/expires/actions and username/role/status/joined), empty states noInviteCodes/noUsers, badge strings in InviteStatusBadge (statusActive/statusUsed/statusExpired), RoleBadge (roleAdmin/roleUser), StatusBadge (accountActive/accountInactive), buttons delete/yesDelete/cancel/deactivate/yesDeactivate, role toggle makeUser/makeAdmin, confirm common.confirm, table aria-labels, toasts inviteGenerated/inviteDeleted/roleUpdated/accountDeactivated/somethingWrong.

    Any string lacking a matching key gets added to BOTH en.json and de.json under the correct group with real English + German (no stubs), preserving key parity.
  </action>
  <verify>
    <automated>cd apps/frontend && pnpm test --run && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All six page files contain `useTranslation` (`grep -l useTranslation` matches each)
    - Each page's document.title effect dependency array includes `i18n.language`
    - DashboardPage renders streak via `t('dashboard.streakDays'` with a count option
    - ExplorePage interpolates `deck.title` as a value inside `t('explore.addedToLibrary'` (deck.title never used as a key)
    - `pnpm --filter @kartex/frontend test --run` exits 0 with all existing tests passing
    - No hardcoded English JSX sentence strings remain in these six files (D-07 exclusions only)
  </acceptance_criteria>
  <done>All six pages call useTranslation; document.title effects use t() with [t, i18n.language] deps; toasts, form errors, aria-labels, plurals, and interpolations render via t(); user content interpolated as values only; full test suite green. No hardcoded UI strings remain in these six files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translate complex pages (Decks, DeckDetail, StudySession)</name>
  <files>
    apps/frontend/src/pages/DecksPage.tsx,
    apps/frontend/src/pages/DeckDetailPage.tsx,
    apps/frontend/src/pages/StudySessionPage.tsx
  </files>
  <read_first>
    - apps/frontend/src/pages/DecksPage.tsx, DeckDetailPage.tsx, StudySessionPage.tsx (read each — confirm VisibilityBadge/PermissionBadge subcomponents, SIZE_OPTIONS and EXAM_DURATIONS arrays, mode cards, dynamic document.title, dynamic aria-labels, CardActionCell)
    - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx, StudySessionPage.test.tsx (existing assertions — must stay green; these use getByText with English strings which the real i18n test setup returns)
    - .planning/phases/09-internationalization/09-RESEARCH.md (§File Inventory DecksPage/DeckDetailPage/StudySessionPage; §Locale Key Structure groups decks, deckDetail, study, visibility, permission, table, common, rating; Edge Cases 1 DeckDetail dynamic title special case, 5 interpolation, 6 pluralization)
    - apps/frontend/src/locales/en.json (decks.*, deckDetail.*, study.*, visibility.*, permission.*, table.*)
  </read_first>
  <behavior>
    - DecksPage: VisibilityBadge renders t('visibility.public'/'shared'/'private'); sharedBy renders t('decks.sharedBy', { username }); card counts use t('common.nCards', { count })
    - DeckDetailPage: document.title is `${deck.title} — Kartex` when deck loaded (user content, NOT through t()) else t('deckDetail.title'); ownedBy interpolates username; per-tag table aria-label renders t('deckDetail.cardsTableAriaLabel', { tag }); tag-group header renders t('deckDetail.nCardsInGroup', { count })
    - StudySessionPage: SIZE_OPTIONS All due/Custom translated, numeric 10/20 stay literal; EXAM_DURATIONS render t('study.nMinutes', { count }); mode card titles/descriptions/stats translated; session-complete stats interpolate counts and m/s
  </behavior>
  <action>
    For each file add `import { useTranslation } from 'react-i18next'` and `const { t, i18n } = useTranslation()` at the top of the component (and within VisibilityBadge/PermissionBadge/CardActionCell subcomponents that render strings).

    DecksPage.tsx: document.title `t('decks.title')` with [t, i18n.language]; pageHeading, both New Deck buttons render t('decks.newDeck'); VisibilityBadge strings use the visibility group; `t('decks.sharedBy', { username: deck.sharedByUsername })`; plural card count `t('common.nCards', { count })`; empty state noDecksYet/createFirst; buttons study/open/edit/delete; confirm-delete common.confirm/common.yesDelete/common.cancel; toasts decks.deckDeleted/decks.failedToLoad/common.serverUnreachable/common.somethingWrong.

    DeckDetailPage.tsx: document.title special case (Edge Case 1) — set `document.title = deck ? \`${deck.title} — Kartex\` : t('deckDetail.title')` with [t, i18n.language, deck] deps; deck.title stays raw user content (D-07). `t('deckDetail.ownedBy', { username })`; buttons studyDeck/editDeck/deleteDeck/addCard/addUser/revokeAccess; CardActionCell edit/delete via common.edit/common.delete; confirm-delete common.*; table headers via table group plus deckDetail.userColumn/permissionColumn; tag-section header `t('deckDetail.nCardsInGroup', { count })`; empty states noCardsYet/addFirstCard/deckHasNoCards; shareThisDeck; notShared; PermissionBadge uses permission group (read/edit/manage); VisibilityBadge uses visibility group; share permission Select options use permission group; username placeholder t('deckDetail.usernamePlaceholder'); per-tag table aria-label `t('deckDetail.cardsTableAriaLabel', { tag })`; toasts decks.deckDeleted/deckDetail.cardDeleted/common.somethingWrong/deckDetail.failedToLoadCards/common.serverUnreachable; error t('deckDetail.failedToAddUser'). Card front/tag CONTENT stays untranslated (D-07).

    StudySessionPage.tsx: document.title `t('study.title')` with [t, i18n.language]; backToDeck; study-deck label uses a study.* key with deckTitle interpolated as user content; chooseMode; filterByTag; sessionSize plus srModeOnly; SIZE_OPTIONS — translate 'All due' to t('study.sizeAllDue') and 'Custom' to t('study.sizeCustom'); leave '10'/'20' as numeric literals; selectTimeLimit placeholder; EXAM_DURATIONS labels render `t('study.nMinutes', { count: 5 })` etc.; mode card titles srTitle/deckModeTitle/examTitle; descriptions srDescription/deckModeDescription/examDescription; mode stats `t('study.nCardsDue', { count })` and `t('study.nCardsTotal', { count })`; startExam; leaveSession button plus aria-label t('a11y.leaveSession'); timesUp banner; empty state noCardsToStudy/allCaughtUp/returnToDashboard; sessionComplete; session-done stats `t('study.reviewedCardsWithTime', { count, min, sec })` (exam) and `t('study.reviewedCards', { count })` (other); rating counts use the rating group labels (Again:/Hard:/Good:/Easy: — confirm whether the trailing colon is in the key or the JSX during read_first); returnToDashboard/restartSession buttons; loadingCards; couldNotLoad toast.

    Any missing key gets added to BOTH en.json and de.json under the right group with real English + German, preserving parity.
  </action>
  <verify>
    <automated>cd apps/frontend && pnpm test --run src/pages/__tests__/DeckDetailPage.test.tsx src/pages/__tests__/StudySessionPage.test.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All three page files contain `useTranslation`
    - DeckDetailPage document.title uses `deck.title` raw (not wrapped in t()) when deck is loaded — `grep` finds `${deck.title} — Kartex` not passed to t()
    - StudySessionPage EXAM_DURATIONS render via `t('study.nMinutes'` with a count option; SIZE_OPTIONS 'All due'/'Custom' render via t()
    - DeckDetailPage.test.tsx and StudySessionPage.test.tsx pass (exit 0)
    - No hardcoded English JSX sentence strings remain in these three files (D-07 exclusions only)
  </acceptance_criteria>
  <done>All three pages call useTranslation; DeckDetailPage dynamic title keeps deck.title untranslated; SIZE_OPTIONS/EXAM_DURATIONS/mode cards translated with interpolation; DeckDetailPage and StudySessionPage tests stay green. No hardcoded UI strings remain in these three files.</done>
</task>

<task type="auto">
  <name>Task 3: Phase verification gate</name>
  <files>apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/locales/en.json, de.json (final state after all wrapping)
    - .planning/phases/09-internationalization/09-VALIDATION.md (§Manual-Only Verifications, §Validation Sign-Off)
    - .planning/phases/09-internationalization/09-RESEARCH.md (§German Translation Approach key-parity script; §Validation Architecture Phase Requirements → Test Map)
  </read_first>
  <action>
    Run the full verification gate and fix any failures found (no new feature work — only close gaps surfaced by the checks):

    1. Key parity: run the en/de deep key comparison node script (the same one-liner used in Plan 01 Task 2 verify, embedded in this task's automated verify). It must report identical key sets — if any key was added during Plans 02/03 to only one file, add it to the other with the correct translation.

    2. Full test suite: `pnpm --filter @kartex/frontend test --run` — all tests (65 existing plus LanguageToggle) green.

    3. TypeScript: `pnpm --filter @kartex/frontend typecheck` — clean. Type-safe keys (CustomTypeOptions) catch any `t('typo.key')` — fix any reported missing keys.

    4. Hardcoded-string sweep (I18N-02 manual check, partial automation): grep tsx files for JSX text that looks like an untranslated English sentence and review each match. Filter out comment and import lines so header comments do not produce false counts. Confirm any remaining matches are legitimately excluded per D-07 (user content, brand "Kartex", numeric literals, technical terms SM-2/KaTeX/Typst) or fix them.

    Record in the SUMMARY: the final key count, test pass count, typecheck result, and the list of intentionally-unwrapped strings (with the D-07 reason for each).
  </action>
  <verify>
    <automated>cd apps/frontend && node -e "const en=require('./src/locales/en.json'),de=require('./src/locales/de.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v!==null?flat(v,p+k+'.'):[p+k]);const ek=flat(en).sort(),dk=flat(de).sort();const miss=ek.filter(k=>!dk.includes(k)),ex=dk.filter(k=>!ek.includes(k));if(miss.length||ex.length){console.error('MISMATCH',{miss,ex});process.exit(1)}console.log('OK',ek.length)" && pnpm test --run && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - en.json and de.json have identical key sets (parity one-liner exits 0)
    - `pnpm --filter @kartex/frontend test --run` exits 0 — all tests green
    - `pnpm --filter @kartex/frontend typecheck` exits 0 — no missing-key type errors
    - Hardcoded-string grep sweep produces only D-07-excluded matches, each justified in the SUMMARY
  </acceptance_criteria>
  <done>en/de key parity confirmed, full test suite green, typecheck clean, and the hardcoded-string sweep shows only intentionally-excluded strings (documented in SUMMARY). Phase 9 success criteria all satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user content → t() interpolation | Deck titles, card text, tags, usernames, filenames must be interpolated as values, never used as t() keys (D-07) |
| backend error text → UI | Raw backend errors must not surface; only generic translated labels shown (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | t() key argument across pages | mitigate | All keys are compile-time string literals validated by CustomTypeOptions; user content (deck.title, card text, username, filename) is interpolated as values, never used as keys (D-07, RESEARCH.md §Security Domain) |
| T-09-06 | Information Disclosure | backend error text in toasts | mitigate | Error toasts use generic translated labels (common.somethingWrong / common.serverUnreachable); raw backend strings stay in console.error only (D-08) |
</threat_model>

<verification>
- en/de key-parity one-liner exits 0
- `pnpm --filter @kartex/frontend test --run` — all tests green
- `pnpm --filter @kartex/frontend typecheck` — clean
- Hardcoded-string grep sweep reviewed — only D-07 exclusions remain
</verification>

<success_criteria>
- All 9 page files fully wrapped in t() (I18N-02)
- document.title updates on language switch
- Toasts, form errors, aria-labels, plurals, interpolations all via t()
- User content never passed as a t() key (D-07); backend errors never shown raw (D-08)
- en/de key parity, full suite green, typecheck clean
- Phase 9 ROADMAP success criteria 1–3 satisfied
</success_criteria>

<output>
Create `.planning/phases/09-internationalization/09-03-SUMMARY.md` when done.
</output>
