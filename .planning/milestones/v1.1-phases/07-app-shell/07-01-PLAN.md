---
phase: 07-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/__tests__/AppShell.test.tsx
  - apps/frontend/vite.config.ts
  - apps/frontend/src/vite-env.d.ts
  - apps/frontend/src/components/AppShell.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03]

must_haves:
  truths:
    - "On screens narrower than 768px the sidebar is hidden (class `hidden md:flex` present on aside) and a hamburger button is visible (`flex md:hidden` on topbar header)"
    - "Tapping the hamburger sets drawerOpen=true; the drawer panel slides in from the left (translate-x-0) over page content without shifting the right column"
    - "Tapping the backdrop or clicking any nav link sets drawerOpen=false; the drawer slides out (-translate-x-full)"
    - "A footer is always visible at the bottom of the right column on every page, showing version v0.1.0, © Raphael Müßeler, GitHub link, and Docs link"
    - "All footer external links carry rel=noopener noreferrer"
    - "__APP_VERSION__ is a build-time constant (not runtime fetch) provided by Vite define in vite.config.ts"
  artifacts:
    - path: "apps/frontend/src/components/__tests__/AppShell.test.tsx"
      provides: "Failing-then-passing unit tests for SHELL-01, SHELL-02, SHELL-03"
    - path: "apps/frontend/vite.config.ts"
      provides: "define block with __APP_VERSION__ from createRequire"
      contains: "createRequire"
    - path: "apps/frontend/src/vite-env.d.ts"
      provides: "TypeScript global declaration for __APP_VERSION__"
      contains: "declare const __APP_VERSION__: string"
    - path: "apps/frontend/src/components/AppShell.tsx"
      provides: "Responsive app shell with mobile topbar, overlay drawer, and footer"
      min_lines: 185
  key_links:
    - from: "AppShell.tsx (hamburger button)"
      to: "drawer panel #mobile-nav-drawer"
      via: "aria-controls + id attributes + drawerOpen state"
      pattern: "aria-controls=\"mobile-nav-drawer\""
    - from: "vite.config.ts define block"
      to: "AppShell.tsx footer __APP_VERSION__ usage"
      via: "Vite build-time replacement"
      pattern: "__APP_VERSION__"
    - from: "drawer NavLink onClick handlers"
      to: "drawerOpen = false"
      via: "onClick={() => setDrawerOpen(false)}"
      pattern: "setDrawerOpen\\(false\\)"
---

<objective>
Implement the Phase 7 App Shell: add a mobile topbar with hamburger trigger (SHELL-01), an overlay drawer with 200ms slide animation and backdrop (SHELL-02), and a footer strip with build-time version injection (SHELL-03). All changes are confined to AppShell.tsx (~127 → ~195 lines), vite.config.ts, and vite-env.d.ts.

Purpose: Make the app usable on mobile without breaking the desktop layout; add attribution and version visibility to every page.

Output:
- AppShell.test.tsx — unit tests covering all three requirements (RED before implementation, GREEN after)
- vite.config.ts — define block injecting __APP_VERSION__ at build time via createRequire
- vite-env.d.ts — TypeScript global declaration for __APP_VERSION__
- AppShell.tsx — responsive shell with topbar, drawer, backdrop, footer (per D-01 through D-11)
</objective>

<execution_context>
@C:/Users/rapha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rapha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-app-shell/07-CONTEXT.md
@.planning/phases/07-app-shell/07-RESEARCH.md
@.planning/phases/07-app-shell/07-UI-SPEC.md
@.planning/phases/07-app-shell/07-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Create failing AppShell test stub (SHELL-01, SHELL-02, SHELL-03)</name>
  <files>apps/frontend/src/components/__tests__/AppShell.test.tsx</files>

  <read_first>
    apps/frontend/src/components/AppShell.tsx — current structure (127 lines); identifies imports, navItems shape, component export name
    apps/frontend/src/context/AuthContext.tsx — useAuth return shape: { user: User | null, loading: boolean, setUser, logout }; User has { id, username, role, isActive, createdAt }
    apps/frontend/src/context/ThemeContext.tsx — useTheme return shape: { theme: 'light'|'dark', toggleTheme }
    apps/frontend/src/components/__tests__/CardFlip.test.tsx — established vi.mock() pattern for this project; globals: true so no import of describe/it/expect needed
    apps/frontend/vitest.config.ts — environment: jsdom, globals: true, setupFiles includes @testing-library/jest-dom
  </read_first>

  <behavior>
    - Test SHELL-01a: AppShell renders an aside element that has class `hidden` in its className (mobile-hidden sidebar): query the aside element and assert its className includes the string "hidden"
    - Test SHELL-01b: AppShell renders a header element containing a button with aria-label "Open navigation menu" (hamburger) inside a container that has class "md:hidden"
    - Test SHELL-02a: Clicking the hamburger button causes the drawer panel with id "mobile-nav-drawer" to have class "translate-x-0" (open state)
    - Test SHELL-02b: Clicking the backdrop (div.fixed.inset-0 with aria-hidden="true") causes the drawer panel to have class "-translate-x-full" (closed state)
    - Test SHELL-02c: Clicking any NavLink inside the drawer causes the drawer to close (drawer panel has class "-translate-x-full")
    - Test SHELL-03a: AppShell renders a footer element containing the text "Raphael Müßeler"
    - Test SHELL-03b: The footer contains an anchor element with href containing "github.com/raphaelmue/kartex" and rel="noopener noreferrer"
    - Test SHELL-03c: The footer contains an anchor element with href containing "#readme" and rel="noopener noreferrer"
    - Test SHELL-03d: The footer contains text matching /v\d+\.\d+\.\d+/ (version string with v prefix)
  </behavior>

  <action>
    Create `apps/frontend/src/components/__tests__/AppShell.test.tsx`.

    Mocking requirements (all vi.mock() calls must appear before imports — use vi.hoisted() for any variables needed inside factory functions):

    1. Mock `@/context/AuthContext` — export `useAuth` returning `{ user: { id: 'u1', username: 'testuser', role: 'USER', isActive: true, createdAt: '2026-01-01' }, loading: false, setUser: vi.fn(), logout: vi.fn() }`.

    2. Mock `@/context/ThemeContext` — export `useTheme` returning `{ theme: 'light', toggleTheme: vi.fn() }`.

    3. Mock `react-router-dom` — re-export everything from the real module (use `vi.importActual`) but override `useLocation` to return `{ pathname: '/dashboard', search: '', hash: '', state: null, key: 'default' }`. Keep `MemoryRouter`, `NavLink`, `Outlet` from the real module so routing works.

    4. Mock `__APP_VERSION__` — Vite's define replaces the global at build time; in Vitest (jsdom), the global does not exist unless you define it. In the test file, before the first describe block, add:
       `(globalThis as Record<string, unknown>).__APP_VERSION__ = '0.1.0'`

    Render helper: wrap `<AppShell />` in `<MemoryRouter initialEntries={['/dashboard']}>` for every test (or use a `renderAppShell` helper that does this).

    All tests in this file will FAIL (RED) when created because AppShell.tsx does not yet have the topbar/drawer/footer. That is the intended starting state — Wave 1 (Task 2+3) makes them GREEN.

    Do NOT add any placeholder assertions that trivially pass (e.g., `expect(true).toBe(true)`). Every test must assert a real behavior introduced in Task 3.
  </action>

  <verify>
    <automated>cd apps/frontend && pnpm test --run src/components/__tests__/AppShell.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <done>
    AppShell.test.tsx exists with at least 9 test cases (SHELL-01: 2, SHELL-02: 3, SHELL-03: 4). Tests run and FAIL with messages like "Unable to find element" or "Expected to include... but received..." — NOT with import or syntax errors. A passing test suite at this stage means the assertions are wrong; fix them before proceeding.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 1a — Inject __APP_VERSION__ via Vite define (SHELL-03)</name>
  <files>apps/frontend/vite.config.ts, apps/frontend/src/vite-env.d.ts</files>

  <read_first>
    apps/frontend/vite.config.ts — current content (32 lines); add createRequire import + define block without touching plugins, resolve, server, or build keys
    apps/frontend/src/vite-env.d.ts — current content (1 line: /// reference types vite/client); append one line, do not remove the reference directive
    apps/frontend/package.json — version field is "0.1.0"; this is what createRequire reads
  </read_first>

  <action>
    Edit `apps/frontend/vite.config.ts` to add version injection.

    Add at the top of the file (before the `import react` line):
    - `import { createRequire } from 'module'`

    After the existing imports (before `export default defineConfig`), add:
    - `const require = createRequire(import.meta.url)`
    - `const pkg = require('./package.json') as { version: string }`

    Inside the `defineConfig({...})` object, add a new top-level `define` key (per D-11, per RESEARCH.md Pattern 3):
    - `define: { __APP_VERSION__: JSON.stringify(pkg.version) }`

    Do NOT modify `plugins`, `resolve`, `server`, or `build` sections. Do NOT add `resolveJsonModule` to any tsconfig — that approach is incompatible with the current moduleResolution setting (RESEARCH.md Pitfall 2).

    Append one line to `apps/frontend/src/vite-env.d.ts` (after the existing `/// <reference types="vite/client" />` line):
    - `declare const __APP_VERSION__: string`

    This declares the global constant TypeScript needs for `__APP_VERSION__` usage in AppShell.tsx footer.
  </action>

  <verify>
    <automated>cd apps/frontend && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>

  <done>
    `pnpm typecheck` exits with code 0 (no TypeScript errors). `vite.config.ts` contains the string `createRequire` and the `define` key. `vite-env.d.ts` contains `declare const __APP_VERSION__: string` on a line after the reference directive.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 1b — Implement mobile topbar, overlay drawer, and footer in AppShell.tsx (SHELL-01, SHELL-02, SHELL-03)</name>
  <files>apps/frontend/src/components/AppShell.tsx</files>

  <read_first>
    apps/frontend/src/components/AppShell.tsx — full current file (127 lines); all existing imports, navItems array, component body, and JSX structure must be preserved and extended
    apps/frontend/src/components/__tests__/AppShell.test.tsx — the failing tests from Task 1 define the exact observable behaviors this task must produce
    apps/frontend/src/components/ui/button.tsx — Button component for hamburger (variant="ghost" size="icon" pattern already used for theme toggle)
    .planning/phases/07-app-shell/07-UI-SPEC.md — authoritative source for all class names, dimensions, colors, aria attributes, and copy
    .planning/phases/07-app-shell/07-RESEARCH.md — Pattern 1 (right column wrapper), Pattern 2 (CSS transform drawer), Pattern 4 (NavLink onClick), Pattern 5 (page title derivation)
  </read_first>

  <behavior>
    - Aside element gains class `hidden md:flex` (hidden on mobile, visible on desktop) — per D-01, SHELL-01
    - A `<header>` with class `flex items-center gap-2 h-16 px-4 bg-card border-b border-border md:hidden` is the new mobile topbar — per D-01, D-02, UI-SPEC §Dimension 1
    - Header contains: hamburger Button (variant="ghost" size="icon" aria-label="Open navigation menu" aria-expanded={drawerOpen} aria-controls="mobile-nav-drawer") with Menu icon h-5 w-5; then brand span "Kartex" text-xl font-bold; then page title span (text-sm text-foreground) derived from navItems — per D-02, D-03, UI-SPEC §Dimension 2
    - drawerOpen state: useState(false) declared inside AppShell — per UI-SPEC §Dimension 5
    - Drawer panel: div with id="mobile-nav-drawer", class includes "fixed top-0 left-0 h-full w-60 bg-card border-r border-border z-50 flex flex-col transition-transform duration-200 ease-in-out" plus cn() conditional "translate-x-0" when drawerOpen else "-translate-x-full"; aria-hidden={!drawerOpen} — per D-04, RESEARCH.md Pattern 2, UI-SPEC §Dimension 5
    - Drawer nav mirrors desktop aside exactly: same navItems map with identical NavLink className function PLUS onClick={() => setDrawerOpen(false)} on each NavLink; same conditional Admin NavLink — per D-06
    - Drawer user area mirrors desktop: username span, theme toggle Button, logout Button — all as siblings inside the drawer's flex flex-col
    - Backdrop: conditional render (`{drawerOpen && ...}`), div with class "fixed inset-0 bg-black/50 z-40", aria-hidden="true", onClick={() => setDrawerOpen(false)} — per D-05, D-06, UI-SPEC §Dimension 1
    - Right column wrapper: new `<div className="flex flex-col flex-1 min-h-0">` wrapping the topbar header, main, and footer — min-h-0 is mandatory for Safari flex overflow fix (RESEARCH.md Pitfall 1)
    - Main element: unchanged className "flex-1 overflow-y-auto bg-background p-8" — still contains `<Outlet />`
    - Footer: `<footer className="h-10 shrink-0 border-t border-border flex items-center justify-between px-4 text-xs text-muted-foreground">` — per D-07, D-08, SHELL-03
    - Footer left: `<span>v{__APP_VERSION__}&nbsp;·&nbsp;© Raphael Müßeler</span>` — per D-09
    - Footer right: two `<a>` elements with target="_blank" rel="noopener noreferrer" — GitHub (href GITHUB_URL) labeled "GitHub", Docs (href DOCS_URL) labeled "Docs" — per D-10
    - Footer URL constants at top of component body: `const GITHUB_URL = 'https://github.com/raphaelmue/kartex'` and `const DOCS_URL = 'https://github.com/raphaelmue/kartex#readme'` — canonical URL per RESEARCH.md Pitfall 6
    - Page title derivation: `const location = useLocation()` added to component; `const currentLabel = navItems.find(item => location.pathname.startsWith(item.to))?.label ?? (location.pathname.startsWith('/admin') ? 'Admin' : 'Kartex')` — per D-03, RESEARCH.md Pattern 5, handles admin special case
    - Import additions to AppShell.tsx: add `Menu` to lucide-react import line; add `useLocation` to react-router-dom import line; add `useState` to react import (new)
    - Final file must be under 500 lines (projected ~195 lines)
  </behavior>

  <action>
    Edit `apps/frontend/src/components/AppShell.tsx` to implement all three requirements inline (no extraction to a separate file — 195 lines is well within the 500-line limit).

    Step 1 — Update imports:
    - Add `useState` import from 'react' (new top-level react import)
    - Add `Menu` to the lucide-react named import list (alongside BookOpen, Compass, etc.)
    - Add `useLocation` to the react-router-dom named import list (alongside NavLink, Outlet)

    Step 2 — Add state and derivation inside AppShell() component body, after the existing hook calls:
    - `const [drawerOpen, setDrawerOpen] = useState(false)`
    - `const location = useLocation()`
    - `const currentLabel = navItems.find(item => location.pathname.startsWith(item.to))?.label ?? (location.pathname.startsWith('/admin') ? 'Admin' : 'Kartex')`
    - `const GITHUB_URL = 'https://github.com/raphaelmue/kartex'`
    - `const DOCS_URL = 'https://github.com/raphaelmue/kartex#readme'`

    Step 3 — Modify the aside element: change its className from `"w-60 bg-card border-r border-border flex flex-col h-full"` to `"hidden md:flex w-60 bg-card border-r border-border flex-col h-full"` — note: replace `flex flex-col` with `md:flex flex-col` (the `flex` must not be unconditional; `md:flex` enables it only on desktop). The full class string: `"hidden md:flex w-60 bg-card border-r border-border flex-col h-full"`.

    Step 4 — Replace the current `<main>` direct child of the outer flex container with a right-column wrapper and its children. The outer `<div className="flex h-screen overflow-hidden">` gains a new second child:

    ```
    <div className="flex flex-col flex-1 min-h-0">
      [mobile topbar header — see below]
      [main element — unchanged content]
      [footer element — see below]
    </div>
    ```

    Step 5 — Mobile topbar header (new, inside right-column wrapper, before main):
    - `<header className="flex items-center gap-2 h-16 px-4 bg-card border-b border-border md:hidden">`
    - Inside: hamburger Button, brand span, page title span (see behavior section for exact attributes)
    - Hamburger button aria-expanded={drawerOpen} (boolean — React renders as aria-expanded="true" or aria-expanded="false")

    Step 6 — Backdrop (place outside the right-column wrapper, as a sibling of the outer flex div children, or immediately before the drawer panel — both work; position it after the aside element and before the right-column wrapper):
    - `{drawerOpen && <div className="fixed inset-0 bg-black/50 z-40" aria-hidden="true" onClick={() => setDrawerOpen(false)} />}`

    Step 7 — Drawer panel (place after the backdrop, still as a child of the outer flex root div OR as a portal-adjacent fixed element — since it is `position: fixed` the DOM placement does not affect visual position; place it immediately after the backdrop for logical grouping):
    - Always in DOM (not conditional render) for CSS transform animation
    - id="mobile-nav-drawer"
    - className uses cn() for conditional translate classes (see behavior section)
    - aria-hidden={!drawerOpen}
    - Contains full nav mirror: brand area div h-16, nav element (aria-label="Main navigation"), navItems.map with NavLink + onClick={() => setDrawerOpen(false)}, admin NavLink (conditional) + onClick={() => setDrawerOpen(false)}, user area div h-16

    Step 8 — Footer (inside right-column wrapper, after main):
    - `<footer className="h-10 shrink-0 border-t border-border flex items-center justify-between px-4 text-xs text-muted-foreground">`
    - Left span: `v{__APP_VERSION__}&nbsp;·&nbsp;© Raphael Müßeler`
    - Right div: two anchor elements with class `"hover:text-foreground underline-offset-4 hover:underline"`, target="_blank", rel="noopener noreferrer"

    Do NOT use fenced code blocks in this action — directive prose only.
    Do NOT add resolveJsonModule to tsconfig.
    Do NOT use display:none instead of translate for the drawer.
    Do NOT place backdrop at z-50 — it must be z-40 (behind the drawer at z-50).
  </action>

  <verify>
    <automated>cd apps/frontend && pnpm test --run src/components/__tests__/AppShell.test.tsx 2>&1 | tail -30</automated>
  </verify>

  <done>
    All 9+ AppShell tests pass (GREEN). File is under 500 lines. The aside has class "hidden md:flex". A header with class "md:hidden" exists. The drawer panel has id="mobile-nav-drawer". Footer contains "Raphael Müßeler" and two anchors with rel="noopener noreferrer". Running `pnpm test --run` across the full suite shows no regressions.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wave 2 — Full suite + typecheck verification gate</name>
  <files></files>

  <read_first>
    apps/frontend/src/components/AppShell.tsx — verify final line count is under 500 and __APP_VERSION__ usage is present
    apps/frontend/vite.config.ts — verify createRequire and define block are present
  </read_first>

  <action>
    Run the full frontend test suite and TypeScript typecheck. Do not skip any tests or suppress errors.

    Commands (in order):
    1. `pnpm --filter @kartex/frontend test --run` — all tests must pass
    2. `pnpm --filter @kartex/frontend typecheck` — must exit 0

    If any test fails: read the error output, identify which assertion is failing, trace back to AppShell.tsx to fix the implementation, then re-run. Do not modify test assertions to make tests pass — fix the implementation.

    If typecheck fails with errors in AppShell.tsx related to `__APP_VERSION__`: verify that `vite-env.d.ts` contains `declare const __APP_VERSION__: string` and that vitest.config.ts includes the src/ directory in the TypeScript path. Do not add resolveJsonModule.

    After both commands exit 0, verify security contract:
    - grep for `rel="noopener noreferrer"` in AppShell.tsx — must appear exactly twice (one per footer link). Use a filtered count: `grep -v '^//' apps/frontend/src/components/AppShell.tsx | grep -c 'noopener noreferrer'`
    - Confirm result is 2.
  </action>

  <verify>
    <automated>cd apps/frontend && pnpm test --run && pnpm typecheck</automated>
  </verify>

  <done>
    `pnpm test --run` exits 0 with all AppShell tests passing and no regressions in KartexRenderer or CardFlip tests. `pnpm typecheck` exits 0. The `grep` count for `noopener noreferrer` in AppShell.tsx equals 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build → Browser | __APP_VERSION__ is injected at build time by Vite define; the value originates from package.json version field (not user input) |
| Browser → External | Footer links open github.com in a new tab; target="_blank" without rel attributes would expose the opener context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Footer `<a target="_blank">` links | mitigate | `rel="noopener noreferrer"` on both footer anchors (GITHUB_URL and DOCS_URL); verified by Task 4 grep gate |
| T-07-02 | Tampering | `__APP_VERSION__` build constant | accept | Version is read from package.json at build time — not user input, not runtime-injectable. No sanitization needed; the value is a semver string under developer control. |
| T-07-03 | — | Drawer keyboard focus leak | mitigate | `aria-hidden={!drawerOpen}` on drawer panel div removes it from the accessibility tree when closed; prevents keyboard Tab into off-screen drawer |
</threat_model>

<verification>
## Phase Gate Verification

Run after all tasks complete:

1. Full test suite green: `pnpm --filter @kartex/frontend test --run`
2. TypeScript clean: `pnpm --filter @kartex/frontend typecheck`
3. Security gate: `grep -v '^//' apps/frontend/src/components/AppShell.tsx | grep -c 'noopener noreferrer'` must print `2`
4. File size: AppShell.tsx line count must be under 500

## Manual Verification (human-check — do after automated gate)

Open the app in a browser at 375px viewport width (Chrome DevTools device emulation):

1. SHELL-01: Sidebar is not visible; a topbar with hamburger icon and "Kartex" brand appears at the top
2. SHELL-01: The topbar shows the current page label (e.g., "Dashboard" when on /dashboard)
3. SHELL-02: Tapping the hamburger opens the left drawer as an overlay — the right content area does not shift
4. SHELL-02: A dark semi-transparent backdrop covers the content behind the drawer
5. SHELL-02: Tapping the backdrop closes the drawer with a 200ms slide animation
6. SHELL-02: Clicking a nav link inside the drawer closes the drawer and navigates
7. SHELL-03: A footer is visible at the bottom of the viewport on every page
8. SHELL-03: Footer shows the version string (v0.1.0), "© Raphael Müßeler", GitHub link, Docs link
9. At 1024px viewport (desktop): sidebar is visible, topbar is hidden, footer still visible
</verification>

<success_criteria>
- SHELL-01: `<aside>` has class `hidden md:flex`; `<header>` with hamburger button has class `md:hidden`
- SHELL-02: Drawer panel has `id="mobile-nav-drawer"`, `aria-hidden={!drawerOpen}`, transition classes; backdrop is conditional render with `bg-black/50 z-40`
- SHELL-03: `<footer>` exists with `h-10 shrink-0`; contains `v{__APP_VERSION__}`, "© Raphael Müßeler", GitHub link, Docs link; both links have `rel="noopener noreferrer"`
- Version injection: `vite.config.ts` uses `createRequire` to read package.json; `vite-env.d.ts` declares `__APP_VERSION__`
- All 9+ unit tests pass; full suite has zero regressions; typecheck exits 0
- AppShell.tsx is under 500 lines
</success_criteria>

<output>
Create `.planning/phases/07-app-shell/07-01-SUMMARY.md` when done.
</output>
```
