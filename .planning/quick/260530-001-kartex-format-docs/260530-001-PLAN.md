---
quick_id: 260530-001
slug: kartex-format-docs
description: Add .kartex format documentation so an LLM can generate decks from learning content
date: 2026-05-30
status: planned
must_haves:
  truths:
    - docs/kartex-format.md exists and is committed
    - File covers all parser-implemented features (header, card blocks, multi-line fields, comments, rich content)
    - LLM prompt template is included so the doc is self-contained for generation
    - Import bundle format (.kartex.zip) is documented
  artifacts:
    - docs/kartex-format.md
---

# Quick Task 260530-001: .kartex Format Documentation

## Goal

Write user-facing documentation for the `.kartex` file format at `docs/kartex-format.md`.

The primary use case: an LLM receives this file + learning content and produces a valid `.kartex` deck.

## Context

- Parser: `packages/shared/src/lib/kartex-parser.ts`
- Existing spec stub: `docs/design.md §7`
- Sample files: `test-fixtures/sample.kartex`, `test-fixtures/sample-warnings.kartex`
- Todo originated from: `.planning/todos/pending/2026-05-28-add-kartex-format-documentation.md`

## Tasks

### T-01: Write docs/kartex-format.md

**Files:** `docs/kartex-format.md`

**Action:**
Create comprehensive documentation covering:
1. Introduction and purpose (LLM-generation use case)
2. Full file structure with annotated example
3. YAML header fields: `deck:` (required), `author:` (optional), `tags:` (optional array)
4. Card block delimiters: `:: card` … `::`
5. Card fields: `front:` (required), `back:` (required), `tags:` (optional)
6. Multi-line field values (continue until next field name)
7. Comment syntax (`# …`, excluding `#typst`)
8. Rich content: inline math (`$…$`), block math (`$$…$$`), Typst blocks (`#typst`), code fences, `media://` references
9. Lenient parsing rules (D-01: malformed cards skipped; D-02: header is fatal)
10. Import bundle format (`.kartex.zip`)
11. LLM system prompt template for generation
12. Multiple worked examples

**Verify:** File exists at `docs/kartex-format.md`, covers all sections above.

**Done:** Committed with message `docs: add .kartex format reference`
