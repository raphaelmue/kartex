---
quick_id: 260526-001
slug: quality-baseline
description: "Quality baseline: record tests-per-phase decision, fix Phase 2 code smells, install ESLint + Prettier"
date: 2026-05-26
status: planned
tasks:
  - id: T1
    title: Record quality decision in planning docs
    files:
      - .planning/PROJECT.md
      - .planning/ROADMAP.md
    action: Add "tests per phase (Option A)" to Key Decisions in PROJECT.md; add quality note to ROADMAP.md cross-cutting constraints; add ESLint/Prettier as baseline infrastructure note
    done: PROJECT.md Key Decisions has entry; ROADMAP.md has quality note
  - id: T2
    title: Fix Phase 2 code smells
    files:
      - apps/frontend/src/pages/DecksPage.tsx
      - apps/frontend/src/App.tsx
      - apps/frontend/src/components/CardEditorModal.tsx
      - apps/frontend/src/components/DeckFormModal.tsx
    action: |
      1. DecksPage.tsx: replace silent catch on load with toast.error()
      2. App.tsx: add <ErrorBoundary> component wrapping routes
      3. CardEditorModal.tsx:79 — replace card!.id with card?.id guard
      4. DeckFormModal.tsx:73 — replace deck!.id with deck?.id guard
    done: No silent catches, ErrorBoundary exists in App.tsx, no ! assertions on optional props
  - id: T3
    title: Install ESLint + Prettier with TypeScript support
    files:
      - package.json
      - eslint.config.js
      - .prettierrc
      - apps/frontend/package.json (add lint script)
      - apps/backend/package.json (add lint script)
      - packages/shared/package.json (add lint script)
    action: |
      Install: eslint @eslint/js typescript-eslint eslint-plugin-react-hooks prettier eslint-config-prettier
      Create eslint.config.js (flat config) at root for all workspaces
      Create .prettierrc with standard settings
      Add "lint" and "format" scripts to root and workspace package.json files
    done: yarn lint runs without errors; yarn format applies Prettier formatting
must_haves:
  - truths:
    - Tests-per-phase decision recorded in PROJECT.md Key Decisions
    - ESLint installs and runs on all three workspaces without error
    - No silent catch blocks remain on page load paths
    - App.tsx has an ErrorBoundary wrapping the route tree
    - Non-null assertions on optional props replaced with safe guards
