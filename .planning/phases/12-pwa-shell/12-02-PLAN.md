---
phase: 12-pwa-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/index.ts
autonomous: true
requirements:
  - PWA-04
  - PWA-05

must_haves:
  truths:
    - "Every HTTP response from the Hono server carries Cross-Origin-Opener-Policy: same-origin"
    - "Every HTTP response from the Hono server carries Cross-Origin-Embedder-Policy: require-corp"
    - "GET /sw.js returns Cache-Control: no-store"
    - "GET /workbox-*.js files return Cache-Control: no-store"
  artifacts:
    - path: "apps/backend/src/index.ts"
      provides: "Hono server entry with secureHeaders middleware + sw.js route"
      contains: "secureHeaders"
  key_links:
    - from: "apps/backend/src/index.ts"
      to: "hono/secure-headers"
      via: "import { secureHeaders } from 'hono/secure-headers'"
      pattern: "secureHeaders"
    - from: "app.use('*', secureHeaders(...))"
      to: "all Hono responses"
      via: "first registered middleware"
      pattern: "crossOriginEmbedderPolicy"
---

<objective>
Add COEP/COOP security headers as the first middleware in the Hono server, and add explicit routes for sw.js and workbox-*.js with Cache-Control: no-store before the serveStatic catch-all.

Purpose: PWA-04 requires COOP/COEP on all production responses (currently only present in the Vite dev server via server.headers). PWA-05 requires sw.js and workbox chunks to bypass HTTP caching so redeployments propagate immediately. Both changes are in apps/backend/src/index.ts only — no new packages needed (hono/secure-headers ships with hono ^4.7.9; readFileSync is already imported).

Output: Modified apps/backend/src/index.ts with secureHeaders middleware at position 0, explicit /sw.js GET route before serveStatic, and onFound callback on the existing serveStatic call.
</objective>

<execution_context>
@C:/Users/rapha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rapha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-pwa-shell/12-RESEARCH.md
@.planning/phases/12-pwa-shell/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add secureHeaders middleware (COEP/COOP) as position-0 middleware</name>
  <read_first>
    - apps/backend/src/index.ts (full file — must see existing middleware registration order before inserting; PATTERNS.md has excerpt but read the live file)
    - apps/backend/package.json (confirm hono version is ^4.7.9 — secureHeaders is bundled with hono, no install needed)
    - .planning/phases/12-pwa-shell/12-RESEARCH.md (Pattern 2: exact secureHeaders config; Anti-Patterns section on middleware order)
    - .planning/phases/12-pwa-shell/12-PATTERNS.md (Modification A and B in the index.ts section)
  </read_first>
  <action>
    Make two edits to apps/backend/src/index.ts:

    Edit 1 — Add import after the existing `import { cors } from 'hono/cors'` line:
      import { secureHeaders } from 'hono/secure-headers'

    Edit 2 — Insert a new middleware block immediately before the existing "1. Health endpoint" comment block (which is currently the first app.use/app.get call after `const app = new Hono()`):

      // ─── 0. Cross-origin isolation headers (PWA-04 + Typst WASM in production) ──
      // MUST be first — applies COOP/COEP to ALL responses (API, static, SPA fallback)
      // Dev-only workaround in vite.config.ts server.headers is superseded by this
      app.use(
        '*',
        secureHeaders({
          crossOriginOpenerPolicy: 'same-origin',
          crossOriginEmbedderPolicy: 'require-corp',
        }),
      )

    Keep the existing numbered-step comment convention already present in the file (// ─── 1., // ─── 2., etc.). The secureHeaders block uses step 0 to signal it runs before all other middleware.

    Do NOT change any existing middleware, routes, or their registration order. Do NOT add any other options to secureHeaders beyond crossOriginOpenerPolicy and crossOriginEmbedderPolicy.

    Note: crossOriginEmbedderPolicy is DISABLED by default in secureHeaders — it MUST be explicitly set to 'require-corp'. Do not omit it assuming it defaults to require-corp.
  </action>
  <verify>
    <automated>
      Select-String -Path apps/backend/src/index.ts -Pattern "crossOriginEmbedderPolicy" -Quiet
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/backend/src/index.ts contains `import { secureHeaders } from 'hono/secure-headers'`
    - apps/backend/src/index.ts contains `crossOriginOpenerPolicy: 'same-origin'`
    - apps/backend/src/index.ts contains `crossOriginEmbedderPolicy: 'require-corp'`
    - The secureHeaders app.use('*', ...) call appears BEFORE the `app.get('/api/health', ...)` line in the file
    - TypeScript compilation passes: yarn workspace @kartex/backend typecheck
  </acceptance_criteria>
  <done>secureHeaders middleware is registered as step 0, before the health endpoint, applying COOP/COEP to all Hono responses</done>
</task>

<task type="auto">
  <name>Task 2: Add /sw.js no-store route and onFound callback to serveStatic</name>
  <read_first>
    - apps/backend/src/index.ts (must re-read after Task 1 edits to see current state — specifically lines around the existing serveStatic call at step 7 and the SPA fallback at step 8)
    - .planning/phases/12-pwa-shell/12-RESEARCH.md (Pattern 3: sw.js route + onFound callback; the readFileSync + c.body() pattern)
    - .planning/phases/12-pwa-shell/12-PATTERNS.md (Modification C: exact code shape; Shared Patterns: readFileSync error handling from SPA fallback)
  </read_first>
  <action>
    Make two edits to apps/backend/src/index.ts, both targeting the area between the existing step 6 (admin routes) and step 7 (serveStatic):

    Edit 1 — Insert a new explicit GET route for /sw.js BEFORE the existing serveStatic line at step 7:

      // ─── 7a. sw.js — Cache-Control: no-store (PWA-05) ───────────────────────────
      // Explicit route before serveStatic catch-all — prevents browser from caching the SW
      app.get('/sw.js', (c) => {
        try {
          const sw = readFileSync('./public/sw.js', 'utf8')
          c.header('Cache-Control', 'no-store')
          c.header('Content-Type', 'application/javascript')
          return c.body(sw)
        } catch {
          return c.text('Service worker not found', 404)
        }
      })

    Edit 2 — Replace the existing bare serveStatic call (currently `app.use('*', serveStatic({ root: './public' }))`) with a version that includes an onFound callback to set no-store on workbox chunks. Relabel the comment from step 7 to step 7b:

      // ─── 7b. Static files — no-store for workbox chunks (PWA-05), default for others
      app.use(
        '*',
        serveStatic({
          root: './public',
          onFound: (filePath, c) => {
            if (/workbox-[^/]+\.js$/.test(filePath)) {
              c.header('Cache-Control', 'no-store')
            }
          },
        }),
      )

    The readFileSync import is already at line 1 of index.ts — do NOT add it again. The serveStatic import is already present — do NOT add it again.

    The SPA fallback at step 8 (app.get('*', ...) with readFileSync('./public/index.html', ...)) must remain unchanged after step 7b.

    Use the parameter name filePath (not path) for the onFound callback to avoid shadowing the Node.js 'path' module if it were imported later.
  </action>
  <verify>
    <automated>
      Select-String -Path apps/backend/src/index.ts -Pattern "workbox-" -Quiet
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/backend/src/index.ts contains `app.get('/sw.js', (c) => {`
    - apps/backend/src/index.ts contains `c.header('Cache-Control', 'no-store')` inside the /sw.js route body
    - apps/backend/src/index.ts contains `onFound:` inside the serveStatic call
    - apps/backend/src/index.ts contains the workbox regex pattern: `workbox-[^/]+\.js`
    - The /sw.js route appears BEFORE the serveStatic call in file order
    - The SPA fallback `app.get('*', (c) => {` with readFileSync('./public/index.html') is still present after serveStatic
    - TypeScript compilation passes: yarn workspace @kartex/backend typecheck
  </acceptance_criteria>
  <done>/sw.js is served with Cache-Control: no-store via an explicit route; workbox-*.js files get no-store via the onFound callback; serveStatic handles all other static files as before</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Hono | All HTTP responses must carry COOP/COEP to achieve cross-origin isolation |
| Browser HTTP cache → sw.js | Browser must not cache sw.js across deployments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering (side-channel) | All Hono responses — COEP missing | mitigate | secureHeaders middleware at position 0 with crossOriginEmbedderPolicy: 'require-corp' applies to every response |
| T-12-03 | Tampering | sw.js HTTP cache — stale SW after redeploy | mitigate | Explicit /sw.js route with Cache-Control: no-store before serveStatic; workbox chunks covered by onFound |
| T-12-04 | Information Disclosure | /api/* served from SW cache | accept | NetworkOnly rule in SW (planned in 12-03) ensures no API response is ever cached; no caching code exists server-side |
</threat_model>

<verification>
After both tasks complete:
- Run: yarn workspace @kartex/backend typecheck
  Expected: 0 errors
- Confirm secureHeaders is registered before health endpoint by reading file line order
- Confirm /sw.js route appears before serveStatic in file order
- (Production smoke test deferred to Plan 12-04 after the build is available)
</verification>

<success_criteria>
- apps/backend/src/index.ts compiles cleanly with TypeScript
- secureHeaders('*') with COOP + COEP is the first middleware registered (step 0)
- Explicit /sw.js GET route with Cache-Control: no-store exists before serveStatic
- serveStatic includes onFound callback that sets Cache-Control: no-store for paths matching /workbox-[^/]+\.js$/
</success_criteria>

<output>
Create .planning/phases/12-pwa-shell/12-02-SUMMARY.md when done
</output>
