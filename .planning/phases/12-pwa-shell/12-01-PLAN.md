---
phase: 12-pwa-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/public/logo.svg
  - apps/frontend/public/pwa-192x192.png
  - apps/frontend/public/pwa-512x512.png
  - apps/frontend/public/apple-touch-icon.png
  - apps/frontend/index.html
autonomous: false
requirements:
  - PWA-01

must_haves:
  truths:
    - "A square SVG logo exists at apps/frontend/public/logo.svg"
    - "PNG icons at 192x192 and 512x512 are present in apps/frontend/public/"
    - "apple-touch-icon.png is present in apps/frontend/public/"
    - "index.html contains theme-color meta and apple-touch-icon link tags"
  artifacts:
    - path: "apps/frontend/public/logo.svg"
      provides: "SVG source for icon generation"
      contains: "<svg"
    - path: "apps/frontend/public/pwa-192x192.png"
      provides: "192x192 PWA icon referenced in manifest"
    - path: "apps/frontend/public/pwa-512x512.png"
      provides: "512x512 PWA icon referenced in manifest"
    - path: "apps/frontend/public/apple-touch-icon.png"
      provides: "iOS Safari home screen icon"
    - path: "apps/frontend/index.html"
      provides: "HTML entry point with PWA meta tags"
      contains: "theme-color"
  key_links:
    - from: "apps/frontend/index.html"
      to: "apps/frontend/public/apple-touch-icon.png"
      via: '<link rel="apple-touch-icon">'
      pattern: 'apple-touch-icon'
---

<objective>
Create the Kartex logo SVG, generate PWA icon assets from it, and add required PWA meta tags to index.html. This plan gates on a human legitimacy check for @vite-pwa/assets-generator before running the CLI tool.

Purpose: PWA installability (PWA-01) requires a valid web app manifest with 192x192 and 512x512 PNG icons. The @vite-pwa/assets-generator CLI generates all required icon variants from a single SVG source. The apple-touch-icon link in index.html ensures Lighthouse PWA audit passes and iOS Safari shows the correct home screen icon.

Output: logo.svg, pwa-192x192.png, pwa-512x512.png, apple-touch-icon.png in apps/frontend/public/; updated apps/frontend/index.html with theme-color meta and apple-touch-icon link.
</objective>

<execution_context>
@C:/Users/rapha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rapha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-pwa-shell/12-RESEARCH.md
@.planning/phases/12-pwa-shell/12-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify @vite-pwa/assets-generator package legitimacy before install</name>
  <what-built>
    The RESEARCH.md Package Legitimacy Audit flags @vite-pwa/assets-generator as [ASSUMED] — slopcheck was unavailable in the research environment. Before this package is run via npx, human confirmation is required.
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@vite-pwa/assets-generator
    2. Confirm: publisher is the "vite-pwa" npm org (same org as vite-plugin-pwa)
    3. Confirm: weekly downloads are in the hundreds of thousands range
    4. Confirm: the package links to github.com/vite-pwa/assets-generator
    5. Confirm: no suspicious postinstall scripts listed under "Scripts" on the npm page
  </how-to-verify>
  <resume-signal>Type "approved" once all five checks pass, or describe what you found if any check fails</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create logo SVG and generate PWA icons</name>
  <read_first>
    - apps/frontend/public/ (directory listing — confirm no existing logo.svg or pwa-*.png before creating)
    - .planning/phases/12-pwa-shell/12-RESEARCH.md (Pattern 4: icon generation command and expected output files)
    - .planning/phases/12-pwa-shell/12-PATTERNS.md (No Analog Found section: confirms no existing icon assets in project)
  </read_first>
  <action>
    Create apps/frontend/public/logo.svg as a minimal square Kartex letter-mark SVG. The SVG must meet these constraints: viewBox="0 0 512 512", a filled background rectangle (color #4f46e5 — Indigo-600, matching the app's primary color scheme), and a centered white letter "K" rendered using a path or text element with font-size approximately 320px. The SVG must be valid and self-contained with no external references (no xlink:href to external fonts).

    After writing logo.svg, run the assets generator:
      npx @vite-pwa/assets-generator --preset minimal-2023 apps/frontend/public/logo.svg

    The command outputs into apps/frontend/public/ by default. Confirm the following files were created: pwa-192x192.png, pwa-512x512.png, apple-touch-icon.png. If the command also creates other files (favicon.ico, pwa-64x64.png, etc.), keep them — they are harmless.

    Do NOT manually create placeholder PNGs — the generator must produce them so they are correctly sized and formatted for the Lighthouse PWA audit.
  </action>
  <verify>
    <automated>
      Test-Path apps/frontend/public/pwa-192x192.png -and (Test-Path apps/frontend/public/pwa-512x512.png) -and (Test-Path apps/frontend/public/apple-touch-icon.png)
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/frontend/public/logo.svg exists and contains viewBox="0 0 512 512"
    - apps/frontend/public/pwa-192x192.png exists (binary PNG, non-zero bytes)
    - apps/frontend/public/pwa-512x512.png exists (binary PNG, non-zero bytes)
    - apps/frontend/public/apple-touch-icon.png exists (binary PNG, non-zero bytes)
    - Running `npx @vite-pwa/assets-generator --preset minimal-2023 apps/frontend/public/logo.svg` completes without error output
  </acceptance_criteria>
  <done>Three PNG icon files and the source SVG exist in apps/frontend/public/; the assets generator ran cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Add PWA meta tags to index.html</name>
  <read_first>
    - apps/frontend/index.html (current file state — PATTERNS.md confirmed it has 13 lines; must see exact current content before editing)
    - .planning/phases/12-pwa-shell/12-RESEARCH.md (Pattern 5: exact meta tag content)
    - .planning/phases/12-pwa-shell/12-PATTERNS.md (index.html modification pattern — two tags to add after viewport meta)
  </read_first>
  <action>
    Edit apps/frontend/index.html to add two tags inside &lt;head&gt; immediately after the existing viewport meta tag (line 4):

      &lt;meta name="theme-color" content="#ffffff" /&gt;
      &lt;link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180" /&gt;

    Do NOT add &lt;link rel="manifest"&gt; manually — vite-plugin-pwa injects it automatically during build. Do NOT change any other existing content. The final &lt;head&gt; order must be: charset meta, viewport meta, theme-color meta, apple-touch-icon link, title.
  </action>
  <verify>
    <automated>
      Select-String -Path apps/frontend/index.html -Pattern 'theme-color' -Quiet
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/frontend/index.html contains exactly: &lt;meta name="theme-color" content="#ffffff" /&gt;
    - apps/frontend/index.html contains exactly: &lt;link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180" /&gt;
    - The file does NOT contain &lt;link rel="manifest"&gt; (vite-plugin-pwa injects this during build)
    - All original tags (charset, viewport, title, script) are still present and unmodified
  </acceptance_criteria>
  <done>index.html has theme-color meta and apple-touch-icon link inside &lt;head&gt;</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx → local filesystem | @vite-pwa/assets-generator CLI writes PNG files to public/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | npx @vite-pwa/assets-generator install | mitigate | Blocking human checkpoint (Task 1) requires manual npmjs.com legitimacy verification before CLI runs |
| T-12-SC | Tampering | npm/npx installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] package before install |
</threat_model>

<verification>
After Task 3 completes:
- Confirm apps/frontend/public/ contains: logo.svg, pwa-192x192.png, pwa-512x512.png, apple-touch-icon.png
- Confirm apps/frontend/index.html contains both meta tags
- Run existing frontend test suite to confirm no regression: yarn workspace @kartex/frontend test --run
</verification>

<success_criteria>
- PWA icon assets exist in apps/frontend/public/ at the sizes required by the manifest (192x192, 512x512)
- apple-touch-icon.png exists for iOS Safari / Lighthouse compliance
- index.html carries theme-color and apple-touch-icon meta tags required by Lighthouse PWA audit
</success_criteria>

<output>
Create .planning/phases/12-pwa-shell/12-01-SUMMARY.md when done
</output>
