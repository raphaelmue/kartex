---
phase: 27-zip-deck-update
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - apps/frontend/src/pages/DeckDetailPage.tsx
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
autonomous: true
requirements:
  - DECKU-01
user_setup: []

must_haves:
  truths:
    - "The deck-update file picker on DeckDetailPage offers both .kartex and .kartex.zip files in the OS file dialog"
    - "The deck-update parse-error copy mentions .kartex.zip in both English and German"
  artifacts:
    - "apps/frontend/src/pages/DeckDetailPage.tsx with accept='.kartex,.kartex.zip'"
    - "deckUpdate.parseError updated in en.json and de.json"
  key_links:
    - "DeckDetailPage update file input accept attribute -> DeckUpdateModal -> backend zip apply path (27-01)"
---

<objective>
Expose the zip deck-update capability in the frontend: the deck-update file picker accepts `.kartex.zip` (not just `.kartex`), and the parse-error copy is updated to mention zip in both locales.

Purpose: Completes DECKU-01 on the client side so a deck owner can actually choose a `.kartex.zip` bundle from the OS file dialog. The backend (27-01) already accepts and processes it.

Output: One attribute change in DeckDetailPage.tsx + a parity i18n copy update in en.json and de.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-zip-deck-update/27-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 27-02)

Modified files only (no new symbols):
- `apps/frontend/src/pages/DeckDetailPage.tsx` — update file input `accept` attribute
- `apps/frontend/src/locales/en.json` — `deckUpdate.parseError` copy
- `apps/frontend/src/locales/de.json` — `deckUpdate.parseError` copy (German parity)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Accept .kartex.zip in the deck-update file picker</name>
  <files>apps/frontend/src/pages/DeckDetailPage.tsx</files>
  <read_first>
    - apps/frontend/src/pages/DeckDetailPage.tsx (the hidden update file input, around line 567–578, ref updateFileInputRef)
  </read_first>
  <action>
On the hidden file input referenced by `updateFileInputRef` (the deck-update picker), change the attribute `accept=".kartex"` to `accept=".kartex,.kartex.zip"`. Do not touch the `onChange` handler, the `sr-only` class, `aria-hidden`, or the `ref`. This is the only change in this file. Note: this `accept` value is a client-side convenience filter only — it is NOT a security control; the backend re-validates the file extension and magic bytes (27-01).
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend build</automated>
  </verify>
  <acceptance_criteria>
    - The update file input in DeckDetailPage.tsx has `accept=".kartex,.kartex.zip"`
    - No other attribute on that input changed
    - `yarn workspace @kartex/frontend build` succeeds
  </acceptance_criteria>
  <done>The deck-update file picker offers both .kartex and .kartex.zip in the OS dialog.</done>
</task>

<task type="auto">
  <name>Task 2: Update deckUpdate.parseError copy in en.json and de.json (parity)</name>
  <files>apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/locales/en.json (deckUpdate section, around line 420–437)
    - apps/frontend/src/locales/de.json (deckUpdate section, around line 420–437)
  </read_first>
  <action>
In `en.json`, change the `deckUpdate.parseError` value from `"Could not parse the file. Check it is a valid .kartex file."` to `"Could not parse the file. Check it is a valid .kartex or .kartex.zip file."`.

In `de.json`, change the matching `deckUpdate.parseError` value to the German equivalent mentioning both formats, e.g. `"Datei konnte nicht verarbeitet werden. Bitte prüfen Sie, ob es eine gültige .kartex- oder .kartex.zip-Datei ist."`.

Both files MUST be edited in this same task (i18n parity — a missing de key falls back to the raw key string, per the project i18n convention). Do not add, remove, or rename any other key. Keep JSON valid (no trailing commas).
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend build</automated>
  </verify>
  <acceptance_criteria>
    - `en.json` `deckUpdate.parseError` contains the substring `.kartex.zip`
    - `de.json` `deckUpdate.parseError` contains the substring `.kartex.zip`
    - Both JSON files remain syntactically valid (build/typecheck passes)
    - No other i18n key was added, removed, or renamed
  </acceptance_criteria>
  <done>Parse-error copy mentions .kartex.zip in both locales with no key drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser file dialog -> app | The HTML `accept` attribute is an advisory client-side filter; it is not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-07 | Spoofing | client `accept` attribute | low | accept | The `accept=".kartex,.kartex.zip"` filter is advisory only; a user can still pick any file. Authoritative validation (extension + magic bytes + owner gate) is enforced server-side in deckUpdate.ts (27-01), so client bypass has no security impact |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced by this plan — frontend-only string/attribute edits |
</threat_model>

<verification>
- `yarn workspace @kartex/frontend build` — frontend builds with updated copy
- Manual grep: `accept=".kartex,.kartex.zip"` present in DeckDetailPage.tsx; `.kartex.zip` present in the `parseError` value of both en.json and de.json
</verification>

<success_criteria>
- DECKU-01 (client side): the deck-update file picker accepts both `.kartex` and `.kartex.zip`; user-facing parse-error copy reflects both formats in English and German
</success_criteria>

<output>
Create `.planning/phases/27-zip-deck-update/27-02-SUMMARY.md` when done
</output>
