---
phase: 16-import-update-feature
plan: 03
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - apps/frontend/src/components/DeckUpdateModal.tsx
  - apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx
autonomous: true
requirements: [IMP-01, IMP-02, IMP-06]
must_haves:
  truths:
    - "DeckUpdateModal auto-triggers preview fetch when opened with a File object"
    - "Uploading state renders Loader2 spinner and 'Uploading...' label"
    - "Previewing state renders 2x2 diff chip grid with correct counts and keepRemoved Switch (default: true)"
    - "Applying state disables the Apply button (disabled + aria-busy=true)"
    - "Error state wraps error text in role=alert"
    - "On successful apply, toast.success fires, onSuccess() called, modal closes"
    - "All 6 frontend stubs (T-16-FE-01..06) are replaced with passing assertions"
  artifacts:
    - path: "apps/frontend/src/components/DeckUpdateModal.tsx"
      provides: "Two-phase (preview/apply) Dialog component for deck update flow"
      exports: ["DeckUpdateModal"]
    - path: "apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx"
      provides: "Full test coverage for DeckUpdateModal"
      contains: "T-16-FE-01"
  key_links:
    - from: "apps/frontend/src/components/DeckUpdateModal.tsx"
      to: "/api/decks/:id/update/preview"
      via: "api.postForm in runPreview()"
      pattern: "api\\.postForm.*update/preview"
    - from: "apps/frontend/src/components/DeckUpdateModal.tsx"
      to: "/api/decks/:id/update/apply"
      via: "api.postForm in runApply()"
      pattern: "api\\.postForm.*update/apply"
---

<objective>
Create `DeckUpdateModal.tsx` — a two-phase Dialog component (uploading → previewing → applying → done/error) for the deck import-update flow. Replace the 6 Wave 0 frontend stubs with real assertions in `DeckUpdateModal.test.tsx`.

The component manages its own fetch lifecycle: auto-triggers preview on open, displays diff counts and keepRemoved toggle, and posts the file again on apply. It does not store server-side session state.

Purpose: Delivers the complete frontend modal for IMP-01, IMP-02, IMP-06 visual contract per UI-SPEC §DeckUpdateModal.
Output: `DeckUpdateModal.tsx` (new), `DeckUpdateModal.test.tsx` (6 stubs → real tests).
</objective>

<execution_context>
@C:/Users/rapha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/rapha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-import-update-feature/16-RESEARCH.md
@.planning/phases/16-import-update-feature/16-PATTERNS.md
@.planning/phases/16-import-update-feature/16-UI-SPEC.md
@.planning/phases/16-import-update-feature/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DeckUpdateModal.tsx</name>
  <files>apps/frontend/src/components/DeckUpdateModal.tsx</files>
  <read_first>
    - apps/frontend/src/components/CardEditorModal.tsx (role-match analog — Dialog structure, open/onOpenChange props, useEffect auto-trigger, state machine, api call pattern, toast.success, footer buttons)
    - apps/frontend/src/components/ui/dialog.tsx (confirm Dialog/DialogContent/DialogHeader/DialogFooter/DialogTitle/DialogDescription import names)
    - apps/frontend/src/components/ui/switch.tsx (confirm Switch import name)
    - apps/frontend/src/lib/api.ts (confirm api.postForm signature)
    - apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx (test pattern analog for Wave 0 stubs replacement)
  </read_first>
  <behavior>
    - DeckUpdateModalProps: `{ open: boolean; onOpenChange: (open: boolean) => void; deckId: string; file: File | null; onSuccess: () => void }`
    - State type `UpdateStep = 'uploading' | 'previewing' | 'applying' | 'done' | 'error'`
    - `useState<UpdateStep>('uploading')` — initial state is uploading
    - `useState<{ added: number; updated: number; unchanged: number; removed: number } | null>(null)` for preview
    - `useState(true)` for keepRemoved
    - `useState<string | null>(null)` for errorMsg
    - useEffect on [open, file]: when `open && file`, call `runPreview()` (auto-trigger)
    - `runPreview`: append file to FormData, `api.postForm('/api/decks/${deckId}/update/preview', formData)` → setStep('previewing') + setPreview(data) on success; setStep('error') + setErrorMsg on failure
    - `runApply`: append file + String(keepRemoved) to FormData, `api.postForm('/api/decks/${deckId}/update/apply', formData)` → setStep('done') + toast.success(t('deckUpdate.successToast')) + onSuccess() + onOpenChange(false) on success; setStep('error') + setErrorMsg on failure
    - Uploading state body: `<Loader2 className="h-6 w-6 animate-spin" aria-hidden="true" />` + `<p className="text-sm text-muted-foreground">{t('deckUpdate.uploading')}</p>`; no footer buttons
    - Previewing state body: diff 2x2 grid + Switch row; footer: Cancel (outline) + Apply (default, disabled when step==='applying')
    - Applying state body: same Loader2 + `t('deckUpdate.applying')` label; Apply button disabled + aria-busy=true
    - Error state body: `<div role="alert"><p className="text-sm font-semibold text-foreground">{t('deckUpdate.errorTitle')}</p><p className="text-sm text-muted-foreground">{errorMsg}</p></div>`; footer: Close button only
    - Diff chip grid: `<div className="grid grid-cols-2 gap-3">` with 4 chips; each chip: `<div className="border border-border rounded-lg p-4 min-h-[44px]" role="region" aria-label={label}>` with `<p className="text-xs font-normal text-muted-foreground uppercase tracking-wide">{label}</p>` and `<p className="text-xl font-semibold text-foreground">{value}</p>`
    - keepRemoved Switch row: `<div className="flex items-start gap-3 mt-4 min-h-[44px]">` with Switch `id="keep-removed-switch"` + label `htmlFor="keep-removed-switch"`
    - Dialog max-w-md; DialogTitle always rendered (Radix a11y requirement)
    - Loading states: `aria-busy="true"` on loading container divs; Loader2 `aria-hidden="true"`
  </behavior>
  <action>
    Create `apps/frontend/src/components/DeckUpdateModal.tsx` as a new file.

    Required imports:
    - `{ useEffect, useState }` from `'react'`
    - `{ useTranslation }` from `'react-i18next'`
    - `{ toast }` from `'sonner'`
    - `{ api }` from `'@/lib/api'`
    - `{ Button }` from `'@/components/ui/button'`
    - `{ Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle }` from `'@/components/ui/dialog'`
    - `{ Switch }` from `'@/components/ui/switch'`
    - `{ Loader2 }` from `'lucide-react'`

    Export as named export: `export function DeckUpdateModal({ open, onOpenChange, deckId, file, onSuccess }: DeckUpdateModalProps)`

    State management:
    - `step`: `UpdateStep` with initial `'uploading'`
    - `preview`: `{ added: number; updated: number; unchanged: number; removed: number } | null` with initial `null`
    - `keepRemoved`: `boolean` with initial `true`
    - `errorMsg`: `string | null` with initial `null`

    State reset on close: when `open` changes to false, reset step to `'uploading'`, preview to `null`, errorMsg to `null`, keepRemoved to `true`.

    `runPreview` async function:
    - `setStep('uploading')`
    - Build `new FormData()`, append `'file', file`
    - `const res = await api.postForm('/api/decks/${deckId}/update/preview', formData)`
    - On success (res.ok or check by try/catch): parse JSON, `setPreview(data)`, `setStep('previewing')`
    - On error: `setErrorMsg(err.message ?? t('deckUpdate.parseError'))`, `setStep('error')`

    `runApply` async function:
    - `setStep('applying')`
    - Build `new FormData()`, append `'file', file`, append `'keepRemoved', String(keepRemoved)`
    - `const res = await api.postForm('/api/decks/${deckId}/update/apply', formData)`
    - On success: `setStep('done')`, `toast.success(t('deckUpdate.successToast'))`, `onSuccess()`, `onOpenChange(false)`
    - On error: `setErrorMsg(err.message ?? t('deckUpdate.parseError'))`, `setStep('error')`

    useEffect triggers runPreview when `open && file` both truthy. Use `void runPreview()` call.

    Diff chip array for the grid (inside previewing state render block):
    - `{ key: 'added', label: t('deckUpdate.added', { count: preview.added }), value: preview.added }`
    - `{ key: 'updated', label: t('deckUpdate.updated', { count: preview.updated }), value: preview.updated }`
    - `{ key: 'unchanged', label: t('deckUpdate.unchanged', { count: preview.unchanged }), value: preview.unchanged }`
    - `{ key: 'removed', label: t('deckUpdate.removed', { count: preview.removed }), value: preview.removed }`

    File must remain under 200 lines (new component — no existing lines to worry about).
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend build 2>&1 | tail -10</automated>
  </verify>
  <done>
    `DeckUpdateModal.tsx` exists, exports `DeckUpdateModal`, `yarn workspace @kartex/frontend build` exits 0 with no TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade DeckUpdateModal test stubs to real assertions (T-16-FE-01..06)</name>
  <files>apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx</files>
  <read_first>
    - apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx (current stub file from Wave 0 — to be replaced)
    - apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx (exact analog — vi.mock pattern, import-after-mock, render + screen assertions)
    - apps/frontend/src/components/DeckUpdateModal.tsx (just created — read to understand actual prop interface, state keys, and aria attributes before writing tests)
  </read_first>
  <action>
    Rewrite `apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx`, replacing all `it.todo` stubs with real assertions.

    Imports:
    - `{ describe, it, expect, vi, beforeEach }` from `'vitest'`
    - `{ render, screen, waitFor }` from `'@testing-library/react'`
    - `{ userEvent }` from `'@testing-library/user-event'` (if needed for toggle interaction)

    Mock setup (before component import — per established project pattern):
    - `vi.mock('react-i18next', () => ({ useTranslation: () => ({ t: mockT }) }))` where `mockT = vi.fn((key: string, opts?: { count?: number }) => opts?.count !== undefined ? key.replace('{{count}}', String(opts.count)) : key)`
    - `vi.mock('sonner', () => ({ toast: { success: vi.fn() } }))` 
    - `vi.mock('@/lib/api', () => ({ api: { postForm: vi.fn() } }))`
    - Import `{ DeckUpdateModal }` from `'@/components/DeckUpdateModal'` AFTER mock declarations
    - Import `{ api }` from `'@/lib/api'` AFTER mocks to get the mocked version

    Use `const mockApiPostForm = api.postForm as ReturnType<typeof vi.fn>` to type the mock.

    `beforeEach`: call `vi.clearAllMocks()`

    Default props helper: `const defaultProps = { open: true, onOpenChange: vi.fn(), deckId: 'deck-123', file: new File(['front: A\n---\nback: B'], 'test.kartex'), onSuccess: vi.fn() }`

    Test T-16-FE-01 (uploading state shows spinner):
    - Mock `api.postForm` to return a promise that never resolves (use `new Promise(() => {})`)
    - Render DeckUpdateModal with defaultProps
    - Assert `screen.getByRole('dialog')` present and Loader2 aria indicator — check for `deckUpdate.uploading` key text or `animate-spin` class

    Test T-16-FE-02 (previewing state shows diff counts):
    - Mock `api.postForm` to resolve with `{ ok: true, json: async () => ({ added: 2, updated: 1, unchanged: 3, removed: 0 }) }`
    - Render and `await waitFor(() => screen.getByText(/deckUpdate\.added/))`  (or the interpolated form)
    - Assert all 4 diff values are visible in the document

    Test T-16-FE-03 (keepRemoved default true):
    - Same setup as T-16-FE-02
    - After preview loads, assert the Switch is checked: `screen.getByRole('switch')` has `data-state="checked"` or `aria-checked="true"`

    Test T-16-FE-04 (Apply button triggers apply fetch with keepRemoved):
    - Preview resolves; after previewing state renders, user clicks Apply button
    - Assert `api.postForm` called second time with URL containing `update/apply`
    - Assert second FormData argument contains `keepRemoved: 'true'`

    Test T-16-FE-05 (error state shows error message):
    - Mock `api.postForm` to reject with `new Error('Parse error')`
    - Render and `await waitFor(() => screen.getByRole('alert'))`
    - Assert error container with `role="alert"` is present

    Test T-16-FE-06 (done: calls onSuccess and closes modal):
    - Preview resolves; apply resolves with `{ ok: true, json: async () => ({ added: 1, updated: 0, unchanged: 0, removed: 0, deckId: 'deck-123' }) }`
    - User clicks Apply
    - `await waitFor(() => expect(defaultProps.onSuccess).toHaveBeenCalled())`
    - Assert `defaultProps.onOpenChange` called with `false`

    Use `act(async () => { ... })` only if required by Vitest 2.1.9 (per decision 08-02: avoid `act(async () => { await waitFor(...) })` — use direct `waitFor` instead).
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend test --run --reporter=verbose 2>&1 | grep -E "(T-16-FE-|PASS|FAIL)"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 T-16-FE test cases pass (no todos, no failures)
    - `yarn workspace @kartex/frontend test --run` exits 0
    - No existing frontend tests broken
  </acceptance_criteria>
  <done>
    All 6 T-16-FE stubs replaced with passing assertions. `yarn workspace @kartex/frontend test --run` exits 0 with the DeckUpdateModal suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/decks/:id/update/preview | File selected by user; modal passes it to backend — backend is authoritative for validation |
| modal → onSuccess callback | Modal calls onSuccess() only after confirmed 200 response from apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-FE-01 | Tampering | api.postForm FormData | mitigate | keepRemoved sent as `String(keepRemoved)` — FormData always serialises as string; backend reads `!== 'false'` (Pitfall 6) |
| T-16-FE-02 | Tampering | file input accept | mitigate | `accept=".kartex"` on hidden input (set in Plan 16-04); backend validates extension regardless |
| T-16-FE-03 | Information Disclosure | error state | mitigate | Error message shown inline in `role="alert"` — server error messages shown as-is; no stack traces from backend |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages — all dependencies pre-installed |
</threat_model>

<verification>
Run after both tasks:
1. `yarn workspace @kartex/frontend test --run` — 6 T-16-FE tests passing
2. `yarn workspace @kartex/frontend build` — exits 0
3. `yarn test` — full suite green
</verification>

<success_criteria>
- `DeckUpdateModal.tsx` exists, exports `DeckUpdateModal`, stays under 200 lines
- All 6 T-16-FE-01..06 tests pass
- UI-SPEC honored: Dialog max-w-md, diff 2x2 grid matching StatsSummaryPanel chip pattern, Switch with keepRemoved default true, error with role=alert, aria-busy on Apply during applying state
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/16-import-update-feature/16-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol / Path | Type | Consumed By |
|---------------|------|-------------|
| `DeckUpdateModal` | named export React component | `apps/frontend/src/pages/DeckDetailPage.tsx` (Plan 16-04) |
| `DeckUpdateModalProps` | TypeScript interface | `DeckDetailPage.tsx` (Plan 16-04) |
| `apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx` | test suite (6 passing) | CI / verification gate |
