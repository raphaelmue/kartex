---
phase: 16-import-update-feature
plan: 04
type: execute
wave: 3
depends_on: [16-03]
files_modified:
  - apps/frontend/src/pages/DeckDetailPage.tsx
  - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx
autonomous: true
requirements: [IMP-01]
must_haves:
  truths:
    - "'Update from file' button is visible to deck owner and hidden from non-owners"
    - "Clicking the button opens the OS file picker without any loading state on the button"
    - "Selecting a .kartex file opens DeckUpdateModal with the selected File object"
    - "DeckUpdateModal onSuccess triggers the existing card list refresh"
    - "DeckDetailPage remains under 500 lines after edits"
    - "T-16-FE-07 and T-16-FE-08 tests pass"
  artifacts:
    - path: "apps/frontend/src/pages/DeckDetailPage.tsx"
      provides: "Hidden file input + Update from file button + DeckUpdateModal mount (owner-only)"
      contains: "DeckUpdateModal"
    - path: "apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx"
      provides: "T-16-FE-07 and T-16-FE-08 tests for button owner visibility"
      contains: "T-16-FE-07"
  key_links:
    - from: "apps/frontend/src/pages/DeckDetailPage.tsx"
      to: "apps/frontend/src/components/DeckUpdateModal.tsx"
      via: "import { DeckUpdateModal } from '@/components/DeckUpdateModal'"
      pattern: "DeckUpdateModal"
---

<objective>
Wire `DeckUpdateModal` into `DeckDetailPage`: add hidden file input, trigger button (owner-only), useState for selected file, and modal mount that calls the existing card-refresh callback on success. Add T-16-FE-07 and T-16-FE-08 tests to `DeckDetailPage.test.tsx` to assert owner/non-owner button visibility.

DeckDetailPage is currently 558 lines (over the 500-line limit). The edit must add approximately 20 net lines and stay under 580 lines total — acceptable since the modal is extracted as a separate component. Goal: stay as close to the current count as possible; adding state + button + modal mount adds ~20 lines.

Purpose: Completes IMP-01 — the user-facing entry point for the import-update flow.
Output: Updated `DeckDetailPage.tsx` with ~20 net new lines, plus 2 new tests in `DeckDetailPage.test.tsx`.
</objective>

<execution_context>
@C:/Users/rapha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/rapha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-import-update-feature/16-RESEARCH.md
@.planning/phases/16-import-update-feature/16-PATTERNS.md
@.planning/phases/16-import-update-feature/16-UI-SPEC.md
@.planning/phases/16-import-update-feature/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DeckUpdateModal into DeckDetailPage</name>
  <files>apps/frontend/src/pages/DeckDetailPage.tsx</files>
  <read_first>
    - apps/frontend/src/pages/DeckDetailPage.tsx (read the FULL file — must know: line count, existing imports, useState declarations, owner-only block location, existing modal mounts, the function name used in DeckFormModal/CardEditorModal onSuccess for card list refresh)
    - apps/frontend/src/components/DeckUpdateModal.tsx (confirm prop interface: open, onOpenChange, deckId, file, onSuccess)
  </read_first>
  <action>
    Edit `apps/frontend/src/pages/DeckDetailPage.tsx` with these targeted changes:

    1. Add `useRef` to the existing `useState` / `useEffect` import line (React imports). If `useRef` is already imported, skip this step.

    2. Add import at the top with other component imports (after DeckFormModal import or similar location):
       `import { DeckUpdateModal } from '@/components/DeckUpdateModal'`

    3. Add two new state declarations near the existing useState declarations (not at the bottom — group with other state):
       `const [updateFile, setUpdateFile] = useState<File | null>(null)`
       `const updateFileInputRef = useRef<HTMLInputElement>(null)`

    4. Inside the existing owner-only block (`{deck.ownerId === user?.id && (...)}`) — identified during file read — add after the "Edit deck" button:
       ```
       <Button
         size="sm"
         variant="outline"
         onClick={() => updateFileInputRef.current?.click()}
       >
         {t('deckUpdate.updateFromFile')}
       </Button>
       ```

    5. Add the hidden file input adjacent to the existing modal JSX blocks (near DeckFormModal or other modal mounts, not inside the owner-only block):
       ```
       <input
         ref={updateFileInputRef}
         type="file"
         accept=".kartex"
         className="sr-only"
         aria-hidden="true"
         onChange={(e) => {
           const file = e.target.files?.[0]
           if (file) setUpdateFile(file)
           e.target.value = ''
         }}
       />
       ```
       The `e.target.value = ''` reset is required (RESEARCH.md Pitfall 4) — allows same file to re-trigger onChange.

    6. Mount DeckUpdateModal adjacent to the existing modal mounts. The `onSuccess` callback must be the same function used by CardEditorModal (or DeckFormModal) to refresh the card list — identify this by reading the file. The `onOpenChange` callback sets updateFile to null when open becomes false: `(open) => { if (!open) setUpdateFile(null) }`.
       ```
       <DeckUpdateModal
         open={updateFile !== null}
         onOpenChange={(open) => { if (!open) setUpdateFile(null) }}
         deckId={deckId!}
         file={updateFile}
         onSuccess={/* same refresh function as CardEditorModal uses */}
       />
       ```

    After edits: verify line count is under 600. If any single section can be shortened to compensate, note it — but do NOT restructure existing working code, only add the new lines.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/src/pages/DeckDetailPage.tsx` imports `DeckUpdateModal` from `'@/components/DeckUpdateModal'`
    - File contains `updateFileInputRef` and `setUpdateFile`
    - File contains `accept=".kartex"` on a hidden input
    - File contains `e.target.value = ''` inside the file input onChange
    - File contains `<DeckUpdateModal` mount with `open`, `onOpenChange`, `deckId`, `file`, `onSuccess` props
    - `yarn workspace @kartex/frontend build` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>
    DeckDetailPage builds clean. The "Update from file" button and DeckUpdateModal mount are present in the owner-only section. File line count verified after edit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add T-16-FE-07 and T-16-FE-08 to DeckDetailPage.test.tsx</name>
  <files>apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx</files>
  <read_first>
    - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx (read the FULL file — understand existing vi.mock setup, mockApiGet.mockImplementation pattern, deck fixtures, user.id values used in existing owner/non-owner tests)
    - apps/frontend/src/pages/DeckDetailPage.tsx (just edited — confirm the i18n key used for the button: 'deckUpdate.updateFromFile')
  </read_first>
  <action>
    Add two test cases to the existing `DeckDetailPage.test.tsx` file. Do NOT rewrite the file — append the new tests to the end of the existing describe block (or add a new nested describe block if the existing structure uses nested describes).

    Existing test file pattern: read `mockApiGet.mockImplementation(url =>)` call to understand how deck data is set up. Identify the owner userId value used in existing tests and the deckId fixture.

    Mock DeckUpdateModal at the top of the file (add to existing vi.mock section if one exists, or insert before existing imports):
    - `vi.mock('@/components/DeckUpdateModal', () => ({ DeckUpdateModal: () => null }))`
    This prevents DeckUpdateModal from triggering its own api calls in DeckDetailPage tests.

    Test T-16-FE-07: "Update from file button visible when deck.ownerId === user.id"
    - Set up mock data where `deck.ownerId === user.id` (use existing owner fixture pattern from the file)
    - Render DeckDetailPage (with existing test wrapper/providers)
    - `await waitFor(() => screen.getByText('deckUpdate.updateFromFile'))` — the mockT function returns the key as the label
    - Assert button is present: `expect(screen.getByText('deckUpdate.updateFromFile')).toBeInTheDocument()`

    Test T-16-FE-08: "Update from file button absent when user is not owner"
    - Set up mock data where `deck.ownerId !== user.id` (use non-owner fixture — check existing tests that already test this scenario)
    - Render DeckDetailPage
    - After render, assert button is NOT present: `expect(screen.queryByText('deckUpdate.updateFromFile')).not.toBeInTheDocument()`

    Important: use `mockApiGet.mockImplementation(url =>)` (not mockResolvedValueOnce) for these tests, per decision 08-01 (parallel api.get calls in DeckDetailPage require mockImplementation).

    The mockT function in the existing test file returns the key string as the text. Use `'deckUpdate.updateFromFile'` as the expected button text.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend test --run --reporter=verbose 2>&1 | grep -E "(T-16-FE-07|T-16-FE-08|PASS|FAIL)"</automated>
  </verify>
  <acceptance_criteria>
    - T-16-FE-07 passes: button visible for owner
    - T-16-FE-08 passes: button absent for non-owner
    - All previously passing DeckDetailPage tests still pass (no regressions)
    - `yarn workspace @kartex/frontend test --run` exits 0
  </acceptance_criteria>
  <done>
    Both new tests pass. Full frontend test suite green. `yarn test` exits 0 (all suites — backend and frontend).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser file picker → DeckDetailPage state | File object from OS — extension validated client-side (accept=".kartex") and server-side |
| DeckDetailPage owner check → DeckUpdateModal visibility | Frontend conditionally renders button only for owner; backend is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-FE-04 | Elevation of Privilege | DeckDetailPage button visibility | mitigate | `{deck.ownerId === user?.id && ...}` hides button for non-owners; backend 403 is the authoritative guard |
| T-16-FE-05 | Tampering | hidden file input accept | mitigate | `accept=".kartex"` filters OS picker; backend validates extension regardless of client-side filtering |
| T-16-FE-06 | Tampering | file re-selection | mitigate | `e.target.value = ''` reset ensures same-file re-selection re-triggers onChange (Pitfall 4) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
Run after both tasks:
1. `yarn workspace @kartex/frontend test --run` — T-16-FE-07 and T-16-FE-08 passing; no regressions
2. `yarn workspace @kartex/frontend build` — exits 0
3. `yarn test` — full suite (backend + frontend) green
4. Verify DeckDetailPage line count: `wc -l apps/frontend/src/pages/DeckDetailPage.tsx` — should be under 600
</verification>

<success_criteria>
- "Update from file" button renders inside the owner-only block
- DeckUpdateModal mounted with correct props (open driven by updateFile !== null, onSuccess = card refresh function)
- T-16-FE-07 and T-16-FE-08 pass
- DeckDetailPage line count does not exceed 600 (ideally stays near 580)
- Full test suite green — `yarn test` exits 0
- Phase 16 IMP-01 through IMP-06 all implemented and tested
</success_criteria>

<output>
Create `.planning/phases/16-import-update-feature/16-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol / Path | Type | Consumed By |
|---------------|------|-------------|
| `DeckDetailPage` (updated) | React page component | End users via `/decks/:id` route |
| `updateFileInputRef` | `useRef<HTMLInputElement>` | "Update from file" button onClick |
| `updateFile` state | `File | null` | DeckUpdateModal `open` prop and `file` prop |
| T-16-FE-07, T-16-FE-08 | passing tests | CI / `/gsd-verify-work` gate |
