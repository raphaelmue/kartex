---
phase: 16-import-update-feature
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/backend/src/routes/__tests__/deck-update.test.ts
  - apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03, IMP-04, IMP-05, IMP-06]
must_haves:
  truths:
    - "Test suite runs green (all stubs pass as `it.todo`) before any implementation exists"
    - "Backend stub file has 12 named it.todo entries covering T-16-01 through T-16-12"
    - "Frontend stub file has 6 named it.todo entries covering T-16-FE-01 through T-16-FE-06"
  artifacts:
    - path: "apps/backend/src/routes/__tests__/deck-update.test.ts"
      provides: "Wave 0 RED test scaffold for deckUpdate routes"
      contains: "it.todo"
    - path: "apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx"
      provides: "Wave 0 RED test scaffold for DeckUpdateModal component"
      contains: "it.todo"
  key_links: []
---

<objective>
Create Wave 0 test stub files for the import-update feature — backend route tests and frontend component tests. These stubs establish the test contract before any implementation exists. All stubs use `it.todo` and must pass (Vitest counts todos as passing, not failing). Implementation plans (Waves 1-3) replace these stubs with real assertions.

Purpose: Nyquist compliance — every task in subsequent waves has a test file to target. Prevents executor drift.
Output: Two new test files with named stubs for all 18 test cases defined in RESEARCH.md.
</objective>

<execution_context>
@C:/Users/rapha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/rapha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-import-update-feature/16-RESEARCH.md
@.planning/phases/16-import-update-feature/16-PATTERNS.md
@.planning/phases/16-import-update-feature/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend deck-update test stub file (T-16-01..T-16-12)</name>
  <files>apps/backend/src/routes/__tests__/deck-update.test.ts</files>
  <read_first>
    - apps/backend/src/routes/__tests__/stats-summary.test.ts (exact analog — describe/it.todo structure, vitest import pattern)
    - apps/backend/src/routes/__tests__/kartex-parser-id.test.ts (second analog — confirm describe grouping style)
  </read_first>
  <action>
    Create `apps/backend/src/routes/__tests__/deck-update.test.ts` as a new file. Import only `{ describe, it }` from `'vitest'` — no other imports needed for stubs.

    Structure the file with two `describe` blocks:

    Block 1: `'POST /api/decks/:id/update/preview — deck update preview (T-16-01..T-16-05)'`
    - `it.todo('T-16-01: 403 when caller is not deck owner')`
    - `it.todo('T-16-02: 404 when deckId does not exist')`
    - `it.todo('T-16-03: 422 when file is not a .kartex file')`
    - `it.todo('T-16-04: returns correct added/updated/unchanged/removed counts')`
    - `it.todo('T-16-05: cards without kartexId in file → counted as added')`

    Block 2: `'POST /api/decks/:id/update/apply — deck update apply (T-16-06..T-16-12)'`
    - `it.todo('T-16-06: 403 when caller is not deck owner')`
    - `it.todo('T-16-07: creates new cards for added bucket')`
    - `it.todo("T-16-08: updates front/back/tags for matched cards; CardProgress untouched")`
    - `it.todo('T-16-09: keepRemoved=true — absent deck cards remain')`
    - `it.todo('T-16-10: keepRemoved=false — absent deck cards deleted')`
    - `it.todo('T-16-11: transaction is atomic — if update fails, no partial changes')`
    - `it.todo("T-16-12: userId is taken from JWT (c.get('userId')), never from request body")`

    Do NOT add any imports other than `{ describe, it }` from `'vitest'`. Do NOT add beforeEach, mocks, or setup code — those belong in Wave 1 implementation.
  </action>
  <verify>
    <automated>yarn workspace @kartex/backend test --run --reporter=verbose 2>&1 | grep -E "(T-16-|PASS|FAIL|todo)"</automated>
  </verify>
  <acceptance_criteria>
    `yarn workspace @kartex/backend test --run` passes with 12 todo entries visible in the output for the deck-update suite. No failing tests introduced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create frontend DeckUpdateModal test stub file (T-16-FE-01..T-16-FE-06)</name>
  <files>apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx</files>
  <read_first>
    - apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx (exact analog — describe/it.todo structure, vi.mock setup, import-after-mock pattern)
    - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx (confirm DeckDetailPage test exists; we will add T-16-FE-07/08 there in Wave 3 — note its location)
  </read_first>
  <action>
    Create `apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx` as a new file.

    Import `{ describe, it }` from `'vitest'` only — no component imports or mocks in stub phase.

    Structure with one `describe` block:
    `'DeckUpdateModal (T-16-FE-01..T-16-FE-06)'`
    - `it.todo('T-16-FE-01: uploading state — shows spinner while preview fetch is in-flight')`
    - `it.todo('T-16-FE-02: previewing state — shows diff counts after preview fetch succeeds')`
    - `it.todo('T-16-FE-03: keepRemoved toggle — default state is checked (true)')`
    - `it.todo('T-16-FE-04: Apply button triggers apply fetch with keepRemoved value')`
    - `it.todo('T-16-FE-05: error state — shows error message on preview fetch failure')`
    - `it.todo('T-16-FE-06: done — calls onSuccess() and closes modal on successful apply')`

    Do NOT add any component imports, vi.mock calls, or render calls — this file is stub-only. Wave 3 replaces the stubs with real assertions.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend test --run --reporter=verbose 2>&1 | grep -E "(T-16-FE-|PASS|FAIL|todo)"</automated>
  </verify>
  <acceptance_criteria>
    `yarn workspace @kartex/frontend test --run` passes with 6 todo entries for the DeckUpdateModal suite. No failing tests introduced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → source | Test stubs have no trust boundary — no production code touched in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase — all dependencies present |
</threat_model>

<verification>
Run `yarn test` after both tasks. Output must show:
- `deck-update.test.ts`: 12 todos, 0 failures
- `DeckUpdateModal.test.tsx`: 6 todos, 0 failures
- All previously passing tests still pass (no regressions)
</verification>

<success_criteria>
- Both test stub files exist and contain exactly the named stubs from RESEARCH.md §Test Coverage
- `yarn test` exits 0
- No existing tests broken
</success_criteria>

<output>
Create `.planning/phases/16-import-update-feature/16-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol / Path | Type | Consumed By |
|---------------|------|-------------|
| `apps/backend/src/routes/__tests__/deck-update.test.ts` | test stub file | Plan 16-02 (replaces stubs with assertions) |
| `apps/frontend/src/components/__tests__/DeckUpdateModal.test.tsx` | test stub file | Plan 16-03 (replaces stubs with assertions) |
