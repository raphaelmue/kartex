---
phase: 13-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "A developer who clones the repo can follow README.md to set up .env and run docker compose up -d without consulting any other file"
    - "README.md exists at the repository root"
    - "Quick-start section references all required env vars (JWT_SECRET, DB_PASSWORD, ADMIN_USERNAME, ADMIN_PASSWORD)"
    - "README.md links to docs/design.md and docs/kartex-format.md"
  artifacts:
    - path: "README.md"
      provides: "Project overview and quick-start guide"
      contains: "docker compose up -d"
  key_links:
    - from: "README.md"
      to: "docs/design.md"
      via: "markdown link"
      pattern: "docs/design\\.md"
    - from: "README.md"
      to: "docs/kartex-format.md"
      via: "markdown link"
      pattern: "docs/kartex-format\\.md"
---

<objective>
Create README.md at the repository root so a developer who clones the repo cold can set up and run the app without consulting any other file.

Purpose: DOCS-01 — the repo currently has no README.md at root. New developers and returning users have no entry point for setup instructions.
Output: README.md at repo root with project overview, accurate tech stack, Docker Compose quick-start, feature list, and links to detailed docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docker-compose.yml
@.env.example
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md at repo root (DOCS-01)</name>
  <files>README.md</files>
  <read_first>
    - docker-compose.yml — ground truth for services (backend + db only, NO nginx), ports, and env var names
    - .env.example — ground truth for all required env vars and their descriptions
    - package.json — confirms yarn@4.15.0 and workspaces config
  </read_first>
  <action>
    Create README.md at the repository root. The file does not exist — this is a pure creation. Include the following sections in order:

    1. Title and tagline: "# Kartex" then a one-sentence tagline: "Self-hosted flashcard application with spaced repetition, rich content (LaTeX, Typst, code, media), and PWA support."

    2. Features list (bullet points):
       - SM-2 spaced repetition with Normal / Intensive / Exam Prep study modes
       - Rich card content: Markdown, KaTeX math ($...$, $$...$$), Typst WASM (#typst blocks), images, audio, syntax-highlighted code
       - Import decks from `.kartex` text files or `.kartex.zip` bundles with media
       - Deck sharing between users; public explore page
       - PWA-installable (Add to Home Screen, offline app shell)
       - Invite-only registration; admin user management
       - Dark mode

    3. Tech stack table with these exact rows (no Nginx row, no pnpm row):
       | Layer | Technology |
       | Monorepo | yarn workspaces (yarn@4.15.0) |
       | Frontend | React + Vite + TypeScript |
       | UI Library | shadcn/ui (Radix UI + Tailwind CSS) |
       | Backend | Hono (Node.js, TypeScript) — serves API + React SPA |
       | ORM | Prisma |
       | Database | PostgreSQL 16 |
       | Deployment | Docker Compose (2 services: backend, db) |

    4. Prerequisites section:
       - Docker and Docker Compose
       - No other tools needed on the host (everything runs in containers)

    5. Quick-start section with these exact steps:
       a. Clone: git clone <repo-url> && cd kartex
       b. Copy env file: cp .env.example .env
       c. Edit .env — set these four values (reference them by exact name from .env.example):
          - JWT_SECRET — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
          - DB_PASSWORD — choose any strong password (used inside PostgreSQL container)
          - ADMIN_USERNAME — username for the first admin account
          - ADMIN_PASSWORD — password for the first admin account
       d. Start: docker compose up -d
       e. Open http://localhost:3000 in your browser
       f. Log in with the ADMIN_USERNAME / ADMIN_PASSWORD you set

    6. Configuration section: table listing every env var from .env.example with its purpose:
       DATABASE_URL, JWT_SECRET, ADMIN_USERNAME, ADMIN_PASSWORD, ALLOWED_ORIGIN, BACKEND_PORT, STORAGE_PATH, MAX_UPLOAD_BYTES
       For each, include: variable name, default/example value, description (derived from .env.example comments)

    7. Architecture section (brief, 3 sentences): Hono backend serves both the REST API (/api/*) and the React SPA static files from a single Node.js container on port 3000. PostgreSQL 16 stores all data. Media files (images, audio) are stored on a Docker volume at /app/media.

    8. Documentation links section:
       - [Architecture & Design](docs/design.md) — full data model, auth flow, API overview
       - [Kartex Format Reference](docs/kartex-format.md) — .kartex file format spec for deck import/export

    Do NOT include: contribution guide, development setup (yarn install, yarn dev), CI/CD details, or license section (not requested).
  </action>
  <verify>
    <automated>grep -c "docker compose up -d" README.md && grep -c "JWT_SECRET" README.md && grep -c "DB_PASSWORD" README.md && grep -c "ADMIN_USERNAME" README.md && grep -c "ADMIN_PASSWORD" README.md && grep -c "docs/design.md" README.md && grep -c "docs/kartex-format.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at repo root (C:\Users\rapha\workspace\kartex\README.md)
    - Contains the string "docker compose up -d"
    - Contains "JWT_SECRET", "DB_PASSWORD", "ADMIN_USERNAME", "ADMIN_PASSWORD" as env var names in the quick-start
    - Contains "http://localhost:3000" as the access URL
    - Contains a link to "docs/design.md"
    - Contains a link to "docs/kartex-format.md"
    - Tech stack table does NOT contain the word "nginx" or "pnpm" (case-insensitive)
    - Tech stack table contains "yarn@4.15.0"
    - Tech stack table contains "Hono" with a note that it serves both API and React SPA
    - No fenced code block contains "pnpm" or "npm install"
  </acceptance_criteria>
  <done>README.md exists at repo root and passes all acceptance criteria above. A developer reading only this file knows exactly what env vars to set and what command to run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README quick-start → docker-compose.yml | Env var names in README must match docker-compose.yml exactly or startup will fail silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | README env var names | mitigate | Executor reads .env.example and docker-compose.yml before writing; acceptance criteria check exact var names |
| T-13-02 | Information Disclosure | README quick-start | accept | No secrets in README; .env.example values are clearly marked as placeholders |
</threat_model>

<verification>
After task completes:
- README.md exists: ls README.md returns the file
- No nginx reference in tech stack: grep -i "nginx" README.md returns no matches in the stack/architecture sections
- No pnpm reference: grep -i "pnpm" README.md returns no matches
- All 4 required env vars present in quick-start section
- Both doc links present
</verification>

<success_criteria>
DOCS-01 complete when: README.md exists at repo root, quick-start section lists JWT_SECRET + DB_PASSWORD + ADMIN_USERNAME + ADMIN_PASSWORD, includes "docker compose up -d", and links to both docs files.
</success_criteria>

<output>
Create `.planning/phases/13-documentation/13-01-SUMMARY.md` when done.
</output>
