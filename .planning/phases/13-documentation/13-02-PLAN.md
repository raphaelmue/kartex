---
phase: 13-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design.md
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "docs/design.md contains no references to Nginx or pnpm"
    - "The architecture section accurately reflects Hono serveStatic serving both API and the React SPA"
    - "The Tech Stack table lists yarn workspaces (yarn@4.15.0) instead of pnpm workspaces, and has no Proxy/Nginx row"
    - "The Docker Compose section shows the actual 2-service setup (backend + db) matching docker-compose.yml"
    - "The data model section reflects the current prisma schema fields"
    - "The Future Features section does not list Offline / PWA (shipped in Phase 12)"
  artifacts:
    - path: "docs/design.md"
      provides: "Accurate architecture reference document"
      contains: "yarn workspaces"
  key_links:
    - from: "docs/design.md Section 4"
      to: "apps/backend/src/index.ts"
      via: "architecture description"
      pattern: "serveStatic"
---

<objective>
Fix all stale sections in docs/design.md so it accurately reflects the current architecture: yarn workspaces, no Nginx proxy, Hono serveStatic, 2-service Docker Compose, current Prisma schema, and PWA shipped.

Purpose: DOCS-02 — design.md still describes a 4-service nginx-proxied architecture using pnpm. This misleads new developers who may set up Nginx unnecessarily or use pnpm instead of yarn.
Output: Updated docs/design.md with 7 targeted section edits. No wholesale rewrite — only the stale sections are changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docker-compose.yml
@apps/backend/src/index.ts
@apps/backend/prisma/schema.prisma
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale sections in docs/design.md (DOCS-02)</name>
  <files>docs/design.md</files>
  <read_first>
    - docs/design.md — read the full file before editing; understand current section structure and line numbers
    - docker-compose.yml — ground truth for the corrected Docker Compose YAML to use in Section 12
    - apps/backend/src/index.ts — confirms serveStatic pattern for Section 4 description
    - apps/backend/prisma/schema.prisma — ground truth for the corrected data model in Section 6
    - package.json — confirms "packageManager": "yarn@4.15.0" for Section 2 and Section 3
  </read_first>
  <action>
    Edit docs/design.md with these 7 targeted changes. Read the full file first to confirm current line content before each edit. Do NOT rewrite sections not listed here.

    EDIT 1 — Section 2 Tech Stack table, "Monorepo" row:
    Find: `| Monorepo      | `pnpm workspaces`                        |`
    Replace with: `| Monorepo      | yarn workspaces (yarn@4.15.0)            |`

    EDIT 2 — Section 2 Tech Stack table, remove "Proxy" row entirely:
    Find and delete the entire row: `| Proxy         | Nginx                                    |`
    (The row between "Database" and "Shared Types" rows)

    EDIT 3 — Section 3 Monorepo structure, remove pnpm-workspace.yaml line:
    Find and delete the line: `├── pnpm-workspace.yaml`
    (Inside the code block near the bottom of the monorepo tree)

    EDIT 4 — Section 4 Architecture, replace the full ASCII diagram and Services table.
    Find the entire block starting with the opening ``` fence before `┌─────────` and ending with the closing ``` fence after `└─────────────────┘`, then replace with:
    ```
    ┌─────────────────────────────────────────────┐
    │                  Browser                    │
    │              (React SPA)                    │
    └───────────────────┬─────────────────────────┘
                        │ HTTP / HTTPS
    ┌───────────────────▼─────────────────────────┐
    │          Hono Backend (Node.js/TS)          │
    │  • REST API on /api/*                       │
    │  • Serves React SPA via serveStatic         │
    │  • Port 3000                                │
    └──────────────────┬──────────────────────────┘
                       │ Prisma
    ┌──────────────────▼──────────────────────────┐
    │             PostgreSQL 16                   │
    └─────────────────────────────────────────────┘
    ```

    Then replace the Services table (starting at `| Service` and ending at the row for `proxy`) with:
    | Service   | Image                | Purpose                                |
    |-----------|----------------------|----------------------------------------|
    | `backend` | node:22-slim         | REST API + React SPA (Hono, port 3000) |
    | `db`      | postgres:16-alpine   | Persistence (via Prisma)               |

    EDIT 5 — Section 6 Data Model, replace the full Prisma schema code block.
    The current code block starts with ` ```prisma` and ends with the closing ` ``` ` after `enum Permission`. Replace the entire code block content (keep the ` ```prisma ` and closing ` ``` ` fences) with the actual current schema fields from prisma/schema.prisma. Include all models and enums as they appear in that file. Key differences from the stale version:
    - User: remove `email` field, rename `hashedPassword` to `passwordHash`, add `isActive Boolean @default(true)`, add `studyMode String @default("normal")`, add `refreshTokens RefreshToken[]`, add `inviteCodeUsed InviteCode?`
    - Deck: add `isActive Boolean @default(true)`
    - DeckShare: add `@@unique([deckId, sharedWithUserId])`, change `permission @default(READ)` to use Permission enum with MANAGE value
    - Permission enum: add MANAGE value: `enum Permission { READ EDIT MANAGE }`
    - Add InviteCode model (id, code, expiresAt, usedAt, usedById, usedBy relation, createdAt)
    - Add RefreshToken model (id, userId, user relation, tokenHash, expiresAt, createdAt)
    - Remove `email` field from User entirely
    - CardProgress: field `nextReview` (not `nextReviewAt`) confirmed as-is
    - datasource block: `provider = "postgresql"` with NO `url` field (Prisma 7 driver adapter — url is injected at runtime)

    EDIT 6 — Section 12 Docker Compose, replace the YAML example and deployment commands.
    Find the code block starting with ` ```yaml` that contains `proxy:` and `image: nginx:alpine`. Replace the entire YAML block with the actual contents of docker-compose.yml (2 services: backend and db, no proxy service).
    Also replace the deployment command block:
    Find: `cp .env.example .env   # set JWT_SECRET and DB_PASSWORD`
    Replace with:
    ```bash
    cp .env.example .env
    # Edit .env — set JWT_SECRET, DB_PASSWORD, ADMIN_USERNAME, ADMIN_PASSWORD
    docker compose up -d
    # Available at http://localhost:3000
    ```

    EDIT 7 — Section 14 Future Features, remove the PWA bullet:
    Find and delete the entire bullet line: `- **Offline / PWA**: service worker for already-loaded decks`
    (PWA was shipped in Phase 12 v1.2; it is no longer a future feature)
  </action>
  <verify>
    <automated>grep -ci "nginx" docs/design.md; grep -ci "pnpm" docs/design.md; grep -c "yarn workspaces" docs/design.md; grep -c "serveStatic" docs/design.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -i "nginx" docs/design.md returns 0 matches (case-insensitive; the word "nginx" must not appear anywhere in the file)
    - grep -i "pnpm" docs/design.md returns 0 matches
    - grep -c "yarn workspaces" docs/design.md returns at least 1 match
    - The Services table contains exactly 2 rows: one for "backend" and one for "db" — no "frontend", "proxy", or "nginx" service rows
    - The ASCII diagram does NOT contain "Nginx" or "Proxy"
    - The data model code block contains "passwordHash" (not "hashedPassword")
    - The data model code block contains "studyMode"
    - The data model code block contains "isActive" on the Deck model
    - The data model code block contains "MANAGE" in the Permission enum
    - Section 14 does NOT contain "Offline / PWA"
    - Section 3 monorepo tree does NOT contain "pnpm-workspace.yaml"
  </acceptance_criteria>
  <done>All 7 targeted edits applied. grep -i "nginx" docs/design.md and grep -i "pnpm" docs/design.md both return 0 matches. Design.md accurately describes the 2-service architecture with Hono serveStatic and yarn workspaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design.md → real codebase | Stale docs cause developers to configure infrastructure that doesn't exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | design.md partial edit | mitigate | Executor reads full file before editing; acceptance criteria verify absence of "nginx" and "pnpm" strings globally |
| T-13-04 | Information Disclosure | data model in design.md | accept | Design doc is internal; showing schema fields is intentional documentation |
</threat_model>

<verification>
After task completes:
- grep -i "nginx" docs/design.md → 0 matches
- grep -i "pnpm" docs/design.md → 0 matches
- grep -c "yarn workspaces" docs/design.md → 1+ matches
- grep -c "serveStatic" docs/design.md → 1+ matches
- grep -c "postgres:16-alpine" docs/design.md → 1+ matches (from Docker Compose section)
- grep -c "passwordHash" docs/design.md → 1+ matches (updated data model)
- grep -c "Offline / PWA" docs/design.md → 0 matches
</verification>

<success_criteria>
DOCS-02 complete when: docs/design.md contains zero references to "nginx" or "pnpm" (case-insensitive), the architecture diagram and Services table show only 2 services (backend, db), the data model matches the actual prisma schema, and the Future Features section does not list PWA.
</success_criteria>

<output>
Create `.planning/phases/13-documentation/13-02-SUMMARY.md` when done.
</output>
