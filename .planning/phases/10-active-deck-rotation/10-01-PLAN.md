---
phase: 10-active-deck-rotation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/frontend/src/pages/__tests__/DecksPage.test.tsx
autonomous: true
requirements: [DECK-01]
must_haves:
  truths:
    - "DecksPage.test.tsx exists and is collected by Vitest"
    - "DECK-01 test cases fail RED because DecksPage has no isActive toggle yet"
  artifacts:
    - path: "apps/frontend/src/pages/__tests__/DecksPage.test.tsx"
      provides: "RED test stubs for DECK-01 toggle render, PATCH call, optimistic revert"
      min_lines: 80
  key_links:
    - from: "apps/frontend/src/pages/__tests__/DecksPage.test.tsx"
      to: "@/pages/DecksPage"
      via: "import { DecksPage }"
      pattern: "from '@/pages/DecksPage'"
---

<objective>
Create the Wave 0 test scaffold `DecksPage.test.tsx` that encodes the DECK-01 behaviors (isActive Switch render state, PATCH call on toggle, optimistic revert + toast.error on PATCH failure). These tests MUST fail RED on creation because the DecksPage toggle does not exist yet — they go GREEN when Plan 03 implements the toggle.

Purpose: This file is the validation gate for DECK-01. Per 10-VALIDATION.md it is the single Wave 0 MISSING reference; all toggle implementation work in Plan 03 verifies against it. It must exist before Plan 03 so the executor has a concrete RED→GREEN target.
Output: `apps/frontend/src/pages/__tests__/DecksPage.test.tsx` with 4 failing test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-active-deck-rotation/10-RESEARCH.md
@.planning/phases/10-active-deck-rotation/10-PATTERNS.md
@.planning/phases/10-active-deck-rotation/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DecksPage.test.tsx Wave 0 RED stub for DECK-01</name>
  <files>apps/frontend/src/pages/__tests__/DecksPage.test.tsx</files>
  <read_first>
    - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx (mock structure reference: vi.hoisted api mock, react-router-dom mock, sonner mock — replicate exactly)
    - apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx (additional structural analog per 10-PATTERNS.md §DecksPage.test.tsx)
    - apps/frontend/src/pages/DecksPage.tsx (current component — confirm it has NO isActive toggle yet, confirm it does NOT import useAuth, confirm deck list state shape and api.get/api.delete usage)
    - .planning/phases/10-active-deck-rotation/10-PATTERNS.md (§`apps/frontend/src/pages/__tests__/DecksPage.test.tsx` — exact test file structure to follow)
    - .planning/phases/10-active-deck-rotation/10-VALIDATION.md (§Wave 0 Requirements — the four DECK-01 cases)
  </read_first>
  <action>
    Create the test file following the DeckDetailPage.test.tsx mock layout but WITHOUT an AuthContext mock (DecksPage does not import useAuth — confirmed in 10-PATTERNS.md). Required mocks: (1) `vi.mock('react-router-dom', ...)` preserving actual and overriding `useNavigate: () => vi.fn()`; (2) api mock via `vi.hoisted` exposing `mockApiGet` and `mockApiPatch`, with `vi.mock('@/lib/api', () => ({ api: { get: mockApiGet, patch: mockApiPatch, delete: vi.fn() } }))`; (3) `vi.mock('sonner', () => ({ toast: { error: vi.fn(), success: vi.fn() } }))`.

    Add a `makeDeck(id, isActive = true)` factory returning a DeckListItem-shaped object including the fields DecksPage renders (id, title, description, visibility, ownerId, isActive, createdAt, updatedAt, _count.cards) and NO `sharedByUsername` (so the toggle is expected to render — toggle is hidden for shared decks).

    Add a `renderPage()` helper wrapping `<DecksPage />` in `<MemoryRouter>`.

    Write a `describe('DecksPage isActive toggle', ...)` block with `beforeEach` that resets `mockApiGet` and `mockApiPatch`, sets `mockApiGet` to resolve `{ ok: true, json: async () => [decks...] }` for `/api/decks`, and sets `mockApiPatch` default to resolve `{ ok: true }`. Include exactly these four `it` cases keyed by the listed IDs in their test names:
    - `DECK-01a: Switch renders checked when deck.isActive === true` — render with an active deck; assert a control with `aria-label` matching `decks.toggleActive` (use `screen.findByRole('switch')` or `getByLabelText`) is in the checked state (`aria-checked="true"` or `data-state="checked"`).
    - `DECK-01b: Switch renders unchecked when deck.isActive === false` — render with `makeDeck('d2', false)`; assert the switch is unchecked.
    - `DECK-01c: PATCH called with isActive false when toggled off` — render an active deck, fire a click on the switch, assert `mockApiPatch` was called with `/api/decks/d1` and a second arg deep-equal `{ isActive: false }` (use `waitFor`).
    - `DECK-01d: optimistic revert and toast.error on PATCH failure` — set `mockApiPatch` to resolve `{ ok: false }`, toggle off, assert (via `waitFor`) the toast.error mock was called and the switch returns to checked.

    Use the direct `waitFor` pattern (NOT `act(async () => await waitFor(...))`) per STATE.md decision 08-02 (incompatible with Vitest 2.1.9). Use explicit imports `import { describe, it, expect, vi, beforeEach } from 'vitest'` per STATE.md 07-01 (no vitest globals). Do not implement DecksPage changes here — only the test file. These tests are EXPECTED to fail until Plan 03.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend run test --run src/pages/__tests__/DecksPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/frontend/src/pages/__tests__/DecksPage.test.tsx` exists
    - File imports `{ DecksPage }` from `@/pages/DecksPage`
    - File contains exactly four `it(` cases whose names contain `DECK-01a`, `DECK-01b`, `DECK-01c`, `DECK-01d`
    - File contains `vi.hoisted` defining `mockApiPatch`
    - File contains NO `vi.mock('@/context/AuthContext'` (DecksPage does not use useAuth)
    - Running the verify command collects the file and reports the DECK-01 cases as failing (RED) — Vitest exits non-zero with the new cases failing, NOT a collection/import error
  </acceptance_criteria>
  <done>The test file exists, is collected by Vitest, and the four DECK-01 cases fail because the toggle is not implemented yet (RED state confirmed — not a parse or import failure).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan — no runtime trust boundary crossed. Tests assert client behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | DecksPage.test.tsx | accept | Test scaffold only; no production code, no input handling, no install. The DECK-01 access-control threat is covered by Plan 03's threat_model (PATCH owner check). |
</threat_model>

<verification>
- `yarn workspace @kartex/frontend run test --run src/pages/__tests__/DecksPage.test.tsx` collects the file and reports the four DECK-01 cases as failing (RED, not an import error).
- The rest of the suite (67 existing tests) remains unaffected by adding this file.
</verification>

<success_criteria>
DecksPage.test.tsx exists with four RED DECK-01 cases that fail because the toggle is not yet implemented. No import/collection errors. Plan 03 will turn these GREEN.
</success_criteria>

<output>
Create `.planning/phases/10-active-deck-rotation/10-01-SUMMARY.md` when done.
</output>
