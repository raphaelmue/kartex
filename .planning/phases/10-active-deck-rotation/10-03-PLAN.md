---
phase: 10-active-deck-rotation
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - apps/backend/src/routes/study.ts
  - apps/frontend/src/pages/DecksPage.tsx
  - apps/frontend/src/pages/DeckDetailPage.tsx
autonomous: true
requirements: [DECK-01, DECK-02]
must_haves:
  truths:
    - "The /api/study/due global queue only returns cards from active owned decks"
    - "User can toggle a deck active/inactive from the deck list; switch reflects deck.isActive"
    - "User can toggle a deck active/inactive from the deck detail page (owner only)"
    - "Toggle persists via PATCH /api/decks/:id and survives browser refresh"
    - "Optimistic flip reverts and shows toast.error if PATCH fails"
    - "Inactive deck card renders at opacity-60 on DecksPage"
  artifacts:
    - path: "apps/backend/src/routes/study.ts"
      provides: "isActive filter on the owned-deck branch of deckFilter"
      contains: "isActive: true"
    - path: "apps/frontend/src/pages/DecksPage.tsx"
      provides: "isActive Switch + handleToggleActive optimistic update"
      contains: "handleToggleActive"
    - path: "apps/frontend/src/pages/DeckDetailPage.tsx"
      provides: "owner-only isActive Switch + handleToggleActive"
      contains: "handleToggleActive"
  key_links:
    - from: "apps/frontend/src/pages/DecksPage.tsx"
      to: "/api/decks/:id"
      via: "api.patch with { isActive }"
      pattern: "api.patch.*isActive"
    - from: "apps/backend/src/routes/study.ts"
      to: "prisma deck filter"
      via: "ownerId + isActive on owned branch"
      pattern: "ownerId: userId, isActive: true"
---

<objective>
Enforce DECK-02 server-side and deliver the DECK-01 toggle UI on both pages. Add the `isActive: true` condition to the owned-deck branch of `deckFilter` in `/api/study/due` so the global study queue never surfaces cards from inactive owned decks. Add the shadcn `Switch` toggle (with optimistic update + revert) to DecksPage card footers (with opacity-60 on inactive cards) and to the DeckDetailPage header (owner-only). This plan turns the Plan 01 DECK-01 tests GREEN.

Purpose: DECK-02 must be enforced on the server — the client cannot be trusted to omit inactive decks (Pitfall 4). DECK-01 gives the user the controls that drive that filter. Together they satisfy success criteria 1 and 2 of the phase.
Output: Updated study.ts filter, DecksPage toggle, DeckDetailPage toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-active-deck-rotation/10-RESEARCH.md
@.planning/phases/10-active-deck-rotation/10-PATTERNS.md
@.planning/phases/10-active-deck-rotation/10-UI-SPEC.md
@.planning/phases/10-active-deck-rotation/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isActive filter to /api/study/due owned-deck branch (DECK-02)</name>
  <files>apps/backend/src/routes/study.ts</files>
  <read_first>
    - apps/backend/src/routes/study.ts (current deckFilter at lines 23–28 and its two usages: cardProgress.findMany card.deck filter ~line 34, card.findMany deck filter ~line 51; the GET /api/study/deck/:deckId handler ~lines 92–132 which must NOT change)
    - .planning/phases/10-active-deck-rotation/10-PATTERNS.md (§study.ts — exact single-line change and the do-NOT-touch deck/:deckId note)
    - .planning/phases/10-active-deck-rotation/10-RESEARCH.md (§Pattern 5 and §Pitfall 4 — server-side enforcement is mandatory; shared decks NOT filtered in v1.2)
  </read_first>
  <action>
    In the `deckFilter` object in the `GET /api/study/due` handler, change the owned-deck OR branch from `{ ownerId: userId }` to `{ ownerId: userId, isActive: true }`. Leave the shared-deck branch `{ id: { in: sharedDeckIds } }` unchanged — shared deck isActive is explicitly out of scope for v1.2 (REQUIREMENTS.md §Future Requirements; owner-only scope). Both consumers of `deckFilter` (the `cardProgress.findMany` card.deck filter and the `card.findMany` deck filter) receive the change automatically — make no other edits to those queries.

    Do NOT add any isActive filter to the `GET /api/study/deck/:deckId` handler — deck-specific study must always work regardless of isActive (UI-SPEC §1 Note, RESEARCH §Anti-Patterns).
  </action>
  <verify>
    <automated>yarn workspace @kartex/backend run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/routes/study.ts` deckFilter owned branch matches `{ ownerId: userId, isActive: true }`
    - The shared-deck branch still reads `{ id: { in: sharedDeckIds } }` (no isActive added)
    - The `GET /api/study/deck/:deckId` handler contains NO `isActive` reference
    - `yarn workspace @kartex/backend run build` exits 0
  </acceptance_criteria>
  <done>The global /api/study/due query excludes inactive owned decks; deck-specific study endpoint is untouched; backend builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add isActive Switch + optimistic toggle to DecksPage (DECK-01)</name>
  <files>apps/frontend/src/pages/DecksPage.tsx</files>
  <behavior>
    - Switch renders checked when deck.isActive === true, unchecked when false (DECK-01a/b — Plan 01 tests)
    - Toggling off calls api.patch('/api/decks/:id', { isActive: false }) (DECK-01c)
    - On PATCH failure the optimistic flip reverts and toast.error fires (DECK-01d)
    - Switch is hidden for shared decks (deck.sharedByUsername present)
    - Inactive deck card wrapper carries opacity-60
  </behavior>
  <read_first>
    - apps/frontend/src/pages/DecksPage.tsx (current imports lines 1–17; handleDelete optimistic pattern lines 69–82; CardFooter layout lines 132–176; shared-deck conditional at line 139; card render at line 111)
    - apps/frontend/src/pages/__tests__/DecksPage.test.tsx (Plan 01 RED tests — the contract this task must satisfy: aria-label decks.toggleActive, switch role, PATCH /api/decks/d1 with { isActive: false })
    - apps/frontend/src/components/ui/switch.tsx (installed in Plan 02 — Switch props: checked, onCheckedChange, aria-label, id)
    - .planning/phases/10-active-deck-rotation/10-PATTERNS.md (§DecksPage.tsx — exact handler, CardFooter injection, opacity wrapper)
    - .planning/phases/10-active-deck-rotation/10-UI-SPEC.md (§1 isActive Toggle — DecksPage Card Footer; §Color inactive deck treatment)
  </read_first>
  <action>
    Add `import { Switch } from '@/components/ui/switch'` to the imports.

    Add a `handleToggleActive(deckId: string, checked: boolean)` async handler following the handleDelete optimistic pattern: first flip local state with `setDecks(prev => prev.map(d => d.id === deckId ? { ...d, isActive: checked } : d))`; then `await api.patch(\`/api/decks/${deckId}\`, { isActive: checked })`; if `!res.ok` throw; on success `toast.success(checked ? t('decks.activatedToast') : t('decks.deactivatedToast'))`; in catch, revert with `setDecks(prev => prev.map(d => d.id === deckId ? { ...d, isActive: !checked } : d))` and `toast.error(t('decks.failedToToggle'))`.

    In the `<CardFooter>` (which already uses `flex items-center gap-2`), prepend a toggle group guarded by `{!deck.sharedByUsername && (...)}` containing a wrapper `<div className="flex items-center gap-2 mr-auto">` with a `<Switch checked={deck.isActive} onCheckedChange={(checked) => void handleToggleActive(deck.id, checked)} aria-label={t('decks.toggleActive')} id={\`active-${deck.id}\`} />` and a `<label htmlFor={\`active-${deck.id}\`} className="text-sm text-muted-foreground cursor-pointer">{t('decks.activeLabel')}</label>`. The `mr-auto` pushes existing Study/Open/Edit/Delete buttons right.

    Wrap the existing per-deck `<Card>` in `<div className={deck.isActive ? '' : 'opacity-60'}>...</div>` and move the React `key={deck.id}` to the outer wrapper `<div>` (the Card no longer holds the key). Use only semantic tokens — no bg-gray-*/hex.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend run test --run src/pages/__tests__/DecksPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DecksPage.tsx imports `Switch` from `@/components/ui/switch`
    - DecksPage.tsx contains `handleToggleActive` calling `api.patch` with `{ isActive: checked }`
    - The Switch render is guarded by `!deck.sharedByUsername`
    - Inactive cards are wrapped with `opacity-60` and the `key` is on the outer wrapper div
    - All four DECK-01 cases in `DecksPage.test.tsx` pass (GREEN) — verify command exits 0
  </acceptance_criteria>
  <done>DecksPage shows the toggle on owned decks, dims inactive cards, performs optimistic PATCH with revert, and the Plan 01 DECK-01 tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Add owner-only isActive Switch to DeckDetailPage header (DECK-01)</name>
  <files>apps/frontend/src/pages/DeckDetailPage.tsx</files>
  <read_first>
    - apps/frontend/src/pages/DeckDetailPage.tsx (useAuth import line 8; `user` destructured line 122; owner guard + header button group lines 320–339; deck state setter setDeck)
    - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx (existing test — useAuth mocked with user.id 'user-1'; extend or confirm owner-only render behavior here)
    - apps/frontend/src/components/ui/switch.tsx (installed in Plan 02)
    - .planning/phases/10-active-deck-rotation/10-PATTERNS.md (§DeckDetailPage.tsx — owner guard, handleToggleActive shape)
    - .planning/phases/10-active-deck-rotation/10-UI-SPEC.md (§2 isActive Toggle — DeckDetailPage Header; §States — shared deck hides toggle)
  </read_first>
  <action>
    Add `import { Switch } from '@/components/ui/switch'` to the imports.

    Add a `handleToggleActive(checked: boolean)` async handler: guard `if (!deckId || !deck) return`; capture `const prev = deck.isActive`; optimistically `setDeck(d => d ? { ...d, isActive: checked } : d)`; `await api.patch(\`/api/decks/${deckId}\`, { isActive: checked })`; if `!res.ok` throw; on success `toast.success(checked ? t('decks.activatedToast') : t('decks.deactivatedToast'))`; in catch revert `setDeck(d => d ? { ...d, isActive: prev } : d)` and `toast.error(t('decks.failedToToggle'))`.

    In the header button group `<div className="flex items-center gap-2">`, insert as the FIRST item (before the Study button) an owner-only block `{deck.ownerId === user?.id && (<div className="flex items-center gap-2"><Switch checked={deck.isActive} onCheckedChange={(checked) => void handleToggleActive(checked)} aria-label={t('decks.toggleActive')} id="deck-active-toggle" /><label htmlFor="deck-active-toggle" className="text-sm text-muted-foreground cursor-pointer">{t('decks.activeLabel')}</label></div>)}`. No opacity treatment on this full-width detail page (UI-SPEC §2). The toggle must NOT render for non-owners (Pitfall 3 — PATCH is owner-only and returns 403).
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend run test --run src/pages/__tests__/DeckDetailPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DeckDetailPage.tsx imports `Switch` from `@/components/ui/switch`
    - DeckDetailPage.tsx contains `handleToggleActive` calling `api.patch` with `{ isActive: checked }` and reverting via `setDeck` on failure
    - The Switch is wrapped in `deck.ownerId === user?.id &&` (owner-only)
    - `yarn workspace @kartex/frontend run test --run src/pages/__tests__/DeckDetailPage.test.tsx` exits 0
  </acceptance_criteria>
  <done>DeckDetailPage shows the toggle only to the owner, performs optimistic PATCH with revert, and the existing DeckDetailPage test suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /api/decks/:id | Untrusted toggle request crosses into the backend; must validate type + ownership |
| client → GET /api/study/due | Client may request the global queue directly, bypassing the start screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | PATCH /api/decks/:id (toggle another user's deck) | mitigate | Existing handler already enforces `deck.ownerId !== c.get('userId')` → 403 (RESEARCH §Security). UI also hides the toggle for non-owners (DecksPage `!deck.sharedByUsername`, DeckDetailPage `deck.ownerId === user?.id`). No route change needed; verified by ownership check already present. |
| T-10-05 | Tampering | PATCH /api/decks/:id (isActive: "true" string) | mitigate | `isActive: z.boolean().optional()` on UpdateDeckSchema (added Plan 02) — Zod rejects non-boolean → 400. |
| T-10-06 | Tampering | GET /api/study/due bypass of start screen | mitigate | Server-side `isActive: true` filter on the owned-deck branch (Task 1) — inactive owned-deck cards are never returned regardless of client state (DECK-02, Pitfall 4). |
</threat_model>

<verification>
- `yarn workspace @kartex/backend run build` exits 0 with the new filter.
- DecksPage.test.tsx four DECK-01 cases GREEN; DeckDetailPage.test.tsx stays green.
- Manual (per 10-VALIDATION.md, no backend test infra): set a deck inactive, open /study, confirm no cards from that deck appear; confirm inactive deck card shows at 60% opacity on DecksPage.
- Toggle state persists after a browser refresh (PATCH writes isActive to DB).
</verification>

<success_criteria>
DECK-02 enforced server-side (inactive owned decks excluded from /api/study/due); DECK-01 toggle works on both pages with optimistic update + revert; non-owners never see the toggle; Plan 01 DECK-01 tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/10-active-deck-rotation/10-03-SUMMARY.md` when done.
</output>
