---
phase: 10-active-deck-rotation
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - apps/frontend/src/pages/StudySessionPage.tsx
  - apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx
autonomous: true
requirements: [DECK-03, DECK-04]
must_haves:
  truths:
    - "Opening /study shows a start screen (not an auto-started session)"
    - "The start screen lists all active decks, each pre-checked"
    - "Unchecking a deck affects only the session, never the persisted isActive flag"
    - "The start screen has a session size picker (All / 10 / 20 / custom) matching /decks/:id/learn"
    - "Start session is disabled when no decks are selected"
    - "Selected deckIds filter the loaded cards for the committed session"
  artifacts:
    - path: "apps/frontend/src/pages/StudySessionPage.tsx"
      provides: "Global SR start screen with deck picker + session size picker"
      contains: "selectedDeckIds"
    - path: "apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx"
      provides: "DECK-03/DECK-04 test cases for start screen behavior"
      contains: "start screen"
  key_links:
    - from: "apps/frontend/src/pages/StudySessionPage.tsx"
      to: "committedConfig.deckIds"
      via: "handleStartSession spreads [...selectedDeckIds]"
      pattern: "deckIds: \\[\\.\\.\\.selectedDeckIds\\]"
    - from: "apps/frontend/src/pages/StudySessionPage.tsx"
      to: "card load effect"
      via: "deckIds membership filter on fetched cards"
      pattern: "committedConfig.deckIds"
---

<objective>
Replace the `/study` auto-start with a start screen (DECK-03, DECK-04). When `isGlobalSR === true` and `committedConfig === null`, render a screen with: a deck picker listing all active decks (pre-checked checkboxes, per-session uncheck that does NOT change persisted isActive), a session size picker reusing the existing SIZE_OPTIONS segmented row, and a "Start session" CTA disabled when no decks are selected. On start, commit `deckIds` and filter the loaded cards client-side on top of the server's isActive filter. Extend StudySessionPage.test.tsx with the DECK-03/DECK-04 cases.

Purpose: Satisfies phase success criteria 3 and 4 — per-session deck control and a session size picker consistent with /decks/:id/learn. The client-side deckIds filter is additive to (never a replacement for) the server-side isActive filter from Plan 03 (Pitfall 4).
Output: Updated StudySessionPage.tsx start screen; extended test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-active-deck-rotation/10-RESEARCH.md
@.planning/phases/10-active-deck-rotation/10-PATTERNS.md
@.planning/phases/10-active-deck-rotation/10-UI-SPEC.md
@.planning/phases/10-active-deck-rotation/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the /study global start screen (deck picker + size picker + CTA) in StudySessionPage</name>
  <files>apps/frontend/src/pages/StudySessionPage.tsx</files>
  <behavior>
    - When isGlobalSR && committedConfig === null, the start screen renders instead of auto-starting (DECK-03)
    - All active decks appear pre-checked in the picker; unchecking one does not call any PATCH (session-only) (DECK-03)
    - Session size picker shows All / 10 / 20 / custom; "custom" reveals a number input (DECK-04)
    - "Start session" is disabled when selectedDeckIds.size === 0
    - On start, committedConfig.deckIds = [...selectedDeckIds] and the card load effect filters by deckId membership
  </behavior>
  <read_first>
    - apps/frontend/src/pages/StudySessionPage.tsx (CommittedConfig type ~lines 167–172; isGlobalSR auto-commit initializer ~lines 203–206; SIZE_OPTIONS ~lines 217–223; existing deckId prefetch ~lines 229–257; card load effect ~lines 259–307; mode-selector render guard ~line 310; page wrapper class ~line 312; back button ~lines 313–320; section header pattern ~lines 330–334; loading guard ~line 476; SessionRunner return ~line 484)
    - apps/frontend/src/components/ui/checkbox.tsx (installed Plan 02 — Checkbox props: checked, onCheckedChange, id, aria-label)
    - apps/frontend/src/pages/DecksPage.tsx (how /api/decks response is shaped — DeckListItem[] with isActive)
    - .planning/phases/10-active-deck-rotation/10-PATTERNS.md (§StudySessionPage.tsx — exact type extension, initializer change, new state, prefetch effect, deckIds filter, render-order insertion, imports)
    - .planning/phases/10-active-deck-rotation/10-UI-SPEC.md (§3 start screen structure; §4 Deck Picker List; §5 Session Size Picker; §6 Start Session CTA; §States — no active decks empty state)
  </read_first>
  <action>
    Imports: add `import { Checkbox } from '@/components/ui/checkbox'` and `import { DeckListItem } from '@kartex/shared'`. `BookOpen` from lucide-react is already imported (used by the empty state).

    Add a local type `type DeckPickerDeck = { id: string; title: string; dueCount: number }`.

    Extend `CommittedConfig` with `deckIds?: string[]` (undefined = all active decks for legacy/deck-specific paths; the Set lives only on the start screen).

    Change the `committedConfig` initializer from `isGlobalSR ? { mode: 'sr', tags: new Set(), size: 'all', count: 1 } : null` to always `null` (Pitfall 2 — this is the core behavioral change; without it the start screen never renders).

    Add state: `const [activeDecks, setActiveDecks] = useState<DeckPickerDeck[]>([])` and `const [selectedDeckIds, setSelectedDeckIds] = useState<Set<string>>(new Set())`.

    Add a prefetch `useEffect` gated on `isGlobalSR` using the existing `void (async () => {...})()` + `Promise.all` pattern: fetch `/api/decks` and `/api/study/due` in parallel; on both ok, parse decks as `DeckListItem[]`, filter to `d.isActive`, build `DeckPickerDeck[]` with `dueCount = due.filter(c => c.deckId === d.id).length`; `setActiveDecks(picker)` and `setSelectedDeckIds(new Set(picker.map(d => d.id)))` (pre-check all). Dep array `[isGlobalSR]`.

    Add a `toggleDeckSelection(deckId: string)` that immutably adds/removes the id from a new Set in `selectedDeckIds` (never calls any API — session-only, DECK-03).

    Add `handleStartSession()` that sets `committedConfig` to `{ mode: 'sr', tags: new Set(), size: sessionSize, count: customCount, deckIds: [...selectedDeckIds] }` (spread the Set to an array — Set is not the stored shape, RESEARCH §Anti-Patterns).

    In the card load effect, after the existing tag filter produces `tagFiltered`, add `const deckFiltered = committedConfig.deckIds ? tagFiltered.filter(c => committedConfig.deckIds!.includes(c.deckId)) : tagFiltered` and feed `deckFiltered` (instead of `tagFiltered`) into the subsequent size/shuffle step. Do not mutate committedConfig after commit (CR-02/WR-03 discipline).

    Insert a new render branch immediately before the `if (loadingCards || cards === null)` guard: `if (isGlobalSR && !committedConfig) { return (...) }`. Build the start screen inside the existing `<div className="max-w-lg mx-auto py-12 px-4">` wrapper containing, in order:
    1. Ghost back button (ArrowLeft) → `onClick={() => navigate('/dashboard')}`, label `t('study.backToDashboard')` (reuse lines 313–320 pattern).
    2. `<h1 className="text-xl font-semibold mb-2">{t('study.globalTitle')}</h1>` and `<p className="text-sm text-muted-foreground mb-6">{t('study.globalSubtitle')}</p>`.
    3. "Choose decks" section: label `<p className="text-xs font-semibold text-muted-foreground uppercase tracking-wide mb-2">{t('study.chooseDecks')}</p>` then a `<div className="border border-border rounded-lg overflow-hidden">`. If `activeDecks.length === 0`, render the empty state (BookOpen `h-8 w-8` aria-hidden, `t('study.noActiveDecks')`, `t('study.noActiveDecksHint')`) per UI-SPEC §States. Otherwise map `activeDecks` to a clickable row `<div className="flex items-center gap-3 px-4 py-2 cursor-pointer hover:bg-muted/50 ...border-b on all but last..." onClick={() => toggleDeckSelection(deck.id)}>` containing a `<Checkbox checked={selectedDeckIds.has(deck.id)} onCheckedChange={() => toggleDeckSelection(deck.id)} id={\`deck-picker-${deck.id}\`} aria-label={deck.title} />` and a `<div className="flex-1 min-w-0">` with `<p className="text-sm font-semibold truncate">{deck.title}</p>` (deck.title is user content — never t(), D-07) and `<p className="text-xs text-muted-foreground">{t('study.nCardsDue', { count: deck.dueCount })}</p>`.
    4. "Session size" section: reuse the exact SIZE_OPTIONS segmented row from the deck-specific selector (label `t('study.sessionSize')`, buttons with `variant={sessionSize === opt.value ? 'default' : 'outline'}`, `type="button"`, custom number Input when `sessionSize === 'custom'`). Omit the "(SR mode only)" qualifier (UI-SPEC §5).
    5. `<Button size="lg" className="w-full mt-4" disabled={selectedDeckIds.size === 0} onClick={handleStartSession}>{t('study.startSession')}</Button>`.

    Use only semantic CSS tokens (no bg-gray-*/hex). Keep the file under 500 lines — if it would exceed, extract the DeckPickerItem row into a local subcomponent within the file (same approach as prior CardActionCell extraction, STATE.md 08-03).
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - StudySessionPage.tsx imports `Checkbox` from `@/components/ui/checkbox` and `DeckListItem` from `@kartex/shared`
    - The `committedConfig` useState initializer is `null` (no `isGlobalSR ? {...} : null`)
    - CommittedConfig type includes `deckIds?: string[]`
    - File contains `selectedDeckIds`, `toggleDeckSelection`, and `handleStartSession` setting `deckIds: [...selectedDeckIds]`
    - The card load effect contains a `committedConfig.deckIds` membership filter
    - A render branch `if (isGlobalSR && !committedConfig)` returns the start screen with the deck picker, size picker, and a `disabled={selectedDeckIds.size === 0}` Start session button
    - The empty state renders `t('study.noActiveDecks')` when activeDecks is empty
    - `yarn workspace @kartex/frontend run build` exits 0; file stays under 500 lines
  </acceptance_criteria>
  <done>Opening /study renders the start screen with a pre-checked deck picker and size picker; starting commits deckIds and filters loaded cards; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend StudySessionPage.test.tsx with DECK-03/DECK-04 start screen cases</name>
  <files>apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx</files>
  <read_first>
    - apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx (existing mocks: react-router-dom with `useParams: () => ({ id: 'deck-abc' })` at module scope, vi.hoisted mockApiGet, sonner mock, makeCard factory — the deck-specific suite relies on the id param)
    - apps/frontend/src/pages/StudySessionPage.tsx (final start-screen implementation from Task 1 — the contract these tests assert)
    - .planning/phases/10-active-deck-rotation/10-PATTERNS.md (§StudySessionPage.tsx — global prefetch shape: /api/decks returns DeckListItem[], /api/study/due returns [{ deckId }])
    - .planning/phases/10-active-deck-rotation/10-VALIDATION.md (§Per-Task Verification Map — DECK-03/DECK-04 rows)
  </read_first>
  <action>
    The module-scoped `useParams` mock returns `{ id: 'deck-abc' }`, which forces deck-specific mode. To test the GLOBAL start screen (isGlobalSR — no id param), make the `useParams` return value controllable: introduce a `vi.hoisted` mutable holder (e.g. `mockParams = { current: { id: 'deck-abc' } }`) and have the react-router-dom mock's `useParams` return `mockParams.current`. Default it to the deck-specific value so existing tests are unaffected; set `mockParams.current = {}` inside the new global describe block's beforeEach (and reset it back afterwards).

    Add a `describe('StudySessionPage global start screen', ...)` block. In beforeEach, set `mockParams.current = {}`, reset `mockApiGet`, and configure `mockApiGet` to resolve `/api/decks` with two active DeckListItem-shaped decks (include `isActive: true`) plus optionally one `isActive: false` deck, and `/api/study/due` with an array of `{ deckId }` entries. Add these cases:
    - DECK-03a: start screen renders — assert `t`-resolved title text "Study session" / the deck picker is present (e.g. `await screen.findByText` for an active deck title) when no id param.
    - DECK-03b: all active decks are pre-checked — assert the rendered checkboxes are checked on mount; assert the inactive deck does NOT appear in the picker.
    - DECK-03c: unchecking a deck does NOT call api.patch — there is no patch in this page; assert that clicking a deck row toggles the checkbox to unchecked and `mockApiGet` is not re-invoked for a PATCH (no PATCH path exists; assert selection-only by checkbox state).
    - DECK-03d: Start session disabled when all decks unchecked — uncheck every deck, assert the Start session button has the disabled attribute.
    - DECK-04a: session size picker renders the All / 10 / 20 / custom buttons; clicking "custom" reveals the number input.

    Use direct `waitFor` (not act-wrapped) per STATE.md 08-02. Reset `mockParams.current` to `{ id: 'deck-abc' }` after the global block so subsequent files/tests are unaffected (or scope the reset in afterEach within the block).
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend run test --run src/pages/__tests__/StudySessionPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - StudySessionPage.test.tsx contains a `describe('StudySessionPage global start screen'` block
    - The `useParams` mock is driven by a mutable `vi.hoisted` holder so the global block can set `{}` without breaking the deck-specific suite
    - New cases assert: start screen renders, all active decks pre-checked, inactive deck excluded, unchecking is selection-only (no PATCH), Start session disabled when none selected, size picker custom input
    - Both the pre-existing deck-specific cases AND the new global cases pass — verify command exits 0
  </acceptance_criteria>
  <done>The test file covers DECK-03 and DECK-04 start-screen behaviors, the existing deck-specific suite still passes, and the verify command exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client deck picker → committedConfig | Per-session deckIds selection is ephemeral client state; must not write to the persisted isActive flag |
| client → GET /api/study/due | Client filters fetched cards by deckIds on top of the server isActive filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | deck picker uncheck (could it persist?) | mitigate | `toggleDeckSelection` mutates only `selectedDeckIds` state; no api.patch is called from the start screen (DECK-03 session-only). Test DECK-03c asserts no PATCH occurs. |
| T-10-08 | Information Disclosure | client-side deckIds filter as the only enforcement | accept | The client deckIds filter is additive UX only; DECK-02 enforcement (inactive decks excluded) is owned by Plan 03's server-side filter. A client bypassing the start screen still cannot retrieve inactive-deck cards because the server filter excludes them (Pitfall 4). |
</threat_model>

<verification>
- `yarn workspace @kartex/frontend run build` exits 0.
- StudySessionPage.test.tsx: deck-specific suite + new DECK-03/DECK-04 cases all green.
- Manual: open /study → start screen shows with all active decks pre-checked; uncheck one and start → only checked decks' cards appear; uncheck all → Start session disabled; size "custom" reveals number input.
</verification>

<success_criteria>
/study renders a start screen (no auto-start); deck picker lists active decks pre-checked with session-only uncheck; session size picker matches /decks/:id/learn; committed deckIds filter loaded cards; DECK-03/DECK-04 tests green.
</success_criteria>

<output>
Create `.planning/phases/10-active-deck-rotation/10-04-SUMMARY.md` when done.
</output>
