---
phase: 10-active-deck-rotation
plan: 05
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
autonomous: true
requirements: [DECK-01, DECK-03, DECK-04]
must_haves:
  truths:
    - "All new toggle and start-screen strings resolve to real copy in both English and German"
    - "No new key falls back to its raw key string in the German UI"
  artifacts:
    - path: "apps/frontend/src/locales/en.json"
      provides: "5 new decks.* keys + 7 new study.* keys (English)"
      contains: "activatedToast"
    - path: "apps/frontend/src/locales/de.json"
      provides: "same 12 keys in German"
      contains: "activatedToast"
  key_links:
    - from: "apps/frontend/src/locales/de.json"
      to: "apps/frontend/src/locales/en.json"
      via: "identical key set under decks and study namespaces"
      pattern: "globalTitle"
---

<objective>
Add the 12 new i18n keys for Phase 10 to BOTH locale files in the same plan: 5 under the `decks` namespace (activeLabel, toggleActive, activatedToast, deactivatedToast, failedToToggle) and 7 under the `study` namespace (globalTitle, globalSubtitle, chooseDecks, startSession, backToDashboard, noActiveDecks, noActiveDecksHint). The size-picker keys (sessionSize, sizeAllDue, sizeCustom) and nCardsDue already exist and are reused — do not duplicate them.

Purpose: The toggle UI (Plan 03) and start screen (Plan 04) render every string via `t()`. Missing keys in de.json fall back to the raw key name (not the English value), surfacing as literal "decks.activeLabel" text in the German UI (Pitfall 5). Both files MUST be updated together.
Output: Updated en.json and de.json with the 12 new keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-active-deck-rotation/10-PATTERNS.md
@.planning/phases/10-active-deck-rotation/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 12 new keys to en.json and de.json (decks + study namespaces)</name>
  <files>apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/locales/en.json (existing `decks` namespace ~lines 75–88 and `study` namespace ~lines 159–192; confirm sessionSize/sizeAllDue/sizeCustom/nCardsDue already exist — do NOT re-add them)
    - apps/frontend/src/locales/de.json (same structure — German values; confirm the same existing keys present)
    - .planning/phases/10-active-deck-rotation/10-PATTERNS.md (§en.json and §de.json — exact key names and the English + German copy)
    - .planning/phases/10-active-deck-rotation/10-UI-SPEC.md (§Copywriting Contract — authoritative English copy table)
  </read_first>
  <action>
    Add these 5 keys inside the `decks` namespace of BOTH files.

    English (en.json `decks`): `activeLabel` = "Active"; `toggleActive` = "Toggle deck active"; `activatedToast` = "Deck added to study queue"; `deactivatedToast` = "Deck removed from study queue"; `failedToToggle` = "Failed to update deck — try again".

    German (de.json `decks`): `activeLabel` = "Aktiv"; `toggleActive` = "Deck aktivieren/deaktivieren"; `activatedToast` = "Deck zur Lernwarteschlange hinzugefügt"; `deactivatedToast` = "Deck aus der Lernwarteschlange entfernt"; `failedToToggle` = "Deck konnte nicht aktualisiert werden — bitte erneut versuchen".

    Add these 7 keys inside the `study` namespace of BOTH files.

    English (en.json `study`): `globalTitle` = "Study session"; `globalSubtitle` = "Choose which active decks to include in this session."; `chooseDecks` = "Choose decks"; `startSession` = "Start session"; `backToDashboard` = "Back to Dashboard"; `noActiveDecks` = "No active decks"; `noActiveDecksHint` = "Go to My Decks and toggle at least one deck active to study.".

    German (de.json `study`): `globalTitle` = "Lernsitzung"; `globalSubtitle` = "Wählen Sie, welche aktiven Decks in dieser Sitzung enthalten sein sollen."; `chooseDecks` = "Decks auswählen"; `startSession` = "Sitzung starten"; `backToDashboard` = "Zurück zum Dashboard"; `noActiveDecks` = "Keine aktiven Decks"; `noActiveDecksHint` = "Gehen Sie zu \"Meine Decks\" und aktivieren Sie mindestens ein Deck.".

    Do NOT add or modify `study.sessionSize`, `study.sizeAllDue`, `study.sizeCustom`, or `study.nCardsDue` — they already exist and are reused by the start screen. Keep valid JSON (commas, no trailing comma). Preserve existing key ordering style; append the new keys within the correct namespace object in both files.
  </action>
  <verify>
    <automated>node -e "const en=require('./apps/frontend/src/locales/en.json'); const de=require('./apps/frontend/src/locales/de.json'); const dk=['activeLabel','toggleActive','activatedToast','deactivatedToast','failedToToggle']; const st=['globalTitle','globalSubtitle','chooseDecks','startSession','backToDashboard','noActiveDecks','noActiveDecksHint']; const miss=[]; for(const k of dk){if(!en.decks[k])miss.push('en.decks.'+k); if(!de.decks[k])miss.push('de.decks.'+k);} for(const k of st){if(!en.study[k])miss.push('en.study.'+k); if(!de.study[k])miss.push('de.study.'+k);} if(miss.length){console.error('MISSING:',miss.join(', ')); process.exit(1);} console.log('all 24 keys present');"</automated>
  </verify>
  <acceptance_criteria>
    - en.json `decks` contains all 5 keys with the exact English copy above
    - de.json `decks` contains all 5 keys with the exact German copy above
    - en.json `study` contains all 7 new keys; de.json `study` contains the same 7 keys (German)
    - Neither file re-defines sessionSize / sizeAllDue / sizeCustom / nCardsDue
    - Both files remain valid JSON (the verify node script exits 0 with "all 24 keys present")
  </acceptance_criteria>
  <done>All 12 new keys exist in both locale files with correct copy; no duplicate of existing size/count keys; both files are valid JSON and parse-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static translation data only — no runtime trust boundary, no input handling, no install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information Disclosure | locale JSON | accept | Static UI copy only; no secrets, no user data, no executable content. German parity enforced by the verify script to prevent raw-key leakage (Pitfall 5). |
</threat_model>

<verification>
- The node verify script confirms all 24 (12 × 2 locales) keys are present and both files parse as valid JSON.
- After Plan 03 + Plan 04 land, no raw key strings (e.g. "study.globalTitle") appear in the rendered German UI.
</verification>

<success_criteria>
Both locale files carry the 12 new keys with correct English and German copy; no existing reused key is duplicated; JSON validity confirmed by the parse-verify script.
</success_criteria>

<output>
Create `.planning/phases/10-active-deck-rotation/10-05-SUMMARY.md` when done.
</output>
