---
phase: 21-changelog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [CHNG-01, CHNG-02]

must_haves:
  truths:
    - "A user or contributor can open CHANGELOG.md at the repo root and read a versioned record of every shipped release without using git"
    - "Every past milestone (v1.0, v1.1, v1.2, v1.3.0, v1.3.1, v1.3.2) has its own version entry, newest first"
    - "Each version entry lists user-facing change bullets, the requirement IDs satisfied, a Breaking Changes section, and a Migration Notes section"
    - "No changelog bullet contains implementation detail (no 'Prisma', 'Vitest', 'SQL', file names) — all bullets are user-perspective"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog format record of v1.0 through v1.3.2"
      contains: "## [v1.0]"
      min_lines: 120
  key_links:
    - from: "CHANGELOG.md"
      to: "Keep a Changelog format"
      via: "H2 version headings + Added/Changed/Fixed subsections"
      pattern: "## \\[v1\\."
---

<objective>
Backfill a durable, human-readable `CHANGELOG.md` at the repository root covering all six shipped milestones (v1.0, v1.1, v1.2, v1.3.0, v1.3.1, v1.3.2). The file follows the Keep a Changelog format (newest version first) and adds the project-specific sections required by CHNG-02: a Requirement IDs list, a Breaking Changes section, and a Migration Notes section per version.

Purpose: Any user or contributor can read the release history without accessing git history. Satisfies CHNG-01 and CHNG-02.
Output: `CHANGELOG.md` at repo root with six version entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Primary source — contains the complete, verbatim content outline for every version entry
@.planning/phases/21-changelog/21-RESEARCH.md
</context>

<artifacts_this_phase_produces>
New files created by this phase:
- `CHANGELOG.md` (repo root)

New symbols / structural anchors introduced (all are Markdown content, not code):
- H2 version headings: `## [v1.0]`, `## [v1.1]`, `## [v1.2]`, `## [v1.3.0]`, `## [v1.3.1]`, `## [v1.3.2]`
- Per-version subsections: `### Added`, `### Changed`, `### Fixed`, `### Removed` (as applicable), `### Requirement IDs`, `### Breaking Changes`, `### Migration Notes`
- A placeholder marker for pending Phase 22 content: `<!-- TODO Phase 22: add STUDY-04 and STUDY-05 bullets here -->`

No code, no exports, no schema changes, no package installs are produced by this phase.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write CHANGELOG.md at repo root covering v1.0 through v1.3.2</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/21-changelog/21-RESEARCH.md — `## Content Outline for CHANGELOG.md` section contains the exact bullets, requirement IDs, and migration notes for all six versions; `## DB Migration Inventory`, `## Env Var Change Inventory`, and `## Breaking Change Analysis` tables provide the per-version Migration Notes and Breaking Changes content; `## Common Pitfalls` (Pitfall 2) lists the user-facing quick-task bullets (dark mode → v1.1, scrollable card text → v1.3.0) that MUST be included
    - .planning/ROADMAP.md — milestone ship dates (v1.0 2026-05-30, v1.1 2026-06-01, v1.2 2026-06-04, v1.3.0 2026-06-11, v1.3.1 2026-06-12, v1.3.2 2026-06-14) and per-phase requirement lists
    - .planning/REQUIREMENTS.md — confirms CHNG-01/CHNG-02 scope and that STUDY-04/STUDY-05 are pending (Phase 22)
  </read_first>
  <action>
    Create `CHANGELOG.md` at the repository root in Keep a Changelog format. Begin the file with a top H1 `# Changelog`, a one-line intro stating the project adheres to Keep a Changelog and Semantic Versioning, then the six version entries in reverse chronological order (v1.3.2 first, v1.0 last). Do NOT include an `## [Unreleased]` section.

    For each version use an H2 heading of the form `## [v1.3.2] — 2026-06-14`, `## [v1.3.1] — 2026-06-12`, `## [v1.2] — 2026-06-04`, `## [v1.3.0] — 2026-06-11`, `## [v1.1] — 2026-06-01`, `## [v1.0] — 2026-05-30`. Order on the page: v1.3.2, v1.3.1, v1.3.0, v1.2, v1.1, v1.0. Include a short parenthetical milestone name after each heading line or as the first line, matching the names in RESEARCH.md (e.g. "MVP", "Study Experience & Polish", "Study Control & PWA", "Stats & Import Update", "Bug Fixes & Mobile Polish", "UX Polish & Changelog").

    For each version, transcribe the bullets from the RESEARCH.md `## Content Outline for CHANGELOG.md` section into the appropriate `### Added`, `### Changed`, `### Fixed`, and `### Removed` subsections (omit any subsection that has no bullets for that version). Use the bullets verbatim or lightly polished; keep them user-perspective — never mention Prisma, Vitest, SQL, ESLint, file names, or other implementation detail.

    Include the two quick-task bullets called out in RESEARCH.md Pitfall 2 / "Revised bullets to add":
    - v1.1 `### Added`: "Dark mode toggle in application settings (light/dark theme switcher)"
    - v1.3.0 `### Added`: "Study card back content is now scrollable when it overflows the card height, preventing text from being cut off during long study sessions"

    After the change subsections, every version entry MUST include these three sections in this order:
    - `### Requirement IDs` — a comma-separated list of the requirement IDs from RESEARCH.md for that version (e.g. v1.3.1 = MOB-01, DECK-05, LIB-01; v1.3.2 = LIB-02, BRAND-01, BRAND-02, CHNG-01, CHNG-02). Use the full ID lists exactly as given in RESEARCH.md per version.
    - `### Breaking Changes` — write "None" for every version (per RESEARCH.md `## Breaking Change Analysis`, no version has a breaking change). For v1.2 you may add the clarifying note from the outline that active/inactive toggles default to active.
    - `### Migration Notes` — transcribe the per-version DB-migration and env-var notes from the RESEARCH.md content outline and the DB/Env inventory tables. For versions with no migration, state "DB migrations: None." and "Env var changes: None." For v1.0 include the required env vars (JWT_SECRET, DB_PASSWORD; optional MAX_UPLOAD_SIZE_MB, STORAGE_PATH). For v1.2, v1.3.0, v1.3.1 state the new column(s)/table(s) in user-operator terms (what changed, that defaults protect existing data, and that migrations auto-apply on `docker compose up`) — do NOT include raw SQL DDL; describe the change in plain operator language.

    For the v1.3.2 entry: include the three completed bullets (new logo, regenerated PWA icons, remove-deck-from-library) under `### Added`, and the requirement IDs LIB-02, BRAND-01, BRAND-02, CHNG-01, CHNG-02. Because Phase 22 (STUDY-04, STUDY-05) has not yet run, add an HTML comment placeholder line exactly: `<!-- TODO Phase 22: add STUDY-04 and STUDY-05 bullets here -->` inside the v1.3.2 `### Added` section, and append a parenthetical note in the v1.3.2 `### Requirement IDs` line: "(STUDY-04, STUDY-05 to be added after Phase 22 completes)". Keep the v1.3.2 date as 2026-06-14 (milestone close date will be updated when Phase 22 closes).

    Do not write any other files. Do not run package installs, migrations, or code changes.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -c '^## \[v1' CHANGELOG.md | grep -qx 6 && grep -q '^## \[v1.3.2\]' CHANGELOG.md && grep -q '^## \[v1.0\]' CHANGELOG.md && grep -q 'TODO Phase 22' CHANGELOG.md && grep -q '### Breaking Changes' CHANGELOG.md && grep -q '### Migration Notes' CHANGELOG.md && grep -q '### Requirement IDs' CHANGELOG.md && echo PASS</automated>
    <human-check>Read CHANGELOG.md top-to-bottom: confirm six entries newest-first, every bullet is user-facing (no Prisma/Vitest/SQL/filenames), and each version has Requirement IDs + Breaking Changes + Migration Notes sections.</human-check>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` exists at repo root
    - `grep -c '^## \[v1' CHANGELOG.md` returns exactly `6`
    - File contains all six headings: `## [v1.0]`, `## [v1.1]`, `## [v1.2]`, `## [v1.3.0]`, `## [v1.3.1]`, `## [v1.3.2]`
    - Versions appear in reverse chronological order (v1.3.2 above v1.0 in the file)
    - Each of the six version entries contains a `### Requirement IDs` line, a `### Breaking Changes` section, and a `### Migration Notes` section
    - The v1.1 `### Added` section contains a dark mode bullet; the v1.3.0 `### Added` section contains a scrollable study card bullet
    - The v1.3.2 entry contains the literal placeholder `<!-- TODO Phase 22: add STUDY-04 and STUDY-05 bullets here -->`
    - No changelog bullet contains the strings `Prisma`, `Vitest`, `SQL`, `ESLint`, or a `.tsx`/`.ts` filename (implementation-detail leak check)
    - `### Breaking Changes` reads "None" for every version
  </acceptance_criteria>
  <done>CHANGELOG.md exists at repo root with six user-facing version entries (newest first), each carrying Requirement IDs, Breaking Changes, and Migration Notes sections; v1.3.2 carries the Phase 22 placeholder. CHNG-01 and CHNG-02 satisfied.</done>
</task>

</tasks>

<verification>
Manual verification checklist (no automatable behavior — pure documentation file):
- [ ] CHANGELOG.md exists at repo root
- [ ] Exactly 6 versioned entries: v1.0, v1.1, v1.2, v1.3.0, v1.3.1, v1.3.2 (newest first)
- [ ] Each entry has: user-facing bullets, Requirement IDs, Breaking Changes section, Migration Notes section
- [ ] No bullets contain implementation details (e.g. "Prisma", "Vitest", "SQL", filenames) — all user-perspective
- [ ] v1.1 includes dark mode bullet; v1.3.0 includes scrollable card text bullet
- [ ] v1.3.2 includes the Phase 22 TODO placeholder and pending STUDY-04/STUDY-05 note
</verification>

<success_criteria>
- CHANGELOG.md at repo root satisfies CHNG-01 (six milestone entries backfilled)
- Every entry satisfies CHNG-02 (user-facing bullets + requirement IDs + Breaking Changes + Migration Notes)
- The `grep -c '^## \[v1' CHANGELOG.md` automated count returns 6
</success_criteria>

<output>
Create `.planning/phases/21-changelog/21-01-SUMMARY.md` when done
</output>
