---
phase: 18-library-deck-toggle
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - apps/frontend/src/pages/DecksPage.tsx
  - apps/frontend/src/pages/__tests__/DecksPage.test.tsx
  - apps/backend/src/routes/__tests__/library-toggle.test.ts
autonomous: true
requirements:
  - LIB-01

must_haves:
  truths:
    - "Library deck card on DecksPage shows a Switch + Active label in its CardFooter"
    - "Toggling the Switch calls PATCH /api/decks/:id/library with { isActive: boolean }"
    - "Optimistic update reverts if PATCH fails; toast.error shown on failure"
    - "Library deck Switch id is active-lib-{deckId} (distinct from owned-deck active-{deckId})"
    - "Owned-deck behavior unchanged — handleToggleActive still calls PATCH /api/decks/:id"
    - "Frontend and backend test suites pass"
  artifacts:
    - path: "apps/frontend/src/pages/DecksPage.tsx"
      provides: "handleToggleLibraryActive handler + library CardFooter Switch"
      contains: "handleToggleLibraryActive"
    - path: "apps/frontend/src/pages/__tests__/DecksPage.test.tsx"
      provides: "LIB-01 library toggle tests"
      contains: "LIB-01"
    - path: "apps/backend/src/routes/__tests__/library-toggle.test.ts"
      provides: "PATCH /api/decks/:id/library route behavior tests"
      contains: "PATCH /api/decks/:id/library"
  key_links:
    - from: "apps/frontend/src/pages/DecksPage.tsx"
      to: "PATCH /api/decks/:id/library"
      via: "api.patch call in handleToggleLibraryActive"
      pattern: "api\\.patch.*library"
    - from: "Switch onCheckedChange"
      to: "handleToggleLibraryActive"
      via: "void handleToggleLibraryActive(deck.id, checked)"
      pattern: "handleToggleLibraryActive"
---

<objective>
Implement the frontend half of LIB-01: library deck toggle in DecksPage plus test coverage.

This plan adds `handleToggleLibraryActive` to DecksPage, adds a Switch + label to the library deck CardFooter (`deck.ownerId !== user?.id` branch), and adds tests for both the new frontend behavior and the backend PATCH endpoint introduced in Plan 18-01.

Purpose: Without this plan, the backend endpoint exists but the UI has no way to call it — library decks show no toggle, and the study filter fix in Plan 18-01 has no meaningful user control.

Output: Updated DecksPage.tsx with library toggle, expanded DecksPage.test.tsx, new library-toggle.test.ts backend stub.
</objective>

<execution_context>
@C:\Users\rapha\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\rapha\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-library-deck-toggle/18-CONTEXT.md
@.planning/phases/18-library-deck-toggle/18-PATTERNS.md
@.planning/phases/18-library-deck-toggle/18-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol | Type | File |
|--------|------|------|
| `handleToggleLibraryActive` | async function `(deckId: string, checked: boolean) => Promise<void>` | `apps/frontend/src/pages/DecksPage.tsx` |
| Library CardFooter Switch | JSX with `id="active-lib-{deck.id}"` + `onCheckedChange` wired to `handleToggleLibraryActive` | `apps/frontend/src/pages/DecksPage.tsx` |
| `DecksPage library toggle tests` | 3 new test cases in `describe('DecksPage library deck toggle (LIB-01)')` | `apps/frontend/src/pages/__tests__/DecksPage.test.tsx` |
| `library-toggle.test.ts` | Backend test stubs for PATCH /api/decks/:id/library | `apps/backend/src/routes/__tests__/library-toggle.test.ts` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add handleToggleLibraryActive + library Switch to DecksPage</name>
  <files>
    apps/frontend/src/pages/DecksPage.tsx
  </files>
  <read_first>
    - apps/frontend/src/pages/DecksPage.tsx (full file — handleToggleActive lines 102–116 is the analog; library CardFooter branch lines 167–175; owned-deck Switch block lines 177–188)
    - apps/frontend/src/locales/en.json (confirm decks.toggleActive, decks.activeLabel, decks.activatedToast, decks.deactivatedToast, decks.failedToToggle keys exist — D-14: no new keys needed)
    - apps/frontend/src/pages/__tests__/DecksPage.test.tsx (understand existing test structure: AuthContext not mocked, makeDeck factory uses ownerId: 'user-1')
  </read_first>
  <behavior>
    - handleToggleLibraryActive(deckId, true): calls api.patch('/api/decks/{deckId}/library', { isActive: true }), shows activatedToast on success
    - handleToggleLibraryActive(deckId, false): calls api.patch('/api/decks/{deckId}/library', { isActive: false }), shows deactivatedToast on success
    - handleToggleLibraryActive on api.patch failure: reverts deck.isActive to !checked in state, calls toast.error with failedToToggle
    - Library CardFooter Switch: rendered when deck.ownerId !== user?.id; id="active-lib-{deck.id}"; aria-label from t('decks.toggleActive')
    - Owned-deck behavior: handleToggleActive unchanged; still calls /api/decks/{deckId} (not /library)
  </behavior>
  <action>
    Change 1 — handleToggleLibraryActive: After `handleToggleActive` (line 116), add a new function with the exact same structure but targeting the `/library` sub-path:
    - Function signature: `const handleToggleLibraryActive = async (deckId: string, checked: boolean) => {`
    - Optimistic update: `setDecks((prev) => prev.map((d) => (d.id === deckId ? { ...d, isActive: checked } : d)))`
    - API call: `api.patch(\`/api/decks/${deckId}/library\`, { isActive: checked })`
    - Success branch: `toast.success(checked ? t('decks.activatedToast') : t('decks.deactivatedToast'))`
    - Failure branch (catch or !res.ok): revert + `toast.error(t('decks.failedToToggle'))`
    Same structure as `handleToggleActive` — differ only in the URL.

    Change 2 — library CardFooter Switch: In the `deck.ownerId !== user?.id ? (` branch (currently lines 167–175), add a Switch block BEFORE the Study button. The Switch block must:
    - Be wrapped in `<div className="flex items-center gap-2 mr-auto">`
    - Use `<Switch checked={deck.isActive} onCheckedChange={(checked) => void handleToggleLibraryActive(deck.id, checked)} aria-label={t('decks.toggleActive')} id={\`active-lib-${deck.id}\`} />`
    - Use `<label htmlFor={\`active-lib-${deck.id}\`} className="text-sm text-muted-foreground cursor-pointer">{t('decks.activeLabel')}</label>`
    - The `id` prefix is `active-lib-` (not `active-`) to prevent id collision with owned-deck Switch when the same deckId appears in both branches (edge case guard)

    The resulting library CardFooter must have: Switch div (mr-auto), Study button, Open button — three children in that order.

    No other changes to DecksPage.tsx. The opacity wrapper `<div className={deck.isActive ? '' : 'opacity-60'}>` at line 145 already uses `deck.isActive` and works automatically once the backend returns DeckShare.isActive in that field.

    Note: DecksPage imports `useAuth` (line 18) and destructures `user` (line 61) — this is already present; do not add a duplicate import.
  </action>
  <verify>
    <automated>
      grep "handleToggleLibraryActive" apps/frontend/src/pages/DecksPage.tsx
      grep "active-lib-" apps/frontend/src/pages/DecksPage.tsx
      cd apps/frontend && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `DecksPage.tsx` contains `handleToggleLibraryActive` function
    - `handleToggleLibraryActive` calls `api.patch(\`/api/decks/${deckId}/library\`, { isActive: checked })`
    - Library CardFooter (`deck.ownerId !== user?.id` branch) contains a `Switch` with `id={\`active-lib-${deck.id}\`}`
    - Switch `onCheckedChange` calls `void handleToggleLibraryActive(deck.id, checked)`
    - `handleToggleActive` is unchanged — still calls `/api/decks/${deckId}` (no `/library` suffix)
    - `npx tsc --noEmit` in `apps/frontend/` exits 0
  </acceptance_criteria>
  <done>Library deck CardFooter shows the Switch; handleToggleLibraryActive wired to correct endpoint; TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand DecksPage.test.tsx for library toggle + add backend library-toggle.test.ts</name>
  <files>
    apps/frontend/src/pages/__tests__/DecksPage.test.tsx,
    apps/backend/src/routes/__tests__/library-toggle.test.ts
  </files>
  <read_first>
    - apps/frontend/src/pages/__tests__/DecksPage.test.tsx (full file — understand existing mock setup: vi.hoisted mockApiGet + mockApiPatch, existing describe block, makeDeck factory with ownerId: 'user-1'; note line 36 comment "No AuthContext mock — DecksPage does not import useAuth" is now outdated — useAuth IS imported)
    - apps/frontend/src/pages/DecksPage.tsx (confirm library Switch id="active-lib-{id}" and aria-label from t('decks.toggleActive') — needed for test selectors)
    - apps/backend/src/routes/__tests__/sharing.test.ts (stub test file format to match for library-toggle.test.ts)
    - apps/frontend/src/context/AuthContext.tsx (if needed — confirm mock shape)
    - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx lines 1–23 (AuthContext mock pattern)
  </read_first>
  <behavior>
    Frontend tests (library toggle):
    - LIB-01a: Library deck Switch renders with id="active-lib-{id}" and is checked when deck.isActive=true
    - LIB-01b: Library deck Switch is unchecked when deck.isActive=false
    - LIB-01c: Clicking library Switch calls api.patch('/api/decks/d3/library', { isActive: false }) when toggled from active
    - LIB-01d: On PATCH failure, library Switch reverts to checked and toast.error is called

    Backend tests (stubs only — Prisma integration deferred):
    - PATCH returns 200 + { isActive: false } when recipient deactivates
    - PATCH returns 403 when called by deck owner
    - PATCH returns 403 when no share row exists for (deckId, userId)
    - PATCH returns 400 when body is missing isActive or has non-boolean value
  </behavior>
  <action>
    Change 1 — DecksPage.test.tsx: The file needs three additions:

    A) Add AuthContext mock at the top of the file (before or after the existing api mock):
    `vi.mock('@/context/AuthContext', () => ({ useAuth: () => ({ user: { id: 'user-1', username: 'testuser', role: 'USER', isActive: true, createdAt: '2026-01-01' }, loading: false, setUser: vi.fn(), logout: vi.fn() }) }))`
    User id 'user-1' must match the `ownerId` in `makeDeck` for owned-deck tests to continue working (Switch renders only when ownerId === user.id). The existing owned-deck tests rely on `ownerId: 'user-1'` matching the mocked user id — confirm this match is preserved.

    B) Add `makeLibraryDeck` factory function after `makeDeck`:
    ```
    function makeLibraryDeck(id: string, isActive = true) {
      return {
        id,
        title: `Library Deck ${id}`,
        description: null,
        visibility: 'PUBLIC' as const,
        ownerId: 'other-user',   // different from mocked user id 'user-1'
        isActive,
        createdAt: '2026-01-01',
        updatedAt: '2026-01-01',
        _count: { cards: 5 },
        sharedByUsername: 'other-user',
      } as unknown as import('@kartex/shared').DeckListItem & { isActive: boolean }
    }
    ```

    C) Add a new `describe('DecksPage library deck toggle (LIB-01)')` block with four `it` cases:
    - LIB-01a: render library deck; `screen.findByRole('switch', { name: /toggle deck active/i })` returns checked switch; assert id contains 'active-lib-'
    - LIB-01b: render library deck with isActive=false; switch is unchecked (data-state='unchecked' or aria-checked='false')
    - LIB-01c: click library switch; `waitFor(() => expect(mockApiPatch).toHaveBeenCalledWith('/api/decks/d3/library', { isActive: false }))`
    - LIB-01d: PATCH fails (`mockApiPatch.mockResolvedValue({ ok: false })`); waitFor toast.error called; switch reverts to checked

    For LIB-01a, the GET mock must return `[makeLibraryDeck('d3', true)]`. In beforeEach, reset `mockApiPatch` and default GET to return empty list; override per test.

    Remove the stale comment `// No AuthContext mock — DecksPage does not import useAuth` from line 36, replacing it with `// AuthContext mock required — DecksPage uses useAuth for user.id check (ownerId guard)`.

    Change 2 — library-toggle.test.ts: Create new file at `apps/backend/src/routes/__tests__/library-toggle.test.ts`. Use the same stub format as `sharing.test.ts`. Add two `describe` blocks:
    - `describe('PATCH /api/decks/:id/library — isActive toggle (LIB-01)')` with four `it.todo` cases:
      - `'returns 200 + { isActive: false } when share recipient deactivates (D-09)'`
      - `'returns 200 + { isActive: true } when share recipient re-activates (D-09)'`
      - `'returns 403 when called by the deck owner (D-08)'`
      - `'returns 403 when no DeckShare row exists for (deckId, userId) (D-08)'`
      - `'returns 400 when body.isActive is missing or non-boolean (D-17)'`
    - `describe('GET /api/decks — isActive override for shared decks (LIB-01)')` with two `it.todo` cases:
      - `'returns DeckShare.isActive (false) not Deck.isActive (true) for shared deck (D-06)'`
      - `'returns DeckShare.isActive (true) for active shared deck (D-06)'`
  </action>
  <verify>
    <automated>
      grep "LIB-01" apps/frontend/src/pages/__tests__/DecksPage.test.tsx
      grep "makeLibraryDeck" apps/frontend/src/pages/__tests__/DecksPage.test.tsx
      grep "AuthContext" apps/frontend/src/pages/__tests__/DecksPage.test.tsx
      grep "LIB-01" apps/backend/src/routes/__tests__/library-toggle.test.ts
      cd apps/frontend && yarn test --run 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `DecksPage.test.tsx` contains `describe('DecksPage library deck toggle (LIB-01)')` block with 4 `it` cases (not `.todo`)
    - `DecksPage.test.tsx` mocks `@/context/AuthContext` returning user with `id: 'user-1'`
    - `makeLibraryDeck` factory uses `ownerId: 'other-user'` so library branch renders for that deck
    - LIB-01c test asserts `mockApiPatch` called with `'/api/decks/d3/library'` (not `/api/decks/d3`)
    - Existing owned-deck tests (`DECK-01a` through `DECK-01d`) still pass without modification
    - `library-toggle.test.ts` exists with the two `describe` blocks and all stubs as `it.todo`
    - `cd apps/frontend && yarn test --run` exits 0 (all tests pass including new LIB-01 cases)
    - `cd apps/backend && yarn test --run` exits 0 (stubs are `.todo` — they don't fail)
  </acceptance_criteria>
  <done>Library toggle tests pass; backend stub file created; existing DECK-01 tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user interaction → Switch onCheckedChange | Browser event triggers optimistic state update before network confirmation |
| frontend state → PATCH /api/decks/:id/library | Checked boolean sent to backend; backend is authoritative (validates with UpdateLibrarySchema) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering | handleToggleLibraryActive optimistic update | accept | Optimistic update is client-side UX only; backend validates and stores truth; revert on failure ensures eventual consistency |
| T-18-07 | Tampering | Switch id collision (active- vs active-lib-) | mitigate | Library Switch uses `id="active-lib-{id}"`, owned-deck uses `id="active-{id}"` — distinct IDs prevent label-for mismatch and test selector ambiguity |
| T-18-08 | Information Disclosure | Test file exposes auth mock shape | accept | Test files are not shipped; mock shape mirrors AuthContext public interface only |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — Switch component and all dependencies already installed via Phase 10 (10-02-PLAN.md) |
</threat_model>

<verification>
After both tasks complete:
1. `apps/frontend/src/pages/DecksPage.tsx` contains `handleToggleLibraryActive` calling `/api/decks/${deckId}/library`
2. Library CardFooter Switch uses `id={\`active-lib-${deck.id}\`}` and `onCheckedChange` wired to `handleToggleLibraryActive`
3. `handleToggleActive` unchanged — still calls `/api/decks/${deckId}`
4. `DecksPage.test.tsx` passes all 8 test cases (4 existing DECK-01 + 4 new LIB-01)
5. `library-toggle.test.ts` exists with stubbed backend tests (`it.todo`)
6. `npx tsc --noEmit` exits 0 in `apps/frontend/`
7. `cd apps/frontend && yarn test --run` exits 0
8. `cd apps/backend && yarn test --run` exits 0
</verification>

<success_criteria>
- Library deck card on DecksPage renders Switch identical in appearance to owned-deck Switch
- Switch calls PATCH /api/decks/:id/library — not the owned-deck PATCH /api/decks/:id
- Optimistic update + rollback pattern matches owned-deck behavior exactly
- All frontend tests pass; backend stub tests exist without failures
- TypeScript compilation clean
</success_criteria>

<output>
Create `.planning/phases/18-library-deck-toggle/18-02-SUMMARY.md` when done
</output>
