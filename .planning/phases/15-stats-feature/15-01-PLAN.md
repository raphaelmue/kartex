---
phase: 15-stats-feature
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/backend/src/routes/__tests__/stats-summary.test.ts
  - apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx
  - apps/frontend/src/pages/__tests__/DashboardPage.test.tsx
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03, STATS-04]

must_haves:
  truths:
    - "Backend test file exists documenting the four stats behaviors (STATS-01..04)"
    - "Frontend component test file exists documenting StatsSummaryPanel empty/full/null states"
    - "Frontend page test file exists documenting parallel fetch + failure isolation"
    - "All three test files are discovered by Vitest and the suite stays green (todo stubs do not fail)"
  artifacts:
    - path: "apps/backend/src/routes/__tests__/stats-summary.test.ts"
      provides: "RED behavioral contract for GET /api/stats/summary"
      contains: "describe"
    - path: "apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx"
      provides: "RED behavioral contract for StatsSummaryPanel"
      contains: "describe"
    - path: "apps/frontend/src/pages/__tests__/DashboardPage.test.tsx"
      provides: "RED behavioral contract for DashboardPage parallel fetch"
      contains: "describe"
  key_links:
    - from: "apps/backend/src/routes/__tests__/stats-summary.test.ts"
      to: "apps/backend/src/routes/stats.ts (Wave 1)"
      via: "test imports / documents the route under test"
      pattern: "stats/summary"
---

<objective>
Establish the Wave 0 RED test scaffold for Phase 15 (Stats Feature). Create three test files that document the required behavior for the backend stats endpoint, the StatsSummaryPanel component, and the DashboardPage parallel-fetch integration — before any implementation exists.

Purpose: Nyquist validation requires every behavior to have a test that is created first. These stubs lock the behavioral contract so Wave 1/2 executors implement against a fixed target rather than inventing one.
Output: Three test files that Vitest discovers and that keep the suite green (stub assertions use `it.todo`, matching the established `study-rate-reviewlog.test.ts` pattern).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-stats-feature/15-VALIDATION.md
@.planning/phases/15-stats-feature/15-RESEARCH.md
@apps/backend/src/routes/__tests__/study-rate-reviewlog.test.ts
@apps/frontend/src/pages/__tests__/DecksPage.test.tsx
@packages/shared/src/schemas/stats.ts
</context>

<artifacts_this_phase_produces>
Phase 15 creates these new symbols (exclude from drift verification — they do not exist yet):
- `apps/backend/src/routes/stats.ts` (Wave 1) — exports `statsRouter`
- `GET /api/stats/summary` endpoint (Wave 1)
- `apps/frontend/src/components/StatsSummaryPanel.tsx` (Wave 2) — exports `StatsSummaryPanel`, prop type `StatsSummaryPanelProps`
- i18n keys under `dashboard.stats.*` (Wave 1): totalReviewed, thisWeek, retentionRate, difficultyBreakdown, noData, perDeckProgress, masteredColumn, inLearningColumn, easyLabel, goodLabel, hardLabel, againLabel
- Three test files (this plan)

Already exists (Phase 14 — do NOT recreate): `StatsSummarySchema`, `PerDeckProgressSchema`, `DifficultyBreakdownSchema`, `MASTERED_INTERVAL_DAYS`, `MASTERED_REPETITIONS`, `StatsSummary` type, `ReviewLog` Prisma model + `@@index([userId, reviewedAt])`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Backend stats-summary RED test stub</name>
  <files>apps/backend/src/routes/__tests__/stats-summary.test.ts</files>
  <read_first>
    - apps/backend/src/routes/__tests__/study-rate-reviewlog.test.ts (the exact `it.todo` stub pattern to replicate — imports only `describe, it` from vitest)
    - packages/shared/src/schemas/stats.ts (field names that the documented behavior must reference: totalReviewed, weekReviewed, retentionRate, difficultyBreakdown {easy,good,hard,again}, perDeck[] {deckId, deckTitle, dueCount, masteredCount, inLearningCount})
    - .planning/phases/15-stats-feature/15-VALIDATION.md (Per-Task Verification Map rows 15-02..15-04)
  </read_first>
  <action>
    Create the file importing `{ describe, it }` from 'vitest' (no `expect`, no mocks — match study-rate-reviewlog.test.ts exactly). Add one top-level `describe('GET /api/stats/summary — stats aggregation (STATS-01..04)', () => { ... })` block containing `it.todo(...)` entries that document the locked behavior, one per bullet below. Do NOT write executing assertions (todo entries keep the suite green while Wave 1 is unimplemented).
    Document these todos verbatim in intent:
    (STATS-01) "returns totalReviewed = count of CardProgress rows for the authenticated userId (unique card/user pairs, all-time)";
    (STATS-01) "returns weekReviewed = count of CardProgress rows where lastReviewed >= start of current week (Sunday 00:00)";
    (STATS-02) "retentionRate is null when no ReviewLog rows exist for the user in the last 30 days — never 0";
    (STATS-02) "retentionRate equals goodLast30 / totalLast30 (a float in [0,1]) where good = rating >= 3, when ReviewLog rows exist in the last 30 days";
    (STATS-03) "difficultyBreakdown is null when no ReviewLog rows exist in the last 30 days — never a zero-filled object";
    (STATS-03) "difficultyBreakdown maps rating 1->again, 2->hard, 3->good, 4->easy with missing buckets defaulting to 0";
    (STATS-04) "perDeck lists every deck owned by the user, including decks with zero cards (zero counts, never omitted)";
    (STATS-04) "perDeck masteredCount counts cards with progress.interval >= MASTERED_INTERVAL_DAYS (21) AND progress.repetitions >= MASTERED_REPETITIONS (3)";
    (security T-15-01) "every Prisma query is scoped to userId from c.get('userId') — never from URL params or request body".
  </action>
  <verify>
    <automated>yarn workspace @kartex/backend test --run</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/routes/__tests__/stats-summary.test.ts` exists and imports `{ describe, it }` from 'vitest' (no `expect` import)
    - File contains exactly one top-level `describe` whose title includes "STATS-01" or "stats aggregation"
    - File contains at least 9 `it.todo(` entries covering STATS-01 (×2), STATS-02 (×2), STATS-03 (×2), STATS-04 (×2), and the userId-scoping security todo
    - `yarn workspace @kartex/backend test --run` exits 0 (todo stubs report as pending, not failures)
    - No reference to a `stats.ts` import that does not yet exist (todos are descriptive strings only — file must not import `../stats.js`)
  </acceptance_criteria>
  <done>The backend stats behavioral contract exists as `it.todo` stubs; backend suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend StatsSummaryPanel + DashboardPage RED test stubs</name>
  <files>apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx, apps/frontend/src/pages/__tests__/DashboardPage.test.tsx</files>
  <read_first>
    - apps/frontend/src/pages/__tests__/DecksPage.test.tsx (the React Testing Library + vi.hoisted api mock pattern; reuse `import { describe, it } from 'vitest'` minimal form for todo-only stubs)
    - apps/backend/src/routes/__tests__/study-rate-reviewlog.test.ts (the `it.todo` minimal-import convention)
    - apps/frontend/src/pages/DashboardPage.tsx (current single-fetch shape that Wave 2 replaces — todos must reference parallel fetch + the existing `if (loading)` hero guard)
    - .planning/phases/15-stats-feature/15-UI-SPEC.md (Interaction Contract: skeleton, empty state, silent-failure behavior)
  </read_first>
  <action>
    Create both files using the minimal `import { describe, it } from 'vitest'` + `it.todo(...)` convention (no rendering yet — Wave 2 fills in real assertions). Do NOT import `StatsSummaryPanel` or render `DashboardPage` (those imports would break before Wave 2 — todos are descriptive strings only).
    In `StatsSummaryPanel.test.tsx` add `describe('StatsSummaryPanel (STATS-01..04)', ...)` with todos:
    "renders skeleton placeholders (aria-busy) when loading is true";
    "renders Total Reviewed chip with totalReviewed value and 'this week' sub-label (STATS-01)";
    "renders 'No data yet' for retention chip when summary.retentionRate === null (STATS-02)";
    "renders rounded percentage when summary.retentionRate is a number (STATS-02)";
    "renders 'No data yet' for difficulty chip when summary.difficultyBreakdown === null (STATS-03)";
    "renders per-deck rows for every deck including zero-card decks with zero counts (STATS-04)";
    "renders all chips in empty/zero state when summary === null".
    In `DashboardPage.test.tsx` add `describe('DashboardPage stats integration (Phase 15)', ...)` with todos:
    "fires /api/dashboard/stats and /api/stats/summary in parallel via Promise.allSettled (STATS-01)";
    "renders the hero section as soon as dashboard stats resolve, without waiting for the stats summary fetch";
    "when /api/stats/summary rejects, the dashboard hero still renders and no toast.error is shown (SC-5, T-15-04)";
    "passes statsLoading to StatsSummaryPanel so skeletons show while the summary fetch is pending".
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend test --run</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx` exists, imports `{ describe, it }` from 'vitest', and contains >= 7 `it.todo(` entries
    - `apps/frontend/src/pages/__tests__/DashboardPage.test.tsx` exists, imports `{ describe, it }` from 'vitest', and contains >= 4 `it.todo(` entries including one referencing "Promise.allSettled" and one referencing "no toast" / SC-5
    - Neither file imports `@/components/StatsSummaryPanel` or renders `DashboardPage` (todo-only stubs; no broken imports before Wave 2)
    - `yarn workspace @kartex/frontend test --run` exits 0
  </acceptance_criteria>
  <done>Both frontend behavioral contracts exist as `it.todo` stubs; frontend suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test-only plan) | This plan creates documentation-only test stubs; no runtime trust boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | stats-summary.test.ts | mitigate | Stub explicitly documents the userId-from-JWT scoping requirement so Wave 1 cannot omit it (todo: "every Prisma query is scoped to userId from c.get('userId')") |
| T-15-04 | Denial of Service (UX) | DashboardPage.test.tsx | mitigate | Stub documents that a stats fetch failure must not blank the dashboard (todo: "hero still renders and no toast.error") |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 15 — RESEARCH.md Package Legitimacy Audit confirms zero new dependencies |
</threat_model>

<verification>
- `yarn workspace @kartex/backend test --run` exits 0 with the new backend stub discovered
- `yarn workspace @kartex/frontend test --run` exits 0 with both new frontend stubs discovered
- No stub imports a not-yet-created module (`stats.ts`, `StatsSummaryPanel`)
</verification>

<success_criteria>
- Three test files exist at the paths in `files_modified`
- All three are todo-only stubs that keep both Vitest suites green
- Every requirement (STATS-01..04) and SC-5 has at least one documented todo
</success_criteria>

<output>
Create `.planning/phases/15-stats-feature/15-01-SUMMARY.md` when done
</output>
