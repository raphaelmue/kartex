---
phase: 11-sm2-preset-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/user.ts
  - apps/frontend/src/context/AuthContext.tsx
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
autonomous: true
requirements: [SM2-01, SM2-04]
must_haves:
  truths:
    - "StudyModeSchema z.enum(['normal','intensive','exam_prep']) is exported from shared"
    - "UserSchema has studyMode: StudyModeSchema.default('normal')"
    - "UpdateStudyModeSchema = z.object({ studyMode: StudyModeSchema }) is exported"
    - "AuthContext User interface has studyMode: string"
    - "en.json and de.json each have settings.* keys including nested settings.modeNames.*"
  artifacts:
    - path: "packages/shared/src/schemas/user.ts"
      provides: "StudyModeSchema, extended UserSchema, UpdateStudyModeSchema"
      contains: "StudyModeSchema"
    - path: "apps/frontend/src/context/AuthContext.tsx"
      provides: "studyMode field in User interface"
      contains: "studyMode"
    - path: "apps/frontend/src/locales/en.json"
      provides: "settings.* keys including modeNames"
      contains: "modeNames"
    - path: "apps/frontend/src/locales/de.json"
      provides: "German settings.* keys"
      contains: "modeNames"
  key_links:
    - from: "packages/shared/src/schemas/user.ts"
      to: "UserResponseSchema"
      via: "UserResponseSchema = UserSchema — inherits studyMode automatically"
      pattern: "UserResponseSchema = UserSchema"
    - from: "packages/shared/src/index.ts"
      to: "StudyModeSchema"
      via: "export * from './schemas/user' — no index.ts change needed"
      pattern: "export \\* from"
---

<objective>
Lay the type foundation for SM-2 preset modes:
1. Add `StudyModeSchema`, extend `UserSchema` with `studyMode`, add `UpdateStudyModeSchema` to the shared package.
2. Add `studyMode: string` to the `AuthContext` local `User` interface so the context value carries it after Plan 11-02 makes the API return it.
3. Add all `settings.*` i18n keys (including nested `modeNames`) to both locale files atomically.

Purpose: Plans 11-02 (backend) and 11-03/04 (frontend) all depend on these types and keys existing first. No migration needed — `User.studyMode` column already exists from Phase 10.
Output: Updated shared schemas, updated AuthContext, updated locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sm2-preset-modes/11-RESEARCH.md
@.planning/phases/11-sm2-preset-modes/11-PATTERNS.md
@.planning/phases/11-sm2-preset-modes/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StudyModeSchema, extend UserSchema, add UpdateStudyModeSchema</name>
  <files>packages/shared/src/schemas/user.ts</files>
  <read_first>
    - packages/shared/src/schemas/user.ts (current content: UserRole z.enum, UserSchema with id/username/role/isActive/createdAt, UserResponseSchema = UserSchema — add studyMode AFTER isActive in UserSchema field order; add StudyModeSchema above UserSchema; add UpdateStudyModeSchema below UserResponseSchema)
    - packages/shared/src/index.ts (exports `export * from './schemas/user'` — no change needed; all new exports auto-re-exported)
    - .planning/phases/11-sm2-preset-modes/11-PATTERNS.md (§StudyModeSchema definition and §UpdateStudyModeSchema)
  </read_first>
  <action>
    In `packages/shared/src/schemas/user.ts`:

    1. After the `import { z } from 'zod'` line, add:
       ```typescript
       export const StudyModeSchema = z.enum(['normal', 'intensive', 'exam_prep'])
       export type StudyMode = z.infer<typeof StudyModeSchema>
       ```

    2. In `UserSchema`, add `studyMode: StudyModeSchema.default('normal')` after the `isActive: z.boolean()` line and before `createdAt: z.coerce.date()`.

    3. After the `UserResponseSchema` and `UserResponse` exports, add:
       ```typescript
       export const UpdateStudyModeSchema = z.object({
         studyMode: StudyModeSchema,
       })
       export type UpdateStudyModeInput = z.infer<typeof UpdateStudyModeSchema>
       ```

    Do not change any existing field, ordering, or export that is not described above. `UserResponseSchema = UserSchema` will automatically include `studyMode` — no change needed.
  </action>
  <verify>
    <automated>yarn workspace @kartex/shared run build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/schemas/user.ts` exports `StudyModeSchema`, `StudyMode`, `UpdateStudyModeSchema`, `UpdateStudyModeInput`
    - `UserSchema` contains `studyMode: StudyModeSchema.default('normal')`
    - `yarn workspace @kartex/shared run build` exits 0
    - No existing exports removed or renamed
  </acceptance_criteria>
  <done>Shared package exports StudyModeSchema, extended UserSchema with studyMode, and UpdateStudyModeSchema. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add studyMode to AuthContext User interface</name>
  <files>apps/frontend/src/context/AuthContext.tsx</files>
  <read_first>
    - apps/frontend/src/context/AuthContext.tsx (local User interface at lines 7–13: id, username, role, isActive, createdAt — add studyMode after isActive; the hydration at lines 38–48 calls setUser(data) where data comes from GET /api/auth/me — after Plan 11-02 adds studyMode to the select, the API will return it and it will propagate automatically)
  </read_first>
  <action>
    In the local `User` interface in `apps/frontend/src/context/AuthContext.tsx`, add `studyMode: string` after the `isActive: boolean` line.

    No other changes needed — the `setUser(data)` call already assigns the full response body.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `AuthContext.tsx` User interface contains `studyMode: string`
    - `yarn workspace @kartex/frontend run build` exits 0
  </acceptance_criteria>
  <done>AuthContext User interface carries studyMode. Frontend builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add settings.* i18n keys to en.json and de.json atomically</name>
  <files>apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/locales/en.json (current structure — settings is NOT a top-level key yet; the JSON ends with the "lang" key block; add "settings" before "lang" to maintain alphabetical ordering)
    - apps/frontend/src/locales/de.json (same structure — must add identical key paths with German values)
    - .planning/phases/11-sm2-preset-modes/11-UI-SPEC.md (§i18n Key Inventory — exact key names, English and German values)
    - .planning/STATE.md (Pitfall 5 from Phase 10: both locale files must be updated atomically — de.json missing keys fall back to raw key string, not English value)
  </read_first>
  <action>
    Add a new `"settings"` top-level key to both `apps/frontend/src/locales/en.json` AND `apps/frontend/src/locales/de.json`. Both files must be updated in the same task execution.

    **en.json** — add before the `"lang"` key:
    ```json
    "settings": {
      "title": "Settings — Kartex",
      "pageHeading": "Settings",
      "studyModeSection": "Study Mode",
      "studyModeDesc": "Controls how aggressively your SM-2 review intervals are compressed. The raw interval stored per card is never modified — only the next review date shifts.",
      "modeNames": {
        "normal": "Normal",
        "intensive": "Intensive",
        "exam_prep": "Exam Prep"
      },
      "modeNormalDesc": "Standard SM-2 scheduling",
      "modeIntensiveDesc": "Reviews at ½ interval — doubles revisit frequency",
      "modeExamPrepDesc": "Reviews at ¼ interval — maximum revisit frequency",
      "saved": "Study mode saved",
      "saveFailed": "Failed to save — please try again"
    },
    ```

    **de.json** — add the same key structure with German values before `"lang"`:
    ```json
    "settings": {
      "title": "Einstellungen — Kartex",
      "pageHeading": "Einstellungen",
      "studyModeSection": "Lernmodus",
      "studyModeDesc": "Steuert, wie stark deine SM-2-Wiederholungsintervalle komprimiert werden. Das gespeicherte Intervall pro Karte wird nie verändert — nur das Datum der nächsten Wiederholung verschiebt sich.",
      "modeNames": {
        "normal": "Normal",
        "intensive": "Intensiv",
        "exam_prep": "Prüfungsvorbereitung"
      },
      "modeNormalDesc": "Standard-SM-2-Planung",
      "modeIntensiveDesc": "Wiederholungen bei ½ Intervall — doppelte Häufigkeit",
      "modeExamPrepDesc": "Wiederholungen bei ¼ Intervall — maximale Häufigkeit",
      "saved": "Lernmodus gespeichert",
      "saveFailed": "Speichern fehlgeschlagen — bitte erneut versuchen"
    },
    ```

    Ensure valid JSON in both files (no trailing commas, proper comma before `"lang"`).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('apps/frontend/src/locales/en.json','utf8')); JSON.parse(require('fs').readFileSync('apps/frontend/src/locales/de.json','utf8')); console.log('JSON valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `en.json` contains `"settings": { ... "modeNames": { "normal": ..., "intensive": ..., "exam_prep": ... } ... }` with all 10 keys
    - `de.json` contains the same key structure with German values
    - Both files parse as valid JSON
    - No keys missing from either file relative to the other (locale parity)
  </acceptance_criteria>
  <done>Both locale files have all settings.* keys including nested modeNames. JSON is valid in both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared schemas → frontend + backend | StudyModeSchema enum constrains valid studyMode values at runtime via Zod parse |
| locale files → rendered UI | User-visible mode names come from locale strings, never from raw enum values (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | StudyModeSchema enum | mitigate | z.enum enforces only ['normal','intensive','exam_prep'] — UpdateStudyModeSchema.safeParse rejects any other string in PATCH /me handler |
| T-11-02 | Spoofing | studyMode raw string in UI | mitigate | All mode names rendered via `t('settings.modeNames.${studyMode}')` — never raw enum value (D-07 compliance) |
</threat_model>

<verification>
- `yarn workspace @kartex/shared run build` exits 0.
- `grep -n 'StudyModeSchema' packages/shared/src/schemas/user.ts` returns the z.enum definition.
- `grep -n 'studyMode' apps/frontend/src/context/AuthContext.tsx` returns the User interface field.
- Both locale files validate as JSON; each contains `settings.modeNames.normal/intensive/exam_prep`.
</verification>

<success_criteria>
Shared package exports StudyModeSchema, UpdateStudyModeSchema, and extended UserSchema with studyMode. AuthContext User carries studyMode. Both locale files have settings.* keys with modeNames nested.
</success_criteria>

<output>
Create `.planning/phases/11-sm2-preset-modes/11-01-SUMMARY.md` when done.
</output>
