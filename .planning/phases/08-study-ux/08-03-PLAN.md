---
phase: 08-study-ux
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - apps/frontend/src/pages/DeckDetailPage.tsx
autonomous: true
requirements: [STUDY-04]

must_haves:
  truths:
    - "DeckDetailPage.tsx exports a named pure function `groupCardsByFirstTag(cards: Card[]): { tag: string; cards: Card[] }[]` — sections sorted alphabetically, 'Untagged' always last — per D-12, D-14, D-15"
    - "The flat single <Table> is replaced with a grouped section layout: each group has an <h3> header and its own <Table> — per D-12, D-13"
    - "Cards with multiple tags appear under their first tag only; no duplication across sections — per D-14"
    - "The empty state (cards.length === 0) renders outside the grouped sections — per RESEARCH.md Pitfall 5"
    - "All DeckDetailPage STUDY-04 tests pass GREEN and the full suite shows no regressions"
    - "TypeScript compiles cleanly — DeckDetailPage.tsx under 500 lines after edits"
  artifacts:
    - path: "apps/frontend/src/pages/DeckDetailPage.tsx"
      provides: "groupCardsByFirstTag helper (exported) + tag-sectioned card layout replacing flat Table"
      min_lines: 530
  key_links:
    - from: "groupCardsByFirstTag"
      to: "card.tags[0] ?? 'Untagged'"
      via: "Map accumulator grouping cards by first tag"
      pattern: "tags\\[0\\]"
    - from: "Untagged sort"
      to: "sections.filter(tag !== 'Untagged').sort + push Untagged last"
      via: "filter alpha-sorted entries then push Untagged section"
      pattern: "Untagged"
    - from: "section header h3"
      to: "group.tag + group.cards.length"
      via: "JSX h3 rendering tag name and card count"
      pattern: "groupCards\\.length"
---

<objective>
Wave 2 (final): Implement STUDY-04 in DeckDetailPage.tsx — add the `groupCardsByFirstTag` pure helper and replace the flat card table with a tag-sectioned layout. Runs the complete verification gate (full test suite + typecheck). Makes the RED DeckDetailPage tests from 08-01-PLAN.md go GREEN.

Purpose: Cards are easier to find and review on the deck detail page when grouped by topic (tag).

Output: Updated DeckDetailPage.tsx with exported `groupCardsByFirstTag` helper, tag-sectioned card rendering, and a verified full test suite.
</objective>

<execution_context>
@C:/Users/rapha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rapha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-study-ux/08-CONTEXT.md
@.planning/phases/08-study-ux/08-RESEARCH.md
@.planning/phases/08-study-ux/08-UI-SPEC.md
@.planning/phases/08-study-ux/08-01-SUMMARY.md
@.planning/phases/08-study-ux/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add groupCardsByFirstTag helper and replace flat Table with tag sections (STUDY-04)</name>
  <files>apps/frontend/src/pages/DeckDetailPage.tsx</files>

  <read_first>
    apps/frontend/src/pages/DeckDetailPage.tsx — full file (current state, ~517 lines); understand: Card type import, TagChips component (lines 72–90), the flat Table block being replaced (lines 326–400 approximately — confirm exact lines by reading), canEdit/canDelete pattern, existing empty state JSX (BookOpen icon + "No cards yet"), all existing imports
    apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx — the failing tests from 08-01-PLAN.md; defines observable behaviors this task must produce; specifically the named import `import { groupCardsByFirstTag } from '@/pages/DeckDetailPage'` — this function must be exported
    .planning/phases/08-study-ux/08-RESEARCH.md — Pattern 5 (groupCardsByFirstTag implementation), Pattern 6 (tag section JSX replacing the flat Table), Pitfall 5 (empty state placement outside grouped sections)
    .planning/phases/08-study-ux/08-UI-SPEC.md — §Component Contracts §4 Tag Section Header, §5 Per-Section Mini-Table; exact class strings for h3 header, section container div, table aria-label format
    packages/shared/src/schemas/card.ts — Card type shape; confirm `tags: string[]` field name
  </read_first>

  <behavior>
    - `groupCardsByFirstTag` is a named export (not default) declared before the component: `export function groupCardsByFirstTag(cards: Card[]): { tag: string; cards: Card[] }[]`
    - Implementation uses a `Map<string, Card[]>` accumulator; for each card, key is `card.tags[0] ?? 'Untagged'`; appends card to the group array
    - Return order: all non-Untagged entries sorted by `tag.localeCompare(other)` (alphabetical), then the Untagged entry appended last if present
    - In the DeckDetailPage JSX, the single `<Table aria-label="Cards in deck">` block is replaced with a conditional: if `cards.length === 0` render the existing empty state (BookOpen + "No cards yet" copy + conditional Add Card button) unchanged; else render `groupCardsByFirstTag(cards).map(({ tag, cards: groupCards }) => <div key={tag} className="mb-6">...</div>)`
    - Each section div contains: an `<h3 className="text-sm font-semibold text-muted-foreground uppercase tracking-wide mb-2">` with the tag name as text and a `<span className="font-normal normal-case tracking-normal ml-1">— {groupCards.length} cards</span>`; followed by a `<Table aria-label={\`Cards tagged ${tag}\`}>` with TableHeader, TableBody matching existing columns
    - Table columns per section: `<TableHead className="w-12">#</TableHead>`, `<TableHead>Front</TableHead>`, `<TableHead>Tags</TableHead>`, `<TableHead>Actions</TableHead>` — identical to existing flat table headers
    - Row body: `{groupCards.map((card, i) => <TableRow key={card.id}>...)}` where `i + 1` is the row number (#), `card.frontContent` is the front cell (with existing `max-w-xs truncate` class), `<TagChips tags={card.tags} />` is the tags cell (existing component, unchanged), Edit/Delete action buttons use the existing `canEdit`/`canDelete` pattern — copy the cell content verbatim from the existing flat table rows
    - The existing empty state block (currently inside the flat Table as a TableRow with colSpan) is lifted out and rendered directly as a `<div>` when `cards.length === 0` — same content, no Table wrapper needed in empty state
    - `groupCardsByFirstTag(cards)` may be called inline in JSX (acceptable for DeckDetailPage's re-render frequency per RESEARCH.md anti-patterns note); no useMemo required unless lint warns
  </behavior>

  <action>
    Edit `apps/frontend/src/pages/DeckDetailPage.tsx` to implement STUDY-04.

    Step 1 — Add `groupCardsByFirstTag` as a named export. Place it after the `TagChips` local component function (after line ~90) and before the main `DeckDetailPage` component. Use the implementation from RESEARCH.md Pattern 5 exactly: Map accumulator, tags[0] ?? 'Untagged', filter non-Untagged entries for alpha sort then push Untagged last, return as `{ tag, cards }[]` array. The function must be `export function groupCardsByFirstTag(...)` — not a const arrow function — so the test's named import resolves.

    Step 2 — Replace the flat Table block. Read the exact line range of the current `<Table aria-label="Cards in deck">...</Table>` block (confirm by reading the source). Replace the entire block with the two-branch conditional described in the behavior section:

    Branch A (empty state): `cards.length === 0` — render the BookOpen icon, "No cards yet" heading, and conditional Add Card button as a `<div className="flex flex-col items-center justify-center py-16 gap-3 text-muted-foreground">` (same content as the current empty state, just outside a Table wrapper). Copy the exact JSX from the existing empty state rather than rewriting it.

    Branch B (grouped sections): `groupCardsByFirstTag(cards).map(...)` — render one section div per group. Each section has the h3 header and a self-contained Table with header row + body rows. The table body rows copy cell content from the existing flat table rows exactly — same `card.frontContent` truncation, same `<TagChips>`, same Edit/Delete button pattern with `canEdit`/`canDelete` guard.

    Step 3 — Verify file stays under 500 lines. The current file is ~517 lines; the replacement removes the single-table pattern and adds per-section tables. Net change should be roughly neutral (removes colSpan empty state, adds h3 headers per section, adds table headers per section). If the file exceeds 500 lines, extract `groupCardsByFirstTag` into a shared util file and import it (update the test import path accordingly).

    Constraints:
    - Do NOT remove or modify the `TagChips` component — it is used in the Tags column of every section's table
    - Do NOT change the Edit or Delete action handlers — only the rendering structure changes
    - Do NOT add any new imports beyond what is already present (all needed components: Table, TableHeader, TableRow, TableHead, TableBody, TableCell — confirm they are imported, add only if missing)
    - The `groupCardsByFirstTag` function must be a named export for the test's named import to work
    - Use exact class strings from UI-SPEC §Component Contracts §4 and §5 for h3 and section container
  </action>

  <verify>
    <automated>cd apps/frontend && pnpm test --run src/pages/__tests__/DeckDetailPage.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <done>
    All DeckDetailPage tests pass (GREEN). DeckDetailPage.tsx exports `groupCardsByFirstTag`. The h3 section headers with tag names render in the component. File is under 500 lines (or split into util if necessary). No regressions in rest of suite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full suite verification gate — all tests GREEN + typecheck clean</name>
  <files></files>

  <read_first>
    apps/frontend/src/pages/StudySessionPage.tsx — verify Math.random appears in useEffect body (not render path); verify setAvailableTags is called in prefetch useEffect
    apps/frontend/src/pages/DeckDetailPage.tsx — verify groupCardsByFirstTag is exported; verify file line count
  </read_first>

  <action>
    Run the full frontend test suite and TypeScript typecheck. Do not skip or suppress anything.

    Commands in order:
    1. `pnpm --filter @kartex/frontend test --run` — all tests (existing 52 + new Phase 8 tests) must pass
    2. `pnpm --filter @kartex/frontend typecheck` — must exit 0

    If any test fails: read the error message, trace to the failing assertion, fix the implementation in the relevant page file. Do not modify test assertions to make tests pass.

    If typecheck fails: read the error, trace to the offending line, fix the type issue. Common issues:
    - `selectedTags.has()` on a plain object instead of Set — ensure useState initial value is `new Set()` not `{}`
    - `sessionSize` type mismatch if the type annotation does not include all four variants

    After both commands exit 0, run the following verification checks:

    Security: tag names are rendered via React JSX (auto-escaped); the custom Input has `min={1}` and the onChange guard `Math.max(1, parseInt(..., 10) || 1)` — confirm with grep:
    `grep -n 'Math.max(1' apps/frontend/src/pages/StudySessionPage.tsx` — must show the customCount onChange handler.

    Shuffle placement: `grep -n 'Math.random' apps/frontend/src/pages/StudySessionPage.tsx` — must appear inside the useEffect body, not in the JSX return block.

    groupCardsByFirstTag export: `grep -n 'export function groupCardsByFirstTag' apps/frontend/src/pages/DeckDetailPage.tsx` — must show the export.

    File sizes:
    `wc -l apps/frontend/src/pages/StudySessionPage.tsx` — must be under 500
    `wc -l apps/frontend/src/pages/DeckDetailPage.tsx` — must be under 500
  </action>

  <verify>
    <automated>cd apps/frontend && pnpm test --run 2>&1 | tail -10 && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>

  <done>
    `pnpm test --run` exits 0 with all tests passing (52 pre-existing + all new Phase 8 tests). `pnpm typecheck` exits 0. All verification grep checks pass. Both modified page files are under 500 lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → grouped render | `card.tags[]` strings are used as Map keys and rendered in h3 headers via JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | Tag name used as Map key and rendered in h3 `{tag}` | accept | React JSX auto-escapes string interpolation in text nodes. Tag strings originate from the database (validated at API boundary via Zod). No XSS surface in JSX text rendering. |
| T-08-07 | Denial of Service | Deck with very many unique tags → many section headers + tables | accept | DeckDetailPage is a read operation on user's own deck; a malicious tag list only affects the deck owner's own view. No rate limit needed at this layer. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. |
</threat_model>

<verification>
## Phase 8 Final Gate

After all plans complete (08-01, 08-02, 08-03):

1. Full test suite green (52 existing + Phase 8 tests):
   `pnpm --filter @kartex/frontend test --run`

2. TypeScript clean:
   `pnpm --filter @kartex/frontend typecheck`

3. STUDY-01 — tag filter: no tags selected = all cards; any tag selected = OR filter; untagged excluded:
   Covered by StudySessionPage.test.tsx STUDY-01a/b/c tests.

4. STUDY-02 — session size: SR mode slices; Deck/Exam modes do not; Custom reveals Input:
   Covered by StudySessionPage.test.tsx STUDY-02a/b/c/d tests.

5. STUDY-03 — shuffle in useEffect (not render path):
   `grep -n 'Math.random' apps/frontend/src/pages/StudySessionPage.tsx`

6. STUDY-04 — tag sections render and groupCardsByFirstTag sorts correctly:
   Covered by DeckDetailPage.test.tsx STUDY-04a/b/c tests.

7. File sizes (CLAUDE.md 500-line rule):
   `wc -l apps/frontend/src/pages/StudySessionPage.tsx`
   `wc -l apps/frontend/src/pages/DeckDetailPage.tsx`

## Manual Verification (human-check — do after automated gate)

StudySessionPage (/decks/:id/learn):
1. Open deck detail page and click Learn; verify config section appears above mode cards with tag chips for all deck tags
2. Select one tag chip — it fills (primary variant); only cards with that tag enter the session
3. Select "Custom" size button — number input appears inline; enter 5 — session capped at 5 cards
4. Start SR session — cards are in different order on each session start
5. Navigate to /study (global route) — no config section shown (mode selector skipped, goes directly to SR)

DeckDetailPage (/decks/:id):
6. Open a deck with tagged cards — cards are grouped under h3 section headers with tag names
7. A card with two tags appears in only one section (first tag only)
8. Cards with no tags appear under "UNTAGGED" section at the bottom
9. Edit/Delete actions work the same as before within each section's table rows
</verification>

<success_criteria>
- STUDY-04: `groupCardsByFirstTag` exported from DeckDetailPage.tsx; sections sorted alphabetically; Untagged last
- STUDY-04: Flat table replaced with per-section tables; same columns (#, Front, Tags, Actions) per D-13
- STUDY-04: Empty state (no cards) renders outside grouped sections with existing BookOpen copy
- Full Phase 8 test suite passes: all StudySessionPage + DeckDetailPage tests GREEN, no regressions
- TypeScript: both page files compile cleanly
- Both page files under 500 lines
</success_criteria>

<output>
Create `.planning/phases/08-study-ux/08-03-SUMMARY.md` when done.
</output>
