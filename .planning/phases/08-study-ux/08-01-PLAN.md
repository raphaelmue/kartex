---
phase: 08-study-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx
  - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx
autonomous: true
requirements: [STUDY-01, STUDY-02, STUDY-03, STUDY-04]

must_haves:
  truths:
    - "StudySessionPage.test.tsx exists with at least 11 failing tests covering STUDY-01a/b/c/d, STUDY-02a/b/c/d, STUDY-03a/b"
    - "DeckDetailPage.test.tsx exists with at least 3 failing tests covering STUDY-04a/b/c"
    - "Both test files fail with assertion errors (not import/syntax errors) when run"
    - "Test failures describe the missing behavior — not trivially-passing placeholder assertions"
  artifacts:
    - path: "apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx"
      provides: "Failing test stubs for STUDY-01 (tag filter), STUDY-02 (session size), STUDY-03 (shuffle)"
    - path: "apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx"
      provides: "Failing test stubs for STUDY-04 (groupCardsByFirstTag helper + tag section rendering)"
  key_links:
    - from: "StudySessionPage.test.tsx mocks"
      to: "api.get mock sequence"
      via: "mockResolvedValueOnce × 3 matching Promise.all order in StudySessionPage prefetch"
      pattern: "mockResolvedValueOnce"
    - from: "DeckDetailPage.test.tsx"
      to: "groupCardsByFirstTag export"
      via: "named import from DeckDetailPage"
      pattern: "groupCardsByFirstTag"
---

<objective>
Wave 0: Create failing test stubs (RED phase) for all four Phase 8 requirements. Two new test files are created under `apps/frontend/src/pages/__tests__/`. No production code is touched.

Purpose: Establish the automated verification contract before any implementation begins. Each test must fail because the target behavior does not yet exist — not because of import errors or bad mock setup.

Output:
- StudySessionPage.test.tsx — 11 failing tests for STUDY-01/02/03 (tag filter, session size picker, shuffle)
- DeckDetailPage.test.tsx — 3 failing tests for STUDY-04 (groupCardsByFirstTag pure helper + tagged section rendering)
</objective>

<execution_context>
@C:/Users/rapha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rapha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-study-ux/08-CONTEXT.md
@.planning/phases/08-study-ux/08-RESEARCH.md
@.planning/phases/08-study-ux/08-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing StudySessionPage test stub (STUDY-01, STUDY-02, STUDY-03)</name>
  <files>apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx</files>

  <read_first>
    apps/frontend/src/pages/StudySessionPage.tsx — full file; understand current state variables (lines 166–175), prefetch useEffect (lines 182–207), loadCards useEffect (lines 210–240), and the mode selector JSX branch (if (!selectedMode)); confirms useAuth is NOT imported
    apps/frontend/src/components/__tests__/AppShell.test.tsx — established vi.mock patterns for this project: vi.hoisted(), vi.mock('react-router-dom', async ...), vi.mock('@/lib/api', ...), vi.mock('sonner', ...), explicit describe/it/expect/vi imports
    apps/frontend/vitest.config.ts — environment: jsdom, globals: true, include: src/**/*.{test,spec}.{ts,tsx}; test files under src/pages/__tests__/ are picked up
    apps/frontend/src/test/setup.ts — confirms @testing-library/jest-dom is imported
    .planning/phases/08-study-ux/08-RESEARCH.md — Mock Strategy for StudySessionPage Tests (§Validation Architecture); api.get mock sequence for the three concurrent Promise.all calls; no useAuth mock needed for StudySessionPage
  </read_first>

  <behavior>
    - STUDY-01a: When no tags are selected (default state), all cards from the API response are passed to SessionRunner — test confirms cards with and without tags all appear when selectedTags is empty
    - STUDY-01b: When tag "bio" is selected, the config section renders a chip for "bio" with variant="default", and only cards tagged "bio" reach SessionRunner; untagged cards are excluded
    - STUDY-01c: When two tags "bio" and "chem" are selected, cards matching either tag appear (OR logic); a card with neither tag is excluded
    - STUDY-01d: Clicking a deselected tag chip toggles it to selected (variant="default"); clicking a selected chip toggles it back to deselected (variant="outline")
    - STUDY-02a: When sessionSize=10 and mode is SR, SessionRunner receives at most 10 cards even when more are available
    - STUDY-02b: When sessionSize=10 and mode is Deck, SessionRunner receives all cards (slice not applied outside SR mode — per D-08)
    - STUDY-02c: When sessionSize="custom" and customCount=5, SessionRunner receives at most 5 cards in SR mode
    - STUDY-02d: Clicking the "Custom" segmented button causes a number Input to appear in the config section
    - STUDY-03a: Cards passed to SessionRunner are in a different order from the API fetch order (shuffle applied)
    - STUDY-03b: Shuffling is non-mutating — the original array from the API mock is not mutated
  </behavior>

  <action>
    Create `apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx`.

    Import style: explicit named imports — `import { describe, it, expect, vi, beforeEach } from 'vitest'`. Import `render`, `screen`, `fireEvent`, `waitFor`, `act` from `@testing-library/react`. Import `StudySessionPage` (default export) from `@/pages/StudySessionPage`. Wrap renders in `MemoryRouter` from `react-router-dom`.

    Mock declarations — all vi.mock() calls at top-level module scope before imports where required; use vi.hoisted() for variables needed inside factory functions:

    1. `vi.mock('react-router-dom', async () => { const actual = await vi.importActual<typeof import('react-router-dom')>('react-router-dom'); return { ...actual, useParams: () => ({ id: 'deck-abc' }), useNavigate: () => vi.fn() } })`

    2. `const { mockApiGet } = vi.hoisted(() => { const mockApiGet = vi.fn(); return { mockApiGet } })` then `vi.mock('@/lib/api', () => ({ api: { get: mockApiGet, post: vi.fn() } }))`

    3. `vi.mock('sonner', () => ({ toast: { error: vi.fn(), success: vi.fn() } }))`

    No useAuth mock — StudySessionPage does not import useAuth (confirmed by source read).

    Test data setup: define two helper card arrays inside a beforeEach that resets mockApiGet:

    - `mockCards` — array of 15 DueCard objects: 5 tagged ["bio"], 5 tagged ["chem"], 5 with tags []. Each card has shape: `{ id: string, frontContent: string, backContent: string, tags: string[], deckId: 'deck-abc', dueAt: '2026-01-01', interval: 1, easeFactor: 2.5, repetitions: 0 }`.

    The api.get mock must respond in the order the three concurrent calls are made in the prefetch useEffect: first call → deck info `{ id: 'deck-abc', name: 'Test Deck', isOwner: true }`, second call → all deck cards (the mockCards array), third call → due cards (first 5 cards tagged bio only). Use `mockApiGet.mockResolvedValueOnce` three times in beforeEach.

    For the loadCards useEffect (triggered by mode selection), set up a fourth `mockResolvedValueOnce` in each test that needs it — use the same mockCards.

    Test structure: one `describe('StudySessionPage config section', ...)` block grouping STUDY-01/02/03 tests.

    STUDY-01d chip toggle test: render the mode selector screen (no mode clicked), find chip buttons via their text content, click one, assert its className includes the `bg-primary` class (variant="default"), click again, assert it no longer includes `bg-primary` (variant="outline"). Use `getByRole('button', { name: /bio/i })`.

    STUDY-02d custom input test: click "Custom" button, assert `getByRole('spinbutton')` (input type="number") is in the document.

    STUDY-03a shuffle test: verify that cards passed to SessionRunner are all present (set-equality) and that the call to setCards happened. Specifically: after triggering mode selection and card load, collect all card IDs visible in the component output (or captured via mock); assert that the set of IDs exactly matches the set of IDs from the mock response (no cards lost, no cards duplicated). Do NOT assert a specific order — shuffle order is non-deterministic and sort-order assertions against `Array.sort()` with a comparator are implementation-defined in V8.

    STUDY-03b non-mutation test: capture the original array reference from the mock response, trigger mode selection, assert the original array object is unchanged (same order).

    All tests in this file MUST FAIL at this stage with assertion errors like "Unable to find element", "Expected 10 to be less than or equal to 10" on wrong result, or "Expected false to be true" — NOT with TypeScript or import errors. Every test must assert a real behavior that will be added in Wave 1 (Task 2 in 08-02-PLAN.md).

    Do NOT add placeholder assertions (`expect(true).toBe(true)`). Do NOT import from files that do not yet exist.
  </action>

  <verify>
    <automated>cd apps/frontend && pnpm test --run src/pages/__tests__/StudySessionPage.test.tsx 2>&1 | tail -30</automated>
  </verify>

  <done>
    StudySessionPage.test.tsx exists under `apps/frontend/src/pages/__tests__/` with at least 10 tests. The test run shows failures like "Unable to find role='button'" or assertion mismatches — NOT syntax or import errors. Zero tests pass (all RED).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failing DeckDetailPage test stub (STUDY-04)</name>
  <files>apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx</files>

  <read_first>
    apps/frontend/src/pages/DeckDetailPage.tsx — full file; understand current imports (line 7 imports useAuth), TagChips component (lines 72–90), current flat Table structure (lines 326–400), canEdit pattern, api.get calls
    apps/frontend/src/context/AuthContext.tsx — useAuth return shape: { user: User | null, loading: boolean, setUser: vi.fn(), logout: vi.fn() }; User shape: { id, username, role, isActive, createdAt }
    apps/frontend/src/components/__tests__/AppShell.test.tsx — vi.mock patterns for this project (vi.hoisted, vi.importActual, explicit imports)
    .planning/phases/08-study-ux/08-RESEARCH.md — DeckDetailPage Tests — Mock Strategy (§Validation Architecture); groupCardsByFirstTag function signature and expected behavior (Pattern 5)
  </read_first>

  <behavior>
    - STUDY-04a: groupCardsByFirstTag([card-bio, card-chem, card-untagged]) returns sections in order: [{tag:'bio', cards:[card-bio]}, {tag:'chem', cards:[card-chem]}, {tag:'Untagged', cards:[card-untagged]}] — alphabetical, Untagged last
    - STUDY-04b: A card with tags ['chem', 'bio'] appears only in the 'chem' section (first tag wins; not duplicated in 'bio')
    - STUDY-04c: DeckDetailPage renders a section header containing "bio" text (h3) when the deck has a card tagged "bio"
  </behavior>

  <action>
    Create `apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx`.

    Import style: explicit named imports — `import { describe, it, expect, vi, beforeEach } from 'vitest'`. Import `render`, `screen`, `waitFor` from `@testing-library/react`. Import `DeckDetailPage` (default export) from `@/pages/DeckDetailPage`. Import `groupCardsByFirstTag` as a named export from `@/utils/groupCardsByFirstTag` — this module will be created in Wave 2 (08-03-PLAN.md). At this stage the import will fail to resolve the module (it does not exist yet), causing the test to error — that is acceptable RED state. Wrap renders in `MemoryRouter` from `react-router-dom`.

    Mocks required (DeckDetailPage imports useAuth, useParams, useNavigate, api, toast):

    1. `vi.mock('@/context/AuthContext', () => ({ useAuth: () => ({ user: { id: 'user-1', username: 'testuser', role: 'USER', isActive: true, createdAt: '2026-01-01' }, loading: false, setUser: vi.fn(), logout: vi.fn() }) }))`

    2. `vi.mock('react-router-dom', async () => { const actual = await vi.importActual<typeof import('react-router-dom')>('react-router-dom'); return { ...actual, useParams: () => ({ id: 'deck-abc' }), useNavigate: () => vi.fn() } })`

    3. `const { mockApiGet } = vi.hoisted(() => { const mockApiGet = vi.fn(); return { mockApiGet } })` then `vi.mock('@/lib/api', () => ({ api: { get: mockApiGet, post: vi.fn(), delete: vi.fn() } }))`

    4. `vi.mock('sonner', () => ({ toast: { error: vi.fn(), success: vi.fn() } }))`

    Test card data: define inside tests or in a shared const:
    - `cardBio`: `{ id: 'c1', frontContent: 'Front bio', backContent: 'Back bio', tags: ['bio'], deckId: 'deck-abc', createdAt: '2026-01-01', updatedAt: '2026-01-01' }`
    - `cardChem`: `{ id: 'c2', frontContent: 'Front chem', backContent: 'Back chem', tags: ['chem', 'bio'], deckId: 'deck-abc', createdAt: '2026-01-01', updatedAt: '2026-01-01' }`
    - `cardUntagged`: `{ id: 'c3', frontContent: 'Front untagged', backContent: 'Back untagged', tags: [], deckId: 'deck-abc', createdAt: '2026-01-01', updatedAt: '2026-01-01' }`

    Test structure: two describe blocks:

    Block 1 — `describe('groupCardsByFirstTag (pure function)', ...)` — tests STUDY-04a and STUDY-04b. These test the exported pure helper directly without rendering. Since `groupCardsByFirstTag` is not yet exported from DeckDetailPage.tsx, these tests will fail with "not a function" or import resolution error — that is correct RED state.

    Block 2 — `describe('DeckDetailPage tag section rendering', ...)` — test STUDY-04c. Mock api.get to return deck info and cards array in the expected sequence: first call (deck meta) → `{ id: 'deck-abc', name: 'Test Deck', isOwner: true, cardCount: 2, shareCode: null, sharedBy: null }`, second call (cards) → `[cardBio, cardChem, cardUntagged]`. After render + waitFor, assert `screen.getByRole('heading', { level: 3, name: /bio/i })` is in the document — this heading does not exist in the current flat Table layout, so the test fails RED.

    All tests must fail for behavioral reasons (missing export, missing heading element) — not for mock setup failures. Verify the mock sequence matches DeckDetailPage's actual api.get call pattern by reading the source file first.
  </action>

  <verify>
    <automated>cd apps/frontend && pnpm test --run src/pages/__tests__/DeckDetailPage.test.tsx 2>&1 | tail -30</automated>
  </verify>

  <done>
    DeckDetailPage.test.tsx exists under `apps/frontend/src/pages/__tests__/` with at least 3 tests (STUDY-04a, STUDY-04b, STUDY-04c). Tests fail due to missing `groupCardsByFirstTag` export or missing heading element — NOT due to TypeScript compilation errors. Zero tests pass (all RED).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test input → component | All test data is synthetic; no real API calls made — all mocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Tag name rendered in chip via `{tag}` JSX | accept | React JSX auto-escapes string interpolation; no XSS surface. Tag strings originate from API response, already handled upstream. |
| T-08-02 | Tampering | Custom session size `<Input type="number">` — NaN injection | mitigate | `Math.max(1, parseInt(e.target.value, 10) \|\| 1)` guard in onChange handler — NaN and sub-1 values coerce to 1 silently. Tested in STUDY-02c. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. Package legitimacy gate not required. |
</threat_model>

<verification>
## Wave 0 Gate

After both tasks complete:

1. Both test files exist:
   - `ls apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx`
   - `ls apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx`

2. Tests run and fail (RED — not syntax errors):
   - `cd apps/frontend && pnpm test --run src/pages/__tests__/ 2>&1 | grep -E 'FAIL|PASS|Error'`

3. Existing test suite is unaffected (52 tests still pass):
   - `cd apps/frontend && pnpm test --run 2>&1 | tail -5`
</verification>

<success_criteria>
- StudySessionPage.test.tsx: 10+ tests, all RED (failing with assertion/element errors)
- DeckDetailPage.test.tsx: 3+ tests, all RED (failing with missing export or missing element)
- No test fails due to TypeScript syntax error or unresolvable import (except the intentional groupCardsByFirstTag named export which does not exist yet)
- Existing 52 tests continue to pass — Wave 0 adds only new test files, touches no production code
</success_criteria>

<output>
Create `.planning/phases/08-study-ux/08-01-SUMMARY.md` when done.
</output>
