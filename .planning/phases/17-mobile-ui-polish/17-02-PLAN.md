---
phase: 17-mobile-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/pages/DecksPage.tsx
  - apps/frontend/src/components/ui/dropdown-menu.tsx
  - apps/frontend/src/components/ui/alert-dialog.tsx
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
autonomous: true
requirements:
  - DECK-05
must_haves:
  truths:
    - "Owned deck CardFooter shows: Switch+label (left, mr-auto), Study button, Open button, ⋮ DropdownMenu trigger — no inline Edit or Delete buttons"
    - "DropdownMenu content has Edit item (calls openEdit) and Delete item (opens AlertDialog)"
    - "AlertDialog shows title 'Delete deck?' and body 'This cannot be undone.' with Cancel and destructive Delete buttons"
    - "confirmDeleteId state is removed — AlertDialog open state controls the delete confirmation flow"
    - "Shared/library deck CardFooter (deck.sharedByUsername is set) shows only Study and Open buttons — no ⋮ menu"
    - "At 375px and 1280px viewports, no button or element overflows the card boundary"
    - "Three new i18n keys present in both en.json and de.json: decks.moreActions, decks.deleteConfirmTitle, decks.deleteConfirmBody"
  artifacts:
    - path: "apps/frontend/src/pages/DecksPage.tsx"
      provides: "Restructured CardFooter with DropdownMenu + AlertDialog; confirmDeleteId removed"
      contains: "DropdownMenu"
    - path: "apps/frontend/src/components/ui/dropdown-menu.tsx"
      provides: "shadcn DropdownMenu component (copy-pasted via npx shadcn@latest add)"
    - path: "apps/frontend/src/components/ui/alert-dialog.tsx"
      provides: "shadcn AlertDialog component (copy-pasted via npx shadcn@latest add)"
    - path: "apps/frontend/src/locales/en.json"
      provides: "New keys: decks.moreActions, decks.deleteConfirmTitle, decks.deleteConfirmBody"
      contains: "moreActions"
    - path: "apps/frontend/src/locales/de.json"
      provides: "New keys (German): decks.moreActions, decks.deleteConfirmTitle, decks.deleteConfirmBody"
      contains: "moreActions"
  key_links:
    - from: "DecksPage.tsx DropdownMenu Delete item"
      to: "AlertDialog open state"
      via: "state setter (deleteTargetId or similar per-deck ref)"
      pattern: "AlertDialog"
    - from: "DecksPage.tsx AlertDialog confirm button"
      to: "handleDelete(id)"
      via: "onClick handler"
      pattern: "handleDelete"
---

<objective>
Restructure the `DecksPage` deck card `CardFooter` so action buttons never overflow their container.
Replace the inline Edit + Delete buttons (and the inline `confirmDeleteId` confirm UI) with a ⋮
DropdownMenu that holds Edit and Delete. Delete opens an AlertDialog for confirmation.

Install shadcn `dropdown-menu` and `alert-dialog` components first. Add three new i18n keys to both
locale files. Rewrite the CardFooter JSX in DecksPage.tsx per the D-01 through D-04 decisions and
the UI-SPEC interaction contract.

Purpose: DECK-05 — deck card buttons fully contained at 375px and 1280px viewports.
Output: dropdown-menu.tsx, alert-dialog.tsx installed; DecksPage.tsx CardFooter restructured;
en.json + de.json updated with 3 new keys.
</objective>

<execution_context>
@C:/Users/rapha/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/rapha/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-mobile-ui-polish/17-CONTEXT.md
@.planning/phases/17-mobile-ui-polish/17-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn DropdownMenu and AlertDialog components</name>
  <files>
    apps/frontend/src/components/ui/dropdown-menu.tsx
    apps/frontend/src/components/ui/alert-dialog.tsx
  </files>
  <read_first>
    - apps/frontend/src/components/ui/ directory listing — confirm dropdown-menu.tsx and alert-dialog.tsx do NOT already exist before running install
    - apps/frontend/src/components/ui/dialog.tsx — inspect to understand the existing Radix Dialog pattern; AlertDialog follows the same structure
    - apps/frontend/package.json — confirm @radix-ui/react-dropdown-menu and @radix-ui/react-alert-dialog are not already listed; they will be added by shadcn add
  </read_first>
  <action>
    From the apps/frontend directory, run:
      npx shadcn@latest add dropdown-menu alert-dialog

    This copies two files into apps/frontend/src/components/ui/:
      - dropdown-menu.tsx (Radix DropdownMenu primitives with shadcn styling)
      - alert-dialog.tsx (Radix AlertDialog primitives with shadcn styling)

    And installs any missing Radix peer packages (@radix-ui/react-dropdown-menu,
    @radix-ui/react-alert-dialog) via yarn.

    Per D-04 and UI-SPEC Component Inventory §New.

    After the command completes, verify both files were created. Do NOT modify the generated files.
  </action>
  <verify>
    <automated>ls apps/frontend/src/components/ui/dropdown-menu.tsx apps/frontend/src/components/ui/alert-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/frontend/src/components/ui/dropdown-menu.tsx exists and is non-empty
    - apps/frontend/src/components/ui/alert-dialog.tsx exists and is non-empty
    - Both files export named components (e.g. DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem; AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogAction, AlertDialogCancel)
    - `cd apps/frontend && npx tsc --noEmit` exits 0 (no type errors from the new component files)
  </acceptance_criteria>
  <done>dropdown-menu.tsx and alert-dialog.tsx are in src/components/ui/ and type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add three new i18n keys to en.json and de.json</name>
  <files>
    apps/frontend/src/locales/en.json
    apps/frontend/src/locales/de.json
  </files>
  <read_first>
    - apps/frontend/src/locales/en.json — read the "decks" section in full; confirm decks.moreActions, decks.deleteConfirmTitle, decks.deleteConfirmBody do NOT already exist
    - apps/frontend/src/locales/de.json — read the "decks" section in full; same check
  </read_first>
  <action>
    In apps/frontend/src/locales/en.json, add three new keys to the "decks" object (add after the
    existing "failedToToggle" key, before the closing brace of "decks"):

      "moreActions": "More actions",
      "deleteConfirmTitle": "Delete deck?",
      "deleteConfirmBody": "This cannot be undone."

    In apps/frontend/src/locales/de.json, add three new keys to the "decks" object (in the same
    position after "failedToToggle"):

      "moreActions": "Weitere Aktionen",
      "deleteConfirmTitle": "Deck löschen?",
      "deleteConfirmBody": "Dies kann nicht rückgängig gemacht werden."

    Both files must remain valid JSON after editing (no trailing commas on the last key in any
    object, no syntax errors).

    Per UI-SPEC Copywriting Contract and the v1.3-research decision "i18n parity — add each to
    both en.json and de.json in same commit".
  </action>
  <verify>
    <automated>node -e "const e=require('./apps/frontend/src/locales/en.json'); const d=require('./apps/frontend/src/locales/de.json'); console.log(e.decks.moreActions, e.decks.deleteConfirmTitle, e.decks.deleteConfirmBody, d.decks.moreActions, d.decks.deleteConfirmTitle, d.decks.deleteConfirmBody)"</automated>
  </verify>
  <acceptance_criteria>
    - en.json decks.moreActions === "More actions"
    - en.json decks.deleteConfirmTitle === "Delete deck?"
    - en.json decks.deleteConfirmBody === "This cannot be undone."
    - de.json decks.moreActions === "Weitere Aktionen"
    - de.json decks.deleteConfirmTitle === "Deck löschen?"
    - de.json decks.deleteConfirmBody === "Dies kann nicht rückgängig gemacht werden."
    - Both files parse without error: `node -e "require('./apps/frontend/src/locales/en.json')"` exits 0 and `node -e "require('./apps/frontend/src/locales/de.json')"` exits 0
    - All pre-existing keys in "decks" are preserved and unchanged
  </acceptance_criteria>
  <done>Both locale files contain the three new decks.moreActions / deleteConfirmTitle / deleteConfirmBody keys with correct translations.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure DecksPage CardFooter with DropdownMenu and AlertDialog</name>
  <files>apps/frontend/src/pages/DecksPage.tsx</files>
  <read_first>
    - apps/frontend/src/pages/DecksPage.tsx — read in full; CardFooter starts at line ~150; confirmDeleteId state at line ~49; handleDelete at line ~70
    - apps/frontend/src/components/ui/dropdown-menu.tsx — read the exports to know the exact named export identifiers (DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator)
    - apps/frontend/src/components/ui/alert-dialog.tsx — read the exports to know the exact named export identifiers (AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger)
    - apps/frontend/src/locales/en.json — confirm decks.moreActions key is present (just added in Task 2)
  </read_first>
  <action>
    Make the following changes to DecksPage.tsx. Read the file first — every change is described
    relative to the current source.

    **1. Remove `confirmDeleteId` state.**
    Delete the line:
      const [confirmDeleteId, setConfirmDeleteId] = useState<string | null>(null)

    Add a new state for the delete dialog target:
      const [deleteTargetId, setDeleteTargetId] = useState<string | null>(null)

    **2. Update `handleDelete`.**
    Replace the `setConfirmDeleteId(null)` call inside handleDelete with `setDeleteTargetId(null)`.
    The rest of handleDelete is unchanged.

    **3. Update imports.**
    Add MoreVertical to the lucide-react import (it is already imported from lucide-react in the
    file — add MoreVertical to the existing destructured import).

    Add these imports from the new shadcn components:
      import {
        DropdownMenu,
        DropdownMenuContent,
        DropdownMenuItem,
        DropdownMenuTrigger,
      } from '@/components/ui/dropdown-menu'
      import {
        AlertDialog,
        AlertDialogAction,
        AlertDialogCancel,
        AlertDialogContent,
        AlertDialogDescription,
        AlertDialogFooter,
        AlertDialogHeader,
        AlertDialogTitle,
      } from '@/components/ui/alert-dialog'

    **4. Rewrite the CardFooter JSX.**
    The current CardFooter (line ~150) has:
      CardFooter className="flex items-center gap-2"
        conditional Switch+label group (mr-auto)
        Study button
        Open button (asChild Link)
        conditional block: Edit button + inline confirm UI OR Delete button

    Replace with the structure per D-01 through D-03 and UI-SPEC Interaction Contracts §DECK-05:

    For OWNED decks (!deck.sharedByUsername), the CardFooter becomes:
      CardFooter className="flex items-center gap-2"
        div className="flex items-center gap-2 mr-auto"   ← left group
          Switch ... (unchanged props)
          label ... (unchanged)
        /div
        Button size="sm" onClick={() => navigate(...)}    ← Study (unchanged)
          {t('decks.studyButton')}
        /Button
        Button size="sm" variant="outline" asChild        ← Open (unchanged)
          Link to={...}
            {t('decks.openButton')}
          /Link
        /Button
        DropdownMenu
          DropdownMenuTrigger asChild
            Button size="sm" variant="ghost" aria-label={t('decks.moreActions')}
              MoreVertical className="h-4 w-4"
            /Button
          /DropdownMenuTrigger
          DropdownMenuContent align="end"
            DropdownMenuItem onClick={() => openEdit(deck)}
              {t('decks.editButton')}
            /DropdownMenuItem
            DropdownMenuItem
              className="text-destructive focus:text-destructive"
              onClick={() => setDeleteTargetId(deck.id)}
              {t('decks.deleteButton')}
            /DropdownMenuItem
          /DropdownMenuContent
        /DropdownMenu
      /CardFooter

    For SHARED decks (deck.sharedByUsername is set), the CardFooter becomes:
      CardFooter className="flex items-center gap-2"
        Button size="sm" onClick={() => navigate(...)}    ← Study
          {t('decks.studyButton')}
        /Button
        Button size="sm" variant="outline" asChild        ← Open
          Link to={...}
            {t('decks.openButton')}
          /Link
        /Button
      /CardFooter

    **5. Add AlertDialog outside the deck map loop (single shared dialog instance).**
    After the closing `</div>` of the deck grid (and before the `<DeckFormModal>` component), add:

      AlertDialog open={deleteTargetId !== null} onOpenChange={(open) => { if (!open) setDeleteTargetId(null) }}
        AlertDialogContent
          AlertDialogHeader
            AlertDialogTitle
              {t('decks.deleteConfirmTitle')}
            /AlertDialogTitle
            AlertDialogDescription
              {t('decks.deleteConfirmBody')}
            /AlertDialogDescription
          /AlertDialogHeader
          AlertDialogFooter
            AlertDialogCancel
              {t('common.cancel')}
            /AlertDialogCancel
            AlertDialogAction
              className="bg-destructive text-destructive-foreground hover:bg-destructive/90"
              onClick={() => { if (deleteTargetId) void handleDelete(deleteTargetId) }}
              {t('decks.deleteButton')}
            /AlertDialogAction
          /AlertDialogFooter
        /AlertDialogContent
      /AlertDialog

    Per D-01, D-02, D-03, D-04 and UI-SPEC §DECK-05 Interaction Contract.

    **Style note:** MoreVertical icon size: className="h-4 w-4" — matches [&_svg]:size-4 Button
    rule. DropdownMenu destructive item uses text-destructive focus:text-destructive (not a
    variant prop — shadcn DropdownMenuItem does not have a variant prop by default; apply
    className directly). AlertDialogAction Delete button uses className to apply destructive
    background color since AlertDialogAction is a styled button, not a shadcn Button variant.
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - DecksPage.tsx no longer contains `confirmDeleteId` as a state variable name
    - DecksPage.tsx contains `deleteTargetId` state variable
    - DecksPage.tsx imports DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem from '@/components/ui/dropdown-menu'
    - DecksPage.tsx imports AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle from '@/components/ui/alert-dialog'
    - DecksPage.tsx imports MoreVertical from 'lucide-react'
    - DecksPage.tsx contains `aria-label={t('decks.moreActions')}` on the ⋮ trigger Button
    - DecksPage.tsx contains `{t('decks.deleteConfirmTitle')}` inside AlertDialogTitle
    - DecksPage.tsx contains `{t('decks.deleteConfirmBody')}` inside AlertDialogDescription
    - DecksPage.tsx does NOT contain any inline confirm JSX (the old `role="alert"` div with "Are you sure?" / "Yes, delete" / "Cancel" buttons is gone)
    - `cd apps/frontend && npx tsc --noEmit` exits 0
    - `cd apps/frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>DecksPage CardFooter uses DropdownMenu+AlertDialog pattern; confirmDeleteId is gone; TypeScript and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry | npm install fetches @radix-ui/react-dropdown-menu and @radix-ui/react-alert-dialog from npmjs.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Spoofing | DropdownMenu Delete action | mitigate | Delete only callable by authenticated users (deck list already gated by auth JWT middleware); deleteTargetId scoped to current user's deck list |
| T-17-02-SC | Tampering | npx shadcn@latest add dropdown-menu alert-dialog | accept | shadcn/ui is the official registry used throughout this project (all existing ui/ components installed the same way); @radix-ui packages are published by the Radix UI org and have been in continuous use since Phase 2; no [ASSUMED] or [SUS] packages |
</threat_model>

<verification>
After all three tasks complete:
- `grep -rn "confirmDeleteId" apps/frontend/src/pages/DecksPage.tsx` returns no output (state fully removed)
- `grep -n "DropdownMenu" apps/frontend/src/pages/DecksPage.tsx` shows import and usage
- `grep -n "AlertDialog" apps/frontend/src/pages/DecksPage.tsx` shows import and usage
- `node -e "const e=require('./apps/frontend/src/locales/en.json'); console.log(e.decks.moreActions)"` outputs "More actions"
- `node -e "const d=require('./apps/frontend/src/locales/de.json'); console.log(d.decks.moreActions)"` outputs "Weitere Aktionen"
- `cd apps/frontend && npm run build` exits 0
</verification>

<success_criteria>
- dropdown-menu.tsx and alert-dialog.tsx are installed in src/components/ui/
- en.json and de.json both contain decks.moreActions, decks.deleteConfirmTitle, decks.deleteConfirmBody
- DecksPage.tsx CardFooter owned-deck layout: Switch+label (mr-auto), Study, Open, ⋮ DropdownMenu (Edit + Delete items)
- DecksPage.tsx CardFooter shared-deck layout: Study, Open only — no ⋮ menu
- AlertDialog is a single shared instance outside the map loop, controlled by deleteTargetId state
- confirmDeleteId state and all inline confirm JSX are gone
- Frontend TypeScript build passes
- DECK-05 acceptance criteria met: no button overflow at 375px or 1280px
</success_criteria>

## Artifacts this phase produces

New file paths:
- `apps/frontend/src/components/ui/dropdown-menu.tsx` — shadcn DropdownMenu copy-paste component
- `apps/frontend/src/components/ui/alert-dialog.tsx` — shadcn AlertDialog copy-paste component

Modified file paths:
- `apps/frontend/src/pages/DecksPage.tsx` — restructured CardFooter JSX; removed confirmDeleteId state; added deleteTargetId state; added DropdownMenu + AlertDialog + MoreVertical imports
- `apps/frontend/src/locales/en.json` — added decks.moreActions, decks.deleteConfirmTitle, decks.deleteConfirmBody
- `apps/frontend/src/locales/de.json` — added decks.moreActions, decks.deleteConfirmTitle, decks.deleteConfirmBody

New symbols introduced (DecksPage.tsx):
- `deleteTargetId` — useState<string | null>(null) replacing confirmDeleteId

Removed symbols (DecksPage.tsx):
- `confirmDeleteId` — deleted; replaced by deleteTargetId + AlertDialog open state

<output>
Create `.planning/phases/17-mobile-ui-polish/17-02-SUMMARY.md` when done.
</output>
