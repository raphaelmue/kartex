---
phase: 19
slug: library-remove-action
status: draft
shadcn_initialized: true
preset: "style=default, baseColor=neutral, cssVariables=true"
created: 2026-06-13
---

# Phase 19 — UI Design Contract

> Visual and interaction contract for Phase 19: Library Remove Action.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | style: default, baseColor: neutral, cssVariables: true |
| Component library | Radix UI (via shadcn/ui copy-paste) |
| Icon library | lucide-react |
| Font | System default (no custom font declared in tailwind.config.ts) |

Source: `apps/frontend/components.json`, `apps/frontend/tailwind.config.ts`

---

## Spacing Scale

Standard 8-point scale already established by Tailwind defaults + shadcn component spacing. Phase 19 adds no new layout regions, only elements inside an existing `CardFooter`.

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs | 4px | Icon inside ghost button (`h-4 w-4` = 16px icon, gap-1) |
| sm | 8px | `gap-2` between footer buttons (existing pattern) |
| md | 16px | CardFooter horizontal padding (existing shadcn default) |
| lg | 24px | Not used in this phase |
| xl | 32px | Not used in this phase |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions: Ghost button touch target — use `size="sm"` shadcn Button variant which renders at 32px height; no override needed (not a mobile-only target).

Source: `DecksPage.tsx` existing owned-deck footer — `className="flex items-center gap-2"` pattern (lines 204–240). No change to scale.

---

## Typography

All values inherited from the existing Kartex neutral-scale shadcn theme. Phase 19 introduces no new typographic roles — all text appears inside established shadcn components (AlertDialogTitle, AlertDialogDescription, DropdownMenuItem).

| Role | Size | Weight | Line Height | Usage in this phase |
|------|------|--------|-------------|---------------------|
| Body | 14px (text-sm) | 400 | 1.5 | AlertDialogDescription, DropdownMenuItem labels |
| Label | 14px (text-sm) | 500 | 1.25 | AlertDialogTitle (shadcn default renders as `text-lg font-semibold` — see note) |
| Heading | 18px (text-lg) | 600 (semibold) | 1.25 | AlertDialogTitle — inherits `AlertDialogTitle` shadcn style |
| Display | — | — | — | Not used in this phase |

Note: `AlertDialogTitle` applies `text-lg font-semibold` internally via shadcn class definition. Do not override.

Source: `apps/frontend/src/index.css` CSS variables, `DecksPage.tsx` existing AlertDialog usage (lines 248–271).

---

## Color

All values from `apps/frontend/src/index.css` CSS custom properties. Both light and dark modes are covered.

| Role | Value (light) | Value (dark) | Usage in this phase |
|------|---------------|--------------|---------------------|
| Dominant (60%) | `hsl(0 0% 98%)` — `--background` | `hsl(0 0% 4%)` | Page background, card surface |
| Secondary (30%) | `hsl(0 0% 100%)` — `--card` | `hsl(0 0% 9%)` | Library deck Card component surface |
| Accent (10%) | `hsl(0 0% 96%)` — `--accent` | `hsl(0 0% 15%)` | Ghost button hover state on MoreVertical trigger |
| Destructive | `hsl(0 84% 60%)` — `--destructive` | `hsl(0 62% 30%)` | "Remove from library" DropdownMenuItem text; Remove AlertDialog confirm Button |

Accent reserved for: ghost button hover/focus background on `DropdownMenuTrigger` MoreVertical button only. Not used for any primary action.

Destructive reserved for: (1) `DropdownMenuItem` text color via `className="text-destructive focus:text-destructive"`, and (2) `Button variant="destructive"` in AlertDialogFooter confirm action. No other elements use destructive color.

Source: `apps/frontend/src/index.css` (lines 1–49), Phase 17 decision (17-02) for DropdownMenuItem destructive pattern.

---

## Component Inventory

All components already installed and imported in `DecksPage.tsx`. No new shadcn components required for this phase.

### Reused Without Change

| Component | Import path | Usage |
|-----------|-------------|-------|
| `DropdownMenu`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuTrigger` | `@/components/ui/dropdown-menu` | Library card ⋮ menu |
| `Button` | `@/components/ui/button` | MoreVertical trigger (`size="sm" variant="ghost"`); AlertDialog Remove confirm (`variant="destructive"`) |
| `AlertDialog`, `AlertDialogCancel`, `AlertDialogContent`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogHeader`, `AlertDialogTitle` | `@/components/ui/alert-dialog` | Remove-from-library confirmation dialog |
| `MoreVertical` | `lucide-react` | Icon inside ghost trigger button |

### New State Variable

| Variable | Type | Initial | Purpose |
|----------|------|---------|---------|
| `removeTargetId` | `string \| null` | `null` | Controls AlertDialog open state; holds deck id of the library deck pending removal |

Mirrors the existing `deleteTargetId: string | null` state shape (Phase 17 pattern, line 66 of `DecksPage.tsx`).

---

## Interaction Contract

### DropdownMenu — Library Card Footer

Placement: Right end of the `CardFooter` in the `ownerId !== user?.id` branch, after the "Open" Button. Identical position to the owned-deck ⋮ menu in the `ownerId === user?.id` branch.

Trigger element:
```
<Button size="sm" variant="ghost" aria-label={t('decks.moreActions')}>
  <MoreVertical className="h-4 w-4" />
</Button>
```

Reuses `t('decks.moreActions')` — key already defined in `en.json` as "More actions".

Menu content: Single item only.
```
<DropdownMenuItem
  className="text-destructive focus:text-destructive"
  onClick={() => setRemoveTargetId(deck.id)}
>
  {t('decks.removeFromLibrary')}
</DropdownMenuItem>
```

Clicking the item: sets `removeTargetId` to the deck's id. The DropdownMenu closes. The AlertDialog opens (controlled by `removeTargetId !== null`).

### AlertDialog — Remove Confirmation

Open condition: `removeTargetId !== null`
Close condition: `onOpenChange={(open) => { if (!open) setRemoveTargetId(null) }}`

Structure mirrors the existing delete AlertDialog (lines 248–271 of `DecksPage.tsx`):

| Element | Content |
|---------|---------|
| `AlertDialogTitle` | `{t('decks.removeFromLibraryTitle')}` → "Remove from library?" |
| `AlertDialogDescription` | `{t('decks.removeFromLibraryBody')}` → "Your study progress for this deck will be preserved. You can re-add it from Explore at any time." |
| Cancel button | `<AlertDialogCancel>{t('common.cancel')}</AlertDialogCancel>` |
| Confirm button | `<Button variant="destructive" onClick={() => { if (removeTargetId) void handleRemoveFromLibrary(removeTargetId) }}>{t('decks.removeFromLibraryConfirm')}</Button>` |

The confirm button uses `Button variant="destructive"` (not `AlertDialogAction`) — same pattern as existing delete dialog, enabling controlled dismiss timing.

### Post-Confirmation State Changes

1. API call: `DELETE /api/decks/${id}/library`
2. On success: optimistic removal — `setDecks((prev) => prev.filter((d) => d.id !== removeTargetId))`
3. Reset state: `setRemoveTargetId(null)`
4. Success toast: `toast.success(t('decks.removedFromLibraryToast'))`
5. On error: `toast.error(t('common.somethingWrong'))` — no state rollback needed (deck was not modified, only removed)

---

## Copywriting Contract

All copy pre-populated from D-05 and existing locale patterns.

| Element | i18n key | English copy |
|---------|----------|--------------|
| DropdownMenuItem label | `decks.removeFromLibrary` | "Remove from library" |
| AlertDialog title | `decks.removeFromLibraryTitle` | "Remove from library?" |
| AlertDialog body | `decks.removeFromLibraryBody` | "Your study progress for this deck will be preserved. You can re-add it from Explore at any time." |
| AlertDialog confirm button | `decks.removeFromLibraryConfirm` | "Remove" |
| Success toast | `decks.removedFromLibraryToast` | "Deck removed from your library" |
| Cancel button | `common.cancel` | "Cancel" — already defined, reused |
| Trigger aria-label | `decks.moreActions` | "More actions" — already defined, reused |

**Empty state:** Not applicable — removing a deck cannot result in an empty state (the removal removes the card; any remaining decks stay visible). The existing "No decks yet" empty state (key `decks.noDecksYet`) handles the case where all decks are removed.

**Error state copy:** `common.somethingWrong` — "Something went wrong. Please try again." — already defined, reused for API failure.

**Destructive confirmation approach:** AlertDialog with explicit destructive Button (variant="destructive") labeled "Remove". Title uses question form: "Remove from library?" — consistent with existing delete pattern "Delete deck?".

**German locale:** All 5 new keys (`decks.removeFromLibrary`, `decks.removeFromLibraryTitle`, `decks.removeFromLibraryBody`, `decks.removeFromLibraryConfirm`, `decks.removedFromLibraryToast`) must be added to `de.json` in the same commit as `en.json` (D-10, pattern from decision 10-05).

Source: CONTEXT.md D-05 (body copy confirmed by user), `en.json` existing key conventions.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | DropdownMenu, AlertDialog, Button (all pre-installed) | not required — already in codebase |

No third-party registries. No new shadcn blocks are added in this phase — all required components are already installed.

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|----------------|
| MoreVertical trigger accessible name | `aria-label={t('decks.moreActions')}` — "More actions" |
| AlertDialog focus management | Radix UI AlertDialog manages focus trap and returns focus to trigger on close automatically |
| Destructive action keyboard access | AlertDialog confirm button is focusable; Enter activates; Escape closes (Radix default) |
| Library card section ARIA | No change to existing card ARIA — new DropdownMenu is inside `CardFooter`, same DOM structure as owned-deck cards |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
