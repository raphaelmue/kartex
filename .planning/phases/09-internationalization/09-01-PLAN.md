---
phase: 09-internationalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/package.json
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
  - apps/frontend/src/i18n.ts
  - apps/frontend/src/i18n.d.ts
  - apps/frontend/src/test/setup.ts
  - apps/frontend/src/main.tsx
  - apps/frontend/src/components/__tests__/LanguageToggle.test.tsx
autonomous: false
requirements:
  - I18N-01
  - I18N-03
user_setup: []

must_haves:
  truths:
    - "i18next initializes before React renders and all 65 existing tests stay green"
    - "Both en.json and de.json exist and contain the same key set (no missing keys in either)"
    - "t('nonexistent.key') is a TypeScript compile error via CustomTypeOptions"
    - "A failing LanguageToggle test stub exists describing the toggle behavior (RED)"
  artifacts:
    - path: "apps/frontend/src/locales/en.json"
      provides: "English locale, source of truth for TypeScript types"
      contains: "\"nav\""
    - path: "apps/frontend/src/locales/de.json"
      provides: "German locale, fully translated, same keys as en.json"
      contains: "\"nav\""
    - path: "apps/frontend/src/i18n.ts"
      provides: "i18next init with LanguageDetector + initReactI18next, exports resources"
      contains: "initReactI18next"
    - path: "apps/frontend/src/i18n.d.ts"
      provides: "CustomTypeOptions augmentation referencing resources['en']"
      contains: "CustomTypeOptions"
    - path: "apps/frontend/src/components/__tests__/LanguageToggle.test.tsx"
      provides: "Failing test stub for I18N-03"
      contains: "changeLanguage"
  key_links:
    - from: "apps/frontend/src/main.tsx"
      to: "apps/frontend/src/i18n.ts"
      via: "import './i18n' as first import"
      pattern: "import.*['\"]\\./i18n['\"]"
    - from: "apps/frontend/src/i18n.d.ts"
      to: "apps/frontend/src/i18n.ts"
      via: "import { resources } from './i18n'"
      pattern: "resources"
    - from: "apps/frontend/src/test/setup.ts"
      to: "apps/frontend/src/locales/en.json"
      via: "i18n.init with English resources and initImmediate: false"
      pattern: "initImmediate"
---

<objective>
Stand up the i18n infrastructure for the frontend: install react-i18next + i18next + i18next-browser-languagedetector, create both fully-populated locale files (en.json source of truth, de.json complete German translation), the i18next init module, the TypeScript type augmentation, the test-environment i18n bootstrap that keeps all 65 existing tests green, and a failing LanguageToggle test stub.

Purpose: Every downstream string-wrapping plan depends on `t()`, the locale files, and a test environment where `useTranslation()` does not break existing tests. This plan delivers I18N-01 (infrastructure) and the RED test for I18N-03 (switcher). No visible UI changes.
Output: Locale JSON files, i18n.ts, i18n.d.ts, patched test setup, main.tsx import, failing LanguageToggle.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-internationalization/09-CONTEXT.md
@.planning/phases/09-internationalization/09-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before install</name>
  <read_first>
    - .planning/phases/09-internationalization/09-RESEARCH.md (§Package Legitimacy Audit, §Standard Stack)
  </read_first>
  <what-built>
    Nothing yet — this is a pre-install legitimacy gate. The three packages to be installed are: react-i18next@17.0.8, i18next@26.3.0, i18next-browser-languagedetector@8.2.1. RESEARCH.md §Package Legitimacy Audit marks all three [OK]/Approved via slopcheck 0.6.1, but the audit is treated as ASSUMED until a human confirms registry legitimacy.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-i18next — confirm it is the official React binding for i18next (maintained by the i18next org, millions of weekly downloads).
    2. Open https://www.npmjs.com/package/i18next — confirm it is the core i18next engine (active since 2011).
    3. Open https://www.npmjs.com/package/i18next-browser-languagedetector — confirm it is the official browser language detector under the i18next org.
    4. Confirm none are typosquats and all are the intended packages from CONTEXT.md D-06.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installation, or describe concerns.</resume-signal>
  <verify>
    <human-check>Human confirms all three npm packages are legitimate and intended</human-check>
  </verify>
  <done>Human has approved the three packages for installation.</done>
</task>

<task type="auto">
  <name>Task 2: Install packages, create locale files, i18n init, and type augmentation</name>
  <files>
    apps/frontend/package.json,
    apps/frontend/src/locales/en.json,
    apps/frontend/src/locales/de.json,
    apps/frontend/src/i18n.ts,
    apps/frontend/src/i18n.d.ts,
    apps/frontend/src/main.tsx
  </files>
  <read_first>
    - .planning/phases/09-internationalization/09-RESEARCH.md (§Locale Key Structure — the full en.json key tree; §Architecture Patterns Pattern 1 i18n.ts; Pattern 2 i18n.d.ts; §German Translation Approach)
    - .planning/phases/09-internationalization/09-CONTEXT.md (D-01 through D-06)
    - apps/frontend/src/main.tsx (current render bootstrap — add the i18n import as the FIRST import)
    - apps/frontend/package.json (workspace name @kartex/frontend, dependency style)
  </read_first>
  <action>
    Install dependencies into the frontend workspace only: run `pnpm --filter @kartex/frontend add react-i18next i18next i18next-browser-languagedetector` (resolves react-i18next@17.0.8, i18next@26.3.0, i18next-browser-languagedetector@8.2.1 per RESEARCH.md §Standard Stack; Pitfall 9 — must target the @kartex/frontend filter, not the root).

    Create `apps/frontend/src/locales/en.json` containing the COMPLETE nested key tree exactly as specified in RESEARCH.md §Locale Key Structure — all top-level groups: nav, a11y, auth, common, visibility, dashboard, decks, deckDetail, permission, table, cardEditor, deckForm, study, rating, session, exam, explore, import, admin, media, footer, lang. Use nested objects (not flat dotted keys) per D-03 rationale. Pluralization keys use the `_one`/`_other` suffix convention (common.nCards_one/_other, study.nMinutes_one/_other, import.cardsSkipped_one/_other, dashboard.streakDays). Interpolation variables use `{{doubleCurly}}` syntax. Do NOT stub any value — every leaf must hold the real English string from the §Locale Key Structure block.

    Create `apps/frontend/src/locales/de.json` with the IDENTICAL key tree and complete German translations per D-01 (de.json must be complete, not a stub). Follow the German rules in RESEARCH.md §German Translation Approach: capitalize German nouns (e.g. "Kartenstapel"), keep loanwords "Deck", "SM-2", "KaTeX", "Typst" as-is, preserve all `{{variable}}` interpolation placeholders unchanged, keep `_one`/`_other` plural suffix keys. Translate "Spaced Repetition" as "Spaced Repetition" (kept as the common German tech term). The brand "Kartex" stays untranslated; document.title keys ending in "— Kartex" keep "— Kartex" but translate the leading word (e.g. dashboard.title de = "Dashboard — Kartex", auth.signInTitle de = "Anmelden"). Every key present in en.json MUST exist in de.json with no missing keys.

    Create `apps/frontend/src/i18n.ts` per RESEARCH.md Pattern 1: import i18n from 'i18next', initReactI18next from 'react-i18next', LanguageDetector from 'i18next-browser-languagedetector', en from './locales/en.json', de from './locales/de.json'. Export a `const resources = { en: { translation: en }, de: { translation: de } } as const`. Call `i18n.use(LanguageDetector).use(initReactI18next).init({ resources, fallbackLng: 'en', supportedLngs: ['en', 'de'], interpolation: { escapeValue: false }, detection: { order: ['localStorage', 'navigator'], caches: ['localStorage'], lookupLocalStorage: 'i18nextLng' } })` (D-06 detection order, escapeValue:false because React escapes). Default-export i18n.

    Create `apps/frontend/src/i18n.d.ts` per RESEARCH.md Pattern 2 and Pitfall 6: `import { resources } from './i18n'` then `declare module 'i18next' { interface CustomTypeOptions { defaultNS: 'translation'; resources: typeof resources['en'] } }`. Use the resources export (which carries `as const`) rather than importing the JSON directly, so literal-key type inference works.

    Edit `apps/frontend/src/main.tsx`: add `import './i18n'` as the FIRST import line, above the `import React` line (Pitfall 1 — init must run before React renders). Do not change any other line.
  </action>
  <verify>
    <automated>cd apps/frontend && node -e "const en=require('./src/locales/en.json');const de=require('./src/locales/de.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v!==null?flat(v,p+k+'.'):[p+k]);const ek=flat(en).sort();const dk=flat(de).sort();const miss=ek.filter(k=>!dk.includes(k));const extra=dk.filter(k=>!ek.includes(k));if(miss.length||extra.length){console.error('KEY MISMATCH missing-in-de:',miss,'extra-in-de:',extra);process.exit(1)}console.log('OK',ek.length,'keys match')"</automated>
  </verify>
  <done>Both locale files parse as JSON, share an identical key set, en.json contains all 21 top-level groups, i18n.ts exports `resources`, i18n.d.ts declares CustomTypeOptions, and main.tsx imports './i18n' first. The key-parity script exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Patch test setup for i18n and add failing LanguageToggle test stub</name>
  <files>
    apps/frontend/src/test/setup.ts,
    apps/frontend/src/components/__tests__/LanguageToggle.test.tsx
  </files>
  <read_first>
    - apps/frontend/src/test/setup.ts (currently only imports jest-dom)
    - .planning/phases/09-internationalization/09-RESEARCH.md (§Test Setup — Option C with initImmediate:false; §Common Pitfalls 2 and 3)
    - apps/frontend/src/components/AppShell.tsx (where the language toggle button will live in Plan 02 — desktop sidebar user area near the theme toggle)
    - apps/frontend/src/components/__tests__/AppShell.test.tsx (existing render/mock harness pattern to mirror — AuthContext/ThemeContext mocks, render wrapper)
  </read_first>
  <behavior>
    - LanguageToggle test: rendering AppShell shows a button whose accessible name is the translated value of a11y.switchLanguage and whose visible text is "EN" when i18n.language === 'en"
    - LanguageToggle test: clicking that button calls i18n.changeLanguage('de') (the toggle cycles en -> de)
    - These tests MUST FAIL initially (RED) because the button does not exist in AppShell yet — Plan 02 makes them pass
  </behavior>
  <action>
    Edit `apps/frontend/src/test/setup.ts` to add i18n initialization using Option C from RESEARCH.md §Test Setup (do NOT use the t(key)=>key mock — Option A/B breaks existing getByText regex assertions). After the existing `import '@testing-library/jest-dom'` line, import i18n from 'i18next', initReactI18next from 'react-i18next', and en from '../locales/en.json', then call `i18n.use(initReactI18next).init({ lng: 'en', fallbackLng: 'en', resources: { en: { translation: en } }, interpolation: { escapeValue: false }, initImmediate: false })`. The `initImmediate: false` flag (Pitfall 3) makes init synchronous so the first render in jsdom has translations. This makes `t('study.filterByTag')` return the real English string, keeping all 65 existing tests green unmodified.

    Create `apps/frontend/src/components/__tests__/LanguageToggle.test.tsx` as a FAILING stub for I18N-03. Mirror the render harness in AppShell.test.tsx (mock useAuth and useTheme the same way, wrap in MemoryRouter as that file does). Import i18n from 'i18next' and spy on `i18n.changeLanguage` via vi.spyOn. Write two tests: (1) after rendering AppShell, query the button by accessible name using the translated a11y.switchLanguage label and assert its textContent is "EN"; (2) fire a click on that button and assert `i18n.changeLanguage` was called with 'de'. Add explicit vitest imports (describe, it, expect, vi, beforeEach) — tsconfig has no vitest/globals types (per STATE.md 07-01 decision). These tests are expected to FAIL now because AppShell has no language toggle; do not implement the toggle in this plan.
  </action>
  <verify>
    <automated>cd apps/frontend && pnpm test --run src/components/__tests__/LanguageToggle.test.tsx 2>&1 | tee lt.log; grep -qi "fail\|FAIL\|✕" lt.log && echo RED-AS-EXPECTED; rm -f lt.log</automated>
  </verify>
  <done>setup.ts initializes i18n synchronously with the English locale; all 65 pre-existing tests still pass; LanguageToggle.test.tsx exists and its two tests FAIL (RED) because the toggle is not yet implemented. Running `pnpm --filter @kartex/frontend test --run` shows existing tests green and LanguageToggle failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → client UI | `i18nextLng` value controls display language only; no auth/access impact |
| npm registry → build | Three new packages enter the build supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | i18next interpolation values | accept | `escapeValue: false` is safe because React escapes all rendered values by default; no `dangerouslySetInnerHTML` used with translated content (RESEARCH.md §Security Domain) |
| T-09-02 | Elevation of Privilege | `i18nextLng` localStorage key | accept | Value controls only display language; worst case is wrong UI language, no security impact (RESEARCH.md §Security Domain) |
| T-09-SC | Tampering | pnpm install of 3 i18n packages | mitigate | Blocking human checkpoint (Task 1) verifies each package on npmjs.com before install; slopcheck [OK] in RESEARCH.md §Package Legitimacy Audit |
</threat_model>

<verification>
- `pnpm --filter @kartex/frontend test --run` — all 65 existing tests green, LanguageToggle tests RED
- Key-parity node script (Task 2 verify) exits 0 — en.json and de.json have identical key sets
- `pnpm --filter @kartex/frontend typecheck` — clean (i18n.d.ts compiles; no consumers yet)
</verification>

<success_criteria>
- react-i18next, i18next, i18next-browser-languagedetector installed in apps/frontend
- en.json and de.json both complete with identical key sets (I18N-01)
- i18n.ts initializes with LanguageDetector + initReactI18next + localStorage detection (I18N-01, D-06)
- i18n.d.ts provides type-safe keys via CustomTypeOptions (I18N-01, D-03)
- main.tsx imports './i18n' before React renders
- All 65 existing tests remain green
- LanguageToggle.test.tsx is RED, ready for Plan 02 to turn GREEN (I18N-03)
</success_criteria>

<output>
Create `.planning/phases/09-internationalization/09-01-SUMMARY.md` when done.
</output>
