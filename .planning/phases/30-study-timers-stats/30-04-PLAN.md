---
phase: 30-study-timers-stats
plan: 04
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - apps/frontend/src/components/SessionTimer.tsx
  - apps/frontend/src/components/__tests__/SessionTimer.test.tsx
  - apps/frontend/src/hooks/useStudySession.ts
  - apps/frontend/src/pages/StudySessionPage.tsx
autonomous: true
requirements: [TIMER-01, TIMER-02, TIMER-03]

must_haves:
  truths:
    - "Normal/SR/deck study modes show a count-up mm:ss elapsed timer in the study header; exam mode keeps its countdown unchanged (D-01, D-02, TIMER-01)"
    - "The elapsed timer stops advancing while the tab is hidden and resumes on return, with no visible paused affordance (D-05)"
    - "Each non-exam rating POSTs the first-flip thinking time (front-display→first-flip, excluding hidden time) as thinkingTimeMs (D-03, D-04, TIMER-02)"
    - "A StudySession is started on session begin and completed on session finish for all modes including exam (D-06, D-07, TIMER-03)"
  artifacts:
    - apps/frontend/src/components/SessionTimer.tsx
    - apps/frontend/src/components/__tests__/SessionTimer.test.tsx
    - apps/frontend/src/hooks/useStudySession.ts
    - apps/frontend/src/pages/StudySessionPage.tsx
  key_links:
    - "SessionTimer receives the existing SessionRunner startTime state as its startedAt prop (no new state)"
    - "useStudySession sends thinkingTimeMs on the existing POST /api/study/rate call"
    - "SessionRunner posts /session/start on mount (deckIds = unique card deckIds) and /session/complete on sessionDone"
    - "session lifecycle calls are wrapped in try/catch (DEV-only logging) so they never break the study loop or existing tests"
---

<objective>
Deliver the frontend study-session tier: a new `SessionTimer` count-up component shown in every non-exam mode, first-flip thinking-time capture inside `useStudySession`, and the StudySession start/complete lifecycle calls in `SessionRunner`.

Purpose: This is where TIMER-01 (visible timer), TIMER-02 (thinking-time measurement), and TIMER-03 (session duration signalling) become observable to the user and are sent to the backend. Timing is measured client-side but sent to the server, which remains the source of truth for stored duration (Plan 03 computes it from timestamps).
Output: `SessionTimer.tsx` + its test, `useStudySession.ts` extended with a visibility-aware per-card stopwatch, and `StudySessionPage.tsx` wiring the timer render swap and session lifecycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-study-timers-stats/30-CONTEXT.md
@.planning/phases/30-study-timers-stats/30-PATTERNS.md
@.planning/phases/30-study-timers-stats/30-UI-SPEC.md
@apps/frontend/src/components/ExamTimer.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionTimer count-up component with visibility pause</name>
  <files>apps/frontend/src/components/SessionTimer.tsx, apps/frontend/src/components/__tests__/SessionTimer.test.tsx</files>
  <read_first>
    - apps/frontend/src/components/ExamTimer.tsx (full file — copy the imports, interval-tick primitive, mm:ss formatting, role/aria attributes; drop onExpire and the colorClass ternary)
    - apps/frontend/src/components/__tests__/CardFlip.test.tsx (frontend component test conventions: explicit vitest imports, jsdom render)
    - apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx (react-i18next mock pattern: t returns key with {{...}} interpolation)
    - .planning/phases/30-study-timers-stats/30-UI-SPEC.md (Interaction Contract §1, lines 135-186 — exact markup + a11y contract)
    - .planning/phases/30-study-timers-stats/30-PATTERNS.md (SessionTimer section lines 25-69 — count-up + visibility-pause guidance)
  </read_first>
  <behavior>
    - Given startedAt = now, initial render shows "00:00"
    - After advancing time by 65s, render shows "01:05"
    - The root element has role="timer" and aria-live="off"
    - While document.hidden is true, advancing time does NOT increment the displayed value; after visibility returns it resumes from where it paused
  </behavior>
  <action>
    Create apps/frontend/src/components/SessionTimer.tsx exporting `function SessionTimer({ startedAt }: { startedAt: number })`. Copy ExamTimer's imports (useEffect/useRef/useState, Timer icon, useTranslation). Maintain elapsed via `setInterval(..., 1000)` that computes `elapsed = Math.floor((Date.now() - startedAt - pausedMsRef.current) / 1000)` and stores it in state; clear the interval on unmount (Pitfall 4). Track hidden time with a `pausedMsRef` (accumulated hidden ms) and a `hiddenSinceRef` (timestamp when the tab last went hidden, or null): add a `document` `visibilitychange` listener that, on hidden, sets `hiddenSinceRef = Date.now()`, and on visible, adds `Date.now() - hiddenSinceRef` to `pausedMsRef` and clears `hiddenSinceRef`; remove the listener on unmount. Do not advance the displayed elapsed value while `document.hidden` is true (skip the state update inside the tick when hidden). Format `mins`/`secs` with `padStart(2, '0')` exactly like ExamTimer. Render the div with className `flex items-center gap-1 text-sm font-mono tabular-nums text-foreground` (always text-foreground — NO color-shift thresholds), `role="timer"`, `aria-live="off"`, `aria-label={t('study.sessionElapsedAriaLabel', { time: \`${mins}:${secs}\` })}`, a `<Timer className="h-4 w-4" aria-hidden="true" />` icon, and `<span>{mins}:{secs}</span>`. There is no onExpire prop.
    Create the test at apps/frontend/src/components/__tests__/SessionTimer.test.tsx with explicit vitest imports, the react-i18next mock (t returns the key), and `vi.useFakeTimers()`. Assert the four behaviors above. For the visibility test, set `Object.defineProperty(document, 'hidden', { configurable: true, get: () => true })` and dispatch `document.dispatchEvent(new Event('visibilitychange'))`, advance timers, assert the value did not change, then flip hidden back to false, dispatch again, and assert it resumes.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend test --run SessionTimer</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace @kartex/frontend test --run SessionTimer` passes all four behavior assertions
    - SessionTimer.tsx renders role="timer" and aria-live="off" and uses text-foreground with no color-shift classes
    - SessionTimer has no onExpire prop and takes a single `startedAt: number` prop
    - The component uses the study.sessionElapsedAriaLabel key with a {{time}} value
  </acceptance_criteria>
  <done>SessionTimer counts up in mm:ss, pauses while hidden, mirrors ExamTimer's a11y contract without color shift, and its test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thinking-time capture + session lifecycle wiring</name>
  <files>apps/frontend/src/hooks/useStudySession.ts, apps/frontend/src/pages/StudySessionPage.tsx</files>
  <read_first>
    - apps/frontend/src/hooks/useStudySession.ts (full file — flip at lines 40-54, rate at lines 56-93, the currentIndex/face refs, the POST /api/study/rate call at line 71)
    - apps/frontend/src/pages/StudySessionPage.tsx (SessionRunner lines 26-199 — startTime state line 46, sessionDone effect lines 53-55, the timer slot lines 135-140, ratingCounts, cards prop)
    - apps/frontend/src/lib/api.ts (api.post signature, line 69)
    - packages/shared/src/schemas/study.ts (RateCardSchema thinkingTimeMs, StudySessionStartSchema/StudySessionStartResponseSchema, StudySessionCompleteSchema from Plan 01)
    - .planning/phases/30-study-timers-stats/30-CONTEXT.md (D-03, D-04, D-05, D-06, D-07, D-09)
    - .planning/phases/30-study-timers-stats/30-PATTERNS.md (StudySessionPage section lines 73-112)
  </read_first>
  <behavior>
    - The first front→back flip of a card captures elapsed front-display time; the value passed to POST /api/study/rate as thinkingTimeMs excludes any time the tab was hidden (D-05)
    - Re-showing the same card front never happens in this hook (flip guard blocks non-front flips), so only the first flip's value is stored (D-04)
    - Exam mode still skips POST /api/study/rate (unchanged) but the session is still started and completed
  </behavior>
  <action>
    In useStudySession.ts: add refs `cardShownAtRef` (number — timestamp the current card front became visible), `hiddenAccumMsRef` (number — hidden ms accrued for the current card), `hiddenSinceRef` (number | null), and `capturedThinkingMsRef` (number | null — the thinking time captured at first flip). Reset `cardShownAtRef = Date.now()`, `hiddenAccumMsRef = 0`, `hiddenSinceRef = null`, `capturedThinkingMsRef = null` whenever a new card becomes current — do this in the existing card-advance path (when `currentIndex` changes / on initial mount) via a `useEffect([currentIndex])` or inside the advance branch of `rate`. Add a `visibilitychange` listener (own useEffect, cleaned up on unmount) that accrues hidden time into `hiddenAccumMsRef` using the same hidden/visible bookkeeping as SessionTimer. In `flip`, at the point the front→back transition is confirmed (first flip only — the existing guard already returns for non-front faces), set `capturedThinkingMsRef.current = Math.max(0, Math.round(Date.now() - cardShownAtRef.current - hiddenAccumMsRef.current))`. In `rate`, change the POST body to include `thinkingTimeMs: capturedThinkingMsRef.current ?? undefined` (undefined omits it — schema is optional). Do not change the exam-mode skip, the SM-2 flow, or advancement logic otherwise.
    In StudySessionPage.tsx SessionRunner: import `SessionTimer`. Replace the timer slot (lines 135-140) with the if/else from UI-SPEC §1 — render `<ExamTimer .../>` when `mode === 'exam' && examDurationSeconds !== null`, else render `<SessionTimer startedAt={startTime} />` (non-exam modes get the count-up timer in the same slot). Add a session-start effect that runs once when SessionRunner has cards: compute `deckIds = [...new Set(cards.map((c) => c.deckId))]`, POST `/api/study/session/start` with `{ deckIds }`, and store the returned `{ id }` in a `sessionIdRef` — guard with a ref so it fires exactly once even under strict-mode double-invoke, and wrap in try/catch that logs only in `import.meta.env.DEV` (matching the existing prefetch error handling), so a failure never blocks the session. Extend the existing `sessionDone` effect (lines 53-55): when `sessionDone` becomes true and `sessionIdRef.current` is set, POST `/api/study/session/complete` with `{ sessionId: sessionIdRef.current, cardsReviewed: ratingCounts.again + ratingCounts.hard + ratingCounts.good + ratingCounts.easy }`, also in try/catch with DEV-only logging. The session lifecycle runs for all modes including exam (D-07).
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend typecheck && yarn workspace @kartex/frontend test --run StudySessionPage</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace @kartex/frontend typecheck` exits 0
    - useStudySession's POST /api/study/rate body includes a thinkingTimeMs field sourced from the first-flip capture ref
    - useStudySession resets the per-card stopwatch refs on card advance and accrues hidden time via a visibilitychange listener
    - SessionRunner renders SessionTimer for non-exam modes and ExamTimer for exam mode in the same header slot
    - SessionRunner posts /session/start once (ref-guarded) with unique card deckIds and posts /session/complete on sessionDone, both in try/catch
    - The existing StudySessionPage test suite still passes (no regression; add api.post mocks if needed)
  </acceptance_criteria>
  <done>Thinking time is captured first-flip and sent on rate, the visible timer swaps correctly per mode, and the session start/complete calls fire without breaking the study loop or existing tests.</done>
</task>

</tasks>

## Artifacts this phase produces

**New component:** `apps/frontend/src/components/SessionTimer.tsx` — `SessionTimer({ startedAt: number })` count-up mm:ss timer with visibility pause

**Modified frontend behavior:**
- `useStudySession.ts` — per-card first-flip stopwatch refs (`cardShownAtRef`, `hiddenAccumMsRef`, `hiddenSinceRef`, `capturedThinkingMsRef`); sends `thinkingTimeMs` on POST /api/study/rate
- `StudySessionPage.tsx` (SessionRunner) — SessionTimer render swap for non-exam modes; `sessionIdRef`; session start/complete lifecycle calls

**Test file:** `apps/frontend/src/components/__tests__/SessionTimer.test.tsx`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| study loop → POST /rate | Client-measured thinkingTimeMs sent with each rating |
| study loop → POST /session/* | Client initiates session start/complete; server enforces ownership + computes duration (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-08 | Tampering | client thinkingTimeMs | low | accept | Display-only stat; server stores verbatim, no security decision derives from it; Zod bounds it nonnegative on the server (Plan 01/03) |
| T-30-09 | Denial of Service | visibilitychange listeners / setInterval | low | mitigate | All intervals and listeners are removed on unmount (Pitfall 4) to prevent leaks / setState-after-unmount |
| T-30-10 | Information Disclosure | session lifecycle fetch errors | low | mitigate | Lifecycle calls wrapped in try/catch with DEV-only logging — no raw error surfaced to the user or console in production |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs — SessionTimer is hand-written; uses existing lucide-react + react-i18next |
</threat_model>

<verification>
- `yarn workspace @kartex/frontend typecheck` exits 0
- `yarn workspace @kartex/frontend test --run SessionTimer` passes
- `yarn workspace @kartex/frontend test --run StudySessionPage` passes (no regression)
</verification>

<success_criteria>
Non-exam sessions display a paused-aware count-up timer, each rating carries first-flip thinking time, and sessions are started + completed across all modes without disrupting the core study loop.
</success_criteria>

<output>
Create `.planning/phases/30-study-timers-stats/30-04-SUMMARY.md` when done.
</output>
