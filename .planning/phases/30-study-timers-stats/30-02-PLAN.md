---
phase: 30-study-timers-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
autonomous: true
requirements: [TIMER-01, TIMER-04]

must_haves:
  truths:
    - "The session elapsed-timer aria-label key exists in the study namespace of both locales"
    - "All Recent Sessions + avg-flip-time column labels exist in the dashboard.stats namespace of both locales"
    - "en.json and de.json have identical key sets for the new keys (no fallback-to-key gaps)"
  artifacts:
    - apps/frontend/src/locales/en.json
    - apps/frontend/src/locales/de.json
  key_links:
    - "New keys added to both locales in the same commit (STATE.md 10-05 pitfall prevention)"
    - "sessionElapsedAriaLabel uses {{time}} interpolation; all others are plain strings"
---

<objective>
Add the nine new i18n keys the Phase 30 UI surfaces reference — one aria-label in the `study` namespace and eight labels in the `dashboard.stats` namespace — to both `en.json` and `de.json` atomically.

Purpose: This repo's established pitfall (STATE.md 10-05) is that a key present in `en.json` but missing from `de.json` renders as the raw key string for German users, not the English value. Owning all locale edits in a single Wave-1 plan keeps `en.json`/`de.json` conflict-free for the parallel frontend plans (04, 05) that consume these keys.
Output: nine new keys in each of the two locale files, exactly matching the Copywriting Contract in 30-UI-SPEC.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-study-timers-stats/30-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add study + dashboard.stats i18n keys to both locales</name>
  <files>apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/locales/en.json (existing `study` namespace and `dashboard.stats` namespace — insert new keys alongside; note existing keys like study.deckBadgeAriaLabel, dashboard.stats.noData, dashboard.stats.noDecksYet)
    - apps/frontend/src/locales/de.json (mirror structure exactly)
    - .planning/phases/30-study-timers-stats/30-UI-SPEC.md (Copywriting Contract, lines 328-382 — exact copy and key names, including suggested German translations)
  </read_first>
  <action>
    In BOTH en.json and de.json, add one key to the existing `study` namespace: `sessionElapsedAriaLabel` — en: "Elapsed time: {{time}}", de: "Verstrichene Zeit: {{time}}" (preserve the `{{time}}` interpolation placeholder verbatim in both). Do NOT add an `a11y` prefix — aria-label keys live directly under `study` (matches the existing `study.deckBadgeAriaLabel` convention).
    In BOTH files, add eight keys to the existing `dashboard.stats` namespace, appended after the existing `noDecksYet` key: `avgFlipTimeColumn` (en "Avg. Flip Time" / de "Ø Umdrehzeit"), `recentSessions` (en "Recent Sessions" / de "Letzte Lernsitzungen"), `sessionDateColumn` (en "Date" / de "Datum"), `sessionDeckColumn` (en "Deck(s)" / de "Stapel"), `sessionDurationColumn` (en "Duration" / de "Dauer"), `sessionCardsColumn` (en "Cards" / de "Karten"), `noSessionsYet` (en "No sessions yet." / de "Noch keine Lernsitzungen."), `incompleteSession` (en "Incomplete" / de "Unvollständig"). Do NOT add a `noData` key — it already exists and is reused for the avg-flip empty cell. Keep valid JSON (commas, no trailing comma) and preserve existing indentation.
  </action>
  <verify>
    <automated>node -e "['en','de'].forEach(l=>{const j=require('./apps/frontend/src/locales/'+l+'.json');const need=['avgFlipTimeColumn','recentSessions','sessionDateColumn','sessionDeckColumn','sessionDurationColumn','sessionCardsColumn','noSessionsYet','incompleteSession'];need.forEach(k=>{if(!(k in j.dashboard.stats))throw new Error(l+' missing dashboard.stats.'+k)});if(!('sessionElapsedAriaLabel'in j.study))throw new Error(l+' missing study.sessionElapsedAriaLabel');if(!j.study.sessionElapsedAriaLabel.includes('{{time}}'))throw new Error(l+' sessionElapsedAriaLabel missing {{time}}')});console.log('i18n keys OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both en.json and de.json parse as valid JSON
    - Both files contain `study.sessionElapsedAriaLabel` with a `{{time}}` placeholder
    - Both files contain all eight new `dashboard.stats.*` keys listed above
    - No existing key was removed or renamed (only additions)
  </acceptance_criteria>
  <done>Both locales carry the nine new keys with matching key sets and correct interpolation placeholders.</done>
</task>

</tasks>

## Artifacts this phase produces

**i18n keys (en.json + de.json):**
- `study.sessionElapsedAriaLabel` (with `{{time}}` interpolation)
- `dashboard.stats.avgFlipTimeColumn`
- `dashboard.stats.recentSessions`
- `dashboard.stats.sessionDateColumn`
- `dashboard.stats.sessionDeckColumn`
- `dashboard.stats.sessionDurationColumn`
- `dashboard.stats.sessionCardsColumn`
- `dashboard.stats.noSessionsYet`
- `dashboard.stats.incompleteSession`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static locale files → UI | Translation strings rendered as text via i18next; deck titles are user content interpolated as values, never keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-06 | Information Disclosure | i18n interpolation | low | accept | New keys carry no user data except `{{time}}` (a formatted mm:ss string); deck titles are rendered by consuming components as React text children, not through translation keys |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — locale JSON edits only |
</threat_model>

<verification>
- Both locale files parse as JSON and contain all nine new keys (automated node check)
</verification>

<success_criteria>
en.json and de.json each define the nine new Phase 30 keys with identical key sets, ready for the frontend consumer plans.
</success_criteria>

<output>
Create `.planning/phases/30-study-timers-stats/30-02-SUMMARY.md` when done.
</output>
