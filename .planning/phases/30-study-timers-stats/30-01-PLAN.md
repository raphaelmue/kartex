---
phase: 30-study-timers-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/prisma/migrations/20260704000000_add_study_timers/migration.sql
  - packages/shared/src/schemas/study.ts
  - packages/shared/src/schemas/stats.ts
  - apps/backend/src/routes/__tests__/study-timers-schema.test.ts
autonomous: false
requirements: [TIMER-02, TIMER-03, TIMER-04]

must_haves:
  truths:
    - "ReviewLog gains a nullable thinkingTimeMs column; historical rows keep NULL (no backfill)"
    - "StudySession + StudySessionDeck models exist, following the DeckShare join-table pattern (D-09)"
    - "RateCardSchema accepts an optional non-negative integer thinkingTimeMs (D-04 — re-flips may omit it)"
    - "Shared contract defines session start/complete request shapes and the RecentSession + per-deck avgThinkingTimeMs stats fields"
    - "The hand-written SQL migration is applied to the live database before any runtime verification"
  artifacts:
    - apps/backend/prisma/schema.prisma
    - apps/backend/prisma/migrations/20260704000000_add_study_timers/migration.sql
    - packages/shared/src/schemas/study.ts
    - packages/shared/src/schemas/stats.ts
    - apps/backend/src/routes/__tests__/study-timers-schema.test.ts
  key_links:
    - "packages/shared rebuilt (dist/index.js) so backend + frontend runtime see the new schemas"
    - "prisma generate run so the generated client exposes StudySession / StudySessionDeck / thinkingTimeMs"
    - "migration applied via the Docker Compose entrypoint (prisma migrate deploy) — no prisma db push / migrate dev (repo convention)"
---

<objective>
Deliver the data + contract foundation for Phase 30: the Prisma schema changes (`ReviewLog.thinkingTimeMs`, `StudySession`, `StudySessionDeck`), the hand-written SQL migration that applies them to the live database, and the shared Zod schemas that define every request/response shape the backend routes and frontend consume.

Purpose: `packages/shared` is the single source of truth for all data types — the backend route plan (03) and both frontend plans (04, 05) are thin consumers of the schemas defined here, so no type drift is possible. The migration MUST be applied to the live DB before verification because build/typecheck pass off `schema.prisma` alone and would produce a false-positive green state.
Output: schema models + migration file, extended `study.ts` / `stats.ts` shared schemas, a real runnable Zod-validation test, a rebuilt shared dist, a regenerated Prisma client, and an applied migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-study-timers-stats/30-CONTEXT.md
@.planning/phases/30-study-timers-stats/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema models + hand-written SQL migration + prisma generate</name>
  <files>apps/backend/prisma/schema.prisma, apps/backend/prisma/migrations/20260704000000_add_study_timers/migration.sql</files>
  <read_first>
    - apps/backend/prisma/schema.prisma (current ReviewLog at line 140, DeckShare at line 96, CardProgress at line 125 — copy-source patterns)
    - apps/backend/prisma/migrations/20260609000000_add_reviewlog_and_card_kartexid/migration.sql (exact hand-written migration structure to mirror: header comment, ALTER/CREATE TABLE/CREATE INDEX/ADD CONSTRAINT ordering)
    - apps/backend/entrypoint.sh (confirms migrations apply via `npx prisma migrate deploy` on container start)
    - .planning/phases/30-study-timers-stats/30-PATTERNS.md (schema.prisma section, lines 190-258 — exact model shapes)
  </read_first>
  <action>
    In schema.prisma: add `thinkingTimeMs Int?` to the `ReviewLog` model (nullable, no default — same nullable-column precedent as `Card.kartexId`). Add a new `StudySession` model with fields `id String @id @default(cuid())`, `userId String`, `user User @relation(fields: [userId], references: [id], onDelete: Cascade)`, `startedAt DateTime @default(now())`, `completedAt DateTime?`, `durationSeconds Int?`, `cardsReviewed Int @default(0)`, `decks StudySessionDeck[]`, and `@@index([userId, startedAt])`. Add a new `StudySessionDeck` join model following the `DeckShare` shape minus permission/isActive: `id String @id @default(cuid())`, `studySessionId String`, `studySession StudySession @relation(fields: [studySessionId], references: [id], onDelete: Cascade)`, `deckId String`, `deck Deck @relation(fields: [deckId], references: [id], onDelete: Cascade)`, and `@@unique([studySessionId, deckId])`. Add the back-relation `studySessions StudySession[]` to the `User` model and `studySessionDecks StudySessionDeck[]` to the `Deck` model (Prisma requires both sides of a relation).
    Create the migration file at apps/backend/prisma/migrations/20260704000000_add_study_timers/migration.sql. Mirror the 20260609000000 migration structure exactly: a header comment block stating what/why/safety; `ALTER TABLE "ReviewLog" ADD COLUMN "thinkingTimeMs" INTEGER;` (nullable, no DEFAULT); `CREATE TABLE "StudySession"` with columns id/userId/startedAt (DEFAULT CURRENT_TIMESTAMP)/completedAt (nullable)/durationSeconds (nullable)/cardsReviewed (INTEGER NOT NULL DEFAULT 0) and the primary key constraint; `CREATE TABLE "StudySessionDeck"` with id/studySessionId/deckId and primary key; `CREATE INDEX "StudySession_userId_startedAt_idx" ON "StudySession"("userId", "startedAt");`; `CREATE UNIQUE INDEX "StudySessionDeck_studySessionId_deckId_key" ON "StudySessionDeck"("studySessionId", "deckId");`; and `ADD CONSTRAINT ... FOREIGN KEY ... ON DELETE CASCADE ON UPDATE CASCADE` for StudySession.userId → User.id, StudySessionDeck.studySessionId → StudySession.id, and StudySessionDeck.deckId → Deck.id. Use the timestamp `20260704000000` (lexicographically after the latest existing migration `20260629000000`).
    Run `yarn workspace @kartex/backend exec prisma generate` (works from schema.prisma with no DATABASE_URL) so the generated client exposes the new models and column.
  </action>
  <verify>
    <automated>cd apps/backend && npx prisma validate && cd ../.. && grep -c "thinkingTimeMs" apps/backend/prisma/schema.prisma apps/backend/prisma/migrations/20260704000000_add_study_timers/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` (run in apps/backend) exits 0 — schema is syntactically valid with both new relations wired on both sides
    - schema.prisma contains `model StudySession`, `model StudySessionDeck`, and `thinkingTimeMs Int?` inside the ReviewLog model
    - The migration file exists at the exact path with timestamp 20260704000000_add_study_timers and contains `ALTER TABLE "ReviewLog" ADD COLUMN "thinkingTimeMs"`, `CREATE TABLE "StudySession"`, `CREATE TABLE "StudySessionDeck"`, the `(userId, startedAt)` index, and three `ON DELETE CASCADE` foreign keys
    - The migration uses no `DEFAULT` on thinkingTimeMs (existing rows resolve to NULL)
  </acceptance_criteria>
  <done>Schema has the column + two models, both relation sides declared, the hand-written migration mirrors the 20260609000000 structure, and the Prisma client is regenerated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared Zod contract (study + stats) with real validation test</name>
  <files>packages/shared/src/schemas/study.ts, packages/shared/src/schemas/stats.ts, apps/backend/src/routes/__tests__/study-timers-schema.test.ts</files>
  <read_first>
    - packages/shared/src/schemas/study.ts (current RateCardSchema at lines 13-16, RateCardResponseSchema)
    - packages/shared/src/schemas/stats.ts (current PerDeckProgressSchema at lines 16-22, StatsSummarySchema at lines 25-31, nullable convention)
    - packages/shared/src/index.ts (confirms schemas/study and schemas/stats are re-exported)
    - apps/backend/src/routes/__tests__/stats-summary.test.ts (existing test-file convention in this repo)
    - .planning/phases/30-study-timers-stats/30-PATTERNS.md (shared schema section, lines 262-289)
  </read_first>
  <behavior>
    - RateCardSchema.safeParse with a valid { cardId, rating, thinkingTimeMs: 4200 } succeeds
    - RateCardSchema.safeParse with { cardId, rating } (no thinkingTimeMs) still succeeds (optional)
    - RateCardSchema.safeParse with thinkingTimeMs: -1 fails (nonnegative int)
    - StudySessionStartSchema.safeParse with { deckIds: [] } fails (min 1); with { deckIds: ['d1'] } succeeds
    - StudySessionCompleteSchema.safeParse with { sessionId: 's1', cardsReviewed: 3 } succeeds; cardsReviewed: -1 fails
    - RecentSessionSchema.safeParse of a full valid row (id, startedAt string, durationSeconds, cardsReviewed, completed bool, deckTitles array) succeeds
  </behavior>
  <action>
    In packages/shared/src/schemas/study.ts: add `thinkingTimeMs: z.number().int().nonnegative().optional()` to `RateCardSchema` (optional per D-04 — re-flips may not carry a value; exam mode never rates). Add and export `StudySessionStartSchema = z.object({ deckIds: z.array(z.string().min(1)).min(1) })` with its `StudySessionStartInput` type; add `StudySessionStartResponseSchema = z.object({ id: z.string() })`. Add and export `StudySessionCompleteSchema = z.object({ sessionId: z.string().min(1), cardsReviewed: z.number().int().nonnegative() })` with its `StudySessionCompleteInput` type — note the client does NOT send durationSeconds (server computes it authoritatively from startedAt→completedAt per the threat model). Add and export `StudySessionSchema = z.object({ id: z.string(), startedAt: z.string(), completedAt: z.string().nullable(), durationSeconds: z.number().int().nonnegative().nullable(), cardsReviewed: z.number().int().nonnegative() })` for typed responses.
    In packages/shared/src/schemas/stats.ts: add `avgThinkingTimeMs: z.number().nullable()` to `PerDeckProgressSchema` (nullable per the null-on-empty convention — a deck with zero captured values is null, never 0). Add and export `RecentSessionSchema = z.object({ id: z.string(), startedAt: z.string(), durationSeconds: z.number().int().nonnegative(), cardsReviewed: z.number().int().nonnegative(), completed: z.boolean(), deckTitles: z.array(z.string()) })` with its `RecentSession` type. Add `recentSessions: z.array(RecentSessionSchema)` to `StatsSummarySchema` (always an array — empty when no sessions, never null).
    Create the real runnable test at apps/backend/src/routes/__tests__/study-timers-schema.test.ts importing the new schemas from '@kartex/shared' and asserting the six behaviors above with `describe`/`it`/`expect` (explicit vitest imports — this repo's tsconfig has no vitest globals types in the file pattern; follow the explicit-import convention).
    After editing, rebuild the shared package so runtime consumers see the changes: `yarn workspace @kartex/shared build`.
  </action>
  <verify>
    <automated>yarn workspace @kartex/shared build && yarn workspace @kartex/backend test --run study-timers-schema</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace @kartex/shared build` exits 0 (dist/index.js regenerated with the new exports)
    - `yarn workspace @kartex/backend test --run study-timers-schema` passes all six assertions
    - study.ts exports StudySessionStartSchema, StudySessionCompleteSchema, StudySessionSchema and RateCardSchema includes thinkingTimeMs
    - stats.ts exports RecentSessionSchema, PerDeckProgressSchema includes avgThinkingTimeMs, and StatsSummarySchema includes recentSessions
    - StudySessionCompleteSchema does NOT contain a durationSeconds field (server-authoritative)
  </acceptance_criteria>
  <done>Both shared schema files carry the new fields/schemas, the shared dist is rebuilt, and a real Zod-validation test passes.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply migration to the live database</name>
  <files>apps/backend/prisma/migrations/20260704000000_add_study_timers/migration.sql</files>
  <read_first>
    - apps/backend/entrypoint.sh (the apply mechanism: `npx prisma migrate deploy`)
    - docker-compose.yml (service names for db + backend)
    - apps/backend/prisma.config.ts (runtime datasource URL source)
  </read_first>
  <action>
    Apply the hand-written migration from Task 1 to the live database using the established repo mechanism (per STATE.md 10-02 / 18-01 / 24-01: `prisma migrate deploy` via the Docker Compose entrypoint — NEVER `prisma db push` or `prisma migrate dev`, and there is no DATABASE_URL in the dev shell). Preferred path: ensure the database container is running, then run `docker compose exec backend npx prisma migrate deploy` (or rebuild/restart the backend service so its entrypoint runs migrate deploy). If the Docker environment is not reachable from the execution shell or the command requires interaction that cannot be suppressed, STOP and surface a manual-intervention checkpoint: report the exact command the developer must run and the expected `prisma migrate deploy` success output (it should list `20260704000000_add_study_timers` as applied). Do not mark the plan complete until the migration is confirmed applied.
  </action>
  <verify>
    <human-check>Run `docker compose exec backend npx prisma migrate deploy` (or restart the backend service) and confirm the output reports `20260704000000_add_study_timers` applied with no pending migrations remaining. Then confirm the tables exist, e.g. `docker compose exec db psql -U $POSTGRES_USER -d $POSTGRES_DB -c '\d "StudySession"'` shows the StudySession table and `\d "ReviewLog"` shows the thinkingTimeMs column.</human-check>
  </verify>
  <acceptance_criteria>
    - `prisma migrate deploy` reports `20260704000000_add_study_timers` as applied and "No pending migrations" on a subsequent run
    - The live database has the StudySession and StudySessionDeck tables and the ReviewLog.thinkingTimeMs column
    - No `prisma db push` or `prisma migrate dev` was used
  </acceptance_criteria>
  <done>The migration is confirmed applied to the live database; new tables and column are queryable.</done>
</task>

</tasks>

## Artifacts this phase produces

**Prisma schema (schema.prisma):**
- `ReviewLog.thinkingTimeMs` — new nullable `Int?` column
- `StudySession` model — fields: `id`, `userId`, `startedAt`, `completedAt?`, `durationSeconds?`, `cardsReviewed`, `decks`; `@@index([userId, startedAt])`
- `StudySessionDeck` model — fields: `id`, `studySessionId`, `deckId`; `@@unique([studySessionId, deckId])`
- `User.studySessions StudySession[]` + `Deck.studySessionDecks StudySessionDeck[]` back-relations

**Migration:** `apps/backend/prisma/migrations/20260704000000_add_study_timers/migration.sql`

**Shared Zod schemas / types (packages/shared):**
- `study.ts`: `RateCardSchema.thinkingTimeMs` (optional); `StudySessionStartSchema` + `StudySessionStartInput`; `StudySessionStartResponseSchema`; `StudySessionCompleteSchema` + `StudySessionCompleteInput`; `StudySessionSchema`
- `stats.ts`: `PerDeckProgressSchema.avgThinkingTimeMs` (nullable); `RecentSessionSchema` + `RecentSession`; `StatsSummarySchema.recentSessions`

**Test file:** `apps/backend/src/routes/__tests__/study-timers-schema.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell → live DB | Schema DDL applied via Docker Compose entrypoint; no DATABASE_URL exposed in dev shell |
| client → shared schema | All request bodies validated against Zod before reaching Prisma |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Tampering | migration.sql DDL | medium | mitigate | Nullable column with no DEFAULT + new tables only — zero-downtime, no data mutation of existing rows (mirrors 20260609000000 safety) |
| T-30-05 | Denial of Service | thinkingTimeMs / cardsReviewed inputs | low | mitigate | Zod `int().nonnegative()` bounds at the shared-schema boundary; no unbounded strings or arrays introduced |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this phase — SessionTimer is hand-written, all deps (zod, lucide-react) already present; no legitimacy gate required |
</threat_model>

<verification>
- `cd apps/backend && npx prisma validate` exits 0
- `yarn workspace @kartex/shared build` exits 0
- `yarn workspace @kartex/backend test --run study-timers-schema` passes
- Migration confirmed applied to live DB (Task 3 human-check)
</verification>

<success_criteria>
The database and shared contract are ready: ReviewLog.thinkingTimeMs + StudySession + StudySessionDeck exist in schema, migration, and the live DB; the shared package exports every schema the routes and UI need; the Prisma client and shared dist are rebuilt.
</success_criteria>

<output>
Create `.planning/phases/30-study-timers-stats/30-01-SUMMARY.md` when done.
</output>
