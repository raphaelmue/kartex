---
phase: 30-study-timers-stats
plan: 05
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - apps/frontend/src/components/StatsSummaryPanel.tsx
  - apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx
autonomous: true
requirements: [TIMER-04]

must_haves:
  truths:
    - "The per-deck table has an appended Avg. Flip Time column showing seconds-with-one-decimal, or the noData string when a deck's avgThinkingTimeMs is null (D-10, D-12, TIMER-04)"
    - "A Recent Sessions section lists the last sessions with date, deck badge(s), duration (mm:ss), and card count (D-11, TIMER-04)"
    - "Multi-deck sessions render one secondary Badge per deck; incomplete sessions show an outline Incomplete badge (D-08, D-09)"
    - "Empty recent-sessions renders the noSessionsYet row; a null avg-flip cell renders muted noData, never 0.0s"
  artifacts:
    - apps/frontend/src/components/StatsSummaryPanel.tsx
    - apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx
  key_links:
    - "recentSessions read null-safe via summary?.recentSessions ?? [] (matches perDeck optional-chaining convention)"
    - "avg-flip cell reuses the existing dashboard.stats.noData key for the per-row null state"
    - "formatDuration reuses the mm:ss math from StudySessionPage; formatDate uses toLocaleDateString with i18n.language"
---

<objective>
Deliver the Dashboard stats display: append a per-deck Avg. Flip Time column to the existing `StatsSummaryPanel` table and add a Recent Sessions section below it.

Purpose: This is where TIMER-04 becomes visible to the user. The component is a pure consumer of the `StatsSummary` shape from `packages/shared` (Plan 01) and the data produced by `GET /api/stats/summary` (Plan 03); it must stay null-safe and reuse the existing table/section patterns so the new blocks are visually indistinguishable from the current per-deck table.
Output: extended `StatsSummaryPanel.tsx` and updated component tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-study-timers-stats/30-CONTEXT.md
@.planning/phases/30-study-timers-stats/30-UI-SPEC.md
@apps/frontend/src/components/StatsSummaryPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append avg-flip column + Recent Sessions section with tests</name>
  <files>apps/frontend/src/components/StatsSummaryPanel.tsx, apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx</files>
  <read_first>
    - apps/frontend/src/components/StatsSummaryPanel.tsx (full file — per-deck table lines 122-180, TableHead pattern lines 131-142, TableCell pattern lines 157-173, empty-state colSpan lines 146-154, optional-chaining defaults lines 34-38)
    - apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx (full file — fullSummary fixture lines 19-28 must be extended with the new fields; react-i18next mock pattern)
    - apps/frontend/src/pages/StudySessionPage.tsx (lines 78-81 — the mm:ss construction to reuse for formatDuration)
    - packages/shared/src/schemas/stats.ts (PerDeckProgressSchema.avgThinkingTimeMs, RecentSessionSchema, StatsSummarySchema.recentSessions from Plan 01)
    - .planning/phases/30-study-timers-stats/30-UI-SPEC.md (Interaction Contracts §2 lines 189-227 and §3 lines 229-323 — exact markup + null-safety rules)
  </read_first>
  <behavior>
    - A deck with avgThinkingTimeMs = 3400 renders "3.4s" in the new column; a deck with avgThinkingTimeMs = null renders the noData string (never "0.0s")
    - recentSessions = [] renders a single row with the noSessionsYet key
    - A session with completed = false renders the incompleteSession badge; a completed session does not
    - A session with two deckTitles renders two badges
  </behavior>
  <action>
    In StatsSummaryPanel.tsx: change `const { t } = useTranslation()` to also destructure `i18n`. Add `const recentSessions = summary?.recentSessions ?? []` alongside the existing `perDeck` default (line 38).
    Per-deck column (UI-SPEC §2): append ONE `TableHead` after the "In Learning" head (line 142) with classes `hidden sm:table-cell text-xs font-normal uppercase tracking-wide text-muted-foreground text-right` and text `t('dashboard.stats.avgFlipTimeColumn')`. Append ONE matching `TableCell` after the inLearning cell (line 173) with classes `hidden sm:table-cell text-right`: when `d.avgThinkingTimeMs === null` render `<span className="text-sm text-muted-foreground">{t('dashboard.stats.noData')}</span>`, else `<span className="text-sm text-foreground">{(d.avgThinkingTimeMs / 1000).toFixed(1)}s</span>`. Update the empty-state `colSpan` from 4 to 5 (line 149) since the table now has five columns. Do not reorder existing columns.
    Recent Sessions section (UI-SPEC §3): after the closing of the per-deck `<div className="mt-6">` block (after line 180), add a new `<div className="mt-6">` with a `<p className="text-sm font-semibold text-foreground mb-2">{t('dashboard.stats.recentSessions')}</p>` heading and an `<div className="overflow-x-auto" role="region" aria-label={t('dashboard.stats.recentSessions')}>` wrapping a `Table`. Columns: Date (`sessionDateColumn`), Deck(s) (`sessionDeckColumn`), Duration (`sessionDurationColumn`, right), Cards (`sessionCardsColumn`, right, `hidden sm:table-cell`) — header classes matching the existing muted uppercase pattern. Body: when `recentSessions.length === 0` render one `TableRow` with a `TableCell colSpan={4}` centered muted showing `t('dashboard.stats.noSessionsYet')`; else map each session to a `TableRow key={s.id}` with: a Date cell `{formatDate(s.startedAt)}`; a Deck(s) cell containing `<div className="flex flex-wrap gap-1">` of `<Badge key={title} variant="secondary" className="text-xs">{title}</Badge>` for each `s.deckTitles` (deck titles are user content — plain text, never translated); a Duration cell (right) showing `{formatDuration(s.durationSeconds)}` and, when `!s.completed`, a trailing `<Badge variant="outline" className="ml-2 text-xs">{t('dashboard.stats.incompleteSession')}</Badge>`; and a Cards cell (`hidden sm:table-cell text-right`) showing `{s.cardsReviewed}`.
    Add helpers: `formatDuration(totalSeconds)` → `${String(Math.floor(totalSeconds / 60)).padStart(2,'0')}:${String(totalSeconds % 60).padStart(2,'0')}` (reuse StudySessionPage math); `formatDate(iso)` → `new Date(iso).toLocaleDateString(i18n.language)`. Keep the file focused; extract helpers to module scope if that keeps the component readable (i18n.language must be passed in or read via the hook — if module-scope, pass language as an argument).
    Update StatsSummaryPanel.test.tsx: extend the `fullSummary` fixture so each `perDeck` entry has an `avgThinkingTimeMs` (e.g. 3400 for deck-1, null for deck-2) and add a `recentSessions` array (include one completed multi-deck session and one incomplete session). Add tests covering the four behaviors above. Ensure all pre-existing tests still pass with the extended fixture.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend typecheck && yarn workspace @kartex/frontend test --run StatsSummaryPanel</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace @kartex/frontend typecheck` exits 0 (fixture updated with the new required fields)
    - `yarn workspace @kartex/frontend test --run StatsSummaryPanel` passes, including all pre-existing tests
    - A null avgThinkingTimeMs cell renders the noData key text, not "0.0s" or a blank cell
    - recentSessions empty renders the noSessionsYet key; an incomplete session renders the incompleteSession badge (variant outline)
    - A multi-deck session renders one secondary Badge per deck title
    - The per-deck empty-state colSpan is 5 (five columns)
  </acceptance_criteria>
  <done>The per-deck table shows null-safe avg flip time and the Recent Sessions section lists sessions with badges, duration, and card counts; tests pass.</done>
</task>

</tasks>

## Artifacts this phase produces

**Modified component (apps/frontend/src/components/StatsSummaryPanel.tsx):**
- New per-deck "Avg. Flip Time" column (null-safe, seconds-with-one-decimal)
- New "Recent Sessions" section (Date / Deck(s) / Duration / Cards) with secondary deck badges + outline Incomplete badge
- `formatDuration(totalSeconds)` and `formatDate(iso)` helpers

**Test file:** `apps/frontend/src/components/__tests__/StatsSummaryPanel.test.tsx` (extended fixture + new cases)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/stats/summary → StatsSummaryPanel | Server-provided stats rendered as text; deck titles are user content rendered as React children |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-11 | Information Disclosure | deck-title badges in recent sessions | low | mitigate | Deck titles rendered as React text children (auto-escaped by React), never via dangerouslySetInnerHTML or as translation keys |
| T-30-12 | Denial of Service | recentSessions render | low | mitigate | List is server-capped at 10 (Plan 03 take: 10); component reads null-safe `?? []` so a missing field cannot crash the panel |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs — reuses installed Table + Badge shadcn components and lucide-react |
</threat_model>

<verification>
- `yarn workspace @kartex/frontend typecheck` exits 0
- `yarn workspace @kartex/frontend test --run StatsSummaryPanel` passes
</verification>

<success_criteria>
The Dashboard stats panel shows per-deck average flip time (null-safe) and a Recent Sessions list with deck badges, mm:ss duration, incomplete markers, and card counts — visually consistent with the existing per-deck table.
</success_criteria>

<output>
Create `.planning/phases/30-study-timers-stats/30-05-SUMMARY.md` when done.
</output>
