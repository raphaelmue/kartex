---
phase: 05-import-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - apps/frontend/src/pages/ImportPage.tsx
  - apps/frontend/src/hooks/useImport.ts
  - apps/frontend/src/lib/api.ts
  - apps/frontend/src/App.tsx
autonomous: true
requirements:
  - IMPT-01
  - IMPT-05

must_haves:
  truths:
    - "Visiting `/import` shows the ImportPage (not ComingSoon) for a logged-in user"
    - "User can select a `.kartex` or `.kartex.zip` file via Browse button or drag-and-drop; client-side size check fires immediately (D-11)"
    - "After file selection, the PREVIEW state shows the amber warnings banner (if any), deck metadata block with editable deck name, card count, and a scrollable lazy-rendered card list using KartexRenderer (D-04, D-05, D-06, D-07)"
    - "Each `LazyCard` in the preview renders `KartexRenderer` only when scrolled into view via IntersectionObserver (D-04)"
    - "Clicking 'Import Deck' submits FormData (file + deckName) via `api.postForm()` — NOT `api.post()` (Pitfall 3); on 201, transitions to SUCCESS state"
    - "SUCCESS state shows deck name, card count, 'View Deck' button (navigates to /decks/:deckId), and 'Import another file' button (resets to UPLOAD)"
    - "Error states: inline text for oversized file / invalid extension; `Alert variant='destructive'` for backend 422/network errors; amber `Alert` for parse warnings banner"
    - "`api.postForm()` helper exists in `apps/frontend/src/lib/api.ts` — passes FormData to baseFetch without JSON.stringify (RESEARCH.md Pitfall 3 fix)"
  artifacts:
    - path: "apps/frontend/src/pages/ImportPage.tsx"
      provides: "ImportPage component: 4-state machine (UPLOAD/PARSING/PREVIEW/SUCCESS), LazyCard inline component"
      exports:
        - ImportPage
    - path: "apps/frontend/src/hooks/useImport.ts"
      provides: "useImport hook: state machine, config fetch, file validation, FormData upload"
      exports:
        - useImport
        - ImportStep
    - path: "apps/frontend/src/lib/api.ts"
      provides: "postForm() helper added to api object"
      contains: "postForm"
    - path: "apps/frontend/src/App.tsx"
      provides: "/import route wired to ImportPage"
      contains: "ImportPage"
  key_links:
    - from: "apps/frontend/src/pages/ImportPage.tsx"
      to: "apps/frontend/src/hooks/useImport.ts"
      via: "const { step, ... } = useImport()"
      pattern: "useImport"
    - from: "apps/frontend/src/pages/ImportPage.tsx"
      to: "apps/frontend/src/components/KartexRenderer.tsx"
      via: "<KartexRenderer content={card.front} />"
      pattern: "KartexRenderer"
    - from: "apps/frontend/src/hooks/useImport.ts"
      to: "apps/frontend/src/lib/api.ts"
      via: "api.postForm('/api/import', formData)"
      pattern: "api\\.postForm"
    - from: "apps/frontend/src/App.tsx"
      to: "apps/frontend/src/pages/ImportPage.tsx"
      via: "<Route path='/import' element={<ImportPage />} />"
      pattern: "ImportPage"
---

<objective>
Deliver the complete `/import` frontend page: install the `alert` shadcn component, add `postForm` to `api.ts`, create `useImport.ts` hook and `ImportPage.tsx` with all four states (UPLOAD/PARSING/PREVIEW/SUCCESS), wire the `/import` route in `App.tsx`.

Purpose: Closes the import pipeline loop — user can upload a file, see a live preview, and confirm import. Uses all backend APIs from Plan 05-02.

Output:
- `apps/frontend/src/lib/api.ts` — `postForm` method added
- `apps/frontend/src/hooks/useImport.ts` — state machine hook
- `apps/frontend/src/pages/ImportPage.tsx` — full import page with `LazyCard` inline
- `apps/frontend/src/App.tsx` — `/import` route wired
</objective>

<execution_context>
@C:\Users\rapha\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\rapha\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\rapha\workspace\kartex\.planning\PROJECT.md
@C:\Users\rapha\workspace\kartex\.planning\ROADMAP.md
@C:\Users\rapha\workspace\kartex\.planning\STATE.md

<!-- Prior plan outputs for API types -->
@C:\Users\rapha\workspace\kartex\.planning\phases\05-import-pipeline\05-01-SUMMARY.md
@C:\Users\rapha\workspace\kartex\.planning\phases\05-import-pipeline\05-02-SUMMARY.md

<!-- UI-SPEC — all visual/interaction decisions for every state -->
@C:\Users\rapha\workspace\kartex\.planning\phases\05-import-pipeline\05-UI-SPEC.md

<!-- Analog hooks/pages to replicate structure from -->
@C:\Users\rapha\workspace\kartex\apps\frontend\src\hooks\useStudySession.ts
@C:\Users\rapha\workspace\kartex\apps\frontend\src\pages\DashboardPage.tsx

<!-- Files being modified -->
@C:\Users\rapha\workspace\kartex\apps\frontend\src\lib\api.ts
@C:\Users\rapha\workspace\kartex\apps\frontend\src\App.tsx
</context>

<interfaces>
<!-- api.ts — current api object (end of file); postForm is added to this object: -->
```typescript
export const api = {
  get(url: string, options?: RequestInit): Promise<Response> {
    return baseFetch(url, { ...options, method: 'GET' })
  },
  post(url: string, body?: unknown, options?: RequestInit): Promise<Response> {
    return baseFetch(url, {
      ...options,
      method: 'POST',
      body: body !== undefined ? JSON.stringify(body) : undefined,
    })
  },
  patch(url: string, body?: unknown, options?: RequestInit): Promise<Response> { ... },
  delete(url: string, options?: RequestInit): Promise<Response> { ... },
}
```
<!-- baseFetch already skips Content-Type header when body instanceof FormData (line 17-19 of api.ts) -->
<!-- api.post() CANNOT be used for FormData — it calls JSON.stringify unconditionally (Pitfall 3) -->

<!-- App.tsx — current /import route (line 78): -->
```typescript
<Route path="/import" element={<ComingSoon title="Import" />} />
```

<!-- useStudySession.ts — hook return interface pattern to replicate: -->
```typescript
interface UseStudySessionReturn {
  currentCard: DueCard | null
  face: CardFace
  // ...
  flip: () => void
  rate: (rating: 1 | 2 | 3 | 4) => void
}
export function useStudySession(cards: DueCard[], mode: StudyMode): UseStudySessionReturn { ... }
```

<!-- DashboardPage.tsx — page structure to replicate: -->
```typescript
export function DashboardPage() {
  useEffect(() => { document.title = 'Dashboard — Kartex' }, [])
  // ...
  return (
    <div className="max-w-2xl mx-auto px-4 py-8">
      {/* page content */}
    </div>
  )
}
```

<!-- KartexRenderer interface: -->
```typescript
// apps/frontend/src/components/KartexRenderer.tsx
// Named export: export function KartexRenderer({ content }: { content: string })
// Props: content: string — renders Markdown + KaTeX + Typst + media:// + YouTube + code
```

<!-- ParseWarning type (from 05-01): -->
```typescript
// from @kartex/shared
export interface ParseWarning { cardIndex: number; reason: string }
export interface ParsedCard { front: string; back: string; tags: string[] }
```

<!-- ImportResult type (from 05-01): -->
```typescript
// from @kartex/shared
export interface ImportResult { deckId: string; cardCount: number; warnings: ParseWarning[] }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn alert component, add postForm to api.ts</name>
  <files>
    apps/frontend/src/lib/api.ts
    apps/frontend/src/components/ui/alert.tsx
  </files>

  <read_first>
    - apps/frontend/src/lib/api.ts — read the ENTIRE file; understand the api object shape before adding postForm
    - apps/frontend/src/components/ui/ — list contents to confirm alert.tsx is NOT already present (if it is, skip the shadcn install)
    - .planning/phases/05-import-pipeline/05-UI-SPEC.md §Design System — confirms alert is needed, progress may be needed
  </read_first>

  <action>
    Step A — Install shadcn alert component. Run from the monorepo root:
    ```bash
    cd apps/frontend && npx shadcn@latest add alert --yes
    cd ../..
    ```
    If alert already exists (`apps/frontend/src/components/ui/alert.tsx` is present), skip this step.

    Also check for progress component (used for upload progress bar per UI-SPEC §1b):
    ```bash
    ls apps/frontend/src/components/ui/progress.tsx 2>/dev/null || (cd apps/frontend && npx shadcn@latest add progress --yes && cd ../..)
    ```

    Step B — Add `postForm` method to `api.ts`. Open `apps/frontend/src/lib/api.ts` and add `postForm` to the `api` object, after the `delete` method, before the closing `}`:

    ```typescript
    postForm(url: string, formData: FormData, options?: RequestInit): Promise<Response> {
      return baseFetch(url, { ...options, method: 'POST', body: formData })
    },
    ```

    The `baseFetch` function already handles FormData correctly (skips `Content-Type: application/json` when `body instanceof FormData`). This method passes the FormData directly without JSON.stringify — fixing Pitfall 3.
  </action>

  <verify>
    <automated>cd C:\Users\rapha\workspace\kartex && yarn workspace @kartex/frontend build 2>&1 | grep -E "error|Error" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "postForm" apps/frontend/src/lib/api.ts` returns a match
    - `grep "JSON.stringify" apps/frontend/src/lib/api.ts | wc -l` still returns the same count as before (postForm does NOT call JSON.stringify)
    - `test -f apps/frontend/src/components/ui/alert.tsx` exits 0
    - `test -f apps/frontend/src/components/ui/progress.tsx` exits 0
    - TypeScript: `yarn workspace @kartex/frontend build` exits 0
  </acceptance_criteria>

  <done>
    `alert.tsx` and `progress.tsx` installed. `postForm` added to `api.ts`. TypeScript build passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useImport.ts hook and ImportPage.tsx, wire App.tsx</name>
  <files>
    apps/frontend/src/hooks/useImport.ts
    apps/frontend/src/pages/ImportPage.tsx
    apps/frontend/src/App.tsx
  </files>

  <read_first>
    - apps/frontend/src/hooks/useStudySession.ts — replicate hook structure: typed return interface, flat useState declarations, useCallback async pattern, useEffect for data fetch on mount
    - apps/frontend/src/pages/DashboardPage.tsx — replicate page structure: document.title, max-w-2xl wrapper, try/catch/toast.error pattern
    - apps/frontend/src/App.tsx — read ENTIRE file; understand current import block (line 1–16) and route at line 78 before editing
    - apps/frontend/src/components/KartexRenderer.tsx — confirm export name (named export `KartexRenderer`, prop `content: string`)
    - .planning/phases/05-import-pipeline/05-UI-SPEC.md — READ COMPLETELY; all four states, every copywriting item, accessibility attributes, warning banner JSX, LazyCard JSX, confirm row, success state, error states
    - .planning/phases/05-import-pipeline/05-RESEARCH.md — Pattern 3 (IntersectionObserver LazyCard with `{ threshold: 0.1, rootMargin: '200px' }` from UI-SPEC §1c-iii), Pitfall 3 (use api.postForm not api.post), Pitfall 7 (append deckName to FormData)
    - .planning/phases/05-import-pipeline/05-PATTERNS.md — §useImport.ts and §ImportPage.tsx pattern assignments, §Modified Files for App.tsx
  </read_first>

  <behavior>
    useImport.ts state machine states: `'upload' | 'parsing' | 'preview' | 'importing' | 'success'`

    Key behaviors tested:
    - Config fetch on mount: calls `api.get('/api/import/config')` → sets `maxFileSizeBytes`; if fetch fails, `configError = true` (D-11 graceful degradation)
    - `selectFile(file)`: if `maxFileSizeBytes && file.size > maxFileSizeBytes` → sets inline error, stays in 'upload'; else transitions to 'parsing'
    - In 'parsing' state, for `.kartex` files: reads file as text, calls `parseKartex()` from `@kartex/shared`, transitions to 'preview' with parse result
    - In 'parsing' state, for `.kartex.zip` files: transitions to 'preview' immediately (server does the parsing on submit); stores the file for later
    - `submitImport(deckName)`: appends `formData.append('file', file)` + `formData.append('deckName', deckName)`, calls `api.postForm('/api/import', formData)` (NOT api.post), on 201 → 'success', on error → back to 'preview' with error message
    - `reset()`: resets all state to 'upload'
  </behavior>

  <action>
    Step A — Create `apps/frontend/src/hooks/useImport.ts`:

    ```typescript
    import { useCallback, useEffect, useRef, useState } from 'react'
    import { toast } from 'sonner'
    import type { ParsedCard, ParseWarning, ImportConfig, KartexParseResult } from '@kartex/shared'
    import { parseKartex } from '@kartex/shared'
    import { api } from '@/lib/api'

    export type ImportStep = 'upload' | 'parsing' | 'preview' | 'importing' | 'success'

    interface UseImportReturn {
      step: ImportStep
      maxFileSizeBytes: number | null
      configError: boolean
      selectedFile: File | null
      parseResult: KartexParseResult | null
      importResult: { deckId: string; cardCount: number; warnings: ParseWarning[] } | null
      fileError: string | null
      importError: string | null
      importErrors: { name: string; reason: string }[] | null
      selectFile: (file: File) => void
      submitImport: (deckName: string) => Promise<void>
      reset: () => void
    }

    export function useImport(): UseImportReturn {
      const [step, setStep] = useState<ImportStep>('upload')
      const [maxFileSizeBytes, setMaxFileSizeBytes] = useState<number | null>(null)
      const [configError, setConfigError] = useState(false)
      const [selectedFile, setSelectedFile] = useState<File | null>(null)
      const [parseResult, setParseResult] = useState<KartexParseResult | null>(null)
      const [importResult, setImportResult] = useState<{
        deckId: string
        cardCount: number
        warnings: ParseWarning[]
      } | null>(null)
      const [fileError, setFileError] = useState<string | null>(null)
      const [importError, setImportError] = useState<string | null>(null)
      const [importErrors, setImportErrors] = useState<{ name: string; reason: string }[] | null>(null)

      // Fetch upload config on mount (D-10, D-11)
      const fetchConfig = useCallback(async () => {
        try {
          const res = await api.get('/api/import/config')
          if (res.ok) {
            const data = (await res.json()) as ImportConfig
            setMaxFileSizeBytes(data.maxFileSizeBytes)
          } else {
            setConfigError(true)
          }
        } catch {
          setConfigError(true)
        }
      }, [])

      useEffect(() => {
        void fetchConfig()
      }, [fetchConfig])

      const selectFile = useCallback(
        (file: File) => {
          setFileError(null)
          setImportError(null)
          setImportErrors(null)

          // Validate extension
          const name = file.name
          if (!name.endsWith('.kartex') && !name.endsWith('.kartex.zip')) {
            setFileError('Only .kartex and .kartex.zip files are supported.')
            return
          }

          // Client-side size check (D-11) — skip if config fetch failed (graceful degradation)
          if (maxFileSizeBytes !== null && file.size > maxFileSizeBytes) {
            const fileMB = (file.size / 1024 / 1024).toFixed(1)
            const limitMB = Math.round(maxFileSizeBytes / 1024 / 1024)
            setFileError(
              `File is too large (${fileMB} MB). Maximum allowed size is ${limitMB} MB.`,
            )
            return
          }

          setSelectedFile(file)
          setStep('parsing')

          // For .kartex files: parse on client side for preview (IMPT-01)
          // For .kartex.zip: cannot parse on client (zip extraction is server-side); store file and show preview after server responds
          if (name.endsWith('.kartex') && !name.endsWith('.kartex.zip')) {
            void (async () => {
              try {
                const text = await file.text()
                const result = parseKartex(text)
                if ('fatal' in result) {
                  setFileError(result.message)
                  setStep('upload')
                } else {
                  setParseResult(result)
                  setStep('preview')
                }
              } catch {
                setFileError('Could not read the file. Please try again.')
                setStep('upload')
              }
            })()
          } else {
            // .kartex.zip — no client-side preview of card contents; show a minimal preview state
            // The backend will parse and validate on submit
            // For ZIP files, we show the file name and proceed to PREVIEW with null parseResult
            // The user can still confirm; server will return the full result
            setParseResult(null)
            setStep('preview')
          }
        },
        [maxFileSizeBytes],
      )

      const submitImport = useCallback(
        async (deckName: string) => {
          if (!selectedFile) return
          setStep('importing')
          setImportError(null)
          setImportErrors(null)

          const formData = new FormData()
          formData.append('file', selectedFile)
          formData.append('deckName', deckName.trim())

          try {
            const res = await api.postForm('/api/import', formData)

            if (res.status === 201) {
              const data = (await res.json()) as {
                deckId: string
                cardCount: number
                warnings: ParseWarning[]
              }
              setImportResult(data)
              setStep('success')
            } else if (res.status === 422) {
              const errBody = (await res.json()) as {
                error: string
                files?: { name: string; reason: string }[]
              }
              if (errBody.files && errBody.files.length > 0) {
                setImportErrors(errBody.files)
              } else {
                setImportError(errBody.error ?? 'Import failed. Please check your file.')
              }
              setStep('preview')
            } else if (res.status === 413) {
              setImportError(
                'File is too large. Please reduce the file size and try again.',
              )
              setStep('upload')
            } else {
              const errBody = (await res.json().catch(() => ({ error: '' }))) as {
                error: string
              }
              setImportError(errBody.error || 'Upload failed. Check your connection and try again.')
              setStep('preview')
            }
          } catch {
            setImportError('Upload failed. Check your connection and try again.')
            setStep('preview')
          }
        },
        [selectedFile],
      )

      const reset = useCallback(() => {
        setStep('upload')
        setSelectedFile(null)
        setParseResult(null)
        setImportResult(null)
        setFileError(null)
        setImportError(null)
        setImportErrors(null)
      }, [])

      return {
        step,
        maxFileSizeBytes,
        configError,
        selectedFile,
        parseResult,
        importResult,
        fileError,
        importError,
        importErrors,
        selectFile,
        submitImport,
        reset,
      }
    }
    ```

    Step B — Create `apps/frontend/src/pages/ImportPage.tsx`.

    The file implements the full UI-SPEC contract. Key structural notes:
    - `LazyCard` is defined inline in this file (not a separate file — UI-SPEC §Component Inventory)
    - Drop zone uses `role="button"`, `tabIndex={0}`, drag events (`onDragOver`, `onDrop`), and a hidden `<input type="file" ref={fileInputRef} className="sr-only" />`
    - State machine renders different content based on `step` from `useImport()`
    - Amber warnings banner uses raw `Alert` with `className="border-amber-200 bg-amber-50 text-amber-800 mb-6"` overrides (not `variant="destructive"`)
    - `LazyCard` uses `IntersectionObserver` with `{ threshold: 0.1, rootMargin: '200px' }` (UI-SPEC §1c-iii)

    ```typescript
    import {
      AlertCircle,
      AlertTriangle,
      ArrowLeft,
      CheckCircle2,
      Loader2,
      Upload,
    } from 'lucide-react'
    import { useEffect, useRef, useState } from 'react'
    import { useNavigate } from 'react-router-dom'
    import type { ParsedCard } from '@kartex/shared'
    import { KartexRenderer } from '@/components/KartexRenderer'
    import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
    import { Badge } from '@/components/ui/badge'
    import { Button } from '@/components/ui/button'
    import { Input } from '@/components/ui/input'
    import { Progress } from '@/components/ui/progress'
    import { useImport } from '@/hooks/useImport'

    // ── LazyCard — inline component, only used in ImportPage ──────────────────────
    function LazyCard({ card }: { card: ParsedCard }) {
      const ref = useRef<HTMLDivElement>(null)
      const [visible, setVisible] = useState(false)

      useEffect(() => {
        if (!ref.current) return
        const observer = new IntersectionObserver(
          ([entry]) => {
            if (entry.isIntersecting) {
              setVisible(true)
              observer.disconnect()
            }
          },
          { threshold: 0.1, rootMargin: '200px' },
        )
        observer.observe(ref.current)
        return () => observer.disconnect()
      }, [])

      return (
        <div ref={ref} className="min-h-[80px]">
          {visible ? (
            <div className="border border-border rounded-lg p-4 space-y-2">
              <div className="text-xs font-medium text-muted-foreground uppercase tracking-wide">
                Front
              </div>
              <KartexRenderer content={card.front} />
              <hr className="border-border my-2" />
              <div className="text-xs font-medium text-muted-foreground uppercase tracking-wide">
                Back
              </div>
              <KartexRenderer content={card.back} />
              {card.tags.length > 0 && (
                <div className="flex flex-wrap gap-1 mt-2">
                  {card.tags.map((tag) => (
                    <Badge key={tag} variant="secondary" className="text-xs">
                      {tag}
                    </Badge>
                  ))}
                </div>
              )}
            </div>
          ) : (
            <div
              className="border border-border rounded-lg p-4 min-h-[80px] animate-pulse bg-muted/30"
              aria-hidden="true"
            />
          )}
        </div>
      )
    }

    // ── ImportPage ─────────────────────────────────────────────────────────────────
    export function ImportPage() {
      const navigate = useNavigate()
      const {
        step,
        maxFileSizeBytes,
        selectedFile,
        parseResult,
        importResult,
        fileError,
        importError,
        importErrors,
        selectFile,
        submitImport,
        reset,
      } = useImport()

      const fileInputRef = useRef<HTMLInputElement>(null)
      const [isDragOver, setIsDragOver] = useState(false)
      const [pendingDeckName, setPendingDeckName] = useState('')
      const [isSubmitting, setIsSubmitting] = useState(false)

      useEffect(() => {
        document.title = 'Import — Kartex'
      }, [])

      // Pre-fill deck name when parseResult arrives
      useEffect(() => {
        if (parseResult) {
          setPendingDeckName(parseResult.deck.deck)
        } else if (selectedFile && step === 'preview') {
          // ZIP file: use filename as fallback deck name (strip .kartex.zip)
          const stripped = selectedFile.name
            .replace(/\.kartex\.zip$/, '')
            .replace(/\.kartex$/, '')
          setPendingDeckName(stripped)
        }
      }, [parseResult, selectedFile, step])

      function handleFileSelect(file: File) {
        selectFile(file)
      }

      function handleFileInputChange(e: React.ChangeEvent<HTMLInputElement>) {
        const file = e.target.files?.[0]
        if (file) handleFileSelect(file)
        // Reset input so selecting the same file again triggers onChange
        e.target.value = ''
      }

      function handleDragOver(e: React.DragEvent) {
        e.preventDefault()
        setIsDragOver(true)
      }

      function handleDragLeave() {
        setIsDragOver(false)
      }

      function handleDrop(e: React.DragEvent) {
        e.preventDefault()
        setIsDragOver(false)
        const file = e.dataTransfer.files[0]
        if (file) handleFileSelect(file)
      }

      async function handleConfirmImport() {
        setIsSubmitting(true)
        await submitImport(pendingDeckName)
        setIsSubmitting(false)
      }

      const limitMB = maxFileSizeBytes ? Math.round(maxFileSizeBytes / 1024 / 1024) : 10

      // ── SUCCESS state ─────────────────────────────────────────────────────────
      if (step === 'success' && importResult) {
        return (
          <div
            className="flex flex-col items-center justify-center text-center py-16 gap-4 max-w-md mx-auto"
            role="status"
            aria-live="polite"
          >
            <CheckCircle2 className="h-12 w-12 text-green-500" aria-hidden="true" />
            <h2 className="text-xl font-semibold">Deck imported!</h2>
            <p className="text-sm text-muted-foreground">
              &ldquo;{pendingDeckName}&rdquo; was created with {importResult.cardCount} cards.
            </p>
            <Button
              size="lg"
              className="w-full max-w-xs"
              onClick={() => navigate(`/decks/${importResult.deckId}`)}
            >
              View Deck
            </Button>
            <Button
              variant="outline"
              size="sm"
              className="w-full max-w-xs mt-2"
              onClick={reset}
            >
              Import another file
            </Button>
          </div>
        )
      }

      return (
        <div className="max-w-2xl mx-auto px-4 py-8">
          <h1 className="text-xl font-semibold">Import Deck</h1>
          <p className="text-sm text-muted-foreground mt-1 mb-8">
            Upload a .kartex file or .kartex.zip bundle to import a deck.
          </p>

          {/* ── UPLOAD state ────────────────────────────────────────────────── */}
          {(step === 'upload') && (
            <>
              {/* Backend validation errors shown above drop zone */}
              {importErrors && importErrors.length > 0 && (
                <Alert variant="destructive" className="mb-6">
                  <AlertCircle className="h-4 w-4" />
                  <AlertTitle>Import failed</AlertTitle>
                  <AlertDescription>
                    The following files in your zip failed validation:
                    <ul className="mt-2 space-y-1">
                      {importErrors.map((e, i) => (
                        <li key={i} className="text-sm">
                          {e.name}: {e.reason}
                        </li>
                      ))}
                    </ul>
                    Remove or fix these files and re-upload the zip.
                  </AlertDescription>
                </Alert>
              )}
              {importError && (
                <Alert variant="destructive" className="mb-6">
                  <AlertCircle className="h-4 w-4" />
                  <AlertTitle>Import failed</AlertTitle>
                  <AlertDescription>{importError}</AlertDescription>
                </Alert>
              )}

              {/* Drop zone */}
              <div
                role="button"
                tabIndex={0}
                aria-label="Upload a .kartex file. Click to browse or drag and drop."
                onDragOver={handleDragOver}
                onDragLeave={handleDragLeave}
                onDrop={handleDrop}
                onClick={() => fileInputRef.current?.click()}
                onKeyDown={(e) => {
                  if (e.key === 'Enter' || e.key === ' ') fileInputRef.current?.click()
                }}
                className={`
                  flex flex-col items-center justify-center min-h-[200px] p-8 rounded-lg
                  border-2 border-dashed cursor-pointer
                  transition-colors duration-150
                  ${isDragOver
                    ? 'border-primary bg-primary/5'
                    : 'border-border bg-background hover:bg-muted/20'
                  }
                `}
              >
                <Upload className="h-10 w-10 text-muted-foreground mx-auto mb-4" aria-hidden="true" />
                <p className="text-sm font-normal text-muted-foreground text-center">
                  Drop your file here, or click to browse
                </p>
                <p className="text-xs text-muted-foreground text-center mt-1">
                  .kartex or .kartex.zip &middot; max {limitMB} MB
                </p>
                <Button
                  variant="outline"
                  size="sm"
                  className="mt-4"
                  onClick={(e) => {
                    e.stopPropagation()
                    fileInputRef.current?.click()
                  }}
                  style={{ minHeight: '44px' }}
                >
                  Browse file
                </Button>
              </div>

              <input
                ref={fileInputRef}
                type="file"
                accept=".kartex,.kartex.zip,application/zip"
                className="sr-only"
                aria-hidden="true"
                onChange={handleFileInputChange}
              />

              {fileError && (
                <p className="text-sm text-destructive mt-2 text-center">{fileError}</p>
              )}
            </>
          )}

          {/* ── PARSING state ─────────────────────────────────────────────────── */}
          {step === 'parsing' && (
            <div
              className="flex flex-col items-center justify-center min-h-[200px] p-8 rounded-lg border-2 border-dashed border-border"
              aria-busy="true"
              aria-label="Processing file"
            >
              <Loader2
                className="h-8 w-8 animate-spin text-muted-foreground mx-auto mb-3"
                aria-hidden="true"
              />
              <p className="text-sm font-normal text-muted-foreground text-center">
                Parsing your file...
              </p>
              <Progress
                className="w-full max-w-xs mx-auto mt-4"
                aria-label="Upload progress"
                aria-valuemin={0}
                aria-valuemax={100}
              />
            </div>
          )}

          {/* ── PREVIEW state ─────────────────────────────────────────────────── */}
          {(step === 'preview' || step === 'importing') && (
            <>
              <Button
                variant="ghost"
                size="sm"
                className="mb-4 -ml-1"
                onClick={reset}
                disabled={step === 'importing'}
              >
                <ArrowLeft className="h-4 w-4 mr-1" aria-hidden="true" />
                Back
              </Button>

              {/* Parse warnings banner (D-06) — amber alert, only if warnings exist */}
              {parseResult && parseResult.warnings.length > 0 && (
                <Alert className="border-amber-200 bg-amber-50 text-amber-800 mb-6" role="alert">
                  <AlertTriangle className="h-4 w-4 text-amber-600" />
                  <AlertTitle className="text-amber-800 font-semibold text-sm">
                    {parseResult.warnings.length} card
                    {parseResult.warnings.length > 1 ? 's' : ''} skipped
                  </AlertTitle>
                  <AlertDescription className="text-amber-800 text-sm mt-1">
                    {parseResult.warnings.map((w, i) => (
                      <div key={i}>
                        Card {w.cardIndex}: {w.reason}
                      </div>
                    ))}
                  </AlertDescription>
                </Alert>
              )}

              {/* Backend import errors shown in preview state */}
              {importError && step === 'preview' && (
                <Alert variant="destructive" className="mb-6" role="alert">
                  <AlertCircle className="h-4 w-4" />
                  <AlertTitle>Import failed</AlertTitle>
                  <AlertDescription>{importError}</AlertDescription>
                </Alert>
              )}
              {importErrors && importErrors.length > 0 && step === 'preview' && (
                <Alert variant="destructive" className="mb-6" role="alert">
                  <AlertCircle className="h-4 w-4" />
                  <AlertTitle>Import failed</AlertTitle>
                  <AlertDescription>
                    The following files in your zip failed validation:
                    <ul className="mt-2 space-y-1">
                      {importErrors.map((e, i) => (
                        <li key={i} className="text-sm">
                          {e.name}: {e.reason}
                        </li>
                      ))}
                    </ul>
                    Remove or fix these files and re-upload the zip.
                  </AlertDescription>
                </Alert>
              )}

              {/* Deck metadata block (D-05, D-07) */}
              <div className="border border-border rounded-lg p-4 mb-6 bg-card space-y-3">
                <div>
                  <label
                    htmlFor="deck-name-input"
                    className="text-sm font-normal text-foreground mb-1 block"
                  >
                    Deck name
                  </label>
                  <Input
                    id="deck-name-input"
                    aria-label="Deck name"
                    value={pendingDeckName}
                    onChange={(e) => setPendingDeckName(e.target.value)}
                    disabled={step === 'importing'}
                  />
                </div>
                {parseResult && (parseResult.deck.author || (parseResult.deck.tags && parseResult.deck.tags.length > 0)) && (
                  <div className="space-y-1">
                    {parseResult.deck.author && (
                      <p className="text-sm text-muted-foreground">
                        Author: {parseResult.deck.author}
                      </p>
                    )}
                    {parseResult.deck.tags && parseResult.deck.tags.length > 0 && (
                      <div className="flex flex-wrap gap-1">
                        {parseResult.deck.tags.map((tag) => (
                          <Badge key={tag} variant="secondary" className="text-xs">
                            {tag}
                          </Badge>
                        ))}
                      </div>
                    )}
                  </div>
                )}
                <p className="text-sm text-muted-foreground">
                  {parseResult ? (
                    <>
                      Showing {parseResult.cards.length} cards
                      {parseResult.warnings.length > 0 && (
                        <span className="text-amber-600 ml-1">
                          ({parseResult.warnings.length} skipped)
                        </span>
                      )}
                    </>
                  ) : (
                    selectedFile && `File: ${selectedFile.name}`
                  )}
                </p>
              </div>

              {/* Card preview list — lazy rendered (D-04, IMPT-05) */}
              {parseResult && parseResult.cards.length > 0 && (
                <div
                  className="space-y-3 mb-8"
                  aria-label={`Card preview list, ${parseResult.cards.length} cards`}
                >
                  {parseResult.cards.map((card, i) => (
                    <LazyCard key={i} card={card} />
                  ))}
                </div>
              )}

              {/* ZIP file: no client-side card preview — show informational note */}
              {!parseResult && selectedFile && (
                <div className="mb-8 p-4 border border-border rounded-lg bg-muted/20">
                  <p className="text-sm text-muted-foreground text-center">
                    Card preview is not available for .kartex.zip bundles.
                    Click &ldquo;Import Deck&rdquo; to import — the file will be validated and imported on the server.
                  </p>
                </div>
              )}

              {/* Confirm / Cancel row */}
              <div className="flex gap-3 pt-6 border-t border-border mt-8">
                <Button
                  variant="outline"
                  onClick={reset}
                  disabled={step === 'importing'}
                >
                  Cancel
                </Button>
                <Button
                  onClick={() => void handleConfirmImport()}
                  disabled={
                    !pendingDeckName.trim() || step === 'importing' || isSubmitting
                  }
                  aria-disabled={!pendingDeckName.trim()}
                  title={!pendingDeckName.trim() ? 'Deck name cannot be empty.' : undefined}
                >
                  {step === 'importing' || isSubmitting ? (
                    <>
                      <Loader2 className="h-4 w-4 animate-spin mr-2" aria-hidden="true" />
                      Importing...
                    </>
                  ) : (
                    'Import Deck'
                  )}
                </Button>
              </div>
            </>
          )}
        </div>
      )
    }
    ```

    Step C — Update `apps/frontend/src/App.tsx`:

    1. Add import after the existing page imports (follow alphabetical order — after StudySessionPage):
    ```typescript
    import { ImportPage } from '@/pages/ImportPage'
    ```

    2. Replace the ComingSoon placeholder at line 78:
    FROM: `<Route path="/import" element={<ComingSoon title="Import" />} />`
    TO:   `<Route path="/import" element={<ImportPage />} />`

    Build and verify:
    ```bash
    yarn workspace @kartex/frontend build
    ```
  </action>

  <verify>
    <automated>cd C:\Users\rapha\workspace\kartex && yarn workspace @kartex/frontend build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `yarn workspace @kartex/frontend build` exits 0 with no TypeScript errors
    - `grep "ImportPage" apps/frontend/src/App.tsx` returns 2 lines (import + route element)
    - `grep "ComingSoon.*Import" apps/frontend/src/App.tsx` returns NO match (placeholder removed)
    - `grep "useImport" apps/frontend/src/pages/ImportPage.tsx` returns matches
    - `grep "KartexRenderer" apps/frontend/src/pages/ImportPage.tsx` returns matches
    - `grep "IntersectionObserver" apps/frontend/src/pages/ImportPage.tsx` returns matches
    - `grep "postForm" apps/frontend/src/hooks/useImport.ts` returns a match (NOT api.post)
    - `grep "api\.post(" apps/frontend/src/hooks/useImport.ts` returns NO match (postForm used instead)
    - `grep "formData.append.*deckName" apps/frontend/src/hooks/useImport.ts` returns a match (Pitfall 7 fix)
    - `grep "border-amber-200" apps/frontend/src/pages/ImportPage.tsx` returns a match (amber warnings banner)
    - `grep "LazyCard" apps/frontend/src/pages/ImportPage.tsx` returns matches
    - Full test suite passes: `yarn workspace @kartex/frontend test --run`
  </acceptance_criteria>

  <done>
    `useImport.ts` and `ImportPage.tsx` created. `App.tsx` wired. TypeScript build passes. All tests still green. Committed: `feat(05-03): add ImportPage with lazy card preview and 4-state machine`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file input → selectFile() | The File object from the browser's file picker or drag-and-drop; size and extension validated client-side |
| FormData → api.postForm() → backend | All actual validation is server-authoritative; client-side checks are UX only (D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-UI-01 | Tampering | Malicious deck name in pendingDeckName sent to backend | accept | Deck name is a user-controlled string; backend creates a Deck record with it as `title`; no SQL injection via Prisma parameterized queries; no HTML rendering of the title outside React's XSS-safe JSX |
| T-5-UI-02 | Information Disclosure | KartexRenderer rendering attacker-crafted card content from import | accept | `react-markdown` allowDangerousHtml is false (T-02-07 accepted Phase 2); no raw HTML injection possible; math/Typst rendering is sandboxed by KaTeX/WASM |
| T-5-UI-03 | Denial of Service | Very large card list (1000+ cards) rendering simultaneously | mitigate | IntersectionObserver lazy rendering: only cards in or near viewport render KartexRenderer; skeleton placeholders for off-screen cards |
</threat_model>

<verification>
Manual verification checklist (run the app: `yarn dev`):

1. Visit `/import` — ImportPage renders (no ComingSoon)
2. Select a file with wrong extension — inline error appears below drop zone immediately
3. Select a valid `.kartex` file within size limit — transitions to PARSING then PREVIEW
4. PREVIEW shows amber warnings banner if parse had warnings; no banner if none
5. PREVIEW shows editable "Deck name" field pre-filled from header
6. PREVIEW shows card count; LazyCard placeholders visible before scroll; KartexRenderer renders when scrolled into view
7. Edit deck name, click "Import Deck" — loading spinner appears, then transitions to SUCCESS on 201
8. SUCCESS shows "Deck imported!" with the edited deck name and card count
9. "View Deck" navigates to `/decks/{deckId}`
10. "Import another file" resets to UPLOAD state
11. "Back" button in PREVIEW resets to UPLOAD state
12. Upload a file over the limit — instant rejection error (no network request)
</verification>

<success_criteria>
- `/import` route renders ImportPage for logged-in user
- All 4 states (UPLOAD/PARSING/PREVIEW/SUCCESS) render correctly per UI-SPEC
- Amber warnings banner only shown when warnings exist
- LazyCard renders KartexRenderer via IntersectionObserver (D-04)
- FormData submitted via `api.postForm()` not `api.post()` — file and deckName both present (Pitfall 3 + 7 fixed)
- SUCCESS state shows navigable "View Deck" button
- TypeScript build passes with no errors
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-import-pipeline/05-03-SUMMARY.md` following the summary template at `@C:\Users\rapha\.claude\get-shit-done\templates\summary.md`.

Include:
- Files created: `useImport.ts`, `ImportPage.tsx`
- Files modified: `api.ts` (postForm added), `App.tsx` (route wired)
- Key decisions: LazyCard inline in ImportPage (no cross-page reuse); ZIP files have no client-side card preview (parsing is server-side only); `api.postForm()` used for FormData upload; `pendingDeckName` state pre-filled from parse result
- Test result: full suite green
</output>
