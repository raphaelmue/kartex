---
phase: 22-study-session-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/pages/StudySessionPage.tsx
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
  - apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx
autonomous: true
requirements:
  - STUDY-04
must_haves:
  truths:
    - "During any study session, the current card's source deck name is visible in the progress row — on both front and back"
    - "The deck badge always renders (not conditional on studyMode), placed in the same row as the session progress counter and mode indicator"
    - "The badge uses the same <Badge variant='secondary' className='text-xs shrink-0'> design as the existing mode indicator badge"
    - "Both en.json and de.json are updated atomically with the new study.deckBadgeAriaLabel key"
    - "deckTitle is rendered as interpolated user content, never passed to t() as a key (D-07)"
  artifacts:
    - path: "apps/frontend/src/pages/StudySessionPage.tsx"
      provides: "SessionRunner with deck badge in progress row"
      contains: "currentCard.deckTitle"
    - path: "apps/frontend/src/locales/en.json"
      provides: "study.deckBadgeAriaLabel key"
      contains: "deckBadgeAriaLabel"
    - path: "apps/frontend/src/locales/de.json"
      provides: "study.deckBadgeAriaLabel key (German)"
      contains: "deckBadgeAriaLabel"
  key_links:
    - from: "SessionRunner (StudySessionPage.tsx)"
      to: "currentCard.deckTitle"
      via: "direct property access on DueCard"
      pattern: "currentCard\\.deckTitle"
---

<objective>
Add a deck-name badge to the study session card progress row so users always know which deck the current card belongs to. The badge renders on both the front and back of each card, using the same visual design as the existing study-mode indicator badge (SM2-04).

Purpose: STUDY-04 — users studying a cross-deck global session need to know which deck each card comes from. The `deckTitle` field is already present on `DueCard` (confirmed in `packages/shared/src/schemas/study.ts`) and populated by the backend — zero backend changes needed.

Output: One additional `<Badge>` element in `SessionRunner`'s progress row; two locale file updates (en + de); one new test assertion in the existing test file.
</objective>

<execution_context>
@C:\Users\rapha\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\rapha\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deck badge to SessionRunner progress row</name>
  <files>apps/frontend/src/pages/StudySessionPage.tsx, apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/pages/StudySessionPage.tsx — read the full file before touching; focus on the SessionRunner component and the progress row at line ~153
    - apps/frontend/src/locales/en.json — read the study section (lines 188–228) to understand existing keys and structure
    - apps/frontend/src/locales/de.json — read the study section (lines 188–228) for the German counterpart
    - packages/shared/src/schemas/study.ts — confirms DueCard has deckTitle: z.string()
  </read_first>
  <behavior>
    - New Badge renders in the progress row (the div at line 153) alongside SessionProgress and the optional mode badge
    - Badge always renders when currentCard is non-null — NOT conditional on studyMode
    - Badge content: currentCard.deckTitle (user content, interpolated as value — never passed as i18n key per D-07)
    - Badge aria-label: t('study.deckBadgeAriaLabel', { deckTitle: currentCard.deckTitle })
    - Badge uses same variant and className as the mode indicator: variant="secondary" className="text-xs shrink-0"
    - Placement: the deck badge is inserted BEFORE the mode indicator badge in the progress row div (deck context first, then study mode context)
    - en.json: add "deckBadgeAriaLabel": "Deck: {{deckTitle}}" inside the "study" object
    - de.json: add "deckBadgeAriaLabel": "Deck: {{deckTitle}}" inside the "study" object (same format — deckTitle is a proper noun, not translated)
    - Both locale files updated in the same edit pass (atomic, per STATE.md Pitfall 5 rule)
  </behavior>
  <action>
    In SessionRunner, locate the progress row div (className contains "flex items-center gap-2 mb-3 shrink-0"). After the SessionProgress component and before the conditional mode-indicator badge, insert a new Badge element:

    `<Badge variant="secondary" className="text-xs shrink-0" aria-label={t('study.deckBadgeAriaLabel', { deckTitle: currentCard.deckTitle })}>{currentCard.deckTitle}</Badge>`

    The badge is unconditional — it renders whenever currentCard is non-null (which it always is in the active SessionRunner body, since `if (!currentCard) return null` guards above).

    Add to en.json inside the "study" object (after "noActiveDecksHint"):
    `"deckBadgeAriaLabel": "Deck: {{deckTitle}}"`

    Add to de.json inside the "study" object (after "noActiveDecksHint"):
    `"deckBadgeAriaLabel": "Deck: {{deckTitle}}"`

    No other files require changes. The DueCard type already has deckTitle. The Badge component is already imported at line 8.

    NOTE — pre-existing file size violation: StudySessionPage.tsx currently exceeds the CLAUDE.md 500-line limit (725 lines) as a pre-existing condition from prior phases. This plan's change is a minimal 2-line addition to an existing progress row; extraction of the entire file is out of scope for this targeted fix. The pre-existing violation is tracked and does not block this plan.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/pages/__tests__/StudySessionPage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SessionRunner progress row contains a Badge rendering currentCard.deckTitle (unconditional)
    - Badge has variant="secondary" className="text-xs shrink-0" matching the mode indicator badge design
    - en.json "study.deckBadgeAriaLabel" key is present
    - de.json "study.deckBadgeAriaLabel" key is present
    - All existing StudySessionPage tests pass (no regression)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add STUDY-04 test assertion for deck badge visibility</name>
  <files>apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx</files>
  <read_first>
    - apps/frontend/src/pages/__tests__/StudySessionPage.test.tsx — read the full test file; study the makeCard factory (line 57), setupPrefetchMocks (line 105), and the SM2-04 describe block (line 552) as the pattern to follow for new assertions
    - apps/frontend/src/pages/StudySessionPage.tsx — verify currentCard.deckTitle is what the makeCard factory populates (deckTitle: 'Test Deck')
  </read_first>
  <behavior>
    - New describe block: "StudySessionPage deck badge (STUDY-04)"
    - beforeEach: mockParams.current = { id: 'deck-abc' }, mockApiGet.mockReset(), setupPrefetchMocks()
    - afterEach: mockParams.current = { id: 'deck-abc' }
    - Test STUDY-04a: deck badge shows deckTitle from currentCard
      - mockApiGet.mockResolvedValueOnce({ ok: true, json: async () => [makeCard('c1', [])] }) for the loadCards call
      - renderPage()
      - waitFor: screen.getByText(/filter by tag/i)
      - fireEvent.click on the SR button
      - waitFor: screen.getByText('Card 1 of 1') (session active)
      - assert: screen.getByText('Test Deck') is truthy (deckTitle from makeCard factory = 'Test Deck')
    - Test STUDY-04b: deck badge is present on back face too (same DOM, just isFlipped changes CardFlip presentation — badge is in SessionRunner's progress row, not inside the card)
      - Same setup as STUDY-04a
      - After session starts, fireEvent.click on the card (flip to back)
      - waitFor: badge 'Test Deck' is still visible
  </behavior>
  <action>
    Add a new describe block at the end of the file, after the "StudySessionPage mode indicator (SM2-04)" describe block. Follow the exact pattern of SM2-04a/b/c tests:
    - Import nothing new (all imports already present at the top of the file)
    - Use mockParams, mockStudyMode, mockApiGet, setupPrefetchMocks, makeCard, renderPage — all already in scope
    - The deck badge lives in the SessionRunner progress row (outside CardFlip), so it renders regardless of face state
    - For STUDY-04b: after SR mode starts, fireEvent.click on the card element (use screen.getByRole('button', { name: /flashcard/i }) or the card area) to flip it; then assert 'Test Deck' is still present
    - The CardFlip component renders the card as a button with aria-label from 'a11y.flashcard' key ("Flashcard. Click or press Space to reveal answer.") — use getByRole('button', { name: /flashcard/i }) or getByLabelText(/flashcard/i) to click it
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/pages/__tests__/StudySessionPage.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - New "StudySessionPage deck badge (STUDY-04)" describe block exists in the test file
    - STUDY-04a: passes — 'Test Deck' text found in DOM when session is running
    - STUDY-04b: passes — 'Test Deck' still visible after card flip
    - All previously passing tests still pass (no regression)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → React state | deckTitle from DueCard is user-supplied deck name content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | deckTitle display in Badge | accept | deckTitle is rendered as text content in a React Badge element — React automatically escapes all string values, XSS not possible via JSX text children |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — Badge component already present |
</threat_model>

<verification>
1. Run `cd apps/frontend && npx vitest run src/pages/__tests__/StudySessionPage.test.tsx` — all tests pass including new STUDY-04a and STUDY-04b
2. Verify en.json and de.json both contain `study.deckBadgeAriaLabel`
3. Verify `apps/frontend/src/pages/StudySessionPage.tsx` contains `currentCard.deckTitle` in the progress row (grep: `currentCard\.deckTitle`)
4. Confirm Badge is NOT inside a conditional block (grep: confirm no `studyMode !== 'normal' &&` wrapping the deck badge)
</verification>

<success_criteria>
- `grep -n "currentCard\.deckTitle" apps/frontend/src/pages/StudySessionPage.tsx` returns at least one line
- `grep -n "deckBadgeAriaLabel" apps/frontend/src/locales/en.json` returns 1 result
- `grep -n "deckBadgeAriaLabel" apps/frontend/src/locales/de.json` returns 1 result
- `cd apps/frontend && npx vitest run src/pages/__tests__/StudySessionPage.test.tsx` exits 0 with all tests passing
- The deck badge Badge element is unconditional — not wrapped in `{studyMode !== 'normal' && ...}`
</success_criteria>

## Artifacts this phase produces

**New symbols / elements:**
- `study.deckBadgeAriaLabel` — new i18n key in en.json and de.json (interpolated: `"Deck: {{deckTitle}}"`)
- Inline Badge JSX element in `SessionRunner` rendering `currentCard.deckTitle`
- `describe("StudySessionPage deck badge (STUDY-04)", ...)` — new test describe block in `StudySessionPage.test.tsx`
- `it("STUDY-04a: ...")` and `it("STUDY-04b: ...")` — two new test cases

**Modified files:** `StudySessionPage.tsx` (SessionRunner progress row), `en.json` (study object), `de.json` (study object), `StudySessionPage.test.tsx` (new describe block)

<output>
Create `.planning/phases/22-study-session-ux/22-01-SUMMARY.md` when done
</output>
