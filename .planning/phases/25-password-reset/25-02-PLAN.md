---
phase: 25-password-reset
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
autonomous: true
requirements:
  - RESET-01
  - RESET-06
must_haves:
  truths:
    - en.json contains all 21 new keys for auth.forgotPassword*, auth.sendResetLink, auth.sendingResetLink, auth.resetEmail*, auth.resetPassword*, auth.newPassword, auth.resettingPassword, auth.resetError*, auth.resetLink*, auth.resetSuccess, auth.resetPageTitle, auth.forgotPageTitle, admin.sendPasswordReset, admin.resetNoEmail, admin.resetSentSuccess
    - de.json contains the same 21 keys with German translations
    - Both files are valid JSON after editing
    - Both files are updated in the same commit (per 10-05 decision — missing de.json keys fall back to raw key string, not English)
  artifacts:
    - apps/frontend/src/locales/en.json (updated)
    - apps/frontend/src/locales/de.json (updated)
  key_links:
    - i18n keys consumed by ForgotPasswordPage, ResetPasswordPage, LoginPage, AdminPage (Plans 25-04 and 25-05)
---

# Phase 25 Plan 02: i18n Keys (en.json + de.json)

## Goal

Add all 21 new i18n keys for the password reset feature to both locale files in one atomic commit, preventing German users from seeing raw key strings.

## Artifacts this phase produces

- 18 new keys under `auth.*` in both locale files
- 3 new keys under `admin.*` in both locale files
- Total: 21 keys × 2 files = 42 new string entries

---

## Tasks

### Task 2.1: Add new keys to apps/frontend/src/locales/en.json

<read_first>
- apps/frontend/src/locales/en.json — full file: auth object (lines 23–53) and admin object (lines 309–365) to identify exact insertion points; confirm backToSignIn is the last auth.* key and inviteRevokeSuccess is the last admin.* key before the insertion points
</read_first>

<action>
In `apps/frontend/src/locales/en.json`, add 18 new entries to the `auth` object and 3 new entries to the `admin` object.

For the `auth` object, insert after the existing `"backToSignIn": "Back to sign in"` entry:

Key → English value:
- `forgotPassword` → "Forgot password?"
- `forgotPasswordTitle` → "Forgot your password?"
- `forgotPasswordDesc` → "Enter your email address to receive a reset link."
- `sendResetLink` → "Send reset link"
- `sendingResetLink` → "Sending..."
- `resetEmailSentTitle` → "Check your email"
- `resetEmailSentDesc` → "If that email is registered, a password reset link is on its way. It expires in 1 hour."
- `resetPasswordTitle` → "Reset your password"
- `resetPasswordDesc` → "Enter a new password for your account."
- `newPassword` → "New password"
- `resettingPassword` → "Resetting..."
- `resetErrorTitle` → "Unable to reset password"
- `resetLinkUsed` → "This reset link has already been used."
- `resetLinkExpired` → "This reset link has expired. Request a new one from the login page."
- `resetLinkInvalid` → "This reset link is not valid."
- `resetSuccess` → "Password reset successfully. Please log in."
- `resetPageTitle` → "Reset Password — Kartex"
- `forgotPageTitle` → "Forgot Password — Kartex"

For the `admin` object, insert after the existing `"inviteRevokeSuccess": "Invitation revoked."` entry:

Key → English value:
- `sendPasswordReset` → "Send password reset email"
- `resetNoEmail` → "This user has no email address set"
- `resetSentSuccess` → "Password reset email sent"

Do not reorder any existing keys. Ensure the JSON object structure remains valid (trailing commas on all non-last entries, no comma on the last entry of each object). Validate the final file is parseable.
</action>

<acceptance_criteria>
- `node -e "JSON.parse(require('fs').readFileSync('apps/frontend/src/locales/en.json','utf8'))"` exits 0 (valid JSON)
- `grep -c '"forgotPassword"' apps/frontend/src/locales/en.json` outputs 1
- `grep -c '"resetLinkUsed"' apps/frontend/src/locales/en.json` outputs 1
- `grep -c '"resetLinkExpired"' apps/frontend/src/locales/en.json` outputs 1
- `grep -c '"resetLinkInvalid"' apps/frontend/src/locales/en.json` outputs 1
- `grep -c '"resetSuccess"' apps/frontend/src/locales/en.json` outputs 1
- `grep -c '"sendPasswordReset"' apps/frontend/src/locales/en.json` outputs 1
- `grep -c '"resetNoEmail"' apps/frontend/src/locales/en.json` outputs 1
- `grep -c '"resetSentSuccess"' apps/frontend/src/locales/en.json` outputs 1
</acceptance_criteria>

---

### Task 2.2: Add German translations to apps/frontend/src/locales/de.json

<read_first>
- apps/frontend/src/locales/de.json — auth object and admin object to identify insertion points (same structure as en.json; German strings for existing keys confirm translation style)
</read_first>

<action>
In `apps/frontend/src/locales/de.json`, add the same 21 keys with German translations at the same insertion points as Task 2.1.

For the `auth` object, insert after the existing German `"backToSignIn"` entry:

Key → German value:
- `forgotPassword` → "Passwort vergessen?"
- `forgotPasswordTitle` → "Passwort vergessen?"
- `forgotPasswordDesc` → "Geben Sie Ihre E-Mail-Adresse ein, um einen Zurücksetzungslink zu erhalten."
- `sendResetLink` → "Zurücksetzungslink senden"
- `sendingResetLink` → "Wird gesendet..."
- `resetEmailSentTitle` → "E-Mail überprüfen"
- `resetEmailSentDesc` → "Wenn diese E-Mail-Adresse registriert ist, ist ein Passwort-Zurücksetzungslink unterwegs. Er läuft in 1 Stunde ab."
- `resetPasswordTitle` → "Passwort zurücksetzen"
- `resetPasswordDesc` → "Geben Sie ein neues Passwort für Ihr Konto ein."
- `newPassword` → "Neues Passwort"
- `resettingPassword` → "Wird zurückgesetzt..."
- `resetErrorTitle` → "Passwort zurücksetzen nicht möglich"
- `resetLinkUsed` → "Dieser Zurücksetzungslink wurde bereits verwendet."
- `resetLinkExpired` → "Dieser Zurücksetzungslink ist abgelaufen. Fordern Sie einen neuen über die Anmeldeseite an."
- `resetLinkInvalid` → "Dieser Zurücksetzungslink ist ungültig."
- `resetSuccess` → "Passwort erfolgreich zurückgesetzt. Bitte melden Sie sich an."
- `resetPageTitle` → "Passwort zurücksetzen — Kartex"
- `forgotPageTitle` → "Passwort vergessen — Kartex"

For the `admin` object, insert after the existing German `"inviteRevokeSuccess"` entry:

Key → German value:
- `sendPasswordReset` → "Passwort-Zurücksetzungs-E-Mail senden"
- `resetNoEmail` → "Für diesen Benutzer ist keine E-Mail-Adresse hinterlegt"
- `resetSentSuccess` → "Passwort-Zurücksetzungs-E-Mail gesendet"

Both locale files must be updated together in the same git commit (10-05 decision: missing de.json keys fall back to the raw key string, not the English value, which produces broken German UI).
</action>

<acceptance_criteria>
- `node -e "JSON.parse(require('fs').readFileSync('apps/frontend/src/locales/de.json','utf8'))"` exits 0 (valid JSON)
- `grep -c '"forgotPassword"' apps/frontend/src/locales/de.json` outputs 1
- `grep -c '"resetLinkUsed"' apps/frontend/src/locales/de.json` outputs 1
- `grep -c '"resetLinkExpired"' apps/frontend/src/locales/de.json` outputs 1
- `grep -c '"sendPasswordReset"' apps/frontend/src/locales/de.json` outputs 1
- `grep -c '"resetNoEmail"' apps/frontend/src/locales/de.json` outputs 1
- `grep -c '"resetSentSuccess"' apps/frontend/src/locales/de.json` outputs 1
- Both en.json and de.json appear in the same git commit (commit must touch both files)
</acceptance_criteria>

---

## Verification

Run `node -e "JSON.parse(require('fs').readFileSync('apps/frontend/src/locales/en.json','utf8'))"` and the same for de.json. Both must exit 0.

Count keys added: `grep -c '"forgotPassword\|resetLink\|resetSuccess\|sendPasswordReset\|resetNoEmail\|resetSentSuccess' apps/frontend/src/locales/en.json` and confirm the result matches between the two locale files (same 21 keys in both).

## must_haves

truths:
  - Both locale files contain all 21 new keys
  - Both locale files are valid JSON
  - German values are actual translations, not English copied verbatim (verify visually for the 3 admin keys and the auth error message keys)
  - Both files are committed atomically in a single commit

prohibitions:
  - statement: de.json is left without the new keys (causing German users to see raw key strings such as auth.resetLinkUsed)
    status: resolved
    verification: grep -c '"resetLinkUsed"' apps/frontend/src/locales/de.json outputs 1
  - statement: Either locale file is malformed JSON after the edit
    status: resolved
    verification: node -e "JSON.parse(...)" on each file exits 0

<output>
Create `.planning/phases/25-password-reset/25-02-SUMMARY.md` when done.
</output>
