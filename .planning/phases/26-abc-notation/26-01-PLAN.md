---
phase: 26-abc-notation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/package.json
  - apps/frontend/src/components/KartexRenderer.tsx
autonomous: false
requirements:
  - ABC-01
  - ABC-02
  - ABC-03
must_haves:
  truths:
    - A card with a valid #abc block displays rendered SVG sheet music inline (not raw text, not blank space) — ABC-01
    - A card with invalid ABC notation renders RenderErrorBlock with heading "ABC render error" and abcjs warnings[0] as the message (not a crash, not blank) — ABC-02
    - The SVG container element carries className "w-full dark:invert" providing responsive scaling via abcjs responsive:resize and dark-mode inversion via Tailwind — D-02, D-03, ABC-03
    - abcjs is loaded via lazy import() inside useEffect — not in the initial bundle; loading spinner (Loader2 + "Rendering...") shows until the import resolves — D-04
    - preprocessAbcBlocks converts #abc prefix blocks to fenced ```abc code blocks before ReactMarkdown sees the content, using the same algorithm as preprocessTypstBlocks — D-01
  artifacts:
    - apps/frontend/src/components/KartexRenderer.tsx — contains AbcBlock component, preprocessAbcBlocks function, language-abc branch in kartexComponents.code handler, and preprocessor composition
    - apps/frontend/package.json — contains "abcjs" in dependencies at version ^6.6.3 or compatible
  key_links:
    - containerRef.current is non-null when import('abcjs') resolves because the ref div is always mounted in the DOM (display:none during loading/error states rather than conditionally rendered)
    - preprocessAbcBlocks(preprocessTypstBlocks(content)) composition in KartexRenderer body ensures both preprocessors run before ReactMarkdown
    - kartexComponents.code handler detects className?.includes('language-abc') and routes to AbcBlock; language-typst check is unchanged
---

<objective>
Wire abcjs into KartexRenderer so that #abc fenced blocks in card content render as inline SVG sheet music. This single plan covers all three ABC requirements: rendering (ABC-01), error fallback (ABC-02), and responsive scaling (ABC-03).

Per decisions D-01 through D-05: AbcBlock uses useRef + useEffect with lazy import('abcjs'), preprocessAbcBlocks mirrors preprocessTypstBlocks to convert #abc prefix blocks to fenced code blocks, and the kartexComponents.code handler routes language-abc to AbcBlock. No backend changes. No audio playback. No i18n keys.

Purpose: Card creators can embed ABC notation sheet music in card content; viewers see rendered SVG inline with responsive width and dark-mode support.

Output:
- AbcBlockProps interface and AbcBlock function component in KartexRenderer.tsx
- preprocessAbcBlocks(content: string): string function in KartexRenderer.tsx
- language-abc branch in kartexComponents.code handler
- abcjs@^6.6.3 in apps/frontend/package.json
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/frontend/src/components/KartexRenderer.tsx
@apps/frontend/package.json
@apps/frontend/src/main.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify abcjs package identity before install</name>
  <read_first>
    - apps/frontend/package.json — confirm abcjs is not already listed in dependencies or devDependencies
  </read_first>
  <what-built>
    No code written yet. abcjs was identified in v1.4-research (STATE.md) but no formal Package Legitimacy Audit table exists in a RESEARCH.md file for this phase — the package is treated as [ASSUMED] and requires human identity verification before install.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/abcjs in a browser
    2. Confirm the package name is exactly "abcjs" (not a hyphenated or prefixed variant)
    3. Confirm the publisher/maintainer is "paulrosen" (the known abcjs author) or a trusted organization that took over maintenance
    4. Confirm version 6.6.3 or a later 6.x version is listed in the version history
    5. Confirm the repository link on the npm page points to https://github.com/paulrosen/abcjs
    6. Confirm weekly download count is in the tens of thousands or higher (consistent with a legitimate, actively-used library)
  </how-to-verify>
  <acceptance_criteria>
    - Package "abcjs" on npmjs.com matches the expected publisher identity (paulrosen or successor maintainer)
    - Version 6.6.x is present and published by the same trusted publisher
    - GitHub repository link on the npm page resolves to https://github.com/paulrosen/abcjs
    - Download count is consistent with a legitimate library (no sudden drop indicating a takeover or abandonment)
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming package identity, or describe any discrepancy found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install abcjs@^6.6.3 into apps/frontend workspace</name>
  <files>apps/frontend/package.json</files>
  <read_first>
    - apps/frontend/package.json — read the current dependencies list to confirm abcjs is absent before installing
  </read_first>
  <action>
    Run the following command from the monorepo root. This adds abcjs to the runtime dependencies of the @kartex/frontend workspace package and updates yarn.lock:

    yarn workspace @kartex/frontend add abcjs

    The resolved version must satisfy ^6.6.3. Do not manually edit package.json — let yarn manage version resolution and lockfile updates.

    After the command completes, confirm that no module-scope import of abcjs has been added to any source file. All abcjs usage in KartexRenderer.tsx will use lazy import('abcjs') inside useEffect per D-04 and the v1.4-research DOM-mutation pattern. abcjs belongs in dependencies (not devDependencies) because it is a runtime rendering library required in production.

    Do not import any abcjs CSS file at this stage. Per the UI-SPEC Dependency Contract, the SVG-only render path requires no abcjs stylesheet (audio playback is deferred to v2). Confirm during Task 3 implementation: if abcjs produces blank output without a stylesheet, add an import for 'abcjs/abcjs-audio.css' to apps/frontend/src/main.tsx alongside the existing katex and highlight.js imports — but only if verified as required for SVG rendering.
  </action>
  <verify>
    <automated>node -e "const p = require('./apps/frontend/package.json'); const v = p.dependencies && p.dependencies['abcjs']; if (!v) { console.error('FAIL: abcjs not found in dependencies'); process.exit(1); } console.log('OK: abcjs version:', v)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/frontend/package.json has "abcjs" in the "dependencies" object (not "devDependencies")
    - The version string for abcjs is "^6.6.3" or a compatible specifier resolving to 6.6.x or later 6.x
    - yarn.lock has been updated with abcjs resolution entries (file is modified)
    - No top-level import statement for abcjs exists in KartexRenderer.tsx or any other source file (lazy import only)
  </acceptance_criteria>
  <done>apps/frontend/package.json contains "abcjs" in dependencies at ^6.6.3 or compatible and yarn.lock is consistent</done>
</task>

<task type="auto">
  <name>Task 3: Implement AbcBlock, preprocessAbcBlocks, and kartexComponents.code extension in KartexRenderer.tsx</name>
  <files>apps/frontend/src/components/KartexRenderer.tsx</files>
  <read_first>
    - apps/frontend/src/components/KartexRenderer.tsx — read the complete file (316 lines) before making any edits; study TypstBlock (lines 43–88) as the implementation template, preprocessTypstBlocks (lines 116–136) as the preprocessor template, kartexComponents.code handler (lines 204–210) for the extension point, and the KartexRenderer function body (lines 299–316) for the preprocessor composition point
    - apps/frontend/src/main.tsx — confirm the CSS import baseline (katex, highlight.js) to know where an abcjs CSS import would go if needed
  </read_first>
  <action>
    Read KartexRenderer.tsx completely before any edit. Make exactly five targeted edits. Do not restructure or reformat unchanged sections.

    EDIT 1 — Add useRef to the React named import (line 2).
    Change the named import destructuring from `{ useEffect, useState }` to `{ useEffect, useRef, useState }`. No other change to the import block. This is the only import-line modification.

    EDIT 2 — Add AbcBlockProps interface and AbcBlock component after the closing brace of TypstBlock (after line 88, before the extractTypstSource function at line 92).
    AbcBlock is the DOM-mutation analog of TypstBlock. Key structural differences from TypstBlock: (a) uses useRef<HTMLDivElement>(null) named containerRef instead of a useState for SVG string, (b) abcjs mutates containerRef.current directly via renderAbc() rather than returning an SVG string for dangerouslySetInnerHTML, (c) the ref div must always stay mounted in the DOM so containerRef.current is non-null when the lazy import Promise resolves — use display:none via inline style during loading and error states rather than conditional rendering.

    AbcBlock implementation specifics:
    - Interface: AbcBlockProps with a single field `source: string`
    - State variables: containerRef (useRef<HTMLDivElement>(null)), loading (useState true), error (useState<string | null>(null))
    - Single useEffect with dependency array [source]: declare a `let cancelled = false` guard at the top; call import('abcjs') lazily; in the .then callback, skip if cancelled or containerRef.current is null; call mod.renderAbc(containerRef.current, source, { responsive: 'resize' }) — note: abcjs v6 ships named exports so use mod.renderAbc directly; if TypeScript or runtime reports that renderAbc is undefined on mod, fall back to (mod as any).default.renderAbc which handles CJS interop; capture the return value as result; extract warnings via result?.[0]?.warnings ?? []; if warnings.length > 0, call setError(warnings[0]); always call setLoading(false) on both success and error settle; in the .catch callback, call setError with the error message string and setLoading(false); return the cleanup function `() => { cancelled = true }` from useEffect
    - Return value: a React Fragment containing three elements in order — (1) a conditional span shown only when loading is true, matching TypstBlock's loading state exactly: className "text-muted-foreground text-sm" containing Loader2 with className "h-4 w-4 animate-spin inline mr-1" followed by the text "Rendering..."; (2) a conditional RenderErrorBlock shown only when loading is false AND error is not null, with props heading="ABC render error", errorMessage={error}, rawSource={source}; (3) ALWAYS-present div with className="w-full dark:invert" and ref={containerRef}, with inline style={{ display: 'none' }} when (loading || error !== null) is true and no inline style when both are false — this always-mounted pattern ensures containerRef.current is populated in the DOM throughout the async import lifecycle

    EDIT 3 — Add preprocessAbcBlocks function after the closing brace of preprocessTypstBlocks (after line 136, before the extractTextFromChildren function).
    preprocessAbcBlocks is an exact mirror of preprocessTypstBlocks with '#abc' replacing '#typst'. Include a JSDoc comment describing the same structure: converts #abc prefix blocks (block ends at first blank line or end of content) to fenced ```abc code blocks. Function signature: `function preprocessAbcBlocks(content: string): string`. Implementation: split content on '\n'; iterate lines with index i; when lines[i].trim() === '#abc', collect subsequent non-blank lines as sourceLines (increment i, push to sourceLines while lines[i] !== '' and i < lines.length); push '```abc' then spread sourceLines then '```' to result; else push lines[i] to result and increment i; return result.join('\n').

    EDIT 4 — Extend the kartexComponents.code handler (around lines 204–210).
    Inside the code function body, before the existing `if (className?.includes('language-typst'))` check, add a new branch: if className?.includes('language-abc') is true, extract source using extractTextFromChildren(children).replace(/\n$/, '') (identical to the typst branch), and return AbcBlock with source={source}. The language-typst and language-abc branches are independent; order between them does not matter but placing abc before typst is fine.

    EDIT 5 — Compose preprocessAbcBlocks into the KartexRenderer function body (around line 312).
    In the KartexRenderer component return, change the ReactMarkdown children from `{preprocessTypstBlocks(content)}` to `{preprocessAbcBlocks(preprocessTypstBlocks(content))}`. This composes both preprocessors before the markdown pipeline. Order is arbitrary (prefixes are distinct) but inner-first (typst) then outer (abc) is the convention established here.

    After all five edits are applied, run `yarn workspace @kartex/frontend typecheck` to confirm no TypeScript errors. If TypeScript reports an error on mod.renderAbc (e.g., Property 'renderAbc' does not exist on type), apply the CJS interop cast: `const abcjs = (mod as any).default ?? mod; abcjs.renderAbc(...)`.

    Do not add any CSS import for abcjs to main.tsx unless the manual smoke test in the Verification section reveals blank SVG output without it.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend typecheck</automated>
  </verify>
  <acceptance_criteria>
    - KartexRenderer.tsx line 2 imports useRef: the named import includes useEffect, useRef, and useState
    - AbcBlock function component exists in KartexRenderer.tsx with a source: string prop
    - AbcBlock contains useRef<HTMLDivElement>(null) assigned to containerRef
    - AbcBlock's useEffect contains import('abcjs') — a dynamic import, not a static top-level import
    - AbcBlock calls renderAbc with a third argument object containing `responsive: 'resize'`
    - AbcBlock inspects result?.[0]?.warnings to detect errors (not a try/catch on renderAbc itself)
    - AbcBlock success-state div has className containing both "w-full" (responsive width, D-02) and "dark:invert" (dark mode, D-03)
    - AbcBlock loading state renders Loader2 with className "h-4 w-4 animate-spin inline mr-1" and text "Rendering..." inside a span with className "text-muted-foreground text-sm" — matching TypstBlock exactly (D-04)
    - AbcBlock error state renders RenderErrorBlock with heading="ABC render error" (D-05)
    - preprocessAbcBlocks function exists in KartexRenderer.tsx and checks `lines[i].trim() === '#abc'`
    - kartexComponents.code handler has a branch for className?.includes('language-abc') that returns AbcBlock with extracted source
    - KartexRenderer component passes preprocessAbcBlocks(preprocessTypstBlocks(content)) to ReactMarkdown as children
    - No static top-level `import ... from 'abcjs'` exists in KartexRenderer.tsx (verified by absence of the pattern at module scope)
    - `yarn workspace @kartex/frontend typecheck` exits with code 0
  </acceptance_criteria>
  <done>KartexRenderer.tsx type-checks cleanly with zero errors; AbcBlock, preprocessAbcBlocks, and the kartexComponents.code language-abc branch are all in place per D-01 through D-05</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Card content → abcjs parser | User-authored ABC notation text (card front/back content) is passed as an untrusted string to the third-party abcjs rendering library |
| npm registry → monorepo | abcjs package downloaded from the npm registry during yarn install — supply chain trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-SC | Tampering | yarn add abcjs | high | mitigate | Blocking human checkpoint (Task 1) verifies publisher identity, version provenance, and GitHub repository link at npmjs.com/package/abcjs before the install command runs |
| T-26-01 | Information Disclosure | RenderErrorBlock / abcjs warnings array | low | accept | abcjs warnings are notation-syntax messages ("Unrecognized note", "Missing key") — no file paths or server internals. The same disclosure pattern is accepted for TypstBlock errors (T-03 series) |
| T-26-02 | Denial of Service | abcjs renderAbc() invoked per card render | low | accept | abcjs is a synchronous pure-JS parser with linear-time complexity; malformed ABC notation produces warnings in the return value rather than exceptions or infinite loops. No unbounded recursion or async operation. Risk accepted |
</threat_model>

<verification>
1. TypeScript: `yarn workspace @kartex/frontend typecheck` exits 0
2. Dependency: apps/frontend/package.json has "abcjs" in dependencies (confirmed by Task 2 verify command)
3. No static abcjs import: grep for `from 'abcjs'` at the top of KartexRenderer.tsx returns no matches (only the dynamic import inside useEffect is present)
4. Symbols present: grep KartexRenderer.tsx for "AbcBlock", "preprocessAbcBlocks", "language-abc" — all three must match
5. Manual smoke test: load a card with the following content in the study or card detail view and confirm rendered SVG sheet music appears inline:

   #abc
   X:1
   T:Scale
   M:C
   L:1/8
   K:C
   CDEFGAB c|

6. Manual error test: load a card with the content below and confirm RenderErrorBlock appears with heading "ABC render error" and a warning message from abcjs:

   #abc
   INVALID_TOKEN_XYZ
</verification>

<success_criteria>
- ABC-01 PASS: A card with a valid #abc block renders SVG sheet music inline when the card is viewed (not raw text, not blank space, no console error)
- ABC-02 PASS: A card with invalid ABC notation shows the error fallback block with heading "ABC render error" — no crash, no blank space, no unhandled exception
- ABC-03 PASS: The SVG container carries w-full making the sheet music fill the card width at all viewport sizes; responsive:resize makes abcjs emit a viewBox-based SVG that scales via CSS
</success_criteria>

## Artifacts This Phase Produces

| Symbol | Type | File | Exported |
|--------|------|------|----------|
| AbcBlockProps | TypeScript interface | apps/frontend/src/components/KartexRenderer.tsx | No (file-internal) |
| AbcBlock | React function component | apps/frontend/src/components/KartexRenderer.tsx | No (file-internal) |
| preprocessAbcBlocks | function (string → string) | apps/frontend/src/components/KartexRenderer.tsx | No (file-internal) |
| language-abc branch | code path in kartexComponents.code | apps/frontend/src/components/KartexRenderer.tsx | No (file-internal) |
| abcjs | npm runtime dependency | apps/frontend/package.json | N/A |

<output>
Create `.planning/phases/26-abc-notation/26-01-SUMMARY.md` when done.
</output>
