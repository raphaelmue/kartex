---
phase: 29-user-email-self-service
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/context/AuthContext.tsx
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [EMAIL-09, EMAIL-10, EMAIL-11]

must_haves:
  truths:
    - "AuthContext User type carries email: string | null so consumers can read and derive the no-email state"
    - "All settings.* and admin.* email i18n keys exist in both en.json and de.json (no key falls back to a raw string)"
    - "REQUIREMENTS.md enumerates EMAIL-09/10/11 and no longer implies self-service email is deferred"
  artifacts:
    - apps/frontend/src/context/AuthContext.tsx
    - apps/frontend/src/locales/en.json
    - apps/frontend/src/locales/de.json
    - .planning/REQUIREMENTS.md
  key_links:
    - "i18n keys are consumed by SettingsPage (Plan 03) and AdminPage (Plan 04) — must exist before those pages assert on real translation values"
    - "AuthContext.User.email is the derived-state source for the no-email Alert (Plan 03)"
---

<objective>
Deliver the foundational, dependency-free edits that Wave 2 UI plans require: the client-side User type gains email, all copy keys land in both locales in one commit, and REQUIREMENTS.md is reconciled to reflect that self-service + admin email edit ship this phase.

Purpose: These three edits have zero runtime dependency on the backend but are prerequisites for the Settings and Admin UI plans. Grouping them here keeps Wave 2 focused purely on page implementation. The i18n keys must land atomically in both en.json and de.json (STATE.md 10-05 pitfall: a missing de.json key falls back to the raw key string, not the English value).
Output: `email: string | null` on the AuthContext User interface; all Phase 29 copy keys in en.json + de.json under the settings and admin namespaces; EMAIL-09/10/11 enumerated and the stale "deferred" note corrected in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/29-user-email-self-service/29-UI-SPEC.md
@.planning/phases/29-user-email-self-service/29-CONTEXT.md

@apps/frontend/src/context/AuthContext.tsx
@apps/frontend/src/locales/en.json
@.planning/REQUIREMENTS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols introduced by this plan)

- `email` field added to the `User` interface in `apps/frontend/src/context/AuthContext.tsx`
- New i18n keys (en.json + de.json), settings namespace: `emailSection`, `emailDesc`, `emailLabel`, `saveEmail`, `emailSaving`, `emailSaved`, `emailTaken`, `emailInvalid`, `noEmailWarningTitle`, `noEmailWarningDesc`
- New i18n keys (en.json + de.json), admin namespace: `editEmail`, `editEmailDialogTitle`, `editEmailDialogDesc`, `emailLabel`, `saveEmail`, `keepCurrentEmail`, `emailUpdated`, `emailTaken`, `emailInvalid`
- New REQUIREMENTS.md rows: EMAIL-09, EMAIL-10, EMAIL-11 + Traceability entries
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add email to the AuthContext User interface</name>
  <files>apps/frontend/src/context/AuthContext.tsx</files>
  <read_first>
    - apps/frontend/src/context/AuthContext.tsx lines 8-15 (the local User interface)
    - 29-RESEARCH.md "AuthContext type extension" (lines 361-373)
  </read_first>
  <action>
    Add a new field `email: string | null` to the User interface (after createdAt). This matches the shape GET /me will now return (Plan 01). Do not change any other field or the provider logic — this is a type-only addition so that useAuth().user.email is available to SettingsPage's warning and form default, and setUser({ ...user, email }) typechecks after a successful save.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend typecheck</automated>
  </verify>
  <acceptance_criteria>
    - User interface includes email: string | null
    - Frontend typecheck passes with no other change to AuthContext behavior
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add all Phase 29 i18n keys to en.json and de.json (same commit)</name>
  <files>apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/locales/en.json — locate the existing "settings" and "admin" namespace objects
    - apps/frontend/src/locales/de.json — locate the same namespaces
    - 29-UI-SPEC.md Copywriting Contract (lines 311-410) — the authoritative key list and English/German copy
  </read_first>
  <action>
    Into the existing settings namespace of BOTH en.json and de.json, add the keys listed in the UI-SPEC Copywriting Contract: emailSection, emailDesc, emailLabel, saveEmail, emailSaving, emailSaved, emailTaken, emailInvalid, noEmailWarningTitle, noEmailWarningDesc. Into the existing admin namespace of BOTH files, add: editEmail, editEmailDialogTitle, editEmailDialogDesc, emailLabel, saveEmail, keepCurrentEmail, emailUpdated, emailTaken, emailInvalid. Use the exact English strings from the UI-SPEC en.json block (lines 360-385) and the German strings from the UI-SPEC de.json block (lines 387-410). Do NOT touch or duplicate the existing settings.saveFailed and common.somethingWrong keys — they already exist and are reused as-is for generic failures. Both files must be edited in this one task so neither locale lags (STATE.md 10-05). Preserve existing JSON key ordering conventions and valid JSON (no trailing commas).
  </action>
  <verify>
    <automated>node -e "const en=require('./apps/frontend/src/locales/en.json'); const de=require('./apps/frontend/src/locales/de.json'); const s=['emailSection','emailDesc','emailLabel','saveEmail','emailSaving','emailSaved','emailTaken','emailInvalid','noEmailWarningTitle','noEmailWarningDesc']; const a=['editEmail','editEmailDialogTitle','editEmailDialogDesc','emailLabel','saveEmail','keepCurrentEmail','emailUpdated','emailTaken','emailInvalid']; const miss=[]; for(const k of s){if(!(k in en.settings))miss.push('en.settings.'+k); if(!(k in de.settings))miss.push('de.settings.'+k);} for(const k of a){if(!(k in en.admin))miss.push('en.admin.'+k); if(!(k in de.admin))miss.push('de.admin.'+k);} if(miss.length){console.error('MISSING',miss); process.exit(1);} console.log('all i18n keys present in both locales');"</automated>
  </verify>
  <acceptance_criteria>
    - Every settings.* and admin.* key from the UI-SPEC Copywriting Contract exists in BOTH en.json and de.json
    - Both files remain valid JSON; existing keys untouched
    - The verify script prints "all i18n keys present in both locales"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md — enumerate EMAIL-09/10/11 and fix the stale deferred note</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 13-20 (existing EMAIL-01..08 rows) and the "Future Requirements (deferred)" section (~line 63-66) and the Traceability table (~line 86-123)
    - 29-RESEARCH.md Open Question 2 (lines 407-410) and 29-CONTEXT.md canonical_refs note (lines 65-66)
  </read_first>
  <action>
    Add three new enumerated requirement rows in the EMAIL section (after EMAIL-08), mirroring the existing `- [x] **EMAIL-0N**: ...` bullet style: EMAIL-09 (user can add/update their own email from Settings), EMAIL-10 (Settings shows a no-email warning explaining password reset requires an email), EMAIL-11 (admin can set/update any user's email from the admin panel). Mark them checked ([x]) only if you consider them delivered by end of phase — otherwise leave as [ ] and let the phase-close pass check them; prefer [ ] here since implementation lands in later plans. In the "Future Requirements (deferred)" section, remove or amend the stale "Self-service email update — ... admin update of user email is sufficient for v1.4" line, since Phase 29 supersedes it (self-service + admin edit both ship now); if any residual aspect remains deferred (e.g. the re-verification sub-flow / email verification), keep only that narrower deferral and note it explicitly. In the Traceability table, add rows: EMAIL-09 / Phase 29 / Pending, EMAIL-10 / Phase 29 / Pending, EMAIL-11 / Phase 29 / Pending, and update the closing count note (currently "28/28 requirements mapped") to reflect the new total.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); const need=['EMAIL-09','EMAIL-10','EMAIL-11']; const miss=need.filter(k=>!t.includes(k)); if(miss.length){console.error('MISSING',miss);process.exit(1);} if(/admin update of user email is sufficient for v1\.4/.test(t)){console.error('stale deferred note still present');process.exit(1);} console.log('REQUIREMENTS.md reconciled');"</automated>
  </verify>
  <acceptance_criteria>
    - EMAIL-09, EMAIL-10, EMAIL-11 appear as enumerated requirement rows and in the Traceability table (Phase 29)
    - The stale "admin update of user email is sufficient for v1.4" note is removed/amended
    - The verify script prints "REQUIREMENTS.md reconciled"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a client-side TypeScript type, static i18n JSON, and a planning document — no new runtime trust boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Tampering | i18n JSON validity | low | mitigate | Verify script parses both locale JSON files (require() throws on malformed JSON) so a broken edit fails the task gate rather than shipping |

No untrusted input is processed by this plan; authentication, authorization, and validation all live in the Plan 01 backend tier. No package-manager installs occur.
</threat_model>

<verification>
- `yarn workspace @kartex/frontend typecheck` — AuthContext type change compiles
- i18n key-presence node script — every UI-SPEC key present in both locales, both files valid JSON
- REQUIREMENTS.md reconciliation node script — new IDs present, stale note gone
</verification>

<success_criteria>
- Client User type ready to carry email (unblocks Plan 03 warning + form default)
- All copy keys available in both locales (unblocks Plan 03 and Plan 04 real-translation test assertions)
- REQUIREMENTS.md documents this phase's scope (closes RESEARCH Open Question 2)
</success_criteria>

<output>
Create `.planning/phases/29-user-email-self-service/29-02-SUMMARY.md` when done.
</output>
