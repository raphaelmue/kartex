---
phase: 03-rich-content-rendering
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - packages/shared/src/schemas/media.ts
  - packages/shared/src/index.ts
  - apps/backend/src/routes/media.ts
  - apps/backend/src/index.ts
  - docker-compose.yml
  - apps/frontend/src/components/KartexRenderer.tsx
  - apps/frontend/src/components/MediaUploadToolbar.tsx
  - apps/frontend/src/components/CardEditorModal.tsx
  - apps/frontend/src/components/__tests__/KartexRenderer.test.tsx
autonomous: true
requirements:
  - CARD-09
  - CARD-10
  - CARD-11

must_haves:
  truths:
    - "An image uploaded via the card editor toolbar appears inline on the card when the card content uses ![alt](media://filename)"
    - "An audio file uploaded via the card editor toolbar renders as a native <audio controls> player in the card"
    - "A card containing a YouTube URL (any of youtube.com/watch?v=, youtu.be/, youtube.com/embed/ formats) renders as an embedded <iframe>"
    - "The media://filename reference is inserted at the cursor position in the textarea after a successful upload (D-03)"
    - "GET /api/media/:filename is publicly accessible — no auth cookie required — so browser <img> and <audio> src attributes resolve without CORS issues"
    - "POST /api/media/upload requires authentication (covered by app-level authMiddleware)"
    - "Uploaded filenames are UUID-based — the user-supplied filename is never written to disk verbatim"
    - "CARD-09, CARD-10, CARD-11 unit tests pass"
  artifacts:
    - path: "packages/shared/src/schemas/media.ts"
      provides: "MediaUploadResponseSchema and MediaUploadResponse type"
      exports: ["MediaUploadResponseSchema", "MediaUploadResponse"]
    - path: "apps/backend/src/routes/media.ts"
      provides: "POST /upload (auth-protected) + GET /:filename (public) Hono router"
      exports: ["mediaRouter"]
    - path: "apps/backend/src/index.ts"
      provides: "media router registration: GET /api/media public BEFORE authMiddleware, POST /api/media protected AFTER"
      contains: "mediaRouter"
    - path: "apps/frontend/src/components/MediaUploadToolbar.tsx"
      provides: "Per-field Image + Audio upload toolbar component"
      exports: ["MediaUploadToolbar"]
    - path: "apps/frontend/src/components/KartexRenderer.tsx"
      provides: "img and a component handlers for media:// rewrite, audio embed, YouTube iframe"
      contains: "media://"
    - path: "docker-compose.yml"
      provides: "STORAGE_PATH env var in backend service"
      contains: "STORAGE_PATH"
  key_links:
    - from: "apps/backend/src/index.ts"
      to: "apps/backend/src/routes/media.ts"
      via: "app.route('/api/media', mediaPublicRouter) BEFORE authMiddleware"
      pattern: "mediaPublicRouter"
    - from: "apps/frontend/src/components/KartexRenderer.tsx"
      to: "/api/media/:filename"
      via: "img component src rewrite: media:// → /api/media/"
      pattern: "media://"
    - from: "apps/frontend/src/components/MediaUploadToolbar.tsx"
      to: "POST /api/media/upload"
      via: "raw fetch with credentials: 'include'"
      pattern: "api/media/upload"
    - from: "apps/frontend/src/components/CardEditorModal.tsx"
      to: "apps/frontend/src/components/MediaUploadToolbar.tsx"
      via: "onInsert callback + frontRef/backRef"
      pattern: "MediaUploadToolbar"
---

<objective>
Wire the full media pipeline: shared schema, backend upload + serve routes, Docker Compose env var, KartexRenderer media:// image/audio/YouTube handlers, and the per-field upload toolbar in CardEditorModal.

Purpose: CARD-09 (images), CARD-10 (audio), CARD-11 (YouTube embeds) require coordinated changes across backend, frontend renderer, and card editor. This plan depends on 03-02 because it further extends KartexRenderer.tsx which 03-02 left in a partially extended state.

Output:
- packages/shared/src/schemas/media.ts — MediaUploadResponseSchema
- apps/backend/src/routes/media.ts — Hono media router (POST upload + GET serve)
- apps/backend/src/index.ts — split registration: GET /api/media public, POST /api/media protected
- docker-compose.yml — STORAGE_PATH: /app/media added to backend environment
- apps/frontend/src/components/KartexRenderer.tsx — img + a component handlers for media:// and YouTube
- apps/frontend/src/components/MediaUploadToolbar.tsx — new per-field upload toolbar
- apps/frontend/src/components/CardEditorModal.tsx — toolbar inserted above each Tabs component
- KartexRenderer.test.tsx — CARD-09, CARD-10, CARD-11 stubs replaced with real tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-rich-content-rendering/03-RESEARCH.md
@.planning/phases/03-rich-content-rendering/03-PATTERNS.md
@.planning/phases/03-rich-content-rendering/03-UI-SPEC.md
@.planning/phases/03-rich-content-rendering/03-02-SUMMARY.md

<interfaces>
<!-- Media model already in schema.prisma (NO migration needed): -->
From apps/backend/prisma/schema.prisma:
```prisma
model Media {
  id          String   @id @default(cuid())
  ownerId     String
  filename    String
  mimeType    String
  storagePath String
  sizeBytes   Int
  createdAt   DateTime @default(now())
}
```

<!-- packages/shared/src/index.ts current state (add media export at the end): -->
From packages/shared/src/index.ts:
```typescript
export * from './schemas/user'
export * from './schemas/auth'
export * from './schemas/inviteCode'
export * from './schemas/deck'
export * from './schemas/card'
```

<!-- cards.ts pattern for Hono router setup (replicate in media.ts): -->
From apps/backend/src/routes/cards.ts lines 1-5:
```typescript
import { Hono } from 'hono'
import { prisma } from '../lib/prisma.js'
const cards = new Hono&lt;{ Variables: { userId: string } }&gt;()
// ...
export { cards as cardsRouter }
```

<!-- index.ts current state — critical: GET /api/media must be registered BEFORE authMiddleware at line 32: -->
From apps/backend/src/index.ts:
```typescript
// Line 15: app.get('/api/health', (c) => c.json({ status: 'ok' }))
// Line 29: app.route('/api/auth', authRouter)
// Line 32: app.use('/api/*', authMiddleware)   ← GET /api/media must be BEFORE this
// Line 35: app.route('/api/decks', decksRouter)
```
Registration strategy: Split the mediaRouter into two Hono routers or use separate route registrations:
- Register app.get('/api/media/:filename', ...) as a standalone handler OR register a mediaPublicRouter BEFORE line 32
- Register app.route('/api/media', mediaRouter) AFTER line 32 for the POST /upload route
Recommended: Export TWO routers from media.ts: mediaPublicRouter (GET /:filename, no auth) and mediaRouter (POST /upload, requires auth). Register mediaPublicRouter before authMiddleware and mediaRouter after.

<!-- docker-compose.yml backend service environment block (STORAGE_PATH must be added): -->
From docker-compose.yml:
```yaml
environment:
  DATABASE_URL: postgresql://kartex:${DB_PASSWORD}@db:5432/kartex
  JWT_SECRET: ${JWT_SECRET}
  ADMIN_USERNAME: ${ADMIN_USERNAME}
  ADMIN_PASSWORD: ${ADMIN_PASSWORD}
  ALLOWED_ORIGIN: ${ALLOWED_ORIGIN:-http://localhost:5173}
  NODE_ENV: production
```
Add: STORAGE_PATH: /app/media

<!-- KartexRenderer.tsx state after 03-02 (add img + a component handlers): -->
After 03-02, KartexRenderer has remarkPlugins, rehypePlugins, and a components prop with p and h6 handlers for #typst.
In this plan, extend the kartexComponents object to also include:
- img: media:// → /api/media/ rewrite handler
- a: audio (media://) + YouTube iframe handler
These additions do NOT conflict with existing p/h6 handlers.

<!-- api.ts post() method JSON-stringifies body — upload must use raw fetch: -->
From apps/frontend/src/lib/api.ts line 69-74:
```typescript
post(url: string, body?: unknown, options?: RequestInit): Promise&lt;Response&gt; {
  return baseFetch(url, {
    ...options,
    method: 'POST',
    body: body !== undefined ? JSON.stringify(body) : undefined,
  })
}
```
Use raw fetch for upload (not api.post) with credentials: 'include' and NO Content-Type header set.

<!-- MediaUploadToolbar component interface (from RESEARCH.md Pattern 6 + CONTEXT.md D-02, D-03): -->
```typescript
interface MediaUploadToolbarProps {
  onInsert: (text: string) => void
  fieldRef: React.RefObject&lt;HTMLTextAreaElement&gt;
}
```
Buttons: Image (upload image) and Music (upload audio) from lucide-react.
Variant: ghost, size: icon (h-8 w-8). aria-label: "Upload image" / "Upload audio".
Uploading state: button disabled, shows Loader2 animate-spin icon, aria-busy="true".
On success: toast.success("Image uploaded") / toast.success("Audio uploaded"); call onInsert("![alt](media://filename)") for images, onInsert("[audio](media://filename)") for audio.
On error: toast.error("Upload failed. Please try again.").

<!-- CardEditorModal.tsx structure for toolbar insertion (per D-02: above each Tabs): -->
From apps/frontend/src/components/CardEditorModal.tsx lines 105-131:
```tsx
&lt;FormItem&gt;
  &lt;FormLabel&gt;Front&lt;/FormLabel&gt;
  {/* INSERT MediaUploadToolbar HERE — above Tabs, below FormLabel */}
  &lt;Tabs defaultValue="edit"&gt;
    ...
    &lt;TabsContent value="edit"&gt;
      &lt;FormControl&gt;
        &lt;textarea ref={frontRef} {...field} /&gt;  {/* add ref prop */}
      &lt;/FormControl&gt;
    &lt;/TabsContent&gt;
  &lt;/Tabs&gt;
  &lt;FormMessage /&gt;
&lt;/FormItem&gt;
```
Add useRef&lt;HTMLTextAreaElement&gt;(null) for frontRef and backRef. Pass to MediaUploadToolbar as fieldRef.
onInsert: insert text at cursor using selectionStart/selectionEnd on the ref, then call field.onChange with the updated value.

<!-- YouTube ID extraction utility (from RESEARCH.md Code Examples): -->
```typescript
function extractYouTubeId(url: string): string | null {
  const patterns = [
    /youtube\.com\/watch\?v=([A-Za-z0-9_-]{11})/,
    /youtu\.be\/([A-Za-z0-9_-]{11})/,
    /youtube\.com\/embed\/([A-Za-z0-9_-]{11})/,
  ]
  for (const pattern of patterns) {
    const match = url.match(pattern)
    if (match) return match[1]
  }
  return null
}
```

<!-- Path traversal prevention: UUID regex for GET /:filename -->
Validate filename in GET route: /^[A-Za-z0-9_-]+\.[a-z0-9]{1,10}$/ — rejects any path containing slashes, dots in path component, or directory traversal sequences. Return 400 if regex fails.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared schema, backend media router, Docker Compose env var</name>
  <files>
    packages/shared/src/schemas/media.ts
    packages/shared/src/index.ts
    apps/backend/src/routes/media.ts
    apps/backend/src/index.ts
    docker-compose.yml
  </files>
  <read_first>
    - packages/shared/src/schemas/card.ts — exact schema file pattern to replicate for media.ts (z.object, z.infer, named exports)
    - packages/shared/src/index.ts — current barrel exports before adding media
    - apps/backend/src/routes/cards.ts — Hono router pattern: Hono constructor, prisma import, c.json(), export pattern
    - apps/backend/src/index.ts — CRITICAL: current line ordering around authMiddleware (line 32) to correctly position GET /api/media before auth
    - docker-compose.yml — current backend environment block before adding STORAGE_PATH
    - .planning/phases/03-rich-content-rendering/03-RESEARCH.md — Pattern 5 (Hono multipart upload), Pitfall 7 (Docker volume path mismatch), Security Domain (path traversal via UUID filenames, GET must be unauthenticated)
  </read_first>
  <action>
    Create packages/shared/src/schemas/media.ts:
    - Import z from 'zod'
    - Export MediaUploadResponseSchema as z.object({ filename: z.string(), url: z.string() })
    - Export MediaUploadResponse = z.infer&lt;typeof MediaUploadResponseSchema&gt;
    - Export MediaSchema = z.object({ id: z.string(), ownerId: z.string(), filename: z.string(), mimeType: z.string(), storagePath: z.string(), sizeBytes: z.number(), createdAt: z.string() })
    - Export Media = z.infer&lt;typeof MediaSchema&gt;

    Append to packages/shared/src/index.ts:
    - export * from './schemas/media'

    Create apps/backend/src/routes/media.ts:
    - Import Hono from 'hono', writeFile + mkdir + readFile from 'node:fs/promises', join + extname from 'node:path', randomUUID from 'node:crypto', prisma from '../lib/prisma.js'
    - Create two separate Hono routers:
      1. mediaPublic = new Hono() — contains ONLY GET /:filename (the unauthenticated serve route)
      2. media = new Hono&lt;{ Variables: { userId: string } }&gt;() — contains ONLY POST /upload

    GET /:filename handler in mediaPublic:
    - Extract filename from c.req.param()
    - Validate: if (!/^[A-Za-z0-9_-]+\.[a-z0-9]{1,10}$/.test(filename)) return c.json({ error: 'Invalid filename.' }, 400)
    - storagePath = process.env.STORAGE_PATH ?? '/app/media'
    - fullPath = join(storagePath, filename)
    - Look up prisma.media.findFirst({ where: { filename } }) — if not found, return 404
    - readFile(fullPath) — if file not found on disk, return 404
    - return c.newResponse(bytes, 200, { 'Content-Type': media.mimeType })

    POST /upload handler in media:
    - userId = c.get('userId')
    - body = await c.req.parseBody()
    - file = body['file']; if (!(file instanceof File)) return c.json({ error: 'File is required.' }, 400)
    - storagePath = process.env.STORAGE_PATH ?? '/app/media'
    - await mkdir(storagePath, { recursive: true })
    - ext = extname(file.name); filename = randomUUID() + ext; fullPath = join(storagePath, filename)
    - await writeFile(fullPath, Buffer.from(await file.arrayBuffer()))
    - await prisma.media.create({ data: { ownerId: userId, filename, mimeType: file.type, storagePath: fullPath, sizeBytes: file.size } })
    - return c.json({ filename, url: `/api/media/${filename}` }, 201)

    Export: export { media as mediaRouter, mediaPublic as mediaPublicRouter }

    Update apps/backend/src/index.ts:
    - Add import: import { mediaRouter, mediaPublicRouter } from './routes/media.js'
    - BEFORE the authMiddleware line (line 32): add app.route('/api/media', mediaPublicRouter)
      This makes GET /api/media/:filename public — no auth cookie required
    - AFTER the authMiddleware line (line 32), alongside other protected routes: add app.route('/api/media', mediaRouter)
      This makes POST /api/media/upload auth-protected

    Update docker-compose.yml backend service environment block:
    - Add STORAGE_PATH: /app/media alongside the existing environment variables
    - The media_data volume already exists and mounts at /app/media — this env var makes it explicit
  </action>
  <verify>
    <automated>grep -c "STORAGE_PATH" docker-compose.yml && grep -c "mediaPublicRouter" apps/backend/src/index.ts && grep -c "export.*mediaRouter" apps/backend/src/routes/media.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/schemas/media.ts exists with MediaUploadResponseSchema exported
    - packages/shared/src/index.ts contains `export * from './schemas/media'`
    - apps/backend/src/routes/media.ts exports both mediaRouter (POST /upload) and mediaPublicRouter (GET /:filename)
    - GET /:filename validates filename with regex /^[A-Za-z0-9_-]+\.[a-z0-9]{1,10}$/ before serving
    - POST /upload generates UUID filename: `randomUUID() + extname(file.name)`
    - apps/backend/src/index.ts registers mediaPublicRouter BEFORE the `app.use('/api/*', authMiddleware)` line
    - apps/backend/src/index.ts registers mediaRouter AFTER authMiddleware (alongside decksRouter)
    - docker-compose.yml backend environment block contains `STORAGE_PATH: /app/media`
  </acceptance_criteria>
  <done>
    Shared schema created and exported. Backend media router created with split auth strategy. index.ts registers public GET before authMiddleware. docker-compose.yml has STORAGE_PATH. All grep checks pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KartexRenderer media/video handlers, MediaUploadToolbar, CardEditorModal integration, and tests</name>
  <files>
    apps/frontend/src/components/KartexRenderer.tsx
    apps/frontend/src/components/MediaUploadToolbar.tsx
    apps/frontend/src/components/CardEditorModal.tsx
    apps/frontend/src/components/__tests__/KartexRenderer.test.tsx
  </files>
  <read_first>
    - apps/frontend/src/components/KartexRenderer.tsx — current state after 03-02 (has p/h6 handlers + Typst logic — extend components without removing existing handlers)
    - apps/frontend/src/components/CardEditorModal.tsx — full file (all 186 lines) — understand the FormField structure, Tabs, textarea, useRef insertion point
    - apps/frontend/src/components/__tests__/KartexRenderer.test.tsx — current state after 03-02 (CARD-09, CARD-10, CARD-11 stubs to replace with real tests)
    - .planning/phases/03-rich-content-rendering/03-PATTERNS.md — MediaUploadToolbar pattern (fetch directly, not api.post), cursor insertion approach, shadcn Button pattern
    - .planning/phases/03-rich-content-rendering/03-UI-SPEC.md — Interaction States (Upload toolbar, Audio Player, Inline Images, YouTube Embed), Copywriting Contract (aria-labels, toast messages)
    - .planning/phases/03-rich-content-rendering/03-RESEARCH.md — Pattern 3 (react-markdown components for img/a), Pitfall 5 (media:// in preview tab), Pitfall 6 (FormData Content-Type)
  </read_first>
  <behavior>
    - CARD-09 test: render KartexRenderer with content `![cat](media://carnot.png)`; expect img element with src="/api/media/carnot.png" and no "media://" in src attribute
    - CARD-10 test: render KartexRenderer with content `[audio](media://lecture.mp3)`; expect an audio element with controls attribute and src="/api/media/lecture.mp3"
    - CARD-11 test: render KartexRenderer with content `[video](https://youtube.com/watch?v=dQw4w9WgXcQ)`; expect an iframe element with src containing "youtube.com/embed/dQw4w9WgXcQ"
    - CARD-11 non-YouTube test: render KartexRenderer with content `[link](https://example.com)`; expect an anchor element (not iframe) with href="https://example.com"
    - CARD-09 external image passthrough: render with `![alt](https://example.com/img.png)`; expect img src="https://example.com/img.png" (no rewrite for non-media:// URLs)
  </behavior>
  <action>
    Extend apps/frontend/src/components/KartexRenderer.tsx — add to the kartexComponents object:
    - img handler: ({ src, alt }) — if src?.startsWith('media://'), resolve to `/api/media/${src.slice('media://'.length)}`; else use src as-is. Render &lt;img src={resolvedSrc} alt={alt ?? ''} className="max-w-full rounded-md" /&gt;
    - a handler: ({ href, children }) — check in this order:
      1. if href?.startsWith('media://'), render &lt;audio controls src={`/api/media/${href.slice('media://'.length)}`} className="w-full mt-2" /&gt;
      2. else extract YouTube ID using extractYouTubeId(href ?? '') — if non-null ID, render &lt;iframe src={`https://www.youtube.com/embed/${youtubeId}`} className="w-full aspect-video rounded-md" allowFullScreen title="YouTube video" /&gt;
      3. else render &lt;a href={href} target="_blank" rel="noopener noreferrer"&gt;{children}&lt;/a&gt;
    - Add the extractYouTubeId(url: string): string | null utility function in KartexRenderer.tsx (before the component, not exported). Use the three regex patterns from the interfaces block: youtube\.com\/watch\?v=, youtu\.be\/, youtube\.com\/embed\/
    - DO NOT remove or override the existing p and h6 handlers added in 03-02

    Create apps/frontend/src/components/MediaUploadToolbar.tsx:
    - import { useRef, useState } from 'react'
    - import { Image, Music, Loader2 } from 'lucide-react'
    - import { toast } from 'sonner'
    - import { Button } from '@/components/ui/button'
    - Interface: MediaUploadToolbarProps { onInsert: (text: string) => void; fieldRef: React.RefObject&lt;HTMLTextAreaElement&gt; }
    - Export function MediaUploadToolbar({ onInsert, fieldRef }: MediaUploadToolbarProps)
    - Internal state: uploadingImage: boolean, uploadingAudio: boolean
    - Hidden file inputs: one for image (accept="image/png,image/jpeg,image/webp,image/gif"), one for audio (accept="audio/mpeg,audio/ogg,audio/wav") — use useRef&lt;HTMLInputElement&gt; for each
    - Upload function (shared, parameterized by type):
      async function handleUpload(file: File, type: 'image' | 'audio') {
        const formData = new FormData(); formData.append('file', file)
        const res = await fetch('/api/media/upload', { method: 'POST', credentials: 'include', body: formData })
        // Do NOT set Content-Type — browser sets multipart/form-data with boundary automatically
        if (res.ok) {
          const data = await res.json()
          const text = type === 'image' ? `![image](media://${data.filename})` : `[audio](media://${data.filename})`
          // Insert at cursor position in the textarea:
          const ta = fieldRef.current
          if (ta) {
            const start = ta.selectionStart ?? ta.value.length
            const end = ta.selectionEnd ?? ta.value.length
            const newValue = ta.value.slice(0, start) + text + ta.value.slice(end)
            onInsert(newValue)  // pass the full new textarea value, not just the inserted text
          } else {
            onInsert(text)
          }
          toast.success(type === 'image' ? 'Image uploaded' : 'Audio uploaded')
        } else {
          toast.error('Upload failed. Please try again.')
        }
      }
    - Render: a flex row div (className="flex gap-1 mb-1") with two Buttons:
      * Image button: variant="ghost" size="icon" aria-label="Upload image" disabled={uploadingImage} aria-busy={uploadingImage} onClick={() => imageInputRef.current?.click()}
        Icon: uploadingImage ? &lt;Loader2 className="h-4 w-4 animate-spin" /&gt; : &lt;Image className="h-4 w-4" /&gt;
      * Audio button: same pattern with uploadingAudio state, Music icon, aria-label="Upload audio"
    - Hidden inputs wired to onChange → handleUpload

    Note on onInsert: the callback receives the full new textarea value (not just the inserted snippet) so CardEditorModal's field.onChange can update the controlled value correctly.

    Update apps/frontend/src/components/CardEditorModal.tsx:
    - Add imports: useRef from 'react' (already imported — check before adding), MediaUploadToolbar from '@/components/MediaUploadToolbar'
    - Add two refs at the top of the component function: const frontRef = useRef&lt;HTMLTextAreaElement&gt;(null) and const backRef = useRef&lt;HTMLTextAreaElement&gt;(null)
    - In the frontContent FormField render: add ref={frontRef} to the textarea element (alongside the existing {...field} spread — ref is a separate prop)
    - Insert &lt;MediaUploadToolbar fieldRef={frontRef} onInsert={(val) => field.onChange(val)} /&gt; between &lt;FormLabel&gt;Front&lt;/FormLabel&gt; and &lt;Tabs&gt; (per D-02: above the Tabs component for each field)
    - Repeat for backContent FormField: add ref={backRef} to its textarea; insert MediaUploadToolbar with fieldRef={backRef} and onInsert={(val) => field.onChange(val)} above its Tabs
    - Do NOT change the form structure, submit handler, or any existing logic

    Update apps/frontend/src/components/__tests__/KartexRenderer.test.tsx:
    - Replace CARD-09, CARD-10, CARD-11 placeholder stubs with real tests per the behavior section above
    - Import { screen } from '@testing-library/react' alongside render if not already imported
    - CARD-09 test: render, then screen.getByRole('img') and expect correct src
    - CARD-10 test: render and query for audio element — use container.querySelector('audio')
    - CARD-11 test: render and query for iframe — use container.querySelector('iframe')
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend test --run 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/frontend/src/components/KartexRenderer.tsx has img handler that rewrites media:// to /api/media/ (grep -c "media://" returns >= 2)
    - apps/frontend/src/components/KartexRenderer.tsx has extractYouTubeId function with three URL patterns (grep -c "youtu.be" returns >= 1)
    - apps/frontend/src/components/KartexRenderer.tsx a handler produces &lt;audio&gt; for media:// links (grep -c "audio" returns >= 1)
    - apps/frontend/src/components/KartexRenderer.tsx a handler produces &lt;iframe&gt; for YouTube URLs (grep -c "iframe" returns >= 1)
    - apps/frontend/src/components/MediaUploadToolbar.tsx exports MediaUploadToolbar function
    - apps/frontend/src/components/MediaUploadToolbar.tsx uses raw fetch (not api.post) for upload (grep -v "api.post" MediaUploadToolbar.tsx returns the full file; grep -c "credentials: 'include'" returns >= 1)
    - apps/frontend/src/components/CardEditorModal.tsx imports MediaUploadToolbar (grep -c "MediaUploadToolbar" returns >= 2 — import + two usages)
    - yarn workspace @kartex/frontend test --run exits 0 with all tests passing including CARD-09, CARD-10, CARD-11
  </acceptance_criteria>
  <done>
    KartexRenderer extended with img/a handlers. MediaUploadToolbar created. CardEditorModal has per-field toolbars above each Tabs component. All 10+ tests in KartexRenderer.test.tsx pass. `yarn workspace @kartex/frontend test --run` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/media/upload | Authenticated multipart upload; File object type checked server-side; filename generated server-side |
| GET /api/media/:filename → Docker volume | Public route; filename validated by UUID regex before disk read; no auth required (by design for img/audio src attributes) |
| media:// → /api/media/ rewrite | Client-side URL transform in KartexRenderer img/a components; only applies to strings starting with media:// |
| YouTube iframe → browser | iframe embeds external content from youtube.com; CSP must allow frame-src |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-PATH | Tampering | GET /api/media/:filename | mitigate | UUID filenames generated server-side (randomUUID() + extname) — user-supplied filename never written to disk. GET handler validates filename with /^[A-Za-z0-9_-]+\.[a-z0-9]{1,10}$/ regex before reading from disk — rejects path traversal sequences and double-dot patterns |
| T-03-UPLOAD | Tampering | POST /api/media/upload file type | accept | file.type is browser-supplied (unverified). Phase 3 stores the MIME type as-is. Full MIME + magic bytes validation deferred to Phase 5 (MDIA-01 to MDIA-04). Acceptable for MVP invite-only deployment |
| T-03-SIZE | Denial of Service | POST /api/media/upload file size | accept | No server-side size limit in Phase 3. Phase 5 adds MDIA-04 (configurable max size via env var). Acceptable for 2-5 trusted users on self-hosted instance |
| T-03-MEDIA-AUTH | Information Disclosure | GET /api/media/:filename (unauthenticated) | accept | Media files are accessible to anyone who knows a UUID filename. Mitigation: UUID obscurity makes enumeration impractical. Full authenticated media access is a Phase 5 concern. Required for browser &lt;img&gt; and &lt;audio&gt; src resolution without CORS issues |
| T-03-YT-CSP | Spoofing | YouTube &lt;iframe&gt; embed | mitigate | Nginx CSP header must allow frame-src https://www.youtube.com. Note for Phase 6 Nginx config (03-03 does not own nginx.conf). YouTube URL detection uses strict regex — only validated IDs are embedded |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages installed in this plan (all packages were installed in 03-01 or 03-02). slopcheck completed in RESEARCH.md for all Phase 3 packages |
</threat_model>

<verification>
After both tasks complete:
- `yarn workspace @kartex/frontend test --run` exits 0 with all tests passing
- `grep -c "STORAGE_PATH" docker-compose.yml` returns 1
- `grep -c "mediaPublicRouter" apps/backend/src/index.ts` returns >= 1
- `grep -c "mediaRouter" apps/backend/src/index.ts` returns >= 2 (import + registration)
- `grep -n "mediaPublicRouter\|authMiddleware" apps/backend/src/index.ts` — mediaPublicRouter registration line number LESS THAN authMiddleware line number (confirms public route is before auth)
- `grep -c "MediaUploadToolbar" apps/frontend/src/components/CardEditorModal.tsx` returns >= 2
- `grep -v '^//' apps/frontend/src/components/MediaUploadToolbar.tsx | grep -c "credentials: 'include'"` returns >= 1
</verification>

<success_criteria>
- CARD-09: media://filename images rewrite to /api/media/filename (unit test passes; browser visual verified manually)
- CARD-10: [audio](media://filename) links render as &lt;audio controls&gt; players (unit test passes)
- CARD-11: YouTube URLs render as embedded iframes with /embed/ URL pattern (unit test passes)
- D-01: Upload buttons present in CardEditorModal per field
- D-02: Per-field toolbars appear above each Tabs component (front field and back field)
- D-03: media:// reference inserted at cursor position after upload
- Path traversal: GET /api/media/:filename rejects filenames not matching UUID regex
- Auth split: GET /api/media public (no cookie needed), POST /api/media/upload requires auth cookie
- `yarn workspace @kartex/frontend test --run` exits 0 with all tests green
</success_criteria>

<output>
Create `.planning/phases/03-rich-content-rendering/03-03-SUMMARY.md` when done.
</output>
