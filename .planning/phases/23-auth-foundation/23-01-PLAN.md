---
phase: 23-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/prisma/migrations/20260621000000_add_user_email/migration.sql
  - packages/shared/src/schemas/user.ts
  - apps/backend/src/routes/__tests__/admin-delete.test.ts
  - apps/backend/src/routes/__tests__/admin-mailer.test.ts
autonomous: true
requirements:
  - EMAIL-01
user_setup: []

must_haves:
  truths:
    - "The User model has a nullable, unique email column; existing users keep email = NULL"
    - "The shared UserSchema exposes email so frontend and backend share one email type"
    - "Wave 0 test files exist and define the delete-guard and mailer behaviors as runnable specs"
  artifacts:
    - path: "apps/backend/prisma/schema.prisma"
      provides: "User.email field declaration"
      contains: "email"
    - path: "apps/backend/prisma/migrations/20260621000000_add_user_email/migration.sql"
      provides: "SQL that adds nullable unique email column to User table"
      contains: "ADD COLUMN \"email\""
    - path: "packages/shared/src/schemas/user.ts"
      provides: "email field on UserSchema"
      contains: "email"
    - path: "apps/backend/src/routes/__tests__/admin-delete.test.ts"
      provides: "Wave 0 spec for ADMIN-01/ADMIN-04 delete guards"
      min_lines: 20
    - path: "apps/backend/src/routes/__tests__/admin-mailer.test.ts"
      provides: "Wave 0 spec for EMAIL-02 mailer test endpoint"
      min_lines: 15
  key_links:
    - from: "packages/shared/src/schemas/user.ts"
      to: "apps/backend/prisma/schema.prisma"
      via: "email field shape mirrors the new DB column (nullable string)"
      pattern: "email"
---

<objective>
Lay the data-model foundation for email-based auth: add a nullable, unique `email` column to the `User` model via a hand-written SQL migration, extend the shared `UserSchema` so the email field is type-safe across frontend and backend, and create the Wave 0 backend test files that specify the delete-guard and mailer behaviors built in later plans.

Purpose: EMAIL-01 requires `User.email` to exist before any admin UI can display it or any mailer can address a user. The shared schema change must land first so downstream plans (02, 03, 04) compile against the new type. Wave 0 test scaffolds satisfy the Nyquist validation contract.
Output: Migrated schema, updated shared Zod schema, two Wave 0 test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-auth-foundation/23-CONTEXT.md
@.planning/phases/23-auth-foundation/23-RESEARCH.md
@.planning/phases/23-auth-foundation/23-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols / files created by this plan:
- `User.email` — new nullable unique field on the Prisma `User` model
- `migration.sql` in `20260621000000_add_user_email/` — new migration directory + file
- `email` field added to `UserSchema` in `packages/shared/src/schemas/user.ts`
- `apps/backend/src/routes/__tests__/admin-delete.test.ts` — new test file
- `apps/backend/src/routes/__tests__/admin-mailer.test.ts` — new test file
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add nullable unique email column to User model + hand-written migration</name>
  <read_first>
    - apps/backend/prisma/schema.prisma (current User model — no email field; note field alignment style)
    - apps/backend/prisma/migrations/20260612000000_add_deckshare_isactive/migration.sql (migration file format + the "apply manually if prisma migrate deploy unavailable" comment convention)
    - apps/backend/entrypoint.sh (confirms `npx prisma migrate deploy` runs at container start — migration is applied automatically in Docker)
  </read_first>
  <action>
    In `apps/backend/prisma/schema.prisma`, add an `email String? @unique` field to the `User` model (nullable, unique — multiple NULLs allowed in Postgres). Place it after `studyMode` and before `createdAt`, matching the existing column alignment style.

    Create the migration directory `apps/backend/prisma/migrations/20260621000000_add_user_email/` with a `migration.sql` file containing the statement `ALTER TABLE "User" ADD COLUMN "email" TEXT UNIQUE;` (nullable by default in Postgres; UNIQUE permits multiple NULLs per the SQL standard). Add a leading comment in the same style as the 20260612000000 migration noting this implements EMAIL-01 and that it is applied via `prisma migrate deploy` (entrypoint) — no `prisma migrate dev` (per the 10-02 / 18-01 decisions: DATABASE_URL is unavailable in the dev shell).
  </action>
  <verify>
    <automated>grep -q 'email' apps/backend/prisma/schema.prisma && grep -q 'ADD COLUMN "email"' apps/backend/prisma/migrations/20260621000000_add_user_email/migration.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/prisma/schema.prisma` User model contains a line declaring `email String? @unique`
    - `apps/backend/prisma/migrations/20260621000000_add_user_email/migration.sql` exists and contains `ALTER TABLE "User" ADD COLUMN "email" TEXT UNIQUE;`
    - The migration SQL has no `NOT NULL` constraint on the email column (existing users must remain valid with email = NULL)
  </acceptance_criteria>
  <done>User.email exists in schema as nullable unique; migration file present and applies a nullable unique column.</done>
</task>

<task type="auto">
  <name>Task 2: Add email field to shared UserSchema</name>
  <read_first>
    - packages/shared/src/schemas/user.ts (current UserSchema — no email field; note z.coerce.date() pattern for createdAt and the UserResponseSchema = UserSchema aliasing)
    - .planning/phases/23-auth-foundation/23-RESEARCH.md (Pattern 5 — exact UserSchema extension)
  </read_first>
  <action>
    In `packages/shared/src/schemas/user.ts`, add `email: z.string().email().nullable().optional()` to the `UserSchema` object (after `createdAt`). This propagates to `UserResponseSchema` (already aliased to `UserSchema`) so the API response type includes email. Do not change `UpdateStudyModeSchema` or the enums. Do not add an email-edit schema — email is display-only in Phase 23 per D-14.
  </action>
  <verify>
    <automated>grep -q "email: z.string().email().nullable().optional()" packages/shared/src/schemas/user.ts && yarn workspace @kartex/shared build 2>/dev/null; grep -q "email" packages/shared/src/schemas/user.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/schemas/user.ts` UserSchema contains `email: z.string().email().nullable().optional()`
    - No new exported schema for editing email is added (D-14 — display only)
    - `User` type (z.infer of UserSchema) now includes an optional nullable email field
  </acceptance_criteria>
  <done>UserSchema includes a nullable optional email field; the User type carries email for downstream type safety.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 backend test scaffolds for delete guards and mailer</name>
  <read_first>
    - apps/backend/src/routes/__tests__/library-toggle.test.ts (established Wave 0 stub style — it.todo + structural it() assertions, no live Prisma mock)
    - apps/backend/src/routes/__tests__/sharing.test.ts (structural assertion example — schema-level guarantees verified without runtime DB)
    - apps/backend/vitest.config.ts (include glob `src/**/__tests__/*.test.ts`, node environment)
    - .planning/phases/23-auth-foundation/23-VALIDATION.md (Per-Task Verification Map — exact behaviors each test must cover)
  </read_first>
  <action>
    Create `apps/backend/src/routes/__tests__/admin-delete.test.ts` with `describe`/`it.todo` specs matching the established Wave 0 stub style (see library-toggle.test.ts). Cover ADMIN-01 and ADMIN-04: (a) `DELETE /api/admin/users/:id` returns 200 and the user no longer exists; (b) self-delete (`id === authenticatedUserId`) returns 400; (c) deleting the last active admin (count of role=ADMIN AND isActive=true ≤ 1) returns 400; (d) ordered cascade deletes RefreshToken, DeckShare(sharedWithUserId), CardProgress, Cards-in-user-decks, Decks, InviteCode(usedById), Media before User; (e) media files removed best-effort before the transaction (D-07 — failures logged, not rolled back). Add at least one structural `it()` assertion confirming ReviewLog has `onDelete: Cascade` on userId so it auto-deletes (mirror the SHAR-06 structural-test approach in sharing.test.ts).

    Create `apps/backend/src/routes/__tests__/admin-mailer.test.ts` with `describe`/`it.todo` specs covering EMAIL-02: (a) `POST /api/admin/mailer/test` returns 200 when SMTP is configured and the admin has an email; (b) returns 400 with "Set your email address first" when the admin has no email (D-12); (c) returns a descriptive error when SMTP env vars are missing (mailer not configured, D-10).

    Use explicit imports `import { describe, it, expect } from 'vitest'` (no globals — matches existing test files).
  </action>
  <verify>
    <automated>yarn workspace @kartex/backend test --run src/routes/__tests__/admin-delete.test.ts src/routes/__tests__/admin-mailer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/routes/__tests__/admin-delete.test.ts` exists and the test command exits 0 (todos are pending, structural assertions pass)
    - admin-delete.test.ts references self-delete 400, last-admin 400, and the ordered cascade including Media
    - `apps/backend/src/routes/__tests__/admin-mailer.test.ts` exists and the test command exits 0
    - admin-mailer.test.ts references the no-email 400 case and the SMTP-not-configured case
    - Both files use explicit vitest imports (no reliance on globals)
  </acceptance_criteria>
  <done>Both Wave 0 test files exist, run green (todos pending), and specify the guard + mailer behaviors that Plans 02/03 implement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → database | Schema change altering the live User table; must be non-destructive for existing rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-MIG | Tampering/Integrity | add_user_email migration | mitigate | Column is nullable with no default and no NOT NULL — existing rows remain valid; UNIQUE permits multiple NULLs in Postgres, so no constraint violation on existing users |
| T-23-SC | Tampering | npm/yarn installs | accept | This plan installs no new packages — schema + test scaffolds only; nodemailer install is gated in Plan 02 |
</threat_model>

<verification>
- `apps/backend/prisma/schema.prisma` declares `email String? @unique` on User
- Migration file `20260621000000_add_user_email/migration.sql` adds a nullable unique email column
- `packages/shared/src/schemas/user.ts` UserSchema includes the email field
- `yarn workspace @kartex/backend test --run` passes (Wave 0 stubs green)
- `yarn workspace @kartex/shared build` compiles with the new email field
</verification>

<success_criteria>
EMAIL-01 schema foundation in place: User.email is a nullable unique column, the shared type exposes email, and the two Wave 0 test files run green and define the downstream delete + mailer specs.
</success_criteria>

<output>
Create `.planning/phases/23-auth-foundation/23-01-SUMMARY.md` when done
</output>
