---
phase: 23-auth-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 23-01
  - 23-02
  - 23-03
files_modified:
  - apps/frontend/src/pages/AdminPage.tsx
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - EMAIL-02

must_haves:
  truths:
    - "The admin user table shows each user's email (or an em dash for null)"
    - "Each user row has a 3-dot menu with a 'Delete user' item; selecting it opens an AlertDialog"
    - "The delete dialog lists what will be deleted and requires typing the exact username to enable the confirm button"
    - "Confirming calls DELETE /api/admin/users/:id; self-delete and last-admin errors surface as toasts"
    - "A MailerSection card with a 'Send test email' button posts to /api/admin/mailer/test and toasts success or the no-email error"
    - "All new UI strings exist in both en.json and de.json"
  artifacts:
    - path: "apps/frontend/src/pages/AdminPage.tsx"
      provides: "email column, delete dialog (DropdownMenu + AlertDialog + username Input), MailerSection"
      contains: "deleteTargetId"
    - path: "apps/frontend/src/locales/en.json"
      provides: "new admin.* keys for delete dialog, email column, mailer"
      contains: "deleteUser"
    - path: "apps/frontend/src/locales/de.json"
      provides: "German translations for the same admin.* keys"
      contains: "deleteUser"
  key_links:
    - from: "apps/frontend/src/pages/AdminPage.tsx"
      to: "apps/backend/src/routes/admin.ts"
      via: "DELETE /api/admin/users/:id and POST /api/admin/mailer/test via api client"
      pattern: "/api/admin/(users|mailer)"
    - from: "apps/frontend/src/pages/AdminPage.tsx"
      to: "apps/frontend/src/locales/en.json"
      via: "t('admin.*') keys rendered in the table, dialog, and mailer card"
      pattern: "t\\('admin\\."
---

<objective>
Complete the Phase 23 UI: add the email column (ADMIN-05), the two-step delete flow (ADMIN-01/02/03/04) using the established DropdownMenu + single-AlertDialog + username-Input pattern, and a MailerSection card with a "Send test email" button (EMAIL-02). Add all new strings to both locale files atomically.

Purpose: This is the user-facing half of Phase 23 — it makes the backend delete endpoint and mailer test reachable, and surfaces email addresses in the admin panel. Strict adherence to the UI-SPEC copy and interaction contracts is required.
Output: Updated AdminPage.tsx, en.json, de.json. Ends with a human-verify checkpoint for the interactive delete-dialog and mailer behaviors that cannot be unit-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-auth-foundation/23-CONTEXT.md
@.planning/phases/23-auth-foundation/23-RESEARCH.md
@.planning/phases/23-auth-foundation/23-UI-SPEC.md
@apps/frontend/src/pages/AdminPage.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New symbols / files created by this plan:
- `MailerSection` — new local component in `AdminPage.tsx`
- `deleteTargetId`, `usernameInput` — new state in `UsersSection`
- `email` field added to the `UserRecord` interface in `AdminPage.tsx`
- New i18n keys under `admin.*`: `deleteUser`, `deleteUserConfirmTitle`, `deleteUserConfirmDesc`, `deleteUserTypePlaceholder`, `deleteUserLastAdmin`, `deleteUserSelf`, `deleteUserSuccess`, `deleteUserCancel`, `emailColumn`, `testEmailBtn`, `testEmailSuccess`, `testEmailNoEmail`, `mailerTitle`, `mailerDesc` (added to both en.json and de.json)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the 14 new admin i18n keys to both locale files</name>
  <read_first>
    - apps/frontend/src/locales/en.json (existing admin.* block — match key style and JSON structure; note common.* keys already present like somethingWrong)
    - apps/frontend/src/locales/de.json (German file — same structure; keys must mirror en.json exactly)
    - .planning/phases/23-auth-foundation/23-UI-SPEC.md (Copywriting Contract — exact English strings; i18n key list)
  </read_first>
  <action>
    Add these keys to the `admin` object in BOTH `apps/frontend/src/locales/en.json` and `apps/frontend/src/locales/de.json` in the same commit (Pitfall 6 — missing de keys fall back to the raw key string, not English). English values come from the UI-SPEC Copywriting Contract; German values are accurate translations.

    Keys and English copy:
    - `deleteUser`: "Delete user"
    - `deleteUserConfirmTitle`: "Delete user?"
    - `deleteUserConfirmDesc`: "This will permanently delete their decks, cards, study progress, and review logs. This action cannot be undone."
    - `deleteUserTypePlaceholder`: "Type the username to confirm"
    - `deleteUserConfirmBtn`: "Delete permanently"
    - `deleteUserCancel`: "Keep user"
    - `deleteUserLastAdmin`: "Cannot delete the last admin account"
    - `deleteUserSelf`: "You cannot delete your own account"
    - `deleteUserSuccess`: "User deleted"
    - `emailColumn`: "Email"
    - `testEmailBtn`: "Send test email"
    - `testEmailSuccess`: "Test email sent"
    - `testEmailNoEmail`: "Set your email address first"
    - `mailerTitle`: "Email / SMTP"
    - `mailerDesc`: "Test the SMTP connection by sending a test email to your own address."

    (Note: the UI-SPEC lists `deleteUserBtn` copy as "Delete permanently" — use key `deleteUserConfirmBtn` for the confirm button and `userActionsLabel` is provided below.) Also add `userActionsLabel`: "User actions for {{username}}" for the DropdownMenu trigger aria-label. Ensure German translations are provided for every key; do not leave any English-only.
  </action>
  <verify>
    <automated>node -e "const en=require('./apps/frontend/src/locales/en.json');const de=require('./apps/frontend/src/locales/de.json');const keys=['deleteUser','deleteUserConfirmTitle','deleteUserConfirmDesc','deleteUserTypePlaceholder','deleteUserConfirmBtn','deleteUserCancel','deleteUserLastAdmin','deleteUserSelf','deleteUserSuccess','emailColumn','testEmailBtn','testEmailSuccess','testEmailNoEmail','mailerTitle','mailerDesc','userActionsLabel'];const miss=keys.filter(k=>!en.admin[k]||!de.admin[k]);if(miss.length){console.error('MISSING',miss);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Every listed key exists under `admin` in BOTH en.json and de.json (verify script exits 0)
    - No key is present in en.json but absent in de.json (or vice versa)
    - `deleteUserConfirmDesc` matches the UI-SPEC description copy exactly in en.json
    - `userActionsLabel` uses the `{{username}}` interpolation placeholder
  </acceptance_criteria>
  <done>All 16 new admin i18n keys exist in both locale files with accurate German translations.</done>
</task>

<task type="auto">
  <name>Task 2: Add email column + 3-dot delete dialog to UsersSection</name>
  <read_first>
    - apps/frontend/src/pages/AdminPage.tsx (current UsersSection — UserRecord interface, fetchUsers, the users.map table, existing deactivate confirm pattern, formatDate, badge helpers; AdminPage layout)
    - .planning/phases/23-auth-foundation/23-RESEARCH.md (Pattern 3 — deleteTargetId + AlertDialog + DropdownMenu; the Phase 23 username-Input extension; Pitfall 5 — reset usernameInput on close)
    - .planning/phases/23-auth-foundation/23-UI-SPEC.md (Interaction Contracts — Delete User Flow steps 1-5, Email Column Display; Accessibility Contracts — aria-labels; Copywriting Contract)
  </read_first>
  <action>
    In `apps/frontend/src/pages/AdminPage.tsx`:

    (a) Add `email?: string | null` to the `UserRecord` interface.

    (b) ADMIN-05 email column: add a `<TableHead>{t('admin.emailColumn')}</TableHead>` between the "Joined" and "Actions" heads. In each row add a `<TableCell>` rendering `u.email ?? '—'` with `text-muted-foreground` styling when null (D-13 em dash). Update the empty-state `colSpan` from 5 to 6.

    (c) Delete flow (D-01, D-02, D-03, D-04): import `AlertDialog`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogFooter`, `AlertDialogCancel` from `@/components/ui/alert-dialog`; import `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem` from `@/components/ui/dropdown-menu`; import `MoreVertical` from `lucide-react`. Add state `const [deleteTargetId, setDeleteTargetId] = useState<string | null>(null)` and `const [usernameInput, setUsernameInput] = useState('')`.

    In each row's actions cell, add a DropdownMenu (after the existing role/deactivate buttons) whose ghost icon trigger has `aria-label={t('admin.userActionsLabel', { username: u.username })}` and an `onClick`/`onPointerDown` that calls `e.stopPropagation()` (UI-SPEC note 6). The DropdownMenuContent (`align="end"`) has one `DropdownMenuItem` with `className="text-destructive focus:text-destructive"` and copy `t('admin.deleteUser')` that calls `setDeleteTargetId(u.id)`.

    Add a SINGLE `<AlertDialog>` OUTSIDE the `users.map()` loop, `open={deleteTargetId !== null}`, `onOpenChange={(open) => { if (!open) { setDeleteTargetId(null); setUsernameInput('') } }}` (Pitfall 5 reset). Compute `const deleteTarget = users.find((u) => u.id === deleteTargetId)`. Body: `AlertDialogTitle` = `t('admin.deleteUserConfirmTitle')`; `AlertDialogDescription` = `t('admin.deleteUserConfirmDesc')` (D-03 static list); a label `t('admin.deleteUserTypePlaceholder')` plus a controlled `<Input>` (value=usernameInput, onChange sets it, `aria-label={t('admin.deleteUserTypePlaceholder')}`, placeholder=`deleteTarget?.username ?? ''`). Footer: `<AlertDialogCancel>{t('admin.deleteUserCancel')}</AlertDialogCancel>` and a `<Button variant="destructive" disabled={usernameInput !== (deleteTarget?.username ?? '')} onClick={() => { if (deleteTargetId) void handleDeleteUser(deleteTargetId) }}>{t('admin.deleteUserConfirmBtn')}</Button>` (use Button, NOT AlertDialogAction — Anti-Pattern: AlertDialogAction auto-closes before the async handler).

    (d) Add `handleDeleteUser(id)`: `await api.delete('/api/admin/users/' + id)`. On `res.ok`: `toast.success(t('admin.deleteUserSuccess'))`, refresh via `fetchUsers()`, then `setDeleteTargetId(null); setUsernameInput('')`. On non-ok: parse the JSON error; if `error === 'SELF_DELETE'` → `toast.error(t('admin.deleteUserSelf'))`; if `error === 'LAST_ADMIN'` → `toast.error(t('admin.deleteUserLastAdmin'))`; else `toast.error(t('common.somethingWrong'))`. Wrap in try/catch with `toast.error(t('common.somethingWrong'))`.
  </action>
  <verify>
    <automated>grep -q "deleteTargetId" apps/frontend/src/pages/AdminPage.tsx && grep -q "admin.emailColumn" apps/frontend/src/pages/AdminPage.tsx && grep -q "handleDeleteUser" apps/frontend/src/pages/AdminPage.tsx && yarn workspace @kartex/frontend build</automated>
  </verify>
  <acceptance_criteria>
    - `UserRecord` interface includes `email?: string | null`
    - The users table has an Email column header (`t('admin.emailColumn')`) and renders `u.email ?? '—'` per row; empty-state colSpan updated to 6
    - Each row has a DropdownMenu trigger with `aria-label` using `admin.userActionsLabel` + username interpolation, and `e.stopPropagation()` on the trigger
    - A single `AlertDialog` lives OUTSIDE `users.map()`, controlled by `deleteTargetId`, resetting `usernameInput` on close
    - The confirm Button is `disabled` until `usernameInput === deleteTarget.username` and uses `<Button>` (not `AlertDialogAction`)
    - `handleDeleteUser` maps `SELF_DELETE`→deleteUserSelf toast and `LAST_ADMIN`→deleteUserLastAdmin toast
    - Frontend build/typecheck succeeds
  </acceptance_criteria>
  <done>UsersSection shows the email column and a two-step, username-confirmed delete flow wired to DELETE /api/admin/users/:id with self-delete/last-admin error toasts.</done>
</task>

<task type="auto">
  <name>Task 3: Add MailerSection with the "Send test email" button</name>
  <read_first>
    - apps/frontend/src/pages/AdminPage.tsx (Card/CardHeader/CardContent imports already present; InviteCodesSection structure; AdminPage render order; toast usage)
    - .planning/phases/23-auth-foundation/23-UI-SPEC.md (Interaction Contracts — Mailer Test Email Button; Phase-Specific Notes 4/7 — MailerSection rendered above InviteCodesSection, visually subordinate single-button card)
  </read_first>
  <action>
    Add a new local component `function MailerSection()` in `AdminPage.tsx`. It renders a `<Card>` with `CardHeader` (`CardTitle` = `t('admin.mailerTitle')`, `CardDescription` = `t('admin.mailerDesc')`) and `CardContent` containing a single `<Button variant="outline">` labeled `t('admin.testEmailBtn')`. Add `const [sending, setSending] = useState(false)`; the button is `disabled={sending}` and shows a loading label while sending. On click call `handleTestEmail`: `setSending(true)`; `const res = await api.post('/api/admin/mailer/test', {})`; on `res.ok` → `toast.success(t('admin.testEmailSuccess'))`; on non-ok parse error → if `error === 'NO_EMAIL'` → `toast.error(t('admin.testEmailNoEmail'))` else `toast.error(t('common.somethingWrong'))`; try/catch → `toast.error(t('common.somethingWrong'))`; finally `setSending(false)`.

    In the `AdminPage` component body, render `<MailerSection />` FIRST (above `<InviteCodesSection />`) per UI-SPEC Phase-Specific Note 4 (infrastructure-first ordering).
  </action>
  <verify>
    <automated>grep -q "function MailerSection" apps/frontend/src/pages/AdminPage.tsx && grep -q "/api/admin/mailer/test" apps/frontend/src/pages/AdminPage.tsx && grep -q "NO_EMAIL" apps/frontend/src/pages/AdminPage.tsx && yarn workspace @kartex/frontend build</automated>
  </verify>
  <acceptance_criteria>
    - `AdminPage.tsx` defines `function MailerSection()` rendering a Card with a single "Send test email" outline button
    - The button posts to `/api/admin/mailer/test` and toggles a `sending` disabled state
    - `NO_EMAIL` error maps to `t('admin.testEmailNoEmail')`; success maps to `t('admin.testEmailSuccess')`
    - `<MailerSection />` is rendered above `<InviteCodesSection />` in AdminPage
    - Frontend build succeeds
  </acceptance_criteria>
  <done>A MailerSection card with a working "Send test email" button is rendered first in AdminPage, with success and no-email toasts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of delete dialog, email column, and mailer button</name>
  <action>
    Pause for human verification of the interactive/visual behaviors that unit tests do not cover (VALIDATION.md manual-only: ADMIN-02, ADMIN-03, ADMIN-05, plus EMAIL-02 test send). The executor presents the verification steps below and waits for an "approved" signal before completing the plan.
  </action>
  <what-built>
    The admin panel now shows an Email column, a 3-dot per-row delete menu opening a two-step confirm dialog (type-username-to-enable), self-delete/last-admin guards surfaced as toasts, and a MailerSection "Send test email" button. These are interactive/visual behaviors that unit tests do not cover (per VALIDATION.md manual-only list: ADMIN-02, ADMIN-03, ADMIN-05).
  </what-built>
  <how-to-verify>
    1. Run the app (frontend + backend with a DB; e.g. `docker compose up` or the dev servers) and log in as an admin.
    2. Open the admin panel. Confirm the users table has an "Email" column showing each user's email or "—" for users without one (ADMIN-05).
    3. Click the 3-dot menu on a NON-admin test user → "Delete user". Confirm the dialog title "Delete user?", the description listing decks/cards/progress/review logs (ADMIN-03), and that the confirm button stays DISABLED until you type the exact username (ADMIN-02). Confirm deletion removes the user from the list.
    4. Try to delete your OWN account → expect the toast "You cannot delete your own account" (ADMIN-04).
    5. If only one active admin exists, attempt to delete that admin → expect "Cannot delete the last admin account" (ADMIN-04).
    6. In the MailerSection, click "Send test email". With SMTP unconfigured OR no admin email set, expect an error toast ("Set your email address first" when your account has no email). With SMTP configured and an admin email present, expect "Test email sent" (EMAIL-02).
    7. Switch language to German and confirm all new strings are translated (no raw `admin.*` keys visible).
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors work, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → DELETE/POST admin APIs | Frontend triggers irreversible delete and outbound test email; server-side guards (Plan 02/03) are authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Tampering | client-side delete confirmation | mitigate | The type-username gate is a UX guard only; the authoritative self-delete/last-admin checks live server-side in Plan 03 — the client cannot bypass them |
| T-23-09 | Usability/Integrity | wrong-user delete | mitigate | Single AlertDialog keyed on deleteTargetId + exact-username match prevents accidental deletion of the wrong row; usernameInput reset on close (Pitfall 5) |
| T-23-SC | Tampering | npm/yarn installs | accept | This plan installs no new packages — all shadcn components already present (per UI-SPEC Component Inventory) |
</threat_model>

<verification>
- All 16 new admin keys exist in both en.json and de.json
- AdminPage shows the email column, the 3-dot delete dialog with username confirmation, and the MailerSection button
- `yarn workspace @kartex/frontend build` succeeds
- Human-verify checkpoint passes for ADMIN-02/03/05 and EMAIL-02 interactive behaviors
</verification>

<success_criteria>
ADMIN-01/02/03/04/05 and EMAIL-02 UI complete: email column, two-step username-confirmed delete with guard toasts, and a working test-email button — all strings localized in en + de — confirmed by human verification.
</success_criteria>

<output>
Create `.planning/phases/23-auth-foundation/23-04-SUMMARY.md` when done
</output>