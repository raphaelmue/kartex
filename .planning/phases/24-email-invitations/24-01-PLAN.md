---
phase: 24-email-invitations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/prisma/migrations/20260625000000_replace_invite_code_with_invite_token/migration.sql
  - packages/shared/src/schemas/auth.ts
autonomous: true
requirements: [EMAIL-04, EMAIL-06]
must_haves:
  truths:
    - "The Prisma client exposes an InviteToken model and no longer exposes InviteCode"
    - "A one-time invite token can be persisted with an email, a unique token value, a 7-day expiry, and a nullable usedAt timestamp"
    - "RegisterSchema accepts a token field instead of inviteCode"
  artifacts:
    - "apps/backend/prisma/schema.prisma with InviteToken model and no InviteCode model"
    - "apps/backend/prisma/migrations/20260625000000_replace_invite_code_with_invite_token/migration.sql"
    - "packages/shared/src/schemas/auth.ts with token field on RegisterSchema"
  key_links:
    - "prisma generate must succeed so downstream backend routes can call prisma.inviteToken"
    - "The InviteCode model AND the User.inviteCodeUsed relation must be removed together or prisma generate fails"
---

<objective>
Establish the data and type foundation for email invitations: replace the `InviteCode` model with a new `InviteToken` model (email-linked, one-time, 7-day expiry), author the hand-written SQL migration that drops `InviteCode` and creates `InviteToken`, regenerate the Prisma client, and swap the `inviteCode` field on `RegisterSchema` for a `token` field.

Purpose: Every downstream backend route (`prisma.inviteToken`) and frontend page (`RegisterInput.token`) depends on this contract existing. This is the BLOCKING foundation for the entire phase (D-01, D-02).
Output: Updated schema, applied/staged migration, regenerated Prisma client, updated shared `RegisterSchema`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-email-invitations/24-CONTEXT.md
@.planning/phases/24-email-invitations/24-RESEARCH.md
@.planning/phases/24-email-invitations/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace InviteCode model with InviteToken in schema.prisma</name>
  <files>apps/backend/prisma/schema.prisma</files>
  <read_first>
    - apps/backend/prisma/schema.prisma (current state: User model line 35-51 with `inviteCodeUsed InviteCode?` at line 46; InviteCode model lines 53-61; RefreshToken model lines 63-70)
    - .planning/phases/24-email-invitations/24-PATTERNS.md (section "apps/backend/prisma/schema.prisma" — exact model shape and removal targets)
  </read_first>
  <action>
    Per D-01 and D-02, remove the entire `InviteCode` model block and remove the `inviteCodeUsed InviteCode?` relation line from the `User` model. Both removals MUST happen together — leaving the relation field after dropping the model causes prisma generate to fail with a "relation references model that does not exist" error (RESEARCH Pitfall 4).

    Add a new `InviteToken` model after the `RefreshToken` model with these fields: `id String @id @default(cuid())`, `email String`, `token String @unique`, `expiresAt DateTime`, `usedAt DateTime?`, `createdAt DateTime @default(now())`. The model has NO foreign key to `User` (email-only link — once consumed, the created User row carries the email; this keeps cascade deletes simple per RESEARCH Pattern 6 and A3).

    Do not alter any other model.
  </action>
  <verify>
    <automated>cd apps/backend && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` (run from apps/backend) exits 0
    - schema.prisma contains a `model InviteToken` block with a `token String @unique` field and a `usedAt DateTime?` field
    - schema.prisma contains no `model InviteCode` block
    - The `User` model contains no `inviteCodeUsed` field
  </acceptance_criteria>
  <done>schema.prisma defines InviteToken, no longer defines InviteCode, and the User model has no inviteCodeUsed relation; prisma validate passes.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Author SQL migration, regenerate Prisma client, apply/stage migration</name>
  <files>apps/backend/prisma/migrations/20260625000000_replace_invite_code_with_invite_token/migration.sql</files>
  <read_first>
    - apps/backend/prisma/migrations/20260621000000_add_user_email/migration.sql (format reference — comment header + bare SQL, no Prisma metadata header)
    - apps/backend/prisma/migrations/migration_lock.toml
    - .planning/phases/24-email-invitations/24-PATTERNS.md (section "migration.sql" — exact DDL)
    - .planning/STATE.md (decisions 10-02 and 18-01: `prisma migrate deploy` is unavailable in the dev shell without DATABASE_URL; migrations apply via the Docker Compose entrypoint)
  </read_first>
  <action>
    This is the [BLOCKING] schema-migration task. It MUST run after Task 1 and before any backend route work in Plan 03.

    Create the migration directory `apps/backend/prisma/migrations/20260625000000_replace_invite_code_with_invite_token/` and write `migration.sql` as hand-written SQL (matching the bare-SQL, comment-header format of the add_user_email migration — no Prisma migration metadata block). The SQL must, in order: (1) drop the FK constraint with `ALTER TABLE "InviteCode" DROP CONSTRAINT IF EXISTS "InviteCode_usedById_fkey";` then `DROP TABLE IF EXISTS "InviteCode";` (the usedById FK must be dropped before the table in Postgres); (2) `CREATE TABLE "InviteToken"` with columns id (TEXT NOT NULL), email (TEXT NOT NULL), token (TEXT NOT NULL), expiresAt (TIMESTAMP(3) NOT NULL), usedAt (TIMESTAMP(3) nullable), createdAt (TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP), with PRIMARY KEY on id; (3) `CREATE UNIQUE INDEX "InviteToken_token_key" ON "InviteToken"("token");`. Note: there is no physical `inviteCodeUsed` column on `User` — it was a virtual Prisma relation (A1) — so no `ALTER TABLE "User"` is needed.

    Then regenerate the Prisma client: `cd apps/backend && npx prisma generate`. This is the critical deterministic step — it makes `prisma.inviteToken` available and removes `prisma.inviteCode` types for downstream TypeScript.

    Then attempt to apply the migration: `cd apps/backend && npx prisma migrate deploy`. If this fails because DATABASE_URL is not set in the shell (expected per 10-02/18-01), record in the SUMMARY that the migration is staged and will be applied by the Docker Compose entrypoint on next container start. Do not treat a missing-DATABASE_URL deploy failure as a task failure — the migration file existing plus a successful `prisma generate` is the gate.
  </action>
  <verify>
    <automated>cd apps/backend && npx prisma validate && npx prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - The file apps/backend/prisma/migrations/20260625000000_replace_invite_code_with_invite_token/migration.sql exists
    - migration.sql contains `DROP TABLE IF EXISTS "InviteCode"` and `CREATE TABLE "InviteToken"` and `CREATE UNIQUE INDEX "InviteToken_token_key"`
    - `npx prisma generate` (from apps/backend) exits 0
    - After generate, a TypeScript reference to `prisma.inviteToken` type-checks and `prisma.inviteCode` no longer exists in the generated client (verified by Plan 03 typecheck)
  </acceptance_criteria>
  <done>Migration SQL authored, Prisma client regenerated with InviteToken (InviteCode removed), migration applied or staged for the Docker entrypoint.</done>
</task>

<task type="auto">
  <name>Task 3: Swap inviteCode for token on RegisterSchema</name>
  <files>packages/shared/src/schemas/auth.ts</files>
  <read_first>
    - packages/shared/src/schemas/auth.ts (current RegisterSchema lines 10-23, with `inviteCode: z.string().min(1, 'Invite code is required.')` at line 20)
    - .planning/phases/24-email-invitations/24-PATTERNS.md (section "packages/shared/src/schemas/auth.ts")
  </read_first>
  <action>
    Per D-01, in `RegisterSchema` replace the `inviteCode` field with `token: z.string().min(1, 'Invite token is required.')`. Keep the existing `username` validation (3-20 chars, alphanumeric+underscore regex) and `password` validation (min 8) unchanged. Do NOT add a `confirmPassword` field — confirm-password is a client-only concern handled in the InviteRegisterPage form, never sent to the backend (RESEARCH Pitfall 5). The exported `RegisterInput` type derives from the schema automatically.
  </action>
  <verify>
    <automated>yarn workspace @kartex/shared typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RegisterSchema defines a `token` field and no `inviteCode` field
    - RegisterSchema defines no `confirmPassword` field
    - `yarn workspace @kartex/shared typecheck` exits 0
  </acceptance_criteria>
  <done>RegisterSchema accepts { username, password, token }; shared package type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application → Database | DDL change: dropping a table holding (now-defunct) invite codes; creating a token store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Information Disclosure | InviteToken.token column | high | mitigate | `token` column is `@unique` and will hold a 256-bit `crypto.randomBytes(32)` hex value (generated in Plan 03), not a sequential cuid — non-enumerable |
| T-24-02 | Spoofing | InviteToken.usedAt | medium | mitigate | Nullable `usedAt` column is the single-use marker consumed atomically in Plan 03; schema enables, Plan 03 enforces |
| T-24-03 | Tampering | DROP TABLE InviteCode | low | accept | Existing invite-code rows become inaccessible by design (D-01 clean cut); no data needs preservation |
| T-24-SC | Tampering | npm/prisma installs | low | accept | No new packages installed this plan; `prisma generate` uses already-audited @prisma/client (RESEARCH Package Legitimacy Audit) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `InviteToken` Prisma model — fields: `id`, `email`, `token` (unique), `expiresAt`, `usedAt` (nullable), `createdAt`
- Generated Prisma client member `prisma.inviteToken`
- Migration file `20260625000000_replace_invite_code_with_invite_token/migration.sql`
- `RegisterSchema.token` field (replaces `inviteCode`) in `packages/shared/src/schemas/auth.ts`
- `RegisterInput` type now shaped `{ username, password, token }`
- REMOVED: `InviteCode` model, `User.inviteCodeUsed` relation, `RegisterSchema.inviteCode` field
</artifacts_produced>

<verification>
- `cd apps/backend && npx prisma validate` passes
- `cd apps/backend && npx prisma generate` passes and exposes `prisma.inviteToken`
- `yarn workspace @kartex/shared typecheck` passes
</verification>

<success_criteria>
InviteToken model exists, InviteCode is fully removed (model + relation + migration drop), Prisma client regenerated, and RegisterSchema accepts a token field. Plan 03 and Plan 04 can now compile against the new contract.
</success_criteria>

<output>
Create `.planning/phases/24-email-invitations/24-01-SUMMARY.md` when done. Record whether `prisma migrate deploy` applied directly or was staged for the Docker entrypoint.
</output>
