---
phase: 24-email-invitations
plan: 05
type: execute
wave: 2
depends_on: [24-02]
files_modified:
  - apps/frontend/src/pages/__tests__/AdminPage.test.tsx
  - apps/frontend/src/pages/AdminPage.tsx
autonomous: true
requirements: [EMAIL-03, EMAIL-07, EMAIL-08]
must_haves:
  truths:
    - "The admin panel shows an email input + Send Invite button that posts to /api/admin/invites"
    - "A pending-invites table lists Email, Sent, and Expires for each active invite"
    - "Each row has a revoke control that calls DELETE /api/admin/invites/:id and refreshes the list"
    - "The old InviteCodesSection and InviteCode types are gone"
  artifacts:
    - "InviteTokensSection in apps/frontend/src/pages/AdminPage.tsx"
    - "apps/frontend/src/pages/__tests__/AdminPage.test.tsx"
  key_links:
    - "Section copy comes from the admin.* keys added in Plan 02 — names must match exactly"
    - "Revoke is a direct icon button per UI-SPEC + D-07 (no confirmation dialog)"
---

<objective>
Replace `InviteCodesSection` with `InviteTokensSection` in the admin panel: an email input + "Send Invite" button on top, and a pending-invites table (Email · Sent · Expires · Revoke) below. Remove the obsolete invite-code interface and helpers.

Purpose: This is the admin-facing half of EMAIL-03 (send invite), EMAIL-07 (see pending invites), and EMAIL-08 (revoke). Implements D-01, D-06, D-07, D-08.
Output: Modified AdminPage.tsx + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-email-invitations/24-CONTEXT.md
@.planning/phases/24-email-invitations/24-PATTERNS.md
@.planning/phases/24-email-invitations/24-UI-SPEC.md
@apps/frontend/src/pages/AdminPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Create failing AdminPage.test.tsx</name>
  <files>apps/frontend/src/pages/__tests__/AdminPage.test.tsx</files>
  <read_first>
    - apps/frontend/src/pages/AdminPage.tsx (current InviteCodesSection, UsersSection, MailerSection; api usage; toast usage)
    - apps/frontend/src/pages/__tests__/DeckDetailPage.test.tsx (mocking pattern: mock `@/lib/api`, real i18next, Radix interactions need fireEvent.pointerDown before click — decisions 16-04 and the Radix DropdownMenu note)
    - .planning/phases/24-email-invitations/24-RESEARCH.md (Phase Requirements → Test Map for EMAIL-03/07/08)
    - .planning/phases/24-email-invitations/24-UI-SPEC.md (Surface 1 states and copywriting keys)
  </read_first>
  <action>
    Create the RED test file before InviteTokensSection exists. Mock `@/lib/api` for `api.get('/api/admin/invites')` (returns a pending list with id/email/expiresAt/createdAt), `api.post('/api/admin/invites', ...)`, and `api.delete('/api/admin/invites/:id')`. Mock any AuthContext/admin gating needed to render AdminPage (mirror how DeckDetailPage tests set up the user). Use real i18next so assertions match en.json copy.

    Cover: (EMAIL-03) typing an email and clicking "Send Invite" calls `api.post('/api/admin/invites', { email })` and shows a success toast / refreshes the table; (EMAIL-07) the pending table renders the Email, Sent, and Expires column headers and a row per returned invite with its email; (EMAIL-08) clicking the row revoke control calls `api.delete` with the path containing the invite id. Tests fail (RED) because InviteTokensSection does not yet exist.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend vitest run src/pages/__tests__/AdminPage.test.tsx; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and renders AdminPage
    - It asserts the send-invite POST, the three table columns plus a data row, and the revoke DELETE call
    - Running it fails because InviteTokensSection is missing (RED state confirmed)
  </acceptance_criteria>
  <done>A failing test specifies the admin invite UI behavior for EMAIL-03, EMAIL-07, and EMAIL-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace InviteCodesSection with InviteTokensSection</name>
  <files>apps/frontend/src/pages/AdminPage.tsx</files>
  <read_first>
    - apps/frontend/src/pages/AdminPage.tsx (InviteCode interface, InviteCodeStatus/getInviteCodeStatus helpers, InviteStatusBadge, InviteCodesSection component, and where `<InviteCodesSection />` is rendered; existing Card/Table/Input/Button/toast/formatDate patterns and lucide icon imports)
    - .planning/phases/24-email-invitations/24-PATTERNS.md (section "apps/frontend/src/pages/AdminPage.tsx" — component shape)
    - .planning/phases/24-email-invitations/24-UI-SPEC.md (Surface 1: structure, states, revoke interaction, date formatting, copywriting keys)
  </read_first>
  <behavior>
    - On mount, fetch active invites via api.get('/api/admin/invites') and render them in the table
    - Idle: email input + enabled "Send Invite" button
    - Submitting: button disabled with a Loader2 spinner
    - Send success: toast `admin.inviteSentSuccess` (with email), clear input, refresh table
    - Send error SMTP-missing: toast `admin.inviteSMTPMissing`; general error: toast `common.somethingWrong`
    - Empty list: single row spanning all columns with `admin.pendingInvitesEmpty`
    - Revoke: clicking the row's revoke icon calls DELETE and on success toasts `admin.inviteRevokeSuccess` and removes/refreshes the row
  </behavior>
  <action>
    Per D-01, remove the `InviteCode` interface, the `InviteCodeStatus`/`getInviteCodeStatus` helpers, the `InviteStatusBadge` (no longer needed — D-08 lists active-only so no status pill), and the entire `InviteCodesSection` component. Add an `InviteToken` interface `{ id: string; email: string; expiresAt: string; createdAt: string }` and a new `InviteTokensSection` component.

    Build it per D-06/D-07 and the UI-SPEC Surface 1 contract: a `<Card>` titled `admin.inviteTokensTitle` with description `admin.inviteTokensDesc`; a top row with a `type="email"` `<Input>` (placeholder `admin.inviteEmailPlaceholder`, `flex-1`) and a primary `<Button>` `admin.sendInviteButton`; below, a `<Table>` with headers `admin.inviteColEmail`, `admin.inviteColSent`, `admin.inviteColExpires`, and an unlabeled revoke column. Each row shows email, `createdAt` and `expiresAt` formatted as locale-aware short dates (date only, no time), and a revoke control.

    Revoke is a DIRECT icon button — `Trash2` (lucide, `h-4 w-4`), `size="icon" variant="ghost" className="text-destructive hover:text-destructive"`, with aria-label from `admin.revokeInviteAriaLabel` interpolating the email — and performs the delete immediately with NO confirmation dialog (this follows UI-SPEC Surface 1 and D-07, which supersede the inline-confirm sketch in PATTERNS.md; revoke is low-risk and re-issuable). Wire send and revoke handlers to `api.post('/api/admin/invites', { email })` and `api.delete('/api/admin/invites/' + id)` with the toast/refresh behavior above, using `isConfigured`-style error mapping (detect the SMTP-not-configured error and map to `admin.inviteSMTPMissing`). Replace `<InviteCodesSection />` with `<InviteTokensSection />` in the AdminPage render. Keep UsersSection and MailerSection untouched.
  </action>
  <verify>
    <automated>yarn workspace @kartex/frontend vitest run src/pages/__tests__/AdminPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run src/pages/__tests__/AdminPage.test.tsx` passes (GREEN)
    - AdminPage.tsx defines `InviteTokensSection` and an `InviteToken` interface, and renders `<InviteTokensSection />`
    - AdminPage.tsx no longer contains `InviteCodesSection`, `InviteCode`, or `getInviteCodeStatus`
    - The revoke control is an icon button with the destructive class and performs DELETE with no AlertDialog/confirm step
    - The table renders Email/Sent/Expires headers using the admin.* keys
    - `yarn workspace @kartex/frontend vitest run` (full suite) is green
  </acceptance_criteria>
  <done>The admin panel sends, lists (active-only), and revokes email invitations; the invite-code UI is fully removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → /api/admin/invites | Admin-gated CRUD UI; the backend `requireAdmin` middleware is the real access-control boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-18 | Elevation of Privilege | Client-side admin gating | medium | mitigate | The UI lives behind AdminRoute, but authorization is enforced server-side by `requireAdmin` (Plan 03); the client gate is convenience only |
| T-24-19 | Cross-Site Scripting | Rendering invite email values | low | accept | Emails are rendered as React text children (auto-escaped); no dangerouslySetInnerHTML |
| T-24-20 | Information Disclosure | Token value in admin list | low | mitigate | Backend GET /invites omits the token field (Plan 03 T-24-13); the UI only ever receives id/email/dates |
| T-24-SC | Tampering | npm installs | low | accept | No new packages or shadcn components (UI-SPEC: all required components already installed) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

- `InviteTokensSection` component (apps/frontend/src/pages/AdminPage.tsx)
- `InviteToken` TypeScript interface (frontend)
- `apps/frontend/src/pages/__tests__/AdminPage.test.tsx`
- REMOVED: `InviteCode` interface, `InviteCodeStatus`/`getInviteCodeStatus`, `InviteStatusBadge`, `InviteCodesSection`
</artifacts_produced>

<verification>
- `yarn workspace @kartex/frontend vitest run src/pages/__tests__/AdminPage.test.tsx` passes
- `yarn workspace @kartex/frontend vitest run` full suite green
- `yarn workspace @kartex/frontend typecheck` passes
</verification>

<success_criteria>
An admin can enter an email, send an invitation, see it in the active pending table (Email/Sent/Expires), and revoke it — all wired to the Plan 03 endpoints with the Plan 02 copy.
</success_criteria>

<output>
Create `.planning/phases/24-email-invitations/24-05-SUMMARY.md` when done.
</output>
