---
phase: 24-email-invitations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/locales/en.json
  - apps/frontend/src/locales/de.json
autonomous: true
requirements: [EMAIL-03, EMAIL-05, EMAIL-06, EMAIL-07, EMAIL-08]
must_haves:
  truths:
    - "Every new invite-related i18n key resolves to a real string in both English and German"
    - "The three distinct token-error messages (already used / expired / invalid) have copy in both locales"
    - "Admin invite section copy (title, description, send button, table columns, toasts) exists in both locales"
  artifacts:
    - "apps/frontend/src/locales/en.json with auth.* and admin.* invite keys"
    - "apps/frontend/src/locales/de.json with the same key set in German"
  key_links:
    - "Key names must exactly match those referenced by InviteRegisterPage (Plan 04) and InviteTokensSection (Plan 05)"
    - "en.json and de.json must stay in parity — a missing de.json key falls back to the raw key string (Pitfall 6)"
---

<objective>
Add all new invitation i18n keys to both `en.json` and `de.json` in parity. This isolates the shared locale files into one Wave 1 plan so the two Wave 2 frontend plans (04 invite page, 05 admin UI) can run in parallel without both editing the same locale files.

Purpose: The error copy (D-10) and admin copy (D-06, D-07) must exist before the frontend components and their tests run. Pre-creating the keys prevents German raw-key fallbacks (RESEARCH Pitfall 6).
Output: 23 new keys added to both locale files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-email-invitations/24-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth.* invite keys to en.json and de.json</name>
  <files>apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/locales/en.json (current `auth` and `common` key groups — match nesting and ordering conventions)
    - apps/frontend/src/locales/de.json (current `auth` group — German tone/conventions for existing auth strings)
    - .planning/phases/24-email-invitations/24-UI-SPEC.md (section "Copywriting Contract → InviteRegisterPage" — exact English copy and the canonical new-key list)
  </read_first>
  <action>
    Add these new keys under the `auth` group in BOTH locale files, using the English values from the UI-SPEC Copywriting Contract and idiomatic German equivalents in de.json: `invitePageTitle` ("Create Account — Kartex"), `createYourAccount` ("Create your account"), `inviteWelcome` ("You've been invited to Kartex. Enter your details below."), `email` ("Email"), `confirmPassword` ("Confirm password"), `inviteErrorTitle` ("Unable to register"), `inviteAlreadyUsed` ("This invite has already been used."), `inviteExpired` ("This invite link has expired. Contact an admin for a new invitation."), `inviteInvalid` ("This invite link is not valid."), `passwordMismatch` ("Passwords do not match."), `backToSignIn` ("Back to sign in").

    The three error strings (`inviteAlreadyUsed`, `inviteExpired`, `inviteInvalid`) implement D-10's three distinct messages and must be word-for-word the English values above in en.json. Do NOT add keys already present (e.g. `auth.username`, `auth.password`, `auth.createAccount`, `auth.creatingAccount`, `auth.usernameTaken`, `auth.alreadyHaveAccount`, `auth.signIn`) — verify each before adding to avoid duplicates.
  </action>
  <verify>
    <automated>node -e "const en=require('./apps/frontend/src/locales/en.json');const de=require('./apps/frontend/src/locales/de.json');const ks=['invitePageTitle','createYourAccount','inviteWelcome','email','confirmPassword','inviteErrorTitle','inviteAlreadyUsed','inviteExpired','inviteInvalid','passwordMismatch','backToSignIn'];const miss=ks.filter(k=>!en.auth?.[k]||!de.auth?.[k]);if(miss.length){console.error('MISSING auth keys:',miss);process.exit(1)}console.log('auth keys OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 listed keys exist under `auth` in en.json with non-empty string values
    - All 11 listed keys exist under `auth` in de.json with non-empty German string values
    - en.json `auth.inviteAlreadyUsed` equals "This invite has already been used."
    - The verify node script prints "auth keys OK" and exits 0
  </acceptance_criteria>
  <done>All auth invite keys present and in parity across both locale files.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin.* invite keys to en.json and de.json</name>
  <files>apps/frontend/src/locales/en.json, apps/frontend/src/locales/de.json</files>
  <read_first>
    - apps/frontend/src/locales/en.json (current `admin` key group — match nesting and ordering)
    - apps/frontend/src/locales/de.json (current `admin` group)
    - .planning/phases/24-email-invitations/24-UI-SPEC.md (section "Copywriting Contract → Admin — InviteTokensSection" — exact English copy)
  </read_first>
  <action>
    Add these new keys under the `admin` group in BOTH locale files (English values from UI-SPEC, German equivalents in de.json): `inviteTokensTitle` ("Email Invitations"), `inviteTokensDesc` ("Send one-time invitation links to new users."), `inviteEmailPlaceholder` ("Email address"), `sendInviteButton` ("Send Invite"), `inviteColEmail` ("Email"), `inviteColSent` ("Sent"), `inviteColExpires` ("Expires"), `revokeInviteAriaLabel` ("Revoke invitation for {{email}}"), `pendingInvitesEmpty` ("No pending invitations."), `inviteSentSuccess` ("Invitation sent to {{email}}."), `inviteSMTPMissing` ("Email is not configured. Contact the server administrator."), `inviteRevokeSuccess` ("Invitation revoked.").

    `revokeInviteAriaLabel` and `inviteSentSuccess` use the `{{email}}` interpolation placeholder — keep that exact placeholder token in both locales (D-07 revoke per-row, D-06 admin section). These keys back the admin section introduced in Plan 05.
  </action>
  <verify>
    <automated>node -e "const en=require('./apps/frontend/src/locales/en.json');const de=require('./apps/frontend/src/locales/de.json');const ks=['inviteTokensTitle','inviteTokensDesc','inviteEmailPlaceholder','sendInviteButton','inviteColEmail','inviteColSent','inviteColExpires','revokeInviteAriaLabel','pendingInvitesEmpty','inviteSentSuccess','inviteSMTPMissing','inviteRevokeSuccess'];const miss=ks.filter(k=>!en.admin?.[k]||!de.admin?.[k]);if(miss.length){console.error('MISSING admin keys:',miss);process.exit(1)}if(!en.admin.revokeInviteAriaLabel.includes('{{email}}')){console.error('missing interpolation');process.exit(1)}console.log('admin keys OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 12 listed keys exist under `admin` in en.json with non-empty string values
    - All 12 listed keys exist under `admin` in de.json with non-empty German string values
    - `admin.revokeInviteAriaLabel` and `admin.inviteSentSuccess` retain the `{{email}}` interpolation token in both locales
    - The verify node script prints "admin keys OK" and exits 0
  </acceptance_criteria>
  <done>All admin invite keys present and in parity across both locale files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static locale string additions; no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04 | Information Disclosure | Error message copy | low | accept | The three token-error strings are intentionally generic (no token value, no account existence detail) — they do not leak whether a token or email exists beyond what the link already implies |
| T-24-SC | Tampering | npm installs | low | accept | No packages installed; only JSON edits |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- 11 new `auth.*` i18n keys: `invitePageTitle`, `createYourAccount`, `inviteWelcome`, `email`, `confirmPassword`, `inviteErrorTitle`, `inviteAlreadyUsed`, `inviteExpired`, `inviteInvalid`, `passwordMismatch`, `backToSignIn`
- 12 new `admin.*` i18n keys: `inviteTokensTitle`, `inviteTokensDesc`, `inviteEmailPlaceholder`, `sendInviteButton`, `inviteColEmail`, `inviteColSent`, `inviteColExpires`, `revokeInviteAriaLabel`, `pendingInvitesEmpty`, `inviteSentSuccess`, `inviteSMTPMissing`, `inviteRevokeSuccess`
- All keys present in both `apps/frontend/src/locales/en.json` and `apps/frontend/src/locales/de.json`
</artifacts_produced>

<verification>
- Both node parity scripts print "auth keys OK" and "admin keys OK"
- `yarn workspace @kartex/frontend vitest run` still green (no regressions from JSON edits)
</verification>

<success_criteria>
All 23 new invitation keys resolve in both English and German; no raw-key fallbacks will appear in the Plan 04/05 UIs.
</success_criteria>

<output>
Create `.planning/phases/24-email-invitations/24-02-SUMMARY.md` when done.
</output>
